# 开发任务说明：开单购物车拆行 + 已选商品 Sheet

> **⚠️ 归档说明（R12 · 2026-09-19）**：本文是早期一轮（PRD v1.2 时期）的开发任务说明，购物车拆行 / 已选商品 Sheet 的规则仍然有效；但文中 `staff` 的字段口径（`点客/散客`）**已被 R12 取代** —— 现行模型为 `staffRoles[sid]`（工位 id **数组**）+ `staffExtra[sid]`（顾客指定），见 `PRD-开单结账.md` **§6.11.5**。文中「张三·点客·大工」等示例文案请按「姓名 · 工位 · 顾客指定」理解。

> **用途**：把本文完整交给 AI / 开发即可落地本次改动。  
> **范围**：仅「添加项目/产品进购物车」的合并拆分逻辑，以及底部「已选商品」Sheet。  
> **不要改**：快捷开单流程、s6/s7 结算页业务规则（权益/改价等）、支付/挂单线上逻辑。  
> **设计稿**：[已选商品 Sheet · Figma](https://www.figma.com/design/kaRi8xVZGuJExsUmKkSuxb/%E5%BC%80%E5%8D%95%E7%BB%93%E8%B4%A6-ZM?node-id=388-429)  
> **参考原型**：`开单页面/index.html`（仓库 [XDean-Designer/kaidan](https://github.com/XDean-Designer/kaidan)，commit 含本需求）  
> **PRD**：`开单页面/PRD-开单结账.md` v1.2 §4.4 / §6.12 / 规则 16–18 / 验收 74–80

---

## 0. 背景（改什么、为什么）

### 缺陷
用户分两次及以上把**同一个项目或产品**加入购物车时，若两次的「应付」或「服务员工/销售人员」不同，线上/旧原型会**错误合并成一行**。正确行为是：**拆成多行**。

### 新增
底部 bar 左侧热区打开「已选商品」Sheet（对齐 Figma），从 bar **后一层**向上弹出；与结算页共用同一购物车数据源。

---

## 1. 数据与合并规则（必须严格实现）

### 1.1 明细行字段（概念模型）

每条购物车行至少包含：

| 字段 | 说明 |
|------|------|
| `name` | 项目/产品名称 |
| `isProd` | 是否产品（与项目区分） |
| `kind` | 加购来源，如散客开单 / 会员开单（与现网字段对齐即可） |
| `qty` | 数量，整数 1~999 |
| `payable` | 本行应付总额 |
| `price` / `salePrice` | 原价 / 权益优惠单价（现网已有则保留） |
| `staff` | 服务员工/销售人员集合（见下） |
| 其它 | 分类、缩略图、权益、改价标记等沿用现网，本次不改语义 |

`staff` 需能表达多人，每人至少：`staffId` + 点客/散客 + 工位（大/中/小工）。未选员工时集合为空。

### 1.2 单件应付

```
unitPayable = round2(payable / qty)
```

金额比较容差：`|a - b| > 0.001` 视为不同。

### 1.3 员工合并键

- 员工集合为空 → 键为 `""`（**未选可与另一次未选合并**）。
- 非空：按 `staffId` 排序后拼接，每人包含：`staffId` + 角色/工位 + 点客/散客。  
  示例：`id1:big:1|id2:mid:0`  
- **集合内容或任一人的点客/工位不同 → 键不同 → 不可合并。**

### 1.4 何时合并 / 拆分

对「确定」写入的新行，在已有购物车中查找可合并行，须**同时满足**：

1. 同一 `name`、同一 `isProd`、同一 `kind`（同项目/产品、同加购语境）
2. `unitPayable` 一致
3. 员工合并键一致

| 情况 | 行为 |
|------|------|
| 上述全满足 | **合并**：`qty += 新数量`；`payable = unitPayable × 新 qty`（同步小计/应付等派生字段） |
| **应付不同** 或 **员工不同** | **新开一行**，禁止把数量加到旧行上 |
| 仅数量不同、应付单价与员工相同 | 合并 |

> 用户口径：**只要应付或员工不一样就必须拆行**；其它字段（如改价标记）不作为拆行条件，也不妨碍按上面三键合并。

### 1.5 加购后副作用（对齐现网习惯）

- 底部「应付」金额 = 各行应付之和，重算。
- 左侧分类角标：按「确定」次数 +1（合并也算一次确定）；删一整行 -1。
- 底部购物车角标 = **各行 qty 之和**（不是行数）。
- Toast：`已加入 · 应付 ¥{本次写入金额}`（可用本次应付，不必展示购物车合计）。

---

## 2. 已选商品 Sheet（UI / 交互）

### 2.1 打开入口

- **热区**：底部 bar 上「购物车图标 + 数量角标 + 应付文案与金额」整块可点。  
- **不可触发打开**：挂单、下一步。  
- **空车**：不打开 Sheet，toast「请先选择项目」。

### 2.2 层级（关键）

- Sheet 从底部 bar **后一层**向上弹出。  
- **页面 bottom-bar 始终浮在最前**（Sheet 打开时 bar 仍可见、可点挂单/下一步/热区）。  
- 半透明遮罩在 Sheet 后方、内容页前方；点遮罩关闭 Sheet。  
- 实现提示：遮罩/Sheet 的 z-index **低于** bottom-bar。

### 2.3 结构（对齐 Figma）

1. **头**：左侧「已选商品」；旁「清空」+ 垃圾桶图标；右侧关闭 ×。  
2. **列表**（与结算页同一 `cartItems`）：  
   - 第一行：商品名称  
   - 第二行：**员工摘要**（必须展示，用于区分拆分行）  
     - 有员工：如 `张三·点客·大工`（多人用顿号连接，文案风格与现网员工回填一致）  
     - 无员工：`未选员工`  
   - 下方：左侧 **单件应付价**（¥）；右侧 **− 数量 +**  
3. **不**在 Sheet 内再画一条底部 bar（用页面已有 bar）。

### 2.4 数量步进

- `+` / `−` 改该行 `qty`，按原 `unitPayable` 重算 `payable` 与底部合计、角标。  
- 减到 **0**：**删除该行**；若已无行则关闭 Sheet。  
- 上限 **999**。  
- 若现网「改价」判定依赖「单价×数量」，步进后需按现网规则刷新改价/权益锁定状态。

### 2.5 清空

- 点「清空」→ **二次确认**（文案建议：「确定清空已选商品？」取消 / 清空）。  
- 确认后：清空全部购物车行、分类角标、底部角标与应付，关闭 Sheet，toast「已清空」。

### 2.6 视觉要点（可按设计稿微调）

- 白底、顶圆角约 16px；标题约 16 Medium；清空灰色 14。  
- 数量：左侧灰圆 −，右侧浅红圆 +；中间数量。  
- 底部购物车角标：品牌红小圆点，>99 显示 `99+`。

---

## 3. 与结算页的关系

- Sheet 与开单结算页（s6）**共用同一购物车列表**。  
- 拆行后的多行在 s6 也应是多条明细，不可在进结算时再错误合并。  
- 结账成功清空购物车时，同步清底部角标，并确保 Sheet 关闭。

---

## 4. 验收清单（给 AI 自测 / QA）

1. 同一项目加两次，员工不同 → Sheet/结算均为 **2 行**。  
2. 同一项目加两次，单件应付不同（改价）→ **2 行**。  
3. 同一项目加两次，单件应付与员工都相同（含都未选员工）→ **1 行**，数量相加，应付 = 单件×数量。  
4. 空车点左侧热区 → toast「请先选择项目」，无 Sheet。  
5. 有货点热区 → Sheet 在 bar 后升起，bar 仍在最前。  
6. Sheet 行可见员工摘要；− 到 0 删行；清空需确认。  
7. 挂单/下一步不因误触热区而打开 Sheet（热区不含这两按钮）。

---

## 5. 给 AI 的执行指令（可直接粘贴）

请在当前开单结账代码库中完成以下改动，并保持其余功能不变：

1. **修复购物车合并逻辑**：添加项目/产品时，仅当「同名同类型 + 单件应付一致 + 服务员工完全一致（未选为空键）」时合并数量；应付或员工不同必须新开一行。单件应付 = 行应付÷数量（两位小数）。合并后行应付 = 单件×新数量。  
2. **实现已选商品 Sheet**：底部 bar 左侧（购物车图标+角标+应付金额）为热区；空车 toast「请先选择项目」；有货时从 bar 后一层弹出 Sheet（bar 始终置顶）。Sheet 含标题「已选商品」、清空（二次确认）、关闭；列表行含名称、员工摘要、单件价、±数量（0 删行，上限 999）。数据与结算页购物车同源。  
3. UI 对齐 Figma：https://www.figma.com/design/kaRi8xVZGuJExsUmKkSuxb/开单结账-ZM?node-id=388-429  
4. 对照上文「验收清单」自测通过。  
5. 不要改快捷开单、支付、挂单主流程；不要把不同应付/员工的同项重新合并。

---

## 6. 文案常量

| 场景 | 文案 |
|------|------|
| Sheet 标题 | 已选商品 |
| 清空 | 清空 |
| 确认弹窗 | 确定清空已选商品？ |
| 确认按钮 | 清空 / 取消 |
| 未选员工 | 未选员工 |
| 空车热区 / 空车下一步 | 请先选择项目 |
| 清空成功 | 已清空 |
| 加购成功 | 已加入 · 应付 ¥{金额} |

---

*文档对应产品确认口径：合并键=应付单价+员工（B）；单件应付比对（A）；未选可合并（A）；Sheet 展示员工摘要（B）；± 可删行（A）；清空需确认（A）；热区=图标+应付（A）；Sheet 在 bar 后（A）；空车 toast（A）。*
