# PRD：开单结账2.0（散客开单 · 会员开单 · 直接收款 · 商户端）

| 字段 | 内容 |
|------|------|
| 文档版本 | v2.0-draft（R23 · 2026-09-21） |
| 日期 | 2026-09-21 |
| 交互原型（唯一可视依据） | 同目录 `index.html` |
| 画布 | 390 × 844（演示区默认 1.25×） |
| 目的 | 供前端 / 后端 / 测试及 AI 工具落地「开单结账2.0」；**只需本文 + 交互原型** |

> **v2.0 主链路（问卷锁定 + 纠偏；本轮确认 C1/D1/E1/F1）**：首页（入口下沿贴中线）→ 选择顾客（取挂单右对齐；选中 `#EFF0FF`；行展「开单 / 直接收款」）→ 开单页对齐 demo（无会员卡/直接收款；价目取 demo **美发子集**；分类芯片最左「全部」且进页/切 Tab **默认全部平铺、无分组标题**；点「+」**打开底部「设置内容」sheet**）→ 结算文档流（明细下：**优惠券行** + Figma 备注卡，再到底栏应付）。**全页顾客卡**统一粉白淡色卡；会员**持卡列表**（卡类型 + 卡名 + 剩余权益）**默认收起**，右侧 chevron 展开；**去掉「更换顾客」**；点头像/姓名进 s4。直接收款仍走 s2→s3→s7。

> **R3 本轮变更摘要（2026-09-18）**：
>
> 1. **名称统一**：原型 + 本 PRD 内「快捷开单 / 快速消费」全部改为 **「直接收款」**（含可见文案与代码标识）。
> 2. **s4 选择顾客**：删除原挂单条，标题栏右侧新增 **「取挂单（N）」**；「添加会员」图标换新（橙色 `#FF8956`，行内 **36×36** / 卡片 **24×24**）。
> 3. **s0 首页**：两大按钮下方新增描边 **「取挂单（N）」**（N 实时）。
> 4. **s1/s5 开单页**：**顾客卡片默认收起**、价目表上移贴标题栏；标题栏右侧为顾客入口（会员 icon 20×20 + 姓名 / 散客纯文字），点击展开、再点收起（iOS 动效）；点商品圆形加号改为 **底部「设置内容」sheet**（**标题区只显示商品名，价格行已删除**；点确定入购物车），**删除行内展开区与滚动视差吸顶**；价目表为空时隐藏对应 Tab（演示开关 `?noProduct=1` / `?noProject=1`）；挂单成功 → toast + **s14**。
> 5. **s6/s7 结算页**：删除标题栏日期入口与单号卡片；结账成功 → toast「结账流程说明」+ **s15 开单成功**（清空购物车/角标）。
> 6. **新增页面**：**s14 挂单列表（占位）**、**s15 开单成功**（按 Figma `开单结账-ZM` `151-7794` 还原；内容区 改日期/改单号/看流水；底部 bar = 返回首页图标 + 客户签字/客户详情/继续开单）。
> 7. **左侧导航**：新增「14 挂单列表」「15 开单成功」。
> 8. 新增文案与验收用例见 §9.1 与模块 10.2（第 91–178 条）；**R16 的变更与验收用例见「R16 本轮变更摘要」与模块 10.3（第 309–330 条）**；**R17 见「R17 本轮变更摘要」与模块 10.4（第 331–342 条）**；**R18 见「R18 本轮变更摘要」与模块 10.5（第 343–352 条）**；**R19 见「R19 本轮变更摘要」与模块 10.6（第 353–364 条）**；**R20 见「R20 本轮变更摘要」与模块 10.7（第 365–382 条）**；**R21 见「R21 本轮变更摘要」与用例 370 / 383**；**R22 见「R22 本轮变更摘要」与用例 385–390**；**R23 见「R23 本轮变更摘要」与用例 383 / 391–400**。
>
> **R4 本轮变更摘要（2026-09-18）**：
>
> 1. **图标尺寸**：「添加会员」图标（`ic_add_member_new.svg`，橙色 `#FF8956`）尺寸调整为 **s4 列表行 36×36**（外层保持 44×44 占位，与其它行 44 头像同中轴、行高一致）、**顾客卡片内 24×24**（等比缩小）。
> 2. **会员顾客卡展开区内容**：由原「会员卡 / 储值余额 / 折扣」三宫格指标改为 **持卡列表** —— **一行一张卡**，左侧 = 卡类型（浅色 pill）+ 卡名，右侧 = 卡剩余权益；**展示内容与线上现有版本一致**（示例：`储值卡 美发卡2000元　余额1900.00元`、`计次卡 染烫10次卡　余4次`、`折扣卡 超值折扣卡　8.0折`）。
> 3. **商品「设置内容」sheet**：删除标题区项目名下方**价格行**（红色价格），标题区只保留商品名。
> 4. **s15 开单成功页**：**开单日期 / 单号移入金额卡内作为次要字段**（卡内底部发丝线分隔 + 居中 12px 灰字，删除卡外元信息行）；**「返回首页」图标颜色改为与「客户签字」一致**（`#F32F41`，按钮底仍为 `#FFF2F2`）。
>
> **R5 本轮变更摘要（2026-09-18）**：
>
> 1. **「取挂单」入口去图标**：s4 标题栏右侧与 s0 首页的「取挂单（N）」按钮**统一改为纯文字**（删除前置的 `ic_hold_take.svg` 图标，两处样式对齐）。**R9 §4 修订：s4 标题栏的「取挂单」已恢复前置图标**（改用新图标 `assets/ic_hold_take_nav.svg`）；**s0 首页仍为纯文字**。
> 2. **s15 开单成功页 UX 优化（方案 A–E 全采纳）**：
>    - **A 去双标题**：删除标题栏「支付」（与「开单成功」徽标重复）；徽标行上移，对勾 **22×22**、「开单成功」**18px / 600**；顶部渐变高度 **328 → 300**。
>    - **B 金额卡浮起 + 字号分级**：卡底 `rgba(255,255,255,.6) → .88`，加 1px 白描边 + 柔和投影；金额 **44 → 48**、`¥` **30 → 26**；「支付方式 / 剩余」行统一为**标签 12px 次要色 + 值 15px Medium**；元信息 **11px + 字距 .2px**；卡高 236 → **232**（`no-rest` 210 → **206**）。
>    - **C 操作降权**：改日期 / 改单号 / 看流水 由「白底描边胶囊」改为**无框「图标（14）+ 文字」次级操作组**（`#6C7279`，按下转 `#1a1a1a`）。
>    - **D 渐变过渡**：渐变下沿新增 **68px 过渡带**（`#F7F3E2 → #F7F7F7`），消除与页面底色的硬切断层。**（R9 §6 作废：成功页顶部已换成 Figma `1174-135` 的新渐变，末端直接落到页面底色 `#F7F7F7`，过渡带与装饰遮罩图层一并删除。）**
>    - **E 入场动效**：对勾 `scale .6 → 1` 回弹 → 标题 / 操作组 / 金额卡 / 底栏错峰上浮（延迟 0 / 60 / 120 / 160 / 240ms，总时长 ≤ 580ms，`cubic-bezier(.32,.72,0,1)`）；**金额 0 → 目标值 620ms 计数**（easeOutCubic）。**（R20 已删除该计数 —— 金额进入本页即为终值，见 R20 摘要第 3 条与 §6.23 动效条）**
> 3. **s14 挂单列表 UX 优化（方案 F–L 全采纳）**：
>    - **G 顶部失效提示**：标题栏下新增**全宽浅黄提示条**（`#FFF8E5` / 文字 `#B26B00` / 12px + info 图标，高 40，列表滚动时保持可见）：**「每日24:00后 挂单自动失效并删除」**；原列表底部的「占位页」说明删除。
>    - **F 卡片信息分层**：卡头（顾客名 **16/600** + VIP + 类型 chip …… 时间 12px 数字字体）→ 发丝线（`#E8E8E8`，0.5px）→ 明细摘要（13px、最多两行）→ 字段组（标签左 / 值右）。
>    - **新增「服务员工」字段**：取**全单服务员工并集**（按明细顺序去重），单行显示**全部姓名**（顿号分隔，**不使用「等 N 人」省略**）；无员工显示 `—`。
>    - **新增删除功能**：卡尾垃圾桶图标，**点击直接删除（无二次确认）** + toast「已删除「{顾客}」的挂单」，并同步各处 `N`；删除时卡片高度塌陷 + 淡出 200ms。
>    - **H 金额与按钮主次**：金额 **18 → 20 Medium**（`¥` 13px）；「取挂单」改为 **高 34 / 圆角 17** 的红色胶囊；删除为 **32×32** 圆形灰图标按钮。
>    - **I 标签统一**：类型 chip（开单 `#EFF0FF` / `#4A5BB5`；直接收款 `#FFF2F2` / `#F32F41`）；VIP 换成与结算页一致的**渐变 `◆VIP`**（`#FFE6CE → #FFBA75`）。
>    - **K 动效**：进入页面卡片**错峰淡入上移**（stagger 50ms，仅在首次渲染播放）；取挂单时卡片淡出缩小后再跳转。
>    - **L 空态**：`ic_empty.svg`（72×72）+「暂无挂单」+「挂起的单据会显示在这里」。
>    - **取挂单补正**：`holdOrders.items` 追加每行的 `staffIds / staffRoles / staffExtra` 快照，取挂单时**原样还原工位与顾客指定**（修正此前员工信息丢失的问题）。**R12：`staffRoles` 为工位数组、`staffExtra` 表顾客指定（旧 `staffDesignated` 读取时自动迁移）**。
>
> **R6 本轮变更摘要（2026-09-19）**：
>
> 1. **删除 s10 业绩设置 / s11 提成方案**：线上已重构这两个模块，原型页面不再适用 —— 删除页面、左侧导航项、CSS 与相关交互，PRD 同步删除对应章节。**并在此统一声明：「快捷开单」「快速消费」全局统一改名为「直接收款」**（原型、PRD、代码标识一律使用「直接收款」；旧名不再出现）。
> 2. **s1 / s5 标题栏顾客入口改双图标**（沿用 Figma `1168-39`）：**散客 = 双人图标**（新增 `assets/ic_guest_head.svg`）、**会员 = 单人图标**（`assets/ic_member_head.svg`），**两态图标统一 `#929292`**；图标 20×20，位于名称左侧。此前「散客态不显示图标」的规则作废 —— **两态均显示图标，仅图形不同**。
> 3. **s15 金额卡重做（信息结构 + 分隔线 + 字段规则）** —— **本条已被 R7 §3 修订（支付明细结构），以 R7 为准**：
>    - **发丝线分层**：大字金额与下方信息区以**灰色发丝实线**（0.5px `#E5E5E5`）隔开。
>    - **字段改为左右两栏**：`单号` → `开单日期` → `支付方式` → 卡类字段，统一「**左字段名（12px 次要色）/ 右参数（15px Medium）**」。（R7 修订：**删除「支付方式」汇总行**，改为支付明细行）
>    - **字段间为灰色发丝虚线**（0.5px `#E0E0E0` dashed），**所有划线（含实线与虚线）左右边距均为 16**；卡高由内容决定（不再固定 232/206）。（R7 修订：**「开单日期」下方改为实线**）
>    - **会员卡支付字段规则修正**（原演示数据错误）：**折扣卡 → 折扣权益（如 `8.0折`）**；**储值卡 → `本次扣减` + `卡余额`**；**计次卡 → `扣减次数` + `剩余次数`**；一单用到多张卡时**按卡逐组列出**（顺序：折扣卡 → 储值卡 → 计次卡）。（R7 修订：**改为「卡名 + 金额/次数」主行 + 灰字『剩余 / 折扣』次行**；**R11 再修订：折扣卡不出现在明细**）
>    - **补充支付相关字段规则**（R7 修订）：`支付方式` 取值为线上枚举 —— **会员卡 / 微信 / 支付宝 / 美团 / 抖音 / 口碑 / 大众点评 / 现金 / 银行卡 / 信用卡 / 经理签单 / 其他平台团购**；**一笔单可用多种方式叠加，叠加时每种方式各占一行**（字段名 = 支付方式名或会员卡具体名称，参数 = 该方式金额；会员卡行为「消耗金额或次数」+ 灰字次行剩余权益）；**展示口径与目前线上版本完全一致**（本 PRD 只做记录，不新增规则）。
>    - **操作组文字加深一级**：改日期 / 改单号 / 看流水 `#6C7279 → #55595E`。
>    - **演示数据重造**（R7 修订）：见 R7 §3 演示数据。
>    - **「修改单号」sheet 修复**：补齐 `.sucno-field` 样式（白底圆角 54 高输入行），底部改为**「取消 + 确定」双按钮**。（R7 修订：**删除 sheet 内提示文案**）
>    - **「改日期」改为底部 sheet**：复用标题栏同一日历数据源，以**底部 sheet 形态**（全宽、上圆角 16、含安全区）呈现。
> 4. **s14 挂单删除条件收敛为三种**：① 列表内「删除」按钮（点击即删，无二次确认） ② **每日 24:00 超时自动删除**（进入列表时过滤 + toast「有 N 张挂单已超过 24:00 自动失效并删除」）③ **取挂单后进入结算并开单成功**（`activeHoldId` 命中该条）。**「取挂单」本身不再删除挂单 —— 挂单保留且可重复取**（取回明细覆盖当前草稿）。
>    - **卡片清理**：删除 VIP 标签最左侧的黑色方块（`◆`），只保留 `VIP` 文字；**删除卡片上的「开单」类型 chip**。
>    - **取挂单回跳来源页**：挂单新增 `from` 字段 —— **开单页（s1/s5）挂的单 → 取挂单回开单页**；**结算页（s6）挂的单 → 取挂单回结算页**。演示数据同时包含两种来源。
> 5. **s2 直接收款金额对齐**：「¥」与「请输入金额 / 金额数字」改为**同一 40px 行高中轴对齐**（s3 同步）。
> 6. **s6 底部「应付」可手动修改**（**仅 s6**，s7 保持只读）—— **改价后的权益 / 优惠券规则已被 R7 §4 修订，以 R7 为准**：点击应付弹出**底部数字键盘 sheet**（清零 / 取消 / 确定，金额规则同 5.1）；确认后应付**锁定为手输值**（`coPayOverride`）；界面出现底栏上方说明 + 底栏「**已改价**」标签；**单据内容（增删明细 / 改数量 / 改价 / 换顾客 / 清空购物车 / 取挂单还原）任一变化 → 手输值自动失效**，标签与说明消失、权益与优惠券行恢复可点，应付回到系统计算值；再次点击应付可重新修改（预填当前生效值）。
> 7. **演示数据校正**：过长券名 `店长洗剪吹新人优惠券，洗剪吹套餐立减10元` → **`洗剪吹立减10元券`**；顾客列表由 5 人扩充到 **15 人**（覆盖 C/D/G/H/L/M/S/W/Z 共 9 个字母分组）。
>
> **R7 本轮变更摘要（2026-09-19）**：
>
> 1. **s15 支付明细重做**（修正 R6 对「支付相关字段」的误解）：**删除「支付方式」汇总行**，改为**支付明细行** —— **左 = 支付方式的具体名称（支付宝 / 微信 …）或会员卡的具体卡名（如 `美发卡3000元`），右 = 该方式对应的金额（会员卡则为消耗金额或次数）**；**会员卡下方再跟一行灰色小字 `剩余` + 余额 / 次数**（计次卡为 `剩余` + 剩余次数，折扣卡为 `折扣` + 折扣权益）。**一笔单可多种方式叠加 → 每种方式各占一行**，等同「支付明细排列」。参照 Figma `开单结账-ZM` `151-7806`。**R11 修订：折扣卡不再出现在支付明细（折扣只参与计算、不参与支付）；补齐套餐卡 / 周期卡展示规则，见 6.23.1 C 与 R11 摘要**。
>    - **三类卡的明细规则**：**折扣卡** = 卡名 + 本卡实收金额，次行 `折扣` + `8.0折`；**储值卡** = 卡名 + 本卡实收金额，次行 `剩余` + 卡余额；**计次卡** = 卡名 + 本单扣减次数（如 `1 次`），次行 `剩余` + 剩余次数（**沿用现有规则：计次卡只核销次数、不抵扣金额**）。卡行顺序：折扣卡 → 储值卡 → 计次卡 → 非卡支付方式。**（R11 修订：折扣卡整类不再出现在支付明细；卡行顺序改为 储值卡 → 套餐卡 → 计次卡 → 周期卡；见 6.23.1 C）**
>    - **强制闭合**：**各支付行金额之和 = 顶部大号金额**（可自检）。会员卡未覆盖的部分：**会员单**记 `微信` 一行；**散客单**（明细 > 1 条时）拆 `现金` + `微信`（**演示数据规则：现金 = 金额最大的单条明细，其余为微信**，非线上规则，仅用于演示组合支付）；**整单改价后**权益不参与计算 → 明细区只显示非卡方式一行（`微信` = 手输值）；优惠券使实收低于卡内消耗时，差额从末尾金额行逐行回退以保证闭合。
>    - **分隔线分级**：`单号` ↔ `开单日期` 之间为**虚线**；`开单日期` ↔ 支付明细之间改为**实线**（0.5px `#E5E5E5`）；**支付明细行之间为虚线**；**同一张卡的主行 / 灰色次行之间不画线**；所有划线左右边距 16。
>    - **行内字号**（对齐 Figma）：主行左 = **14px `#1a1a1a`**、右 = **15px Medium**；次行左右 = **12px `#929292`**（数字用数据字体）。
>    - **演示数据重造**（会员单，¥304.40）：`超值折扣卡 ¥78.40 → 折扣 8.0折` / `美发卡3000元 ¥58.00 → 剩余 ¥2942.00` / `染烫10次卡 1 次 → 剩余 3 次` / `微信 ¥168.00`（= 78.40 + 58.00 + 168.00 = 304.40，闭合）。散客单演示 `现金 + 微信` 组合。**（R11 重造为 ¥510.40、5 行、折扣卡不出行，见 6.23.1 D 与 R11 摘要）**
> 2. **文案删除**：① 删除 s0 首页提示「从「开单」进入选择顾客，再开单或直接收款。本页为 2.0 简化首页（问卷 A2）」（含 `.v2-home-hint` 样式）；② 删除 s15「修改单号」sheet 内的提示文案（单号仅支持数字…），sheet 高度随内容收紧。
> 3. **s6 整单改价规则修订**（R6 §6 的权益 / 优惠券口径作废，以本条为准）：
>    - **单条明细改价**：该条明细不可用权益（`改价商品不可用权益`）；
>    - **整单改价（手输底部应付）**：**本单所有明细的权益行全部降权为只读**（文案 `改价商品不可用权益`，点击不响应）；
>    - **优惠券仍可选用**（点击可打开「选择优惠券」sheet，券记入本单、视为已消耗，优惠券行显示券名、**不显示减项**），**但不改变最终结算价** —— **只要手动改过整单价格，之后再填 / 换 / 取消优惠券，结算价都不变**；
>    - **失效判定基准改为「券前合计」**：仅**明细 / 权益变化**才使手输值失效（换券 / 取消券不失效）；手输值被清空或失效后，**优惠券保留并自动恢复参与抵扣**（按门槛重新计算应付）；
>    - **底栏提示文案**：`已按手动应付结算，权益不再参与计算；优惠券可选用但不影响最终结算价`；底栏保留「**已改价**」标签。
>    - **键盘 sheet 内提示同步**：`改价后本单明细权益不再参与计算；优惠券仍可选用，但最终结算价以手输金额为准。`（替换 R6 遗留的「权益与优惠券仍按原规则计算」旧口径）；**手输值失效时立即解除权益行的降权态**（失效判定发生在明细行渲染之前，不会出现「底栏已恢复、明细仍置灰」的中间态）。
> 4. **s15 不体现优惠券**：成功页金额卡只列支付明细（支付方式 / 会员卡 + 金额、剩余权益），优惠券不在卡内出现。
>
> **R8 本轮变更摘要（2026-09-19）**：
>
> 1. **进入成功页的 toast 改为「流程说明」**：原「支付成功」占位文案改为说明原型切掉了「结算 → 成功」之间的支付流程弹窗，开发以线上为准 —— **`原型省略了结算与成功之间的支付流程弹窗（支付方式、收款确认等），开发时以线上为准`**（40 字 / 2 行）。`toast(msg, ms)` 新增**可选时长参数**（缺省仍 1400ms），本条传 **3000ms**；`#toast` 由 `white-space:nowrap` 改为**可换行**（`max-width:330px`，居中改为 `left/right:16 + margin:auto`，避免 `left:50%` 把可用宽度压到 195px 折成 5 行）。
>    - 本 PRD 后续章节中该 toast 统一简称 **「结账流程说明」**（完整文案以本条为准）。
> 2. **s13 券卡右侧重排（修掉单选圈压线）**：券卡高 125px 时，原单选圈位于 `top:78`，**正好压在 84px 处的卡内虚线分隔线上**并落进「使用规则」行。现改为：
>    - **单选圈独占最右列并相对券卡垂直居中**（`right:16px; top:50%; translateY(-50%)`，占 53–73，与 84px 分隔线留出 11px 间隙）；**R10 修订：改为固定 `top:47px`、圈底与门槛文案行底对齐（展开不位移、不再压住规则行）**；
>    - **金额块与门槛文案整体左缩 36px**（`right:16px → right:52px`），与单选圈形成「内容列 + 选择列」两列结构；
>    - **门槛文案由红色改为灰色 `#929292`**（不再与金额抢红色）；
>    - **选中态强化**：选中券加 **1px 红色内描边（`inset 0 0 0 1px #F32F41`）+ 极浅红底 `#FFF8F8`**（原来只有小圆点变化）；
>    - **不满门槛的券**：门槛文案后追加 **`暂不可用` 灰标签**；同时把「整卡 `opacity:.45`」改为「**卡片转浅灰 `#FBFBFB` + 内容降饱和 50%**」，**标签保持全不透明**（否则被压暗后读不出来）。**R10 修订：标签删除；降权不再作用于单选圈（圈保持 100% 不透明）**。
> 3. **价目表按权益显示最优价（s5 会员开单页）**：会员持有折扣权益时，价目表价格区展示 **划线原价（12/400 `#B2B2B2`）+ 红色权益价（15/700 `#F32F41`）**，与「直接收款」会员态（s2/s3）**同一口径**。
>    - **取价口径**：取**该会员持卡中的最优「折扣」权益**（陈女士 = 超值折扣卡 **8.0 折**），与结算页自动匹配的权益**同源**；**计次卡 / 储值卡不参与价目表最优价**（按项目抵扣，规则不同）。
>    - **范围**：**仅 s5**；**s1 散客页维持单色原价**；**展开区「单价（元）」与「应付（元）」仍按原价**（权益仍在结算页折抵），**开单页底部金额与已选商品明细行不变**。
>    - 算法不变：`cartItems.price` 始终存**原价**，权益在结算页 `applyItemBenefit` 按原价折算 —— **价目表只改展示，不动任何金额链路**。
> 4. **新增「权益说明」弹窗**：s6 / s7 结算页「权益」行左侧的 **`?`** 由纯装饰改为**可点**（`cursor:pointer`），点击弹出**居中说明弹窗**（`proto-confirm` 同款视觉：标题「权益说明」+ 可滚动正文 + 红色「我知道了」），文案见 6.13.2。入口**全站仅此一处**（权益 sheet 内不加）。
>    - 点击用**捕获阶段拦截 + `stopPropagation`**：权益行本身是打开「选择权益」sheet 的热区，不能让行级 `onclick` 吞掉这次点击（`?` 位于 `.lbl` 内，与 `.val` 热区互不干扰）。
>    - 正文区 `max-height:440px`（内容约 430px → 通常不出滚动），超出才滚动；弹窗高度随内容自适应（不用 `max-height:72%`，避免被撑高后在按钮上方留出空白）。

> **R9 本轮变更摘要（2026-09-19）**：
>
> 1. **s6 底栏「应付」加可编辑图标**：应付金额右侧内联 **14×14 铅笔图标**（`#F32F41`，距金额 6px、与该行垂直居中），明示此处可点击改价。**R10 修订：改为与金额数字底对齐（基线对齐）**。**R14 修订：铅笔颜色 `#F32F41 → #929292`**（与垃圾桶统一为中性灰，避免与金额红抢色；对齐关系不变）。「应付 ¥{金额} + 铅笔」整组为一个**圆角 6px 的按钮热区**（按下底色 `#FFF2F2`），热区靠「按钮内边距 4/8 + 底栏内边距 8」向外扩（左沿 8px、高 37px、宽随金额自适应），**文字仍落在 16px 栏距上**（`#s6 .co-bar { padding-left:8px }`）。点击行为与 R6/R7 的改价规则完全不变；s7 不加此图标。
> 2. **s15「修改单号」sheet 整洁化**（对齐「修改应付」sheet）：**body 左右内缩 16px**；输入行 **高 56 / 圆角 10 / 白底 / 内距 14**（与按钮同宽；数字右对齐 16px，占位符 14px）；**上留白 16（距标题）、下留白 16（距按钮区）**；底部改为**等宽双按钮（取消 : 确定 = 1 : 1，各 174px + 10px 间距）**，**左右边与输入行对齐（16px）**；**点遮罩空白处关闭**。
> 3. **「选择权益」sheet 权益「不适用」置灰**（只对绑定项目可用的权益）：
>    - **规则**：**次卡类（`计次卡抵扣`）与固定金额类（`固定金额98元（美发卡3000元）`）只对「绑定项目」可用**，其余项目 / 产品**置灰不可选**；**折扣卡 / 储值卡演示为全场通用**（不置灰）。
>    - **演示绑定**（常量 `BENEFIT_SCOPE`）：`计次卡抵扣` → **洗剪吹、时尚造型**；`固定金额98元（美发卡3000元）` → **洗剪吹**。
>    - **置灰样式**：名称 `#B2B2B2` + 行尾 **「不适用」灰标签**（`#F2F2F2` 底 / `#929292` 字 / 10px / 圆角 3）+ **单选圈不透明 45%** + 行底 `#FAFAFA`；**整行不可点**（无 `onclick`、`aria-disabled="true"`），点击**不改变选中态**。**R10 修订：行尾标签删除，置灰与不可点保留**。
>    - **散客 / 无卡顾客**：列表**只保留「不使用权益」一行**（不显示任何卡权益，**不显示空态说明文案**）。
>    - **回落保护**：进入本页时若草稿选中项对当前明细**不适用**（或身份为散客 / 无卡），自动回落为「不使用权益」。
> 4. **s4「取挂单」恢复前置图标**：新增 `assets/ic_hold_take_nav.svg`（**20×20 / `#929292` / 1.6px 描边 / 圆角端点**；图形 = **单据 + 时钟**，时钟与单据边缘的留白由 `mask` 挖出），位于 s4 标题栏「取挂单（N）」文字左侧（间距 6px）。**仅 s4**；**s0 首页仍为纯文字**（R5 的口径对 s0 继续有效）。
> 5. **标题栏右侧文字与标题「基线对齐」（全局）**：所有标题栏右侧文字组整体下移，使**文字基线与标题（18px）基线重合**。
>    - 依据字体度量（Noto Sans SC：ascent `1.16em` / descent `.288em` / 行高 `1.448em`）：标题基线 = 10 + 20.88 = **30.88**；**14px 组**（24px 行内居中，基线 28.11）→ **下移 2.8px**；**12px 组**（基线 27.23）→ **下移 3.7px**；变量 `--nav-shift-14: 2.8px` / `--nav-shift-12: 3.7px`。
>    - **组内图标随文字一同下移**（保持图标与文字同轴）；实测各页右侧文字底缘与标题文字底缘差 **≤1.8px**（改前 4–5.5px）。
> 6. **s15 顶部渐变换色**（替换 R5 的黄色渐变；来源 Figma `RTB老版本优化` 节点 `1174-135`）：改为**纯纵向渐变（桃橙 → 杏黄 → 页面底色）**，**高 328**；色标见 6.23。**渐变末段精确落在页面底色 `#F7F7F7`（与页面无缝）**。
>    - **R5 的黄色渐变、装饰遮罩图层（`assets/suc_bg_mask.svg`，281 高光斑）与 68px 过渡带一并删除**；该 svg 不再被引用。
>    - **与 Figma 的一致性**：Figma 末段是「颜色 → `#FFF`」与「alpha → 0」同时插值，浏览器按预乘 alpha 插值会偏黄（中段蓝通道最大差 ≈24/255）；本稿按 Figma 曲线**预合成到 `#F7F7F7`** 后写成不透明色标，逐点实测与设计稿差 **≤1/255**。

> **R10 本轮变更摘要（2026-09-19）**：
>
> 1. **删除「修改应付」sheet 的提示行**：数字键盘 sheet 内的说明文案「改价后本单明细权益不再参与计算；优惠券仍可选用，但最终结算价以手输金额为准。」**整行删除**（元素 `.paysheet__tip` 一并移除）；腾出的留白补偿到数值行下沿（**下内距 2 → 18px**，数值区到键盘的间距 16px 不变体感）。**改价规则本身不变**（见 6.13.1）；**底栏上方的黄色状态说明保留**（它只在手输改价后出现，是必要的状态标识）。
> 2. **s15 顶部渐变层不透明度 84%**：`.suc-bg` 整层加 `opacity:.84`，叠在页面底色 `#F7F7F7` 上整体再淡一档；**渐变本身色标不变**，末端仍与页面无缝。
> 3. **s6 底栏铅笔改为「与金额数字底对齐」**：铅笔不再随 `align-self:center` 在行盒内居中，而是**参与 `.pay` 的 `align-items:baseline`** —— svg 无基线、以自身**底缘**为基线，于是**铅笔底缘正好落在金额数字的字形底缘（基线）上**（实测下移 1.7px，二者底缘差 0）。
> 4. **s12 / s13 去掉「不适用 / 暂不可用」标签 + 修正优惠券勾选圈**（**→ R14 修订：券卡勾选圈整类删除，本条中关于圈体定位与降权的结论随之作废；权益行仍保留勾选圈，见 R14 摘要 §2 / §3**）：
>    - **s12 权益行、s13 券卡**均**不再出现不可用标签**（`off-tag` / `cp-off` 元素与样式一并删除）；**不可用状态仍保留**（s12 整行置灰 + 不可点，s13 券卡浅灰 + 点击仅 toast）。
>    - **s13 勾选圈改为固定绝对定位**：`right:16px; top:47px`（**不再用 `top:50%` 随券卡高度浮动**），**圈底缘与右侧「门槛文案」行（如 `无门槛`）底缘对齐**（实测圈底偏移 = 门槛行底偏移 = 67）；**券卡展开「使用规则」时圈体完全不动**（原会下移 28px 并压住规则行 15.5px）。
>    - **勾选圈不再参与「不满门槛」的降权**：`is-off` 券的单选圈保持 **100% 不透明**（原先二次降权到 50%，细描边圆看起来「缺一块」），保证任何状态下都是一个完整圆。
> 5. **结算页（s6 / s7）新增「明细白卡盖住顾客卡片」的滚动效果**（与开单页同源）：
>    - **顾客卡片在本页改为 `position:sticky` 吸在滚动区顶部**（`top:12px`，即静止时的边距位置 → 一启动滚动就吸附、**零跳动**），滚动时**不再随内容上移**。
>    - **明细区 / 券·备注区抬到顾客卡之上**（`z-index:2`）且**整块不透明**（底色 = 页面底色 `#F7F7F7`，**静止时视觉零变化**）；卡片之间的 12px 空隙因此**不再「漏底」**（不会透出被盖住的顾客卡）。
>    - **首张明细卡上缘圆角 12px**，与开单页价目白卡（`.v2-price-card`）同一视觉语言。
>    - 滚动时滚动区加 `is-scrolled`，顾客卡降到 `z-index:1`（**与开单页 `.v2-bill-body.is-scrolled .v2-ccard { z-index:1 }` 完全同源**）。
>    - 效果：**明细白卡带着圆角上缘自下而上滑过，把顾客卡片逐步「吃」进白卡之下**（其余内容仍按文档流 1:1 滚动，无额外位移）。

> **R11 本轮变更摘要（2026-09-19）**：
>
> 1. **s15 支付明细：折扣卡不再出现** —— 折扣权益只参与**计算**整单结算价，**不参与支付**，因此**折扣卡不再作为支付明细的一行**；其折扣额**自然并入末尾的非卡支付行**（会员 → `微信`）。若一单只用了折扣卡，明细即为**单行 `微信` = 大号金额**。**同时补齐「套餐卡」「周期卡」两类的展示规则**（见下表），并在原型演示数据中新增这两类卡以保证闭环；**该位置会员卡消费字段的显示规则一律以线上现有版本为准**（原型仅作演示）。
>
>    | 卡种 | 主行右值 | 次行（灰字） |
>    |------|----------|--------------|
>    | 折扣卡 | **不出现在支付明细** | — |
>    | 储值卡 | 本单扣减金额 `¥xx` | `剩余` + 卡余额 |
>    | 套餐卡 | 本单消耗金额 `¥xx` | `剩余` + **剩余金额 / 剩余次数** |
>    | 计次卡 | 本单扣减次数 `N 次` | `剩余` + 剩余次数 |
>    | 周期卡 | 本单消耗次数 `N 次` | **无次行** |
>
> 2. **s15 顶部渐变层不透明度 84% → 70%**：`.suc-bg` 由 `opacity:.84` 调为 `opacity:.70`，叠在页面底色 `#F7F7F7` 上整体更淡；**渐变色标与高度不变**，末端仍与页面无缝。
> 3. **结算页「服务员工」行的上下留白以右侧文字为准**：员工行加**固定上下内距 10px**（行高 = 右侧员工文字块高度 + 20px）。单选 1 位员工时 `min-height:40px` 仍生效 → **与「权益 / 应付」单行行观感完全一致（零变化）**；选 2 位及以上时行盒随文字块长高，**文字不再贴住上下分隔线**（改前 2 行文字时行高 = 文字块 42px、上下留白为 0）。

> **R12 本轮变更摘要（2026-09-19）**：
>
> 1. **「选择服务员工」整套交互与 UI/UX 重做**（对齐「提成设置」的「选择服务员工」；默认态硬编码为 **按工位 + 开「顾客指定」**）：
>    - **废除旧的「两步翻转」选择**（旧的角色归属分支整类删除，不再有该维度）；改为点卡片**横向撑满整行、平铺选项卡**（大工 / 中工 / 小工 **可多选** + 「顾客指定」**独立可勾选**，二者互不排斥；点任一选项后等勾选动效播完再收起；要加选其它工位需再次展开）。
>    - **数据模型**：`staffDesignated` → `staffExtra`；`staffRoles[sid]` 由**单值字符串**改为**工位 id 数组**（可多选）；**去掉人数上限**（原 `STAFF_MAX_PICK = 3`）；旧结构读取时**自动迁移**。
>    - **摘要文案**统一为「姓名 · 工位（多工位拆行）· 顾客指定」；**只勾「顾客指定」也是合法已选**，不再前置「无工位」。
>    - **入口全覆盖**：添加项目 / 产品 sheet、直接收款 s2 / s3、结算页 s6 / s7 只读摘要、已选商品 Sheet、挂单卡片共用同一套渲染与摘要函数。
>    - **工位可点掉**（再点已勾选工位 = 取消）；收缩态员工卡**不再有任何勾选控件**（勾只存在于展开态选项卡），取消入口 = 展开卡片取消勾选 / 入口摘要行 `×`。
>    - 本轮交互细则见 **§6.11.1 – §6.11.8**。

> **R13 本轮变更摘要（2026-09-19）**：
>
> 1. **「选择权益」由整屏页改为底部 sheet（`#s12` 页面撤除）**：
>    - **删除**「确认并设为本单默认支付方式」提示行、底部「确认」通栏按钮、返回按钮与整屏骨架。
>    - **权益是单选，点中即完成选择** → 立即写回该明细权益、重算应付、**sheet 自动收起**，**不再有二次确认**。
>    - **点蒙层关闭 = 取消**（未点中任何行时权益不变，仅关闭）；不可用权益行仍置灰不可点（R9）；散客 / 无卡顾客仍只列「不使用权益」一行。详见 **§6.10**。
> 2. **「选择优惠券」同步改为同一套 sheet 交互（`#s13` 页面撤除）**：
>    - 删除底部「确认」通栏按钮与整屏骨架；**点券卡立即生效并收起**，「不使用优惠券」行同为立即清券并收起。
>    - **「使用规则」仍是独立热区**（展开 / 收起，**不触发选中、不关闭 sheet**）；不满门槛的券点击仅 toast、**不关闭**；点蒙层关闭 = 取消。详见 **§6.21**。
> 3. **s15 新增「打印小票」入口**：金额卡**右上角 28×28 纯图标热区**（18px 线性图标 `#B2B2B2`，按下极浅底）—— 非主要功能、**视觉优先级最低**，不占用操作组与底栏；点击 toast「**已发送打印小票指令**」（打印流程本身以线上为准）。详见 **§6.23**。
> 4. **结算页「服务员工」只读摘要长文本不再被裁切**：解除旧 `max-width:236px` 上限（改用满行可用宽 **302px**），并把工位摘要降为 **13px 灰字**；「3 工位 + 顾客指定」实测 **250px → 完整显示、不换行**（余量 52px），右边缘仍与其它「值」右对齐。详见 **§6.27**。
> 5. **左侧导航的「12 选择权益」「13 选择优惠券」**保留为**演示态入口**（不再对应任何 `.screen`）：分别以「无目标明细（9 行全部可选）」与「订单级券」打开对应 sheet，仅用于评审查看。

> **R14 本轮变更摘要（2026-09-19）**：
>
> 1. **两个 sheet 补齐留白与安全区**（§6.10 / §6.21）：内容区统一 **上 12px · 左右 12px · 下 12px + 34px 安全区**（`calc(12px + var(--safe-bottom))` = 46px）；**卡片之间、首卡与标题栏之间统一 12px**（权益行行距 **8 → 12**；末张券卡取消底部行距，底部只留 12 + 34）。
> 2. **选中态统一为「券卡语言」**：选中的权益行 / 券卡 / 「不使用优惠券」行一律 **1px 红色内描边 + `#FFF8F8` 底**（与券卡选中态同源）；权益行仍**保留右侧勾选圈**（圈与描边**并存**）。
> 3. **券卡删除勾选控件**（§6.21）：券卡不再有单选圈，「不使用优惠券」行的勾选圈**保留**；金额与门槛文案由 `right:52px` **右移到 `right:16px`**（与卡片内边距对齐），券名可用宽度 `209 → 260px`。
> 4. **结算页「优惠券」行已选文字不再用红色**（§6.13）：由 `--brand` 改为**主文字色 `#1a1a1a`**；该行右侧**进入箭头改为 SVG**（`viewBox 0 0 24 24`，14×14，`#C7C7CC`）—— 全仓**最后一处文字箭头**就此消除，**此类图标现全部为 SVG**。
> 5. **图标色统一 `#929292`**：结算页底部 BAR 的**铅笔**（原随金额为红）、**全部垃圾桶图标**（明细删除 / 直接收款删除 / 已选商品「清空」/ 挂单删除）统一 `#929292`。
> 6. **s15「打印小票」换用 Figma 稿图标**（§6.23）：改用 **Figma `1189-136`**（`viewBox 0 0 200 200`、满画布、3 条路径），尺寸仍 **18px**、颜色仍 **`#B2B2B2`**、热区仍 28×28 于卡片右上角。**（R19 已作废：该 200 画布填充稿缩到 17px 后线宽不足 0.5px、视觉不清，改为 24 画布 stroke 1.6 线描稿 18px，见 R19 摘要第 1 条）**
> 7. **s15 总金额 `¥` 与数值之间 +2 间距**（原两段紧贴，盒间为 0 → 现 `margin-right:2px`）。
> 8. **结算页标题栏新增「更多」菜单**（§6.28）：**3 圆点横排**图标、**主文字色 `#1a1a1a`**，24×24 热区、图标 20×20，**图标中心与 18px 标题墨迹中心对齐（实测差 0.00px）**；**s6 与 s7 都加**。点击弹出**下拉菜单**，两项 —— **「选日期」「手工单号」**（**左侧均有图标**，沿用成功页操作组的日历 / 铅笔，避免同类操作两套图标语言），点任一项打开对应 sheet；点菜单外任意处或切换页面即收起。
> 9. **命名与语义**（§6.28）：结算页下拉的两项命名为 **「选日期」「手工单号」**，**成功页操作组仍保留「改日期」「改单号」**（单号在成功页已生成，「改」更准），**同一个 sheet 的标题随入口切换**。
>    - **「手工单号」**＝手工输入一组号码，**代替开单成功时自动产生的单号**；**留空 = 不手工指定**，开单成功时自动生成。
>    - **「选日期」**＝手工指定本单的开单日期，**开单成功后的「开单日期」按所选显示**；**不选即默认今天**。开单日期在原型里**只有一处来源**（标题栏日期控件 / 成功页改日期 / 结算页选日期写同一个值），**不新增第二个字段**。
> 10. **s15「修改单号」sheet 改版 + 输入不叠层**（§6.29）：内容区**取消圆角、四边贴边、完全盖住 sheet 的灰底**（标题栏分隔线到页脚之间整段白底）；单号落在**视觉更弱的输入框**内（浅灰 `#F7F7F7` 填充、8px 圆角、**无描边无投影**，聚焦转 `#F2F2F2`），**点击整行即聚焦**且**默认全选**（直接输入即覆盖旧值）；**输入不再叠加任何子弹层**，手机演示时**唤起系统键盘**，并用 `--kb-h` 让浮层**避开键盘**（§6.30）。
> 11. **s15「开单成功」徽标的红色圆底由 `#FF1A2D` 改为 `#FF571A`**（`assets/suc_check.svg`）。

> **R15 本轮变更摘要（2026-09-19）**：
>
> 1. **s15「修改单号」sheet 的单号输入框定宽 240px 并靠右**（§6.29）：原 `flex:1` 会把输入框撑到 **318px（占 sheet 宽 82%）**，整行读起来是一条宽条、不像「字段」；改为 `flex:0 1 240px; margin-left:auto`，**右缘与页脚「确定」按钮右缘同列（距 sheet 右 16px）**，左侧自然留白。240px = **20 位数字满宽 182 + 内距 24 + 余量 34**，`maxlength=20` 不用改；窄屏自动收缩不溢出。
> **R16 本轮变更摘要（2026-09-20）**：
>
> 1. **价目表取消划线价（全局，s1 / s5 价目卡）**：不再出现「划线原价 + 红色权益价」双价，**任何身份只显示单色原价**，字色 `#929292`。**R8 §3「价目表按权益显示最优价」随本条作废** —— 会员权益不再在价目表预告，只在结算页「权益」行参与折算（`cartItems.price` 恒为原价，金额链路未变）。添加 sheet「价格（元）」默认值同步改为**原价**（不再预置权益价）。
> 2. **添加项目 / 产品 sheet："应付（元）" → "价格（元）"**；**添加项目 sheet 删除「数量」卡片**，删除后改为**通栏单列**（价格格占满整行）；**添加产品 sheet 保留数量卡片**（两列：数量 | 价格）。
> 3. **「选择服务员工」UI/UX 重构为槽位式**（见 §6.11）：**4 行工位**（大工 / 中工 / 小工 / 顾客指定）各带一个 **「＋」空槽位**；点 ＋ → 卡片区 **iOS 风格翻转为员工选择页**（横向卡片网格 + 取消；**R18 起不再显示「最多 3 人」提示**）；点卡片**即落位**（带落位反馈动效）并**回到槽位行**，右侧**补出新空槽位**；**每行最多 3 人**（满员时该行 ＋ 消失、选择页内其余卡片置灰）；**同一人可出现在不同行**、**同一行内不可重复**；**点已落位卡片 = 移除**。**R12 的「点卡片撑满整行平铺选项卡」翻转交互整体废弃。**
> 4. **s4 选择顾客行改版**（见 §6.8）：**删除「最近消费」整行**、**手机号移到姓名下一行**（13px `#929292`）；**删除行展开折叠区**（`is-open` 与 `#EFF0FF` 选中底一并移除，**整行本身不再可点**）；**每行右侧常驻一枚一体化分段胶囊「开单 | 收款」**。
> 5. **s6 / s7 结算页：改为按明细改价与改服务员工，应付总价只读**（见 §6.13.1）：
>    - 明细「应付」行可编辑（右侧 14×14 铅笔 `#929292`，整行热区）→ 数字键盘 sheet（标题「修改应付」，提示「原价 ¥xx」）；**清零 / 留空 = 恢复原价**（视为未改价，权益恢复自动匹配）；改价后**该明细权益锁定**。
>    - 明细「服务员工」行可编辑（右侧 `›`）→ 「修改服务员工」sheet（同一套槽位式选择器）。
>    - **底栏「应付总价」不再可修改**：删除铅笔、「已改价」标签、底栏上方黄色说明条，以及**整单改价（`coPayOverride`）的全部逻辑**；结账实收金额恒为系统计算值。
>    - **s7 同步支持**（R16 前 s7 为只读）。
> 6. **改名**：s4 标题栏「取挂单（N）」→ **「挂单（N）」**；s14 挂单卡「取挂单」→ **「去结单」**。**s0 首页入口仍为「取挂单（N）」（本条只改 s4 与 s14）**。
> 7. **s15 开单成功页**：「改日期 / 改单号 / 看流水 / 客户详情」四项**移到金额卡下方**（一行 **4 等分**白底托板 + 格间发丝竖线 + 每格竖排「图标 + 文字」）；**底栏只保留 3 项**：返回首页（图标）+ 客户签字 + 继续开单。
> 8. **s14 挂单卡 VIP 标签**换成**开单页顾客卡同款红色 VIP 图标**（`assets/ic_vip.svg`，26×10；原渐变胶囊 `#FFE6CE → #FFBA75` 作废）。
>
> 验收用例见模块 10.2 第 **309–330** 条。

---

> **R20 本轮变更摘要（2026-09-20）**：
> 1. **s14 挂单卡按钮文案定稿「去结单」**（§6.22 · §6.22.1）：R16 的「取挂单 → 去开单」再收敛为 **「去结单」** —— 该按钮的动作是**取回挂单进入结算**（不是「开单」），与 s4/s0 的「挂单（N）/取挂单」入口区分开。**功能零变化**：仍不删除挂单、仅还原顾客 + 明细 + 每行 `staffIds / staffRoles / staffExtra`，并按 `from` 回跳 s1/s5/s6。函数名与数据字段不动（`takeHold()` / `activeHoldId`）。
> 2. **选择服务员工按门店员工数分流为两套形态**（§6.11.1 · **§6.11.9**）：
>    - **员工 > 5 人 → 沿用「工位槽位行 + 员工选择页」**（R16 形态），只做两点修改：① 进入选择页时 **隐藏「价格（元）」卡**（`.is-picking`，值仍留在 input 内，退出选择页原样恢复）；② **选择页高度按内容自适应**（`.sp-face--pick` 由固定 `max-height:560px` 改为渲染后实测内容高写入 `--sp-pick-h`）+ **sheet 上限 85% → 92%** —— 4 行卡（12 人）实测 **592px**、5 行卡（15 人）**695px**，都在 776px 上限内，**卡片全部可见、sheet 不滚动**；6 行卡（18 人）起才滚动。
>    - **员工 ≤ 5 人 → 新的紧凑形态**（`.sp-cards`）：**4 个工位标题（大工 / 中工 / 小工 / 顾客指定）各自独占一行、仍在左侧**，标题下一行 **横列全部员工卡**，**第一张卡与标题左对齐**；**点卡即选中＝加入该槽位**（就地切换选中态 + 落位弹跳 + 勾标），**再点即移出**；某行满 3 人后该行其余卡置灰（`is-full`）并 toast「{工位}最多 3 人」；**不再有「＋」虚线槽位、也不再 morph 到选择页**。卡宽 **59**、间距 6：5 张 = 319px，最窄宿主（添加 sheet 内 322px）**恰好落满一行，不横滑、不换行、不滚动**。**R21：卡改为无头像的「姓名 + 职位」双行文字卡**（见 R21 摘要 / §6.11.9）。**R23：加回右上角勾选控件（14×14 挂在卡右上角外侧；未选中 / 置灰 = 白底 + 1px `#C7C7CC` 空心圈，选中 = 品牌实心圆 + 白勾，三态同一坐标零位移、0 遮挡）、「顾客指定」行间距 +6px**（见 R23 摘要 / §6.11.9）。
>    - **两套形态共用同一数据模型**（`staffIds / staffRoles / staffExtra`），摘要、结算、挂单还原、购物车拆行规则**完全不变**；切换阈值 = **门店在职员工总数**（`staffPool().length ≤ 5` → 紧凑形态）。
>    - **落点覆盖 4 个入口**：添加「设置内容」sheet、s6/s7「修改服务员工」sheet、s2/s3 直接收款员工面板。
> 3. **取消进入成功页的数字滚动动效**（§6.23）：删除 `sucCountUp()`（0 → 目标值 620ms easeOutCubic）与 `sucAmtRaf`，**金额进入本页即为终值**（一次 `textContent` 赋值）；对勾回弹、标题 / 操作组 / 金额卡 / 底栏的**错峰上浮淡入全部保留**。
> 4. **左侧导航新增演示入口「16 员工选择（12 人）」**（§3.0）：一手把员工池临时扩到 12 人并直接落到「选择大工」选择页，用于评审「> 5 人」分支；**关闭 sheet 即还原为 5 人**（演示数据 `STAFFS` 本体不变，多出的 7 人在 `STAFF_EXTRA`）。
> 5. **PRD 同步范围**：§3.0 结构图 / 页面清单、§6.11.1 / §6.11.9、§6.22 / §6.22.1、§6.23、模块 10.7（第 365–382 条）。

> **R23 本轮变更摘要（2026-09-21）**：
> 1. **≤ 5 人紧凑态：加回勾选组件（三态常驻同一位置 · 0 位移 · 0 遮挡）**（§6.11.9）：R21 曾因「59×52 文字卡上勾标会压姓名 / 职位」删掉勾标；R23 按需求加回，先试的「内嵌右上角 + 选中态文字整块左移让位」实机观感不佳已否决，最终定为 **方案 1「勾选控件挂在卡片右上角外侧」**：`.sp-card__tick` = **14×14**、`top/right:-4px`（绝对定位基准是卡内边距盒 → 实测外探出卡框 **3px**，只压圆角外的空白角落）；**未选中 / 置灰（`is-full`）= 白底 + 1px `#C7C7CC` 空心圈**（圈内无勾），**选中（`.is-on`）= 品牌实心圆 `#F32F41` + 8px 白勾**、描边同时转 `#fff` 把圆点与卡片品牌描边分开；切换只改 `background-color / border-color`（各 `.18s ease`）。
>    **三态同一坐标零位移**：控件是绝对定位、**不占卡内任何空间** —— 卡尺寸恒 **59×52**、`padding-right` 三态恒 **2px**、文字轴三态恒 **卡左 + 29.5**（实测未选中 `48.00/-3.00` = 选中 `48.00/-3.00`），**不存在「选中瞬间文字位移」**，姓名与职位**永远共轴**。
>    **实测 0 遮挡**：控件圆（半径 7）与姓名 / 职位的内容盒（`Range` 实测）**均不相交**；控件外探的 3px 落在宿主内距里（最窄宿主「添加 sheet」：卡列 322，外层 `.addsheet__body` 还有 12px 内距），**不被 `.sp-face{overflow:hidden}` 裁掉**（实测最小余量 32px）。
>    **置灰卡照常显示**：置灰卡的圈与未选中同款，随整卡 `opacity:.4` 一起淡出 —— **三态口径统一：未选中 / 置灰 = 空心圈，只有选中 = 实心勾**；**> 5 人选择页的 `.sp-pick__tick`（16×16）不动**。
> 2. **> 5 人槽位卡：右上角补「删除 ×」角标**（§6.11.1）：`.sp-slot__x` = **14×14 `#C7C7CC` 实心圆点 + 8px 白 ×**，`top:1px / right:1px`，**`pointer-events:none`**。**删除热区与以前完全一致 = 整张卡**（`data-sr-rm` 与 `aria-label="移除 {姓名}"` 仍挂卡根，**未新增任何独立热区**）—— 角标只是把「点一下 = 移除」这件看不见的事**画出来**。
>    **实测不压任何内容**：角标 14×14 落在 **x 46.5–60.5**，头像右缘 **41.25**（留 5px）、姓名在 **y 34–47** 与角标 **y 1.5–15.5** **垂直不重叠**；`elementFromPoint(× 中心)` 命中的是 **`BUTTON.sp-slot` 本体**，点该位置仍正常移除。
> 3. **「顾客指定」行加大与「小工」的间距**（两套形态同步）：`.sp-row--extra` / `.sp-cards__row--extra` 各加 **`margin-top: 6px`** —— 槽位形态行间 **14.5 → 20.5px**、紧凑形态行间 **6.5 → 12.5px**；发丝线仍贴在该行顶部（随之一起下移），「顾客指定」从「第四个工位」变为**一眼可分的另一组**。
> 4. **范围与实测**：3 项改动覆盖 **4 个入口**（添加「设置内容」sheet、s6·s7「修改服务员工」sheet、s2·s3 直接收款面板）+ **> 5 人选择页**；无头浏览器 **211 项断言全通过**（含空心圈 / 实心勾三态同坐标零位移、控件圆与文字内容盒不相交、控件不被裁剪祖先裁掉、卡列 `scrollWidth = clientWidth`、s2 面板 `max-height:388px` 不滚动、> 5 槽位卡删除 × 整卡热区仍生效）。
> 5. **PRD 同步范围**：§6.11.1、§6.11.9（卡规格 / 三态表 / 对照表）、用例 **383 反转**（由「没有 `.sp-card__tick`」改为「有、三态常驻且不遮字」）+ 模块 10.7 新增 **391–400**。

> **R22 本轮变更摘要（2026-09-20）**：
> 1. **问题**：≤ 5 人紧凑态员工卡，「选中 / 未选中」**视觉上没有差别**。无头浏览器实测确认根因 —— 两态的**姓名计算样式完全相同**（均为 `#1A1A1A / 600`）、**职位也完全相同**（均为 `#8E8E93 / 400`）：R21 改文字卡时，`.sp-card.is-on .sp-card__nm / __role` 的取色沿用了「与未选中同色」的值，这两条规则当时等于**空操作**。唯一真实差异只剩 1px 品牌描边（淡粉底渐变最浅端 `#FFF8F8` 几乎等于白色，在白 sheet 上感知不到）。
> 2. **采纳方向（经确认）**：**选中态一行不改**，改为**把未选中态压弱** —— 描边 `#E5E5EA → #EDEDED`、姓名 `#1A1A1A / 600 → #55595E / 500`、职位 `#8E8E93 → #A6A6AB`。这样「选中 = 深字 + 品牌描边 + 淡粉底」与「未选中 = 浅字 + 发丝描边」从两头拉开，同时 R21 那两条「空操作」规则（`.is-on` 钉死 `#1a1a1a` / `#8E8E93`）**正式生效**。
> 3. **选中态与置灰态均保持原样**（仅加防回归护栏）：`.sp-card.is-on .sp-card__nm` 补 `font-weight: 600`（否则会被压弱后的 500 带跑）；`.sp-card.is-full .sp-card__nm / __role` 新增，把置灰卡钉回 `#1a1a1a / #8E8E93` —— 压弱后置灰卡会跟着变浅（40% 透明合成值 `#A3A3A3 → #BBBDBF`，对比度 **2.52:1 → 1.88:1**，看不清是谁），钉回后置灰态观感与 R22 之前**完全一致**。
> 4. **三态实测（无头浏览器计算样式 + WCAG 对比度）**：未选中 `#55595E / 500` **7.05:1**、选中 `#1A1A1A / 600` **≈15.8:1**、置灰（合成后 `#A3A3A3`）**2.52:1** —— 三档层次清晰且都可读。卡尺寸仍 **59×52**，`scrollWidth = clientWidth = 322`，**不横滑不换行不滚动**。
> 5. **范围**：5 条规则都作用在 `.sp-card` 同一组类上，因此 **4 个入口一并生效**（添加「设置内容」sheet、s6·s7「修改服务员工」sheet、s2·s3 直接收款面板），已逐个实测（24 项断言全通过）。**> 5 人选择页（`.sp-pick`）不受影响**。
> 6. **PRD 同步范围**：§6.11.9（卡面色值改为三态表）、用例 370（改写为三态色值）、模块 10.7 新增 385–390。

> **R21 本轮变更摘要（2026-09-20）**：
> 1. **≤ 5 人紧凑态员工卡改成无头像的文字卡**（§6.11.9）：去掉圆形头像；卡内改为 **上＝姓名（13px / Semibold 600 / `#1A1A1A`）+ 下＝职位 `st.role`（11px / Regular 400 / `#8E8E93`）**；工位仍由行标题「大工 / 中工 / 小工 / 顾客指定」表达，**卡内不重复写工位**。
> 2. **iOS 轻量外壳**：圆角 **10 → 12**、描边 **`#EDEDED` → `#E5E5EA`**、卡高 **62 → 52**（去头像后略矮）；选中态仍为品牌描边 + 淡粉底 + 右上角勾标；按下 `scale(.95)`、落位弹跳、满 3 人置灰等交互**不变**。
> 3. **范围**：仅紧凑态（`.sp-card`）；**> 5 人选择页（`.sp-pick`）仍保留头像 + 姓名 + 职位**，不动。覆盖添加 sheet / s6·s7「修改服务员工」/ s2·s3 直接收款面板。
> 4. **去掉紧凑态右上角勾标**：选中只靠**品牌描边 + 淡粉底**（`aria-pressed` 保留）；59×52 文字卡上勾标会压姓名/职位。**>5 选择页勾标不动**。
> 5. **落位弹跳按「员工 id + 工位行」双键**：只让**刚点的那一张**播 `is-land`；同人在其它行的卡只变选中态、不连带弹跳（修 R20 用纯 `spLandSid` 导致跨行同人一起动的问题）。
> 6. **PRD 同步范围**：§6.11.9、R20 摘要中紧凑卡规格句、模块 10.7 用例 370 / 372 / 383（及新增 384）。

> **R19 本轮变更摘要（2026-09-20）**：
> 1. **s15「打印小票」换图标**（§6.23）：R14 的 Figma 稿 `1189-136` 是 **200 画布填充式**，最细壁厚 5.3/200 —— 缩到 17px 后**渲染线宽不足 0.5px**，叠 `#8E8E93` 浅灰在真机上看不清。改为 **24 画布 stroke 1.6 线描稿**（进纸 + 机身 + 出纸 + 1 枚指示灯；`fill:none` / `stroke:currentColor` / `linecap,linejoin:round`），**渲染线宽 ≈ 1.2px**；尺寸 **17 → 18px**，颜色仍 `#8E8E93`（次要层级不变）。
> 2. **s15「打印小票」去按钮感**（§6.23）：**圆角 12 → 0**、**删除 `:active` 整行浅底 `#F7F7F8`** —— 原先「圆角 + 按压底色」读起来像浮在卡片上的按钮，与卡片里无圆角、无底色的字段行不是一套语言；改为**纯行**：上方仅 **0.5px 实线 `#E5E5E5`**（字段行之间是虚线，这一条是实线：上＝信息、下＝操作），**行高 46 → 44px** 对齐字段行 `min-height:44`，卡片下半部行距完全均匀。
> 3. **按下反馈改为「仅加深」**（§6.23）：`:active` 时**文字 `#55595E → #1A1A1A`、图标 `#8E8E93 → #55595E`**（`transition: color .12s ease`），**不出现任何底色**；整行仍是热区（**319×44**）。
> 4. **几何随之微调**（§6.23）：行高 −2px → 卡高 **286.3 → 283.7px**、卡底 **436 → 434px**；`.suc-body` 的 `top` **仍为 138px**（无需再动），卡内仍**不触发滚动**。
> 5. **PRD 同步范围**：§3.0 结构图 / 页面清单、模块 8 R14 图标补充、§6.23、模块 10.6（第 353–364 条）。
> **R18 本轮变更摘要（2026-09-20）**：
> 1. **直接收款页返回目标修正**（§6.6）：s2 标题栏左侧返回键由 `go('s1')` 改为 **`go('s4')`** —— **从「直接收款」返回一律回到「选择顾客」**（原来错跳散客开单页，与「顾客在 s4 里挑」的信息架构不一致）。s3（去收款）返回仍回 s2，未受影响。
> 2. **员工选择页删除「最多 3 人」提示**（§6.11.1）：标题行只保留 **「选择{行名}」+「取消」**（`.sp-picker__hd .s` 元素与样式一并删除）；**每行上限 3 人的规则完整保留** —— 满员时该行「＋」消失、`spRowFull()` / `spAddToRow()` 仍拦截第 4 人、`SP_ROW_MAX` 仍为 3。
> 3. **「顾客指定」行标签改为主文字色**（§6.11.1）：R17 的橙 `#F97316` **作废**，第 4 行行名与前三行统一为 **`#1a1a1a`**（只改颜色，4 行结构、槽位、摘要文案均不变）。

> **R17 本轮变更摘要（2026-09-20）**：
> 1. **s4 行内「开单 | 收款」去红**（§6.8）：由 R16 的「实心红分段胶囊」改为 **iOS 列表语言的「纯文字 + 发丝竖线」** —— **无底、无框、无描边**，**「开单」= `#F32F41` 13/600（主）**、**「收款」= `#8E8E93` 13/500（次）**，中间 **1px × 14px `#E5E5E5` 竖线**；每段热区高 **44px**。一屏 17 行的**红色面积由「17 个实心红块」降为「34 个字」**（约 −96%），控件总宽 **101px**（R16 103px）。
> 2. **s4 胶囊与 A–Z 字母索引条避让**（§6.8）：实测原「胶囊右缘 378 / 索引条 370–388」**重叠 8px**（首字母 x 375.8–382.2 压在红块上）；**行内距右 12 → 30px**（胶囊右缘 **360**），与索引条**净空 10px**，重叠归零。
> 3. **sheet 内「顾客指定」行标签改用橙色**（§6.11）：`#F32F41` → **`#F97316`**（**仅该行标签**；大工 / 中工 / 小工 仍 `#1a1a1a`；橙不参与「主操作红」语义，避免与红冲突）。**（R18 已作废：改回主文字色 `#1a1a1a`，见 R18 摘要第 3 条）**
> 4. **s15「打印小票」入口重排**（§6.23）：**删除金额卡右上角 28×28 纯图标热区**（同一功能不留双入口），改为**金额卡底部一整行**——图标 **17px `#8E8E93`** + 文案「打印小票」**13px `#55595E`** 居中，**上方 0.5px 灰色实线**（字段行之间仍是虚线：实线明确「上面是信息、下面是操作」），**整行即热区（319×46）**，按下整行浅底 `#F7F7F8`。**（R19 已改：图标换 24 画布 stroke 1.6 线描稿、17 → 18px；圆角 12 → 0、去掉按压浅底，改为文字/图标加深；行高 46 → 44px，见 R19 摘要第 2–4 条）**
> 5. **s15 金额卡上移**（§6.23）：内容区 `.suc-body` 的 `top` **182 → 138px**（上移 44），补掉卡内新增的 46px 入口行 —— 卡底实测 **440.5 → 436px**（仅差 4.5px），卡外 4 等分操作组与底栏位置不变，且**不触发内容区滚动**。
> 6. **PRD 同步范围**：§1.3 交付要点、§3.0 结构图 / 页面清单、§6.8、§6.11.1、§6.23、模块 8 R14 图标补充、模块 10.4（第 331–342 条）。

## 怎么读这份 PRD

正文按模块编排。**不用通读全文**，按角色跳章即可。

| # | 模块 | 前端 | 后端 | 测试 | 说明 |
|---|------|:----:|:----:|:----:|------|
| 1 | 背景 / 目标 / 范围 / 能力映射 | ✓ | ✓ | ✓ | 本期变更叙事、防范围蔓延 |
| 2 | 用户场景与成功标准 | ✓ | 了解 | ✓ | 测「业务对不对」 |
| 3 | 信息架构与页面清单 | ✓ | 了解 | ✓ | 做哪些页、哪些层 |
| 4 | 状态机 | ✓ | **必看** | **必看** | 顾客身份、性别、会员卡展开、购物车、直接收款明细 |
| 5 | 交互细则 / 异常流 | ✓ | 了解 | ✓ | 输入规则、校验、toast |
| 6 | 功能规格 | ✓ | 了解 | ✓ | 逐屏字段与跳转 |
| 7 | 数据模型与接口约定 | ✓ | **必看** | ✓ | 沿用线上接口；新增字段【待联调确认】 |
| 8 | 业务规则 | ✓ | **必看** | ✓ | 金额口径、改价、范围边界 |
| 9 | 文案清单与权限 | ✓ | 部分 | ✓ | 文案；店员/店长/店主 |
| 10 | 验收 P0 + 造数场景 | 了解 | 了解 | **必看** | Given→When→Then，含输入边界 |
| 11 | 非功能（极简） | ✓ | ✓ | ✓ | |
| 12 | 交付物 | ✓ | — | ✓ | 仅 PRD + 原型 |

**先读这几条（贯穿全文）**

1. 本期是基于 **RTB 线上开单结账** 的 **优化改版**，**不改变其他未涉及的功能逻辑**。凡本文未单独描述的交互、文案、接口，均沿用线上现有实现。
2. 页面顶部顾客卡片变化：深色卡统一改为 **粉白淡色卡**（圆角 12）；**点头像或姓名进入顾客列表（s4）**；「更换顾客」按钮已移除。顾客列表新增「男散客 / 女散客」选项与 **「添加会员」行（行下展快捷添加会员表单）**，列表内 **VIP 标签按最新 Figma 资源适配**。
3. 从顾客列表选择 **会员** 后，开单结账页顶部卡片切换为 **会员样式**（粉白淡色卡）：头像 / 姓名 / VIP 标签 / 上次到店；**会员持卡列表默认收起，右侧 chevron 可展开/收起（展开＝一行一张卡：卡类型+卡名 | 剩余权益，与线上一致）**；**「更换顾客」「办卡」按钮均已移除，点头像或姓名进入顾客列表（s4）**。字段显示规则维持现有规则。**VIP 标签按最新 Figma 资源（渐变 `VIP`）适配尺寸与位置**；开发时保持该位置现有 VIP 标签逻辑不变。**（R6：标签内最左的黑色方块 `◆` 已删除。）**
4. 添加项目 / 产品时的 **UI UX 是本期最大的修改**：参照博卡小店通的 sheet 弹窗样式，按现有逻辑做了适配；弹窗内展示 **价格（元）**（R16 由「应付（元）」改名，带笔形编辑图标；**添加项目不设数量卡片、价格格通栏**，**添加产品保留数量卡片**），并在弹窗内**内嵌「选择服务员工」槽位式选择器**（4 行工位各带「＋」空槽位；点 ＋ 翻转到员工选择页，点卡片即落位、每行上限 3 人，见 §6.11）。**同项多次加购时，若价格或服务员工不同必须拆成多行**；购物车有项后点底部左侧热区打开 **已选商品 Sheet**，点「下一步」进入 **开单结算页（s6）**。
5. **新增「直接收款」功能**：无需常规流程的繁琐操作，**只需输入金额即可完成开单结算**，并 **可选填** 服务员工信息和开单内容。为线下调研后决定加入的重要功能；其结算第二步为 **直接收款结算页（s7）**，**金额按原价计算，不做身份折扣**。**直接收款与小程序「自助买单」的纯金额支付不是同一种功能、互不相关**。
6. **补充展示页面**：门店流水（s8，直接收款在流水中的展示）、选择退款项目（s9，直接收款退款）。**入口为原型左侧导航直达，不承载业务链路**；对应业务逻辑以线上为准。**其中「经营分析 → 门店经营报表」的项目 TOP5 排行不包含「直接收款」**（经营分析属另一产品线，此处仅记录本侧规则）。**原 s10 业绩设置、s11 提成方案已删除**（线上模块已重构，详见 R6 §1）。
7. 该模块的 **其他功能保持现有状态**（挂单、支付、会员卡业务等均以线上为准）。**本期已移除会员信息卡上的「办卡」与「更换顾客」按钮**（入口改为点击头像/姓名进 s4）。**开单结账页（s1/s5）原有的搜索栏本期已删除**，原型与文案均不再包含；搜索能力如线上另有入口，以线上现有版本为准。
8. **开单结算 s6 / 直接收款结算 s7 的交互效果与各类弹窗（员工面板、权益选择 sheet、备注 sheet、小计编辑、toast 等）以当前在线版本为准**，原型中的仅为评审演示，原型细节不作为开发验收约束；本 PRD 已明确写出的本期业务规则（直接收款权益、按明细改价与改服务员工、工位槽位式选择、价目单色原价、s4 行内「开单｜收款」纯文字化与索引条避让、sheet「顾客指定」橙色、s15 卡内底部「打印小票」入口等）仍为开发约束。
9. 输入与联动口径（原型已同步）：金额格式 **整数≤6位、小数≤2位、上限 999999.99**；直接收款 **空金额不可「去收款」**；开单内容 **≤50 字**；应付/小计 **可手动编辑**（对焦即清空，失焦为空时还原），**改价后该明细不可使用权益**（改价判定以「应付/小计 ≠ 单价/权益优惠价 × 数量」为准）。

交互行为与文案以 **`index.html` 当前表现** 为准。

---

# 模块 1　背景 / 目标 / 范围 / 能力映射
> 读者：前端 ✓ · 后端 ✓ · 测试 ✓

## 1.1 背景

RTB 线上开单结账经过一段时间运营，线下调研反馈：

- 常规开单需先选顾客、选项目、配员工、填备注，流程较长，存在「只想快速收一笔钱」的场景；
- 顾客卡片与顾客列表在性别表达、头像素材上体验欠佳；
- 会员所持权益信息默认折叠，收银员核对权益时需要额外操作；
- 添加项目/产品时的信息组织与操作路径不符合当前门店操作习惯；
- 直接收款缺少结算第二步、流水展示、退款等闭环展示。

本期在不改变其他功能逻辑的前提下，对开单结账做定点优化，并为直接收款补齐逻辑补充页面。

## 1.2 目标

| 目标 | 说明 |
|------|------|
| 直接收款 | 仅输入金额即可开单结算，选填员工与开单内容 |
| 顾客卡片优化 | 全页统一粉白淡色卡；点头像 / 姓名进入顾客列表（s4）；移除「更换顾客」按钮 |
| 顾客列表优化 | 新增「男散客 / 女散客」散客选项与「添加会员」行（行下展快捷添加会员表单）；VIP 标签按 Figma 适配 |
| 会员权益可见 | 会员持卡列表默认收起，右侧 chevron 展开/收起（一行一张卡：卡类型+卡名 | 剩余权益）；移除「办卡」「更换顾客」按钮 |
| 价目卡价格 | **R16：任何身份只显示单色原价（`#929292`）** —— 原「会员权益价：划线原价 + 红色优惠价」作废；添加 sheet「价格（元）」默认 = 原价 × 数量 |
| 添加体验升级 | 参照博卡小店通 sheet 弹窗样式，数量 + 应付（可编辑）+ 内嵌选择服务员工（工位多选 + 顾客指定） |
| 开单结算页 | 购物车明细在结算页 s6 完整展示（明细卡/权益/应付/优惠券/挂单/结账 → s15） |
| 直接收款结算 | 直接收款结算第二步 s7（直接收款明细/权益/应付/优惠券/挂单禁用/结账 → s15），金额按原价 |
| 直接收款权益 | s7 默认自动匹配（会员=最优折扣卡、散客=无卡权益）；可改选会员卡储值金额、全场项目适用的计次/折扣、全场项目或产品可用的优惠券、满足金额门槛的代金券 |
| 单号 | s6/s7 不再展示单号卡片；单号在 s15 成功页「改单号」维护（数字、1-20 位，留空恢复系统自动生成） |
| 挂单 / 取挂单 | 挂单成功 → 挂单池 + s14 挂单列表；s0 首页与 s4 标题栏「取挂单（N）」入口；直接收款不可挂单 |
| 无产品 Tab | 价目表为空时隐藏对应 Tab；两个都空 → 两个 Tab 都显示 + 空态说明 |
| 逻辑补充页面 | 门店流水 s8、选择退款项目 s9、挂单列表 s14、开单成功 s15（原 s10/s11 已删除，见 R6 §1） |
| 直接收款边界 | 明确直接收款与小程序「自助买单」的纯金额支付不是同一种功能（见 8） |
| 经营分析关联 | 直接收款不计入「经营分析 → 门店经营报表」项目 TOP5 排行（见 8） |
| 回归可控 | 其余功能沿用线上，避免范围蔓延 |

## 1.3 本期做（In）

- 顶部顾客卡片：男女头像更换；点击头像切换散客性别（男散客 / 女散客）；卡片展示性别文案；**点击「男散客 / 女散客」文字进入顾客列表（s4）**
- 顾客列表：新增「男散客」「女散客」选项与「添加会员」行（行下展快捷添加会员）；会员 VIP 标签按最新 Figma 资源适配
- 会员卡：改为 **粉白淡色卡 + 持卡列表默认收起（右侧 chevron 展开/收起）**；**移除「办卡」「更换顾客」按钮**，点头像/姓名进 s4；VIP 标签按最新 Figma 资源适配
- 添加项目/产品 sheet 弹窗：标题（**仅项目名，价格行本轮已删除**）、**价格（元）**（R16 由「应付（元）」改名，带笔形编辑图标，对焦清空、失焦还原）、服务员工（产品为销售人员）—— 槽位式选择器（见 §6.11）、确定；**添加项目不设数量卡片**（价格格通栏），**添加产品保留数量卡片**
- 选择服务员工：点卡片展开选项卡（大工/中工/小工**多选** + 「顾客指定」独立可勾选），多选员工、保留已选、**不设人数上限**（R12）
- 直接收款：金额键盘直接开单 + 选填员工/开单内容（≤50 字）+ 去收款
- 价目卡价格：**R16 起任何身份都只显示单色原价 `#929292`**（原「划线原价 + 红色优惠价」作废）
- 开单结算 s6：购物车明细结算页（明细卡、权益自动匹配/选择、应付、优惠券、挂单、结账 → s15）
- 直接收款结算 s7：直接收款结算第二步（红色闪电直接收款明细、权益、应付、优惠券、挂单、结账 → s15；金额按原价）
- **顾客卡片折叠（s1/s5）**：卡片默认收起，标题栏右侧顾客入口（**散客 = 双人 icon + 「散客 / 男散客 / 女散客」；会员 = 单人 icon + 姓名；两态 icon 均 `#929292`**）点击展开、再点收起（iOS 动效）
- **商品「设置内容」sheet**：点圆形加号 → 底部 sheet 设置**价格（元）/员工**（产品另有数量），点确定入购物车（**删除行内展开区与滚动视差吸顶**）
- **价目表为空处理**：项目/产品其中一个为空 → 隐藏对应 Tab；两个都空 → 两 Tab 都显示 + 空态说明（管理入口本期仅说明）
- **挂单与去结单**：挂单成功 → toast + s14 挂单列表；s0 首页为「取挂单（N）」、s4 标题栏为「挂单（N）」（R16 改名）；s14 卡片按钮「去结单」→ 回来源页
- **开单成功页 s15**：无标题栏 + 成功徽标；金额卡（**卡内底部整行「打印小票」入口，R17 · R19 去按钮感 + 换线描图标**）；卡下 4 等分操作组 改日期/改单号/看流水/客户详情（R16）；底部 bar 返回首页图标 + 客户签字/继续开单
- **逻辑补充页面（原型左侧导航直达）**：s8 门店流水（直接收款展示）、s9 选择退款项目、**s14 挂单列表、s15 开单成功**；**R20 另增「16 员工选择（12 人）」演示入口**（§6.11.9，仅评审用；关闭 sheet 即还原 5 人）
- 下一步：购物车有项 → 进入 **s6 开单结算页**

## 1.4 本期不做（Out）

- 支付 / 收款 / 收银流水（沿用线上；原型以「结账 → s15 开单成功」示意成功结果，**真实支付流程不实现**；s15 为结果展示页）
- 储值/计次卡核销、会员卡业务（办卡/开卡入口不在本期范围，原型已移除会员卡「办卡」按钮，业务逻辑沿用线上）
- ~~线上自有购物车明细 sheet~~（本期已按 Figma 重做「已选商品」Sheet，见 §6.12；s6 内明细展示与删除仍保留）
- **挂单的真实接口与字段**（原型实现挂单池 + s14 列表用于演示，线上落地字段/接口以线上为准）
- **电子签字板与顾客详情页**（s15 的「客户签字 / 客户详情」本期仅 toast 示意）
- **「管理价目表」入口页面**（两价目表都为空时只做说明，不实现管理页）
- 顾客列表搜索 / 新增顾客 / 编辑顾客（沿用线上）
- s8-s9 的业务链路（流水详情、退款审批）以线上为准，原型仅展示页面与交互示意
- 业绩设置 / 提成方案（原 s10 / s11）：线上模块已重构，原型不再展示（见 R6 §1）
- 多门店、权限体系改造（沿用线上现有实现，本期不改造）

## 1.5 旧 → 新能力映射

| 旧体系能力（语义） | 新体系如何表现 |
|--------------------|----------------|
| 散客卡仅展示固定头像 | 全页统一粉白淡色卡；点头像/姓名进 s4；无「更换顾客」按钮 |
| 选择顾客仅会员列表 | 列表顶部新增「男散客」「女散客」散客选项 + 「添加会员」行下展快捷添加会员 |
| 会员卡默认折叠权益 | 持卡列表默认收起，右侧 chevron 展开/收起（展开＝一行一张卡） |
| 添加项目/产品信息组织分散 | sheet 弹窗：标题 + 数量/应付 + 内嵌选员工 + 确定 |
| 无快速路径 | 直接收款：输入金额即可开单，选填员工/开单内容 |
| 常规开单无结算明细页 | 购物车有项 → 下一步 → s6 开单结算页（明细/权益/小计/应付/挂单/结账） |
| 直接收款 → 结账 | 金额 → 去收款（s3）→ 直接收款结算第二步（s7）→ 结账 → **s15 开单成功** |
| 开单完成后无成功页 | s6/s7 结账成功 → toast「结账流程说明」+ **s15 开单成功**（金额计数 + 对勾回弹动效；改日期/改单号/看流水 + 返回首页/客户签字/客户详情/继续开单） |
| 挂单无独立列表 | 挂单成功 → 挂单池 + **s14 挂单列表**（s0 首页 / s4 标题栏「取挂单（N）」可进入） |
| 顾客卡片常驻占位 | s1/s5 卡片**默认收起**，标题栏右侧顾客入口点击展开/收起（iOS 动效） |
| 商品设置靠行内展开区 | 点圆形加号 → **底部「设置内容」sheet**，确定后入购物车（行内展开区 + 视差吸顶已删除） |
| 直接收款无闭环展示 | 门店流水 s8（流水展示）、选择退款项目 s9（退款） |
| 应付金额静态数字 | 应付/小计可编辑（笔形图标、对焦清空、失焦还原），改价后权益锁定 |
| 价目只显示原价 | **R16：任何身份只显示单色原价（`#929292`）**；添加 sheet「价格（元）」默认 = 原价 × 数量（划线价 / 权益价取消） |
| 直接收款 9 折（会员） | 直接收款默认按原价，不做身份折扣；**权益默认自动匹配最优折扣卡**（与 s6 一致，可手动改选，四类权益见 8） |
| 直接收款与自助买单同源 | 明确区分：直接收款是门店收银侧开单能力，与小程序「自助买单」纯金额支付无关（见 8） |

映射不足时以原型字段与行为为准。

---

# 模块 2　用户场景与成功标准
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

## 2.1 角色

| 角色 | 说明 |
|------|------|
| 店员 | 日常收银开单 |
| 店长 | 日常收银开单、门店管理 |
| 店主 | 门店最高权限 |

本期 **仅列角色**，**不定义角色间权限差异**；操作权限沿用线上现有规则。

## 2.2 核心场景

| 场景 | 用户期望 | 成功标准 |
|------|----------|----------|
| 散客开单 | 新客不建档案直接开单 | 顶部显示散客卡；可切换性别；加购项目/产品后下一步进入 s6 结算 |
| 会员开单 | 老客识别身份、查看权益 | 选择会员后卡片切换为会员样式；权益全部展开可见 |
| 直接收款 | 只需输入金额即可收银 | 输入金额 + 选填员工/开单内容 → 去收款 → 直接收款结算 s7 |
| 更换顾客 | 开单中换人 | 顶部卡片、身份状态同步切换 |
| 添加项目 | 加购服务并指定员工、调整应付 | sheet 弹窗数量/应付联动正确；员工可多选；应付可编辑 |
| 结算明细 | 结账前核对明细、权益 | s6/s7 明细完整展示；改价项权益锁定；小计可编辑 |
| 流水查看 | 查看直接收款在流水中的展示 | s8 中直接收款以「⚡直接收款」项展示，含金额/服务人/支付方式 |
| 退款 | 对直接收款发起退款 | s9 全选/部分选择联动；退回方式按选择范围约束 |
| 业绩/提成配置 | 配置直接收款的业绩与提成比例 | **本期不涉及**：线上模块已重构，原 s10/s11 原型页面已删除（见 R6 §1） |

---

# 模块 3　信息架构与页面清单
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

```
开单结账
 ├─ s0 简化首页
 │    ├─ 开单 / 开卡 两大入口
 │    └─ 描边小按钮「取挂单（N）」（N = 挂单实时条数）
 ├─ s1 散客开单
 │    ├─ 标题栏右侧顾客入口（散客：双人 icon + 「散客 / 男散客 / 女散客」；会员：单人 icon + 姓名；两态 icon 均 #929292）
 │    ├─ 顶部散客卡（双头像·性别文案·选择顾客）**默认收起**，点标题栏右侧区域展开 / 再点收起（iOS 动效）
 │    ├─ 项目/产品 Tab + 直接收款胶囊（Tab 上方）
 │    ├─ 左侧分类（选中白底）+ 项目/产品卡片（圆形加号）
 │    └─ 底部 hint-bar + bottom-bar（左侧热区打开已选商品 Sheet / 应付 / 挂单 / 下一步）
 ├─ s2 直接收款
 │    ├─ 顶部身份卡（散客/会员 按身份切换）
 │    ├─ 开单内容 / 收款金额 / 员工
 │    └─ 数字键盘 + 去收款
 ├─ s3 直接收款-去收款
 │    ├─ 顶部身份卡（同上）
 │    ├─ 开单内容 / 收款金额（只读显示，键盘可改）/ 员工
 │    └─ 数字键盘 + 去收款（→ s7）
 ├─ s4 选择顾客
 │    ├─ 搜索栏 + 取消 + 标题栏右侧「取挂单（N）」
 │    ├─ 散客（男散客 / 女散客）
 │    ├─ 会员列表（第一行 姓名 + VIP + 会员卡数 (N) / 第二行 手机号；R16 删除「最近消费」）
 │    └─ 字母索引 A-Z#
 ├─ s5 会员开单
 │    ├─ 标题栏右侧顾客入口（会员：单人 icon + 姓名，icon #929292）
 │    ├─ 顶部会员卡（权益全部展开）**默认收起**，点标题栏右侧区域展开 / 再点收起（iOS 动效）
 │    ├─ 项目/产品 Tab + 直接收款胶囊（Tab 上方）
 │    ├─ 左侧分类 + 项目/产品卡片（圆形加号）
 │    └─ 底部 bottom-bar（左侧热区打开已选商品 Sheet / 应付 / 挂单 / 下一步）
 ├─ 已选商品 Sheet（bottom-bar 后升起）
 │    ├─ 已选商品 · 清空（二次确认）· 关闭
 │    └─ 行：名称 / 员工摘要 / 单件价 / ±步进
 ├─ s6 开单结算（购物车明细结算）
 │    ├─ 顶部身份卡（散客/会员）
 │    ├─ 明细卡列表（员工/权益/小计/应付）
 │    ├─ 订单级行：权益 / 优惠券
 │    └─ 底部 co-bar（**应付可点击改价** + 挂单/结账 → s15）
 ├─ s7 直接收款结算（直接收款第二步）
 │    ├─ 顶部身份卡（散客/会员）
 │    ├─ 直接收款明细卡（员工/权益/小计/应付）
 │    ├─ 订单级行：权益 / 优惠券
 │    └─ 底部 co-bar（应付 + 挂单/结账 → s15）
 ├─ s8 门店流水（逻辑补充）
 │    ├─ 尾款单 / 筛选
 │    ├─ Tab：全部/项目/产品/会员卡/作废
 │    └─ 流水卡（顾客/商品/服务人/支付方式/实付/开单时间）
 ├─ s9 选择退款项目（逻辑补充）
 │    ├─ 提示条（退回方式约束）
 │    ├─ 退款项目列表（勾选）
 │    └─ 底部 rf-bar（全选 + 确认退款）
 ├─ s14 挂单列表
 │    ├─ 标题栏「挂单列表」+ 返回
 │    ├─ 顶部失效提示条「每日24:00后 挂单自动失效并删除」（不随列表滚动）
 │    ├─ 空态（单据图标 + 暂无挂单 + 说明文案）
 │    └─ 挂单卡列表（顾客 + VIP / 时间 / 明细摘要 / 服务员工 / 金额 + 删除 + 「取挂单」）
 ├─ s15 开单成功
 │    ├─ 成功徽标（对勾 22 + 「开单成功」18/600）
 │    ├─ 顶部渐变 328 + 过渡（无标题栏）
 │    ├─ 金额卡（金额 48 → 灰色发丝实线 → 字段行：单号 / 开单日期 → 发丝实线 → 支付明细：支付方式或卡名 + 金额/次数 + 灰字剩余；字段间发丝虚线、左右边距 16）→ 卡内最后一行「打印小票」（0.5px 实线 + 图标 18 + 文案 13；R19 纯行、无圆角无底色）
 │    ├─ 金额卡下方 4 等分操作组（改日期 / 改单号 / 看流水 / 客户详情，图标 + 文字）
 │    └─ 底部 bar：返回首页图标 + 客户签字 / 继续开单
 └─ 弹层
      ├─ addsheet 添加项目/产品（价格（元）+ 员工 + 确定 → 加入购物车；产品另有数量）
      ├─ staff-sheet 选择服务员工（员工网格 + 展开选项卡：工位多选 + 顾客指定）
      ├─ remark-sheet 备注（开单内容 50 字 + 完成）
      ├─ suc-no-sheet 改单号（单号输入行 + 提示 + 取消/确定）
      ├─ date-sheet 改日期（底部 sheet 形态的日历，成功页专用）
      ├─ co-pay-sheet 修改应付（s6 专用，数字键盘 + 清零/取消/确定）
      ├─ ben-pick-sheet 选择权益（**R13：原 s12 整屏页已撤除**，改为底部 sheet，点中即生效并收起）
      └─ cp-pick-sheet 选择优惠券（**R13：原 s13 整屏页已撤除**，改为底部 sheet，点中即生效并收起）
```

> 原「选择权益」底部 sheet 曾被 **s12 选择权益页**取代；**R13 又改回底部 sheet**（`#benPickMask`，见 6.10），**`#s12` 页面已撤除**。「选择优惠券」同理为 **`#cpPickMask` sheet**（见 6.21），**`#s13` 页面已撤除**。

### 3.0 页面流（示意）

```mermaid
flowchart LR
  S0[简化首页] -->|开单| S4[选择顾客]
  S0 -->|取挂单（N）| S14[挂单列表]
  S0 -->|开卡| OUT[[开卡业务（线上）]]
  S1[散客开单] -->|直接收款| S2[直接收款]
  S1 -->|选择顾客| S4[选择顾客]
  S2 -->|去收款| S3[直接收款-去收款]
  S3 -->|去收款| S7[直接收款结算]
  S4 -->|选男/女散客| S1
  S4 -->|挂单（N）| S14
  S4 -->|选会员| S5[会员开单]
  S5 -->|选择顾客| S4
  S1 -->|加购| AS[[添加项目/产品]]
  AS -->|内嵌| SS[[选择员工]]
  AS -->|确定| CART[加入购物车]
  S2 -->|内嵌| SS
  S1 -->|下一步(有项)| S6[开单结算]
  S5 -->|下一步(有项)| S6
  S6 -->|点权益行右侧| S12[选择权益]
  S7 -->|点权益行右侧| S12
  S6 -->|点优惠券行右侧| S13[选择优惠券]
  S7 -->|点优惠券行右侧| S13
  S12 -->|确认| S6
  S13 -->|确认| S6
  S12 -->|确认| S7
  S13 -->|确认| S7
  S1 -->|挂单成功| S14
  S5 -->|挂单成功| S14
  S6 -->|挂单成功| S14
  S14 -->|去结单·来源=结算页| S6
  S14 -->|去结单·来源=开单页| S1
  S14 -->|去结单·来源=开单页(会员)| S5
  S6 -->|结账开单成功| S15[开单成功]
  S7 -->|结账开单成功| S15
  S15 -->|继续开单| S1
  S15 -->|看流水| S8[门店流水]
  S8[[门店流水]] -.逻辑补充.-> S1
  S9[[选择退款项目]] -.逻辑补充.-> S1
```

| 页面/层 | 导航标题（原型） | 说明 |
|---------|------------------|------|
| 简化首页 s0 | — | 开单 / 开卡 两大入口 + 「取挂单（N）」描边按钮（N 实时） |
| 散客开单 s1 | 开单结账 | 标题栏右侧顾客入口（散客/会员）+ 顾客卡默认收起 + 项目/产品选品 + 底部工具栏 |
| 直接收款 s2 | 开单结账 | 金额键盘 + 选填员工/开单内容 + 去收款 |
| 直接收款-去收款 s3 | 开单结账 | 金额显示（键盘可改）+ 员工/开单内容 + 去收款 → s7 |
| 选择顾客 s4 | — | 搜索栏 + 标题栏右侧「**挂单（N）**」（R16 改名）+ 散客/会员列表（右侧常驻「开单 \| 收款」纯文字操作：红字 + 灰字 + 1px 发丝竖线，R17）+ 字母索引 |
| 会员开单 s5 | 开单结账 | 标题栏右侧顾客入口 + 会员卡默认收起 + 项目/产品选品 + 底部工具栏 |
| 开单结算 s6 | 开单结账 | 购物车明细结算（明细卡/权益/应付/优惠券/备注/挂单/结账 → s15） |
| 直接收款结算 s7 | 开单结账 | 直接收款第二步（直接收款明细/权益/应付/优惠券/备注/挂单/结账 → s15） |
| 门店流水 s8 | 门店流水 | 直接收款流水展示（全部/项目/产品/会员卡/作废 Tab） |
| 选择退款项目 s9 | 选择退款项目 | 退款项目勾选 + 全选联动 + 确认退款 |
| 选择权益 sheet | 选择权益 | 底部 sheet（`#benPickMask`）：会员卡权益列表（9 项演示数据），**单选 · 点中即生效并收起**，点蒙层 = 取消（R13 由整屏页改回 sheet） |
| 选择优惠券 sheet | 选择优惠券 | 底部 sheet（`#cpPickMask`）：「不使用优惠券」+ 券卡列表（使用规则默认折叠），**单选 · 点中即生效并收起**（R13） |
| 挂单列表 s14 | 挂单列表 | 顶部失效提示条 + 挂单卡（顾客 + **红色 VIP 图标** / 时间 / 明细 / **服务员工** / 金额）+ 删除 +「**去结单**」+ 空态 |
| 开单成功 s15 | 开单成功 | 无标题栏 + 成功徽标 + 金额卡（发丝线分层字段）+ **卡下 4 等分操作组（改日期/改单号/看流水/客户详情）** + 底部 bar（返回首页/客户签字/继续开单） |
| 添加项目/产品 | — | sheet 弹窗（标题/**价格（元）**/员工/确定 → 加入购物车；产品另有数量） |
| 选择员工 | 选择员工 | **R20 按门店员工数分流**：员工 > 5 人 = **工位槽位行（4 行 × ＋）× 员工选择页**（每行上限 3 人，落位动效，点卡移除；R16 重构 + R20 选择页隐藏价格卡、高度自适应）；员工 ≤ 5 人 = **「标题 + 卡列」紧凑形态**（点卡即落位、再点移除，无「＋」槽位）见 §6.11.9 |
| 备注 | 备注 | 文本框（50 字）+ 完成 |
| 改单号 | — | sheet 弹窗（单号输入行 + 提示 + 取消/确定），仅 s15 使用 |
| 改日期 | — | **底部 sheet 形态日历**（与标题栏日期控件同源），仅 s15 使用 |
| 修改应付 | — | **底部数字键盘 sheet**（清零/取消/确定），**s6 / s7 明细「应付」行入口（R16）** |
| 员工选择（12 人）演示 | 16 员工选择（12 人） | 演示入口（R20，仅评审用）：员工池临时扩到 12 人 → 直达「选择大工」选择页，验证「> 5 人」分支；关闭 sheet 即还原 5 人（§6.11.9） |

> **已删除**：s10 业绩设置、s11 提成方案（线上模块已重构，原型不再适用；R6 §1）。

---

# 模块 4　状态机
> 读者：前端 ✓ · 后端 **必看** · 测试 **必看**

## 4.1 顾客身份 `currentCust`

| 状态 | 含义 | 顶部卡片 |
|------|------|----------|
| `guest` | 散客 | 深色散客卡（双头像 · 性别文案 · 选择顾客） |
| `member` | 会员 | 会员卡（头像/姓名/VIP/上次到店/更换顾客 + 权益展开） |

```
guest ──选会员──► member
member ──选男/女散客──► guest
```

- 默认 `guest`。
- s2 / s3 / s6 / s7 顶部卡片按该状态切换；后续元素随卡片高度下移。
- 从 s4 选会员：回到来源页；s2/s3 返回原页，s1 进入则跳 s5，s6/s7 进入则更新身份并返回原页。
- 从 s4 选男/女散客：回到来源页（从 s5 进入则回 s1，s6/s7 进入则更新身份并返回原页）。
- **散客卡「男散客 / 女散客」文字**：点击进入顾客列表（s4）。（头像点击仍仅切换性别）

## 4.2 散客性别 `data-g`

| 状态 | 卡片文案 | 头像表现 |
|------|----------|----------|
| `male`（默认） | 男散客 | 男头像彩色，女头像灰阶 |
| `female` | 女散客 | 女头像彩色，男头像灰阶 |

- 点击顶部卡片头像可切换性别；切换仅影响展示，不改变身份（仍为散客）。
- **点击「男散客 / 女散客」文字** → 进入顾客列表（s4），可与选择顾客按钮等效；头像点击不受影响。
- s4 散客列表选择「男散客 / 女散客」同样更新。

## 4.3 顾客卡片展开（s1 / s5）

- **默认收起**：进入 s1 / s5 时顶部顾客卡片不占位，价目表大卡片上移贴住标题栏下沿。
- **开关在标题栏右侧**：点击该区域展开/收起顾客卡片。**两种身份都显示图标（仅图形不同），描边色统一 `#929292`**：
  - **散客**：**双人 icon**（`assets/ic_guest_head.svg`）+ 文字「散客 / 男散客 / 女散客」；
  - **会员**：**单人 icon**（`assets/ic_member_head.svg`）+ 会员姓名。
- **iOS 风格动效**：卡片以标题栏右侧区域为展开锚点（`transform-origin: top right`），高度 0 → auto + 透明度 + 轻微缩放/位移，缓动 `cubic-bezier(.32,.72,0,1)`，约 320ms；收起为反向。
- 卡片展开后内部信息（会员权益 chevron 展开态）沿用原规则；**展开/收起状态不做跨页记忆**（重进页面为收起）。
- 标题栏图标与文字**不强制同色**：图标固定 `#929292`，文字沿用标题栏常规色。

## 4.3.1 挂单池 `holdOrders`（s14 数据源）

- 结构：`{ id, custName, custType, member, pay, type, items: [], time, note, from, day }`，`items` 为挂单时的购物车快照；**R6 新增 `from`（来源页 `order` / `checkout`）与 `day`（生成日，用于 24:00 超时判定）**。
- 挂单成功（s1 / s5 / s6 底部「挂单」）→ 写入挂单池（记录 `from`：s1/s5 → `order`，s6 → `checkout`）、清空当前购物车与角标、toast「挂单成功」、跳 **s14 挂单列表**。
- s7（直接收款）挂单：toast「直接收款不可挂单」，不入池（沿用原规则）。
- **「取挂单」（R6 修正）**：还原该挂单的顾客身份与购物车（**不从挂单池移除**，可反复取用），并按 `from` 回到 **s1 / s5（开单页来源）或 s6（结算页来源）**；同时记录 `activeHoldId`。
- **删除条件收敛为三种**（R6）：① 列表内手动删除；② 进入列表时 24:00 超时自动清理；③ 由该挂单发起的结算成功（进入 s15）。详见 §6.22.1。
- 挂单条数 `N` 实时同步到 **s0 首页「取挂单（N）」按钮** 与 **s4 标题栏「取挂单（N）」**（两处同源，`window.holdCount()` → `window.__v2SyncHold()`）；`N` 只计**未失效**挂单。

## 4.3.2 开单成功页上下文 `sucInfo`（s15 数据源）

- 由 s6 / s7 结账写入：`{ amount, pays:[], no, date }`；**R7 收敛支付字段**为 `pays` —— **支付明细行数组**，每行 = `{ name 支付方式名或会员卡具体卡名, val 金额或次数, sub:{ l, r } 灰字次行（`剩余` + 余额/次数；套餐卡为 `剩余` + `金额 / 次数`；计次卡为 `剩余` + 剩余次数；周期卡为 `null`） }`；**折扣卡不入 `pays`**（R11）。不再有 `methods` 汇总字段与独立「支付方式」行，用于成功页按线上规则渲染（各行金额之和 = `amount`）。
- 进入 s15 时 **清空购物车 / 直接收款明细与角标**，并 toast「结账流程说明」。
- 页内动作只影响本页展示，**不回写**结算页：
  - 改日期 → 底部 sheet 选择日期后更新本页「开单日期」；
  - 改单号 → 更新本页单号展示（sheet 内保存生效）；
  - 看流水 → 跳 s8（门店流水）；
  - 继续开单 → 清空本页上下文，回 s1 开单；
  - 客户签字 / 客户详情 → toast 示意（本期不实现签字板与顾客详情）。

## 4.4 购物车明细 `cartItems`（s6 / 已选商品 Sheet 共用数据源）

- 添加 sheet 点「确定」写入明细（散客页面加购计入 s1，会员页面加购计入 s5）。
- 明细字段：名称 / 单价 / 数量 / 应付 / 类型（项目/产品）/ 分类 / 缩略图 / 员工 / 权益 / 改价标记。
- **合并 / 拆分（同项多次加购）**：
  - 比对键 = **同一名称（同项目/产品）** + **单件应付一致**（行应付 ÷ 数量）+ **服务员工/销售人员完全一致**（含工位与顾客指定；员工集合排序后比对）。
  - **应付或员工任一不同 → 必须拆成多行**，不得合并数量。
  - 两者皆相同 → 合并：数量相加，行应付 = 单件应付 × 新数量。
  - **未选员工**视为同一空键，可与另一次未选合并。
- 加购后 s1/s5 应付金额重算、对应左侧分类角标按「确定」次数 +1；删除一行明细时角标 -1。
- 底部 bar 角标 = 各行数量合计。
- s6 结账成功 → 进入 **s15 开单成功**（明细与角标清空）；Sheet「清空」确认后 → 清空明细与角标，回到列表态。
- **已选商品 Sheet**：点底部 bar 左侧热区打开（见 §6.11），与 s6 共用 `cartItems`。

## 4.5 直接收款明细 `qcoItem`（s7 数据源）

- s3 点「去收款」时按当前金额生成直接收款明细（**金额 = 原价，不做身份折扣**），并进入 s7。
- 明细字段：名称「直接收款」/ 金额 / 应付 / 员工 / 开单内容 / 权益 / 改价标记。
- 员工 / 开单内容与 s2/s3 共享同一状态；在 s7 修改后同步回 s2/s3。
- **权益：默认自动匹配**（会员 = 其持有的**全场最优折扣权益**，与 s6 一致；散客 = 「无卡权益」）；点权益行右侧「值 + ›」打开**「选择权益」sheet**改选，**点中即生效并收起**、应付按权益规则重算（见 5.5），取消权益恢复原价。
- s7 删除明细 → 返回 s2 并清空；s7 结账成功 → 进入 **s15 开单成功**（明细与状态清空）。

## 4.6 添加弹窗字段联动

| 字段 | 输入 | 联动 |
|------|------|------|
| 数量 | 步进 1~999 | 应付 = 单价 × 数量 |
| 价格（元）（R16 由「应付（元）」改名） | 手动可改（带笔形图标） | 判定改价（与原价 × 数量不等）；改价明细在 **s6 / s7** 不可用权益 |
| 卡权益折抵 | 会员持有折扣权益时生效 | **R16：价目卡不再预告权益价**；权益只在结算页「权益」行折抵 —— 弹窗「价格（元）」默认 = 原价 × 数量 |

## 4.7 退款勾选（s9）

- 单项勾选 / 取消；全选按钮与列表勾选状态 **双向联动**（全部勾选时「全选」点亮，取消任一项则熄灭）。

---

# 模块 5　交互细则 / 异常流
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

## 5.1 金额输入规则（直接收款金额 + 应付/小计统一）

| 规则 | 说明 |
|------|------|
| 整数位 | ≤ 6 位 |
| 小数位 | ≤ 2 位 |
| 上限 | 999999.99 |
| 下限 | 0（不可为负） |
| 小数点 | 最多 1 个；**禁止以「.」开头** |
| 前导 0 | 自动剔除（输入 `0` 后再输数字视为替换，如 `05` → `5`） |
| 空值 | 不可提交；直接收款空金额点「去收款」toast「请先输入金额」 |

## 5.2 应付 / 小计编辑

- 金额右侧有 **笔形图标**，表示该金额可编辑。
- **对焦即清空原值**，随后输入的内容即为当前金额；**失焦时若为空则还原原值**。
- 金额输入过程规范化：超上限封顶 999999.99，小数第 3 位起截断，负数归零。
- 修改后的应付/小计 **与「单价 / 权益优惠价」× 数量不等** 即视为 **改价**；**改价明细在 s6/s7 中权益锁定**（行文案「改价商品不可用权益」，点击 toast 同文案）。会员权益优惠价作为默认值不判定为改价。

## 5.3 员工 / 开单内容（选填）

- 员工：非必填；直接收款与添加弹窗各自维护选择状态，互不影响；s2/s3/s7 共享同一状态。
- 开单内容：非必填；**≤ 50 字**，实时计数 `n/50`；空 / 全空格视为未填，未填时 s6/s7 明细显示「未输入」。

## 5.4 校验失败 / 交互反馈（toast）

| 条件 | 提示 |
|------|------|
| 直接收款空金额点「去收款」 | 请先输入金额 |
| 下一步（购物车为空） | 请先选择项目 |
| 下一步（购物车有项） | 直接进入 s6（无 toast） |
| s3 去收款 | 直接进入 s7（无 toast） |
| 挂单（s1/s5/s6 有货） | 挂单成功 → 写入挂单池 + 清空购物车 + 跳 s14 |
| 挂单（s7 / 空车） | 按钮置灰；toast「直接收款不可挂单」/「请先添加项目或产品」 |
| 底部「+」按钮 | 请选择项目 |
| 底部「+」按钮（s5 触发） | 选择项目或直接收款 |
| s6/s7 结账（无明细） | 暂无结算项目 |
| s6/s7 结账成功 | 「原型省略了结算与成功之间的支付流程弹窗（支付方式、收款确认等），开发时以线上为准」（**R8 变更**，2 行 / 停留 3000ms；随后进入 s15 开单成功） |
| 取挂单 | 已取回「{顾客}」的挂单 |
| s15「改单号」保存 | 单号已更新 |
| s15「看流水」 | 查看该笔流水（随后进入 s8） |
| s15「继续开单」 | 已开始新的一单 |
| s15「客户签字」/「客户详情」 | 已进入电子签字流程 / 查看客户详情 |
| s7 删除直接收款明细 | 已删除 |
| 改价明细点权益 | 改价商品不可用权益 |
| s8「尾款单」 | 尾款单 |
| s8「筛选」 | 筛选 |
| s9「确认退款」 | 确认退款 |
| s14 挂单超时自动删除 | 有 N 张挂单已超过 24:00 自动失效并删除 |
| s15「改日期」保存 | 开单日期已更新 |
| s6 手动改价确认 | 应付已改为 ¥x.xx / 已恢复系统应付 |

## 5.5 弹层交互

- 遮罩点击关闭；员工选择关闭（点遮罩/完成）**保留已选状态**，同一项目重进不重置；**跨项目（另一个项目/行）打开时改为该项目自己的数据行渲染，不继承其它项目的已选与中途翻牌态**。
- 添加弹窗遮罩关闭不写入购物车；「确定」后写入并 toast「已加入 · 应付 ¥x.xx」。
- 权益选择：单选；**点中权益行即生效并收起 sheet**（R13；原「页内选中 + 底部确认」已废止）；点蒙层关闭 = 取消，权益不变。选「不使用权益」则恢复「无卡权益」且不再被自动匹配覆盖。
- **权益/优惠券入口（s6 / s7）**：只有「权益」行与「优惠券」行的**右侧「值 + ›」区域**是热区（左侧标签不可点）；点「权益」行右侧 → 打开**「选择权益」sheet**；点「优惠券」行右侧 → 打开**「选择优惠券」sheet**。两个 sheet 均为**单选 · 点中即生效并收起**，相互独立、互不跳转。
- **权益自动匹配（s6）**：明细在 **未改价且未手动清除权益** 时，进入 s6 自动匹配——会员匹配其持有的**全场最优折扣权益**（演示数据为超值折扣卡 8.0 折）、散客为「**无卡权益**」；用户手动取消后不再覆盖。**顾客身份切换时，自动匹配的权益按新身份重算；手动选择的权益保留。**
- **订单级优惠券（s6 / s7）**：整单一张券，影响**底部应付**（应付 = 各项权益后应付之和 − 券抵扣；不满门槛则不抵扣）。
  - 散客订单**自动带入首张可用券**（演示为「洗剪吹立减10元券」满减 10 元、无门槛）；会员**不自动带券**，需手动选择。
  - 用户手动选过「不使用优惠券」后，不再自动带入；清空购物车时券一并清空。
- **直接收款权益（s7）**：**默认自动匹配**——会员自动匹配其持有的**全场最优折扣权益**（演示为超值折扣卡 8.0 折，与 s6 规则一致）、散客为「**无卡权益**」；点权益行**右侧「值 + ›」**可改选以下四类权益：
  1. **会员卡内的储值金额**（如九折储值卡：按卡折扣 9 折计价）；
  2. **全场项目可适用的计次或折扣**（如水光三次卡计次核销不抵扣金额；超值折扣卡按 8 折计价）；
  3. **全部项目或全部产品可用的优惠券**（如新人8折券：满 58 减 10）；
  4. **直接收款金额符合使用门槛的代金券**（如满200减30券：金额 ≥200 才可选，减 30）。
  - 金额不满足门槛的代金券在列表中**置灰不可选**；选择权益后应付 = 原金额按规则折算，取消权益恢复原价；权益选择不算改价（可继续改小计）。

---

# 模块 6　功能规格
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

下列字段、按钮、跳转均须与原型一致；视觉以原型为准（品牌色 `#F32F41` 等）。

## 6.1 顶部顾客卡片

**标题栏右侧顾客入口 + 卡片默认收起【本轮变更】**

- **默认收起**：进入 s1 / s5 时顾客卡片不占位，**价目表大卡片直接上移贴住标题栏下沿**。
- 标题栏右侧显示顾客信息入口：`散客` = **纯文字「散客」（不带 icon）**；`会员` = **会员 icon（`assets/ic_member_head.svg`，20×20，`#1a1a1a`）+ 会员姓名**。
- **点击标题栏右侧区域** → 展开顾客卡片；**再次点击** → 收起。动效为 iOS 风格（以标题栏右侧为锚点，高度 + 透明度 + 轻微缩放/位移，缓动 `cubic-bezier(.32,.72,0,1)`、约 320ms，收起反向）。
- 散客与会员两种身份都支持该交互；**不收起的页面**：s2 / s3 / s6 / s7（这些页顶部卡片维持原有常显）。
- 展开 / 收起状态不跨页记忆（重进为收起）。

**散客卡（粉白淡色卡）【本期变更】**

- 头像 + 姓名（「男散客 / 女散客 / 散客」）；**深色卡已全部换成粉白淡色卡**（圆角 12、极浅投影）。
- **点头像或点姓名均进入顾客列表（s4）**；性别由 s4 的「男散客 / 女散客」选项决定。
- **「更换顾客」按钮已移除**（入口改为点卡片）。
- s1 / s2 / s3 / s6 / s7 共用同一散客卡（默认男散客）。

**会员卡（会员身份）【本期变更】**

- 头像 / 姓名 / VIP 标签 / 上次到店（粉白淡色卡，样式同上）。
- **点头像或点姓名进入 s4**；**「更换顾客」「办卡」按钮均已移除**。
- **会员持卡列表默认收起**，右侧 chevron 点击展开 / 收起。
  - **展开区展示内容 = 持卡列表，与线上现有版本显示内容一致**：**一行一张卡**，**左侧 = 会员卡类型 + 会员卡名称**，**右侧 = 会员卡剩余权益**。
  - 卡类型以浅色 pill 标签呈现（计次卡 / 储值卡 / 折扣卡），卡名紧随其后（超长省略号），右侧剩余权益用数字字体、右对齐到卡内右缘（内距 16）。
  - 演示数据（5 张卡）：`计次卡 水光三次卡 → 余3次`、`储值卡 九折储值卡 → 余额2049.00元`、`折扣卡 超值折扣卡 → 8.0折`、**`套餐卡 染烫套餐卡 → 余4次`**、**`周期卡 半年畅享卡 → 不限次`**。
  - 权益值格式沿用线上规则：余N次 / 余额xx.xx元 / x.x折 / 不限次。
- **VIP 标签按 Figma 资源（渐变 `VIP`）实现，尺寸与位置对齐设计稿**；开发保持现有 VIP 标签逻辑不变。**（R6：标签内最左的黑色方块 `◆` 已删除，只保留 `VIP` 文字。）**

**散客卡「添加会员」入口（卡内快捷注册）【本期新增】**

- 仅当散客性别为 **男散客 / 女散客** 时，卡片右侧区域出现「添加会员」图标（**与 s4 顾客列表同款**：`ic_add_member_new.svg`，实心橙色 `#FF8956`，**24×24**，右内距 16px）；默认「散客」不显示该图标。
- 入口在 **s1 / s2 / s3 / s6 / s7 所有共用散客卡的页面** 一致出现。
- 点图标 → **卡片就地向下展开快捷注册表单**（复用 s4 字段样式）：姓名\* / 手机号（选填）/ 性别\*（男 / 女）+ 右下「取消」「确认添加」；再次点图标或点「取消」→ 收起并清空，不保存。
- **性别默认跟随散客性别**：卡片为「男散客」展开时已预选「男」，「女散客」预选「女」；性别为必填单选，点击即选中（提供默认值后不再支持点空，避免误点导致校验失败）。输入框（含未聚焦态）为**白色填充**。
- 校验同 s4：姓名为空 → toast「请输入姓名」；手机号非 11 位合规号码 → 红框 + 「手机号格式不正确」+ toast；未选性别 → toast「请选择性别」；只填姓（1 个字）+ 已选性别 → 自动识别为「\*先生 / \*小姐」。
- 确认添加成功 → **表单收起，顶部卡片就地变为会员卡**：头像按性别、姓名 = 刚注册的会员、显示 VIP 标签、**「上次到店」= 当天日期**；同时顾客身份切为会员（散客卡隐藏、会员卡显示，s2/s3/s6/s7 同步）。
- 散客卡姓名样式与会员卡统一：字色 `#751616`、正常流排布并与头像中轴对齐（s1 / s2 / s3 / s6 / s7 一致）。
- 新注册会员**无任何会员卡 / 权益**：展开区显示空态 **「暂无会员卡」**；**不参与会员折扣**（价目恒为单色原价），结算页「权益」行显示「无卡权益」（不自动套用折扣权益）。
- 注册会员**不改变当前页面**（停留原页继续开单）；**已加入购物车的明细保持原样**，不追溯改写。
- 从 s4 重新选择其他顾客后，顶部卡按所选身份与顾客档案重新渲染（选会员恢复其卡项指标与折扣权益）。

## 6.2 项目 / 产品 Tab 与左侧分类

- Tab：「项目 / 产品」；**Tab 卡片上方两角圆角 12**，**下沿无描边**；选中项红色文字 + 红色指示条。
- 分类芯片（横向滚动）：**最左固定「全部」，且进入页面 / 切换 Tab 时默认选中「全部」**。
- 选中「全部」时右侧为**平铺列表、不显示分组标题**；选中具体分类时仅展示该分类项目。
- 分类右侧红色数字角标：显示该分类已加购数量（初始隐藏，加购后累加，上限显示 99+）。

## 6.3 项目 / 产品卡片

- 卡片：缩略图 + 名称（15/500 `#1a1a1a`）+ 价格（`¥` 12/500 + 数值 15/700，均为 `#929292`，间距 2px）+ 圆形加号（28×28 红底白＋）。
- **点圆形加号 → 打开底部「设置内容」sheet（见 6.9）；原「行内展开区」方案本轮已删除**（不再有展开卡、步进器、员工 Chip、价格药丸、发丝线等行内元素）。
- 从已选商品 Sheet 点明细行回跳编辑时，同样以 **该商品的设置 sheet 回填已保存数据** 呈现（见 6.12）。
- **价格展示（R16 修订，全站统一口径）**：**取消划线价与权益价**，**任何身份（散客 / 会员）都只显示单色原价**，字色 `#929292`（`¥` 12/500 + 数值 15/700）。
  - **作废项**：R8 §3「价目表按权益显示最优价」（划线原价 + 红色权益价）**整体作废**；`.v2-cat-price-old` / `.v2-cat-price-sale` 相关样式与取「最优折扣权益」的取价逻辑（`memberBestDiscountRate`）一并删除。
  - **口径**：价目卡只表达「这一项的原价」；会员权益只在结算页「权益」行参与折算（`cartItems.price` 恒为原价，金额链路未变）。
- 数据为演示数据；开发接入线上价目表实时数据。

## 6.3.1 空价目表时的 Tab 规则【本轮变更】

- 若 **项目 / 产品其中一个价目表为空** → **隐藏对应的 Tab**（例如产品为空时只显示「项目」Tab），并默认选中非空的那个。
- 若 **两个价目表都为空** → **两个 Tab 都显示**，页内给出空态与「管理价目表」入口（**本期仅做说明，不实现管理入口**）。
- 演示开关（评审用，加在 `index.html` 地址后）：`?noProduct=1` 产品为空（隐藏产品 Tab）；`?noProject=1` 项目为空（隐藏项目 Tab）；**两个参数都加 → 两个价目表都空**（两个 Tab 都显示 + 空态说明）。

## 6.4 搜索栏（本期已删除）

- 开单结账页（s1/s5）原有的搜索栏 **本期已删除**，原型不再展示；**s4 选择顾客页新增的真实搜索见 6.8**。

## 6.5 直接收款胶囊

- s1 / s5 中「⚡ 直接收款」胶囊，位于项目/产品 **Tab 上方**；点击 → 设置身份为散客并进入 s2。

## 6.6 直接收款 s2（新功能）【本期变更】

- 顶部身份卡：散客（默认）或会员（从 s4 选会员进入时）。
- 字段顺序（自上而下）：**开单内容 → 收款金额 → 员工**。
- **开单内容**：选填；≤50 字；placeholder「请输入开单内容（50字以内）」。
- **收款金额**：数字键盘输入；placeholder「请输入金额」；格式见 5.1；空金额「去收款」拦截。
- **员工**：选填；点击 → 内嵌展开选择服务员工面板（工位多选 + 顾客指定）；回填格式「姓名 · 工位 · 顾客指定」（多人顿号连接）；未选显示空（不显示「选填」）。
- **返回键（R18 修正）**：标题栏左侧返回键 → **s4 选择顾客**（R18 前为散客开单页 s1）。理由：s2 的主入口是 s4 行内「收款」，返回应回到「刚才挑顾客的那一页」；即使从左侧导航直达 s2，返回也落在 s4。
- 数字键盘：1-9 / 0 / . / ⌫；「去收款」→ s3。

## 6.7 直接收款-去收款 s3【本期变更】

- 结构同 s2（字段顺序同为 **开单内容 → 收款金额 → 员工**）；金额初始取 s2 输入值，键盘可改；员工/开单内容与 s2 共享同一状态。
- 「去收款」→ 进入 **s7 直接收款结算**。

## 6.8 选择顾客 s4【本期变更】

- **标题栏（新增）**：s4 顶部新增标题栏（高 44，位于状态栏之下），左侧返回键 → 返回首页 s0，标题「选择顾客」，**右侧为「挂单（N）」入口**（**R5 起为纯文字；R9 恢复前置图标** —— `assets/ic_hold_take_nav.svg`（20×20 / `#929292` / 1.6px 描边，图形 = 单据 + 时钟，与文字间距 6px）；`N` = 挂单池实时条数；点击 → **s14 挂单列表**）；**该页不含日期控件**。因搜索栏「取消」不再返回首页（见下），返回首页统一走标题栏左侧返回键。
- **旧挂单条已删除**：列表顶部原来的挂单入口条（`pick-holds`）本轮移除，入口统一到标题栏右侧。
- **「添加会员」行图标更新**：行左图标换为最新 Figma 图标（`assets/ic_add_member_new.svg`，**保持橙色 `#FF8956`**），列表行内尺寸 **36×36**（外层 44×44 占位，与其它行 44 头像同中轴、行高一致）；顶部顾客卡片内同图标尺寸 **24×24**（等比缩小，见 6.1）。
- **搜索栏（新增真实搜索）**：胶囊输入框（placeholder「输入顾客姓名/手机号/编号/备注」）+ 右侧「取消」。**输入即实时筛选**，匹配字段为 **姓名 + 手机号**（模糊匹配，忽略大小写与空格）。
  - **搜索态列表**：结果**平铺展示**，隐藏字母分组头、右侧 A-Z 索引条、「添加会员」行与散客组；新增会员（列表内注册）同样可被搜到。
  - **无结果**：显示空状态（圆形搜索图标 +「未找到相关顾客」+「换个姓名或手机号试试」）。
  - **「取消」**：清空输入并退出输入态（**不返回首页**，返回首页见上方标题栏）；无输入时列表恢复分组态。
  - 在搜索态下确认注册新会员时，先自动退出搜索态，以保证新行可见并定位展开。
- **散客组**：新增「男散客」「女散客」两个选项；选择后返回来源页（从 s5 进入则回 s1；s6/s7 进入则更新身份并返回），并更新散客卡性别与身份。
- **添加会员入口（行下展快捷添加）**：散客组之后、「C」分组之前新增「添加会员」行（行高 74、内距 14/28/14/12、间距 10；**图标 36×36**（`ic_add_member_new.svg`，`#FF8956`）；「添加会员」16/700 `#929292`）。
  - 点击该行 → **该行向下展开快捷添加会员表单**（R16：会员表单仍是行下展，但**顾客行本身不再有展开态**，故不再需要互斥处理），字段：**姓名\* / 手机号（选填）/ 性别\***（男、女两个 chip 单选）。
  - **校验**：姓名空 → toast「请输入姓名」；手机号填写时须符合 `^1[3-9]\d{9}$`，不合规 → 输入框红框 + 下方红字「手机号格式不正确」+ toast；性别未选 → toast「请选择性别」。
  - **自动识别命名**：姓名只填 1 个字（只填了姓）且已选性别时，自动生成「**\\*先生 / \\*小姐**」（如「王」+ 女 → 王小姐）；姓名 ≥2 字则用姓名原文。
  - **底部按钮**：右下方「取消」（浅灰次要）+「确认添加」（主色 `#F32F41`）。取消 → 收起且**清空输入、不保存**；确认添加 → 生成新会员并**在列表中定位到该行**（行右侧**常驻**「开单 | 收款」，无需展开）。
  - **新会员落位**：按**姓氏拼音首字母**归入对应字母分组（组头不存在则在字母序正确位置新建组头，如「王」→ W 组）；行内容为**第一行 姓名（会员另有 VIP 标签 + 会员卡数 `(N)`）/ 第二行 手机号**（R16 口径）；头像按性别取男性或女性默认图；点其「开单」进入 s5（顶部顾客卡显示该会员名与对应性别头像）。
  - 顾客列表可纵向滚动，保证展开表单与新增行始终可滚到可视区。
- **行右侧常驻操作区（R16 建立 · R17 改版）**：**删除行展开折叠区**（不再有 `is-open` 展开态、不再有 `#EFF0FF` 选中底色、**整行本身无点击响应**），改为**每行右侧常驻「开单 | 收款」**：
  - **形态（R17）**：**iOS 列表语言的「纯文字 + 发丝竖线」** —— **无底、无框、无描边**（`.pick-row-actions`：`border:0`、`background` 透明、无圆角容器、`overflow:visible`）；**「开单」= `#F32F41` 13/600（主）**、**「收款」= `#8E8E93` 13/500（次，`aria-label="直接收款"`）**，两段之间为 **1px × 14px 的 `#E5E5E5` 竖线**（`.pick-seg__div`）；**点任一段直接进入下一步（各只需 1 次点击）**；按下反馈为整段文字 `opacity:.5`。
  - **取舍（R17）**：R16 的「一整枚实心红胶囊」在一屏 17 行里形成 **17 个纯红块**，页面右侧视觉过重；改为纯文字后**红色只剩 2 个字**，主次由**红字 vs 灰字 + 字重 600 vs 500** 表达；控件总宽 **101px**（R16 103px），比 R16 更短。
  - **行宽与索引条避让（R17）**：操作区**常驻并参与行内布局**（不叠加在行内容之上），**行内距右 12 → 30px**，行高仍 **74px**（单行内容，与列表其它行同高）；实测胶囊右缘 **378 → 360**，与右侧 **A–Z 字母索引条（370–388）净空 10px** —— 修复 R16 遗留的 **8px 重叠**（原首字母 x 375.8–382.2 压在红块上）。
  - **散客行 / 添加会员行**：同样常驻该胶囊（散客行第二行为空，行高不变）；「添加会员」行不放胶囊（它是表单入口）。
- **会员组**：按姓氏分组（A-Z# 索引）；行内容 = **第一行 姓名 + VIP 标签（渐变 `VIP`，R6 已去掉最左黑色方块 `◆`）+ 会员卡数 `(N)`**、**第二行 手机号**（R16：**「最近消费」整行已删除，手机号由第一行右侧移到第二行**）；点行本身**无响应**，入口为右侧常驻「开单 | 收款」（见下条）。
- **演示数据（R6 扩充）**：顾客列表 **15 人**，覆盖 **C / D / G / H / L / M / S / W / Z** 等 ≥8 个字母分组（含散客「男散客 / 女散客」与「添加会员」行）；用于演示分组头、字母索引与搜索定位的正确性。

## 6.9 添加项目 / 产品 sheet【本轮变更】

> 设计来源：Figma `RTB老版本优化` 节点 `176-329`；原型为底部 sheet（`#addSheetMask`）。

- **入口**：s1 / s5 商品卡片右侧 **圆形加号**（原来点加号是「行内展开区」，本轮**删除行内展开方案，改为底部 sheet**）；已选商品 Sheet 点明细行回跳编辑也进本 sheet（回填）。
- **标题区**：**只显示商品名（项目名），标题区不再显示价格行**（原「项目名下方的红色价格行」本轮已删除）。
- **数量（R16：仅产品保留）**：步进器（默认 1，1~999）；步进后价格 = 单价 × 数量。**添加项目 sheet 不再有「数量」卡片**（项目按件计价，数量恒为 1）。
- **价格（元）（R16 改名）**：由「应付（元）」**改名**（字段名只表达「这一项多少钱」，不再暗示「抵扣后的应付」）；**默认值 = 原价 × 数量**（R16 起不再预置会员权益价，与价目表单色原价同一口径）。数字输入 + 右侧 **笔形编辑图标**；对焦清空、失焦为空还原（见 5.2）；金额格式见 5.1。
- **布局（R16）**：
  - **添加项目 sheet**：删掉数量卡片后改**通栏单列** —— `#asGrid` 加 `is-single`、价格格加 `is-wide`，**价格格占满整行**（实测 366px = 390 − 左右各 12px 内距），上下留白重新收匀，不留空洞；
  - **添加产品 sheet**：保留数量卡片，仍为**两列**（数量 | 价格）；
  - 两态由 `paintItemSheet()` 按 `draft.isProd` 动态打类，共用同一份 DOM。
- **员工行**：标签为「服务员工」（项目）/「销售人员」（产品）；点击 → 内嵌 **槽位式员工选择器**（见 6.11）；未选时该行只有一个「＋」空槽位（不再显示「请选择」文案）。
- **确定**：写入购物车（应付总额累加、分类角标 +1），toast「已加入 · 应付 ¥x.xx」（回跳编辑时为「已更新该笔」）；关闭弹层。**取消 / 点遮罩关闭 = 丢弃本次草稿**（不写入购物车）。

## 6.10 选择权益 sheet（原整屏页 s12，R13 改为底部 sheet）

> 设计来源：Figma `开单结账-ZM` 节点 `151-7268`；原型为**底部 sheet**（`#benPickMask`，390 宽、高度随内容自适应、上限 85%），字体全局 Noto Sans SC。

- **入口**：s6 / s7 明细卡「权益」行的**右侧「值 + ›」热区**（左侧标签不可点）；改价明细**无热区、进不来**（行内显示「改价商品不可用权益」）。
- **形态（R13）**：底部 sheet —— 标题头「选择权益」+ 内容区（复用 `.addsheet` 骨架）。**高度随内容自适应**（9 行时实测 **635px ≈ 视口 75%**，**不触发内部滚动**；仅当内容超过 85% 上限时内容区才滚动）。**页级元素（状态栏 / 返回 / 底部通栏按钮 / 提示行）全部取消**。
- **内容区留白（R14 新增）**：**上 12px · 左右 12px · 下 12px + 34px 安全区**（`padding:12px 12px calc(12px + var(--safe-bottom))`，底部合计 **46px**）—— 解决 R13 遗留的「底部贴边、首卡紧贴标题栏」问题。
- **列表**：**9 行单选**（R11 由 7 行扩充），每行 = 名称 + 右侧单选圈（未选 `ic_radio_off` / 选中 `ic_radio_on`）；行高 **56px**、圆角 8、白底、**行距 12px（R14：由 8px 改为与全局卡片间距一致的 12px）**。
- **选中态（R14 新增）**：选中行叠 **1px 红色内描边 + `#FFF8F8` 底**（与券卡选中态同一套语言）；**右侧勾选圈保留，与描边并存**（圈表达「单选归属」，描边表达「当前生效项」，两者职责不同，故不二选一）。
  - 演示数据：`不使用权益` / `5.0折（5折卡）` / `计次卡抵扣` / **`套餐卡抵扣`** / **`周期卡抵扣`** / `充值金额所有项目5.0折(铂金卡)` / `折扣卡` / `充值金额所有项目8.0折(金卡)` / `固定金额98元（美发卡3000元）`。
  - 进入时**回显该明细当前权益**（自动匹配的为「折扣卡」，散客为「不使用权益」）。
- **交互（R13 变更）**：**权益是单选，点中即完成选择** —— 点任意可用权益行 → **立即写回该明细权益 → 重算应付 → sheet 自动收起**（**不再有「确认」按钮与提示行**）。**点蒙层空白处关闭 = 取消**（不改动权益，仅关闭）。
- **规则**：
  - 折扣类按比例折算应付（如 5 折卡 → 应付 × 0.5；8 折卡 → 应付 × 0.8）；
  - 计次卡仅核销次数、**不抵扣金额**；
  - 固定金额卡按卡面金额抵扣（不超过应付）；
  - 选「不使用权益」→ 该明细权益清空，显示「无卡权益」并**不再被自动匹配覆盖**。
  - **权益适用性（R9 新增）**：
    - **只对绑定项目可用的权益**：**次卡类（`计次卡抵扣`）与固定金额类（`固定金额98元（美发卡3000元）`）** —— 仅当当前明细的**项目名在绑定清单内**时可选中，**其余项目 / 产品一律置灰不可选**；**折扣卡 / 储值卡演示为全场通用**（不置灰）。
    - **演示绑定**（常量 `BENEFIT_SCOPE`）：`计次卡抵扣` → **洗剪吹、时尚造型**；`固定金额98元（美发卡3000元）` → **洗剪吹**。
    - **置灰样式（R10 修订）**：名称 `#B2B2B2` + **单选圈不透明 45%** + 行底 `#FAFAFA`；**整行不可点**（无 `onclick`、`aria-disabled="true"`），点击**不改变选中态、不关闭 sheet**。**R10：行尾不再显示「不适用」灰标签**。
    - **散客 / 无卡顾客**：列表**只保留「不使用权益」一行**（不显示任何卡权益，**不显示空态说明文案**）。
    - **回落保护**：打开本 sheet 时若当前权益对当前明细**不适用**（或身份为散客 / 无卡），自动回落为「不使用权益」。
- **入口补充（R13）**：左侧导航「12 选择权益」保留为**演示态入口** —— 以「**无目标明细**」打开本 sheet（**9 行全部可选、不做身份判定、不置灰**），点中行仅更新回显、**不关闭**，便于评审逐项查看。

## 6.21 选择优惠券 sheet（原整屏页 s13，R13 改为底部 sheet）

> 设计来源：Figma `开单结账-ZM` 节点 `151-7329`（订单级「优惠券」行）；原型为**底部 sheet**（`#cpPickMask`），字体全局 Noto Sans SC。

- **入口**：s6 / s7 订单级「优惠券」行的**右侧「值 + ›」热区**（该行位于明细之下、备注卡之上）。
- **形态（R13）**：底部 sheet —— 标题头「选择优惠券」+ 内容区；**高度随内容自适应**（3 张券 + 「不使用优惠券」行实测 **546px ≈ 视口 65%**）。**页级元素（状态栏 / 返回 / 底部通栏按钮）全部取消**。
- **内容区留白（R14 新增）**：**上 12px · 左右 12px · 下 12px + 34px 安全区**（底部合计 **46px**）；**首卡与标题栏之间 12px**、**卡片之间 12px**、**末张券卡不再计入底部行距**（底部恰为 12 + 34 = 46px）。
- **首行固定项**「不使用优惠券」+ 单选圈：**显式清券入口**（**点它立即清券并收起** → 券行回到「请选择」）。**R14：「不使用优惠券」是券 sheet 里唯一保留勾选控件的行**（券卡已全部去圈），选中时同样叠 1px 红色内描边 + `#FFF8F8` 底。
- **券卡列表**（单选；**R8 重排右侧布局**）：
  - 角标（如「满减券」）+ 券名 + 适用范围 + 分隔线 + 「使用规则」折叠行 + 券额（¥xx）+ 门槛文案。
  - **右侧单列结构（R14 重排）**：**券卡不再有单选圈** —— 原「单选圈 + 金额/门槛左缩 52px」的两列结构整体撤除，**券额（`right:16px` / `top:18px`）与门槛文案（`right:16px` / `top:53px`）一起右移到 16px**，与卡片内边距对齐，右侧成一条整齐的边；**券名可用宽度 `209 → 260px`**（演示数据里最长的「全场通用满200减30元优惠券」**不再被截断**）。
    - **选中标识**：由「卡片红色描边 + 极浅红底」**独自承担**（R10 起已有，R14 起成为唯一标识）。
    - **R10 的圈体缺陷随之消失**：原按 `top:50%` 随卡高浮动、以及「展开后圈体位移 / 压住使用规则行」的历史问题，随控件删除**不再适用**。
  - **门槛文案为灰色 `#929292`**（原为红色，与券额重复抢色）；券额仍为 `#F32F41`。
  - **选中态（R14 起为券卡唯一选中标识）**：1px **红色内描边** + 极浅红底 `#FFF8F8`（未选中为白底无描边）。
  - **不满门槛的券**：卡片转浅灰 `#FBFBFB` + 内容降饱和 50%（**R10：不再追加 `暂不可用` 标签**；**门槛文案不参与降权**，保持可读）。
  - **「使用规则」默认折叠**，点击该行展开/收起（展开显示有效期、适用门店、不与其他优惠同享等条目）；展开态不改变选中态。
  - 演示数据：`洗剪吹立减10元券`（满减券 · 项目通用 · 无门槛 · ¥10）/ `全场通用满200减30元优惠券`（满减券 · 全场通用 · 满200可用 · ¥30）/ `烫染项目立减50元代金券`（代金券 · 项目通用 · 满300可用 · ¥50）。
  - **不满门槛的券置灰不可选**（演示订单 ¥60.40 时后两张置灰）；**点击置灰卡 toast「当前订单不满该券使用门槛」且不关闭 sheet**，不改变选中态。
- **交互（R13 变更）**：**点券卡即生效并收起** —— 点可用券卡 → 立即写回订单级券 → 重算应付 → **sheet 自动收起**（**不再有「确认」按钮**）；**「使用规则」仍是独立热区**（`stopPropagation`，**展开 / 收起不触发选中、不关闭 sheet**）；**点蒙层关闭 = 取消**。**R14：券卡整卡即热区**（不再有圈体这个独立热区），点卡片任意处（除「使用规则」行）都是选中。
- **金额影响**：应付 = Σ 明细权益后应付 − 券抵扣；券抵扣 = min(券面额, 应付合计)，不满门槛为 0。
- **入口补充（R13）**：左侧导航「13 选择优惠券」保留为**演示态入口**（按订单级基准打开本 sheet），仅用于评审查看。

## 6.11 选择员工（服务员工）【本期变更 · R12】

> 本节细则编号与原型代码注释一一对应（`index.html` 内标注 `见 PRD §6.11.x`）。

### 6.11.1 形态：工位槽位行 + 员工选择页【R16 重构 · R20 起仅用于员工 > 5 人】

> **R12 的「点卡片横向撑满整行、平铺 4 张选项卡」翻转交互整体废弃**，改为「槽位行 ↔ 选择页」两层翻转。
>
> **R20 起本节只在门店员工 > 5 人时生效**；**员工 ≤ 5 人**走 §6.11.9 的「标题 + 卡列」紧凑形态（两套形态数据模型相同，按门店员工数分流）。

- **第一层：工位槽位行**（`.sp-row`）—— 固定 **4 行**，顺序 **大工 → 中工 → 小工 → 顾客指定**（`SP_ROW_DEFS`）；每行 = 左侧行名（14px `#1a1a1a`，宽 64）+ 右侧槽位区。
  - **槽位区**：已落位员工卡（24 头像 + 姓名，白底、圆角 8、1px `#EAEAEA` 描边）+ **末尾一个「＋」空槽位**（虚线描边、加号 14px `#929292`，与员工卡同尺寸）。
  - **右上角删除 × 角标（R23）**：已落位卡右上角 **14×14 `#C7C7CC` 实心圆点 + 8px 白 ×**（`top:1px / right:1px`，**`pointer-events:none`**，`.sp-slot` 加 `position:relative` 定位）。**删除热区仍只有整张卡** —— `data-sr-rm` 与 `aria-label="移除 {姓名}"` 还在卡根，**未新增任何独立热区**；角标只是把「点一下 = 移除」这件看不见的事**画出来**（实测 `elementFromPoint(× 中心)` 命中 `BUTTON.sp-slot` 本体，点该位置仍正常移除）。实测角标落在 x **46.5–60.5**、y **1.5–15.5**，与头像（右缘 x 41.25）留 5px、与姓名（y 34–47）**垂直不重叠**。
  - **每行最多 3 人**（`SP_ROW_MAX = 3`）：落满 3 人时该行「＋」**消失**（不留占位，槽位区自然收窄）。
- **第二层：员工选择页**（`.sp-pick*`）—— 点任一「＋」后，整块内容**在同一容器内 iOS 风格翻转**到选择页（`sp-box[data-picking="1"]`；槽位行 `max-height:0 + opacity:0 + scale(.985)` 收起，选择页反向展开）。
  - 标题行 **「选择{行名}」**（如「选择大工」）+ **「取消」**（返回槽位行，不改数据）。**R18：原右侧灰字「最多 3 人」提示已删除**（规则仍保留，见下一条「每行最多 3 人」）。
  - 内容为**横向卡片网格**：员工卡 = 头像 + 姓名（14/500）+ 职位（12px `#929292`）。
  - **点卡片即落位**：播放**落位反馈动效**（`spLand`：从选择页位置 scale .55 → 1.09 → 1 落进槽位）后**回到槽位行**，并**在落位卡右侧补出一个新的空槽位**。
  - **已在该行的员工**：卡面加红色描边 + 右上角对勾（`is-on`），**再点不产生变化**（同一行不重复）；**该行已满 3 人**时其余卡片置灰（`is-full`、`aria-disabled="true"`）且点击无响应。
  - **跨行可重复**：同一员工**可同时出现在不同工位行**（如既是「大工」又是「顾客指定」）。
- **空行**：某行始终未选人 → 该行只有「＋」槽位；**4 行全空**时整个选择器就是「4 行 × 1 个＋」的清单。
- **「顾客指定」行标签色（R18 定稿）**：第 4 行行名「顾客指定」**与前三行一致，用主文字色 `#1a1a1a`**（**R17 短暂的橙色 `#F97316` 作废**，`.sp-row--extra .sp-row__lbl` 着色规则整体删除）。4 行工位靠**行名文案**区分，不再靠颜色；`sp-row--extra` 类名保留（业务语义仍在）。
- **「顾客指定」行与「小工」的间距（R23 新增）**：`.sp-row--extra` 加 **`margin-top:6px`** → 行间 **14.5 → 20.5px**（发丝线仍贴在该行顶部、随之一起下移）。「顾客指定」在数据上不是工位（只有提成语义），拉开间距后与前三行**一眼可分**。紧凑形态（§6.11.9）同步改（6.5 → 12.5px）。

### 6.11.2 数据落点与实时回写

- 槽位行的数据落点由宿主容器 `[data-staff-root]` 的 `data-ctx` 决定：`add`（开单页商品明细）/ `v2draft`（添加 sheet 草稿）/ `quick`（s2·s3 直接收款）/ **`co`·`qco`（R16 新增：结算页 s6 / s7 明细的「修改服务员工」sheet）**。
- 每次落位 / 移除后**立即回写宿主并重绘**：添加 sheet 内摘要即时更新；**结算页重绘该明细的「服务员工」摘要行（员工变化不影响金额）**。
- **同一商品重进保留已选**；**跨商品互不串数据**，每次打开清空遗留的翻转态。

### 6.11.3 取消与移除

- **点槽位行中已落位的员工卡 = 移除该员工**（R16 起的唯一移除路径：无需展开、无二次确认），移除后该行末尾**补回「＋」空槽位**。
- 选择页内已在该行的卡片带勾选态，**再点不产生变化** —— 要取消请回槽位行点卡片移除。
- 移除后**不做褪色动效**（槽位直接收回、布局即时重排）。
- **R20（紧凑形态 §6.11.9）**：员工 ≤ 5 人时**没有槽位可点** —— **移除 = 再点一次同一张员工卡**（就地取消选中），与落位是同一个手势。

### 6.11.4 摘要文案与换行
- 文案 = `工位 · 工位 · 顾客指定`（`staffPickSummaryText`）；**只勾「顾客指定」时文案就是「顾客指定」本身，不出现「无工位」**。
- 卡片面（`spSummaryHtml`）：**1 个工位 + 顾客指定 → 单行 `大工 · 顾客指定`**；**≥2 个工位 → 最多 2 个工位占一行、其余每行 1 个**（`spStationPickLines`），**禁止省略号**，行内垂直居中。
- 共用场景：s2 / s3 员工行、**s6 / s7「服务员工」行（每名员工一行；R16 起该行可点，右侧 `›` 进入槽位式编辑）**、已选商品 Sheet 员工摘要、s14 挂单卡片「服务员工」字段（**取全单并集、按明细顺序去重、姓名全显示、可换行**）。

### 6.11.5 数据模型与迁移
- `staff = { id, staffIds[], staffRoles{sid: [工位 id]}, staffExtra{sid: bool}, staffChosen{sid: bool} }`。
- 工位顺序固定 **大工 → 中工 → 小工**（`STAFF_STATION_IDS`）；`staffRoles[sid]` 渲染前按该顺序排序。
- **读取时自动迁移旧结构**（`migrateStaffRowShape`）：`staffDesignated[sid] = true` → `staffExtra[sid] = true`；`staffRoles[sid]` 为字符串 → 单元素数组。

### 6.11.6 入口覆盖与状态隔离
- 入口：**添加项目 / 产品 sheet、直接收款 s2 / s3**；**R16 起结算页 s6 / s7 明细的「服务员工」行也可编辑** —— 右侧 `›` → 打开「修改服务员工」sheet（同一套槽位式选择器，`data-ctx = co / qco`）。
- **同一商品重进保留已选**；**跨商品互不串数据**，每次打开**清空遗留的展开编辑态**。
- 员工不同（含工位与顾客指定）→ 购物车**必须拆行**，不得合并数量。

### 6.11.7 动效时长与播放规则
- 时长变量（`:root`，JS 读计算样式、读不到用回退值）：`--sp-check-red` 60ms、`--sp-check-draw` 110ms；取消 = `--sp-uncheck-draw` 55ms + `--sp-uncheck-red` 30ms（**时长减半、速度加倍**）；收起 Morph `--sp-collapse` 220ms；缓动 `--sp-ease-std`。
- **收起 Morph**：上层选项卡整体淡出 + 下层基础卡面淡入（两层交叉），卡宽 / 位移由 JS 内联 `transition` 给出；**动画终点与重绘后的静态卡面像素级一致**，落定不跳。
- **回弹只在「本次交互真的选中了该员工」时播一次**（`is-pop` 由 JS 一次性标记），避免每次重绘重播。
- `prefers-reduced-motion: reduce` 下所有相关动画 / 过渡关闭，直接落定。

### 6.11.8 门控与已知修正
- 勾选 / 收起动画期间**门控忽略重复点击**，动画播完再应用下一次编辑，避免连点导致状态错乱。
- 已修：重绘重播回弹（`is-pop`）；取消勾选的红色硬切（`is-undraw` 必须**反超** `.is-on` 特异性）；`is-opened` 死字段及其选择器删除。

### 6.11.9 两种形态分流：按门店员工数【R20 新增 · R21 修订卡面 · R22 压弱未选中态 · R23 加回勾选控件（三态常驻）+ 顾客指定行间距】

> **一句话**：**员工 > 5 人**用「槽位行 + 选择页」（§6.11.1）；**员工 ≤ 5 人**用「标题 + 卡列」的紧凑形态（本节）。两套形态**只换皮、不换数据**。
> 演示门店 `STAFFS` 为 5 人 → 默认走紧凑形态；评审「> 5 人」分支用左侧导航 **「16 员工选择（12 人）」**。

**A. 分流判定**
- 判定口径 = **门店在职员工总数**（`staffPool().length`）；`≤ SP_SMALL_MAX（5）` → 紧凑形态，否则槽位 + 选择页形态。
- 5 这个阈值的来历：紧凑形态一张卡 59px + 6px 间距，**5 张 = 319px**，正好落满最窄宿主（添加 sheet 内 322px）；第 6 张就必须换行或横滑，故超过 5 人回到网格选择页。

**B. 紧凑形态（≤ 5 人）**
- **结构**（`.sp-cards`）：4 个工位块，**块内第一行是工位标题**（大工 / 中工 / 小工 / 顾客指定，13px `#1a1a1a`，**仍在左侧、不再与槽位同行**），**第二行是该工位的卡列 —— 横列「全部」员工卡**，**第一张卡与标题左对齐**（同一左内距，标题与卡列成一条竖线）；块之间 **0.5px 发丝线 `#F0F0F0`**。
- **卡**（`.sp-card`，**R21 定为文字卡 · R22 重排三态 · R23 加回勾选组件**）：**59×52**，**无头像**；上 = **姓名 13px**（取自 `st.name`），下 = **职位 `st.role` 11px**（店主 / 美容师 / 店长 / 美甲师…）；白底、**圆角 12**、间距 6。工位由行标题表达，**卡内不写工位名**。
  **三态色值（R22 定稿 · R23 增列勾选控件与文字轴）**：

  | 状态 | 描边 | 姓名 | 职位 | 右上角勾选控件 | 内距 / 文字轴 | 姓名对比度 |
  |---|---|---|---|---|---|---|
  | **未选中** | 0.5px `#EDEDED` | `#55595E` / **500** | `#A6A6AB` / 400 | **空心圈**（14×14 白底 + 1px `#C7C7CC` 环、无勾，`top/right:-4px`） | `padding-right:2px` → 内容 54，文字轴 **卡左+29.5** | **7.05:1** |
  | **选中**（`.is-on`） | 0.5px `#F32F41` + 0.5px 外扩阴影＝视觉 1px | `#1A1A1A` / **600** | `#8E8E93` / 400 | **实心勾**（14×14 `#F32F41` 圆点 + 8px 白勾 + 1px `#fff` 描边，`top/right:-4px`） | `padding-right:2px` → 内容 54，文字轴 **卡左+29.5**（与未选中**同轴**） | **≈15.8:1** |
  | **置灰**（`.is-full`，该行满 3 人） | 沿用未选中描边 + **40% 透明** | `#1A1A1A` / 600（合成后 `#A3A3A3`） | `#8E8E93` / 400 | **空心圈**（与未选中同款，随整卡 40% 一起淡） | `padding-right:2px` → 内容 54，文字轴 **卡左+29.5** | **2.52:1** |

  > **R22 关键点**：选中态**一行没改**，改动全部落在**未选中态**上（`#E5E5EA→#EDEDED`、`#1A1A1A/600→#55595E/500`、`#8E8E93→#A6A6AB`）。
  > R21 时未选中姓名是 `#1A1A1A/600`，与选中**完全同色**，两态只差一条 1px 描边 → 实测「看不出差别」；压弱未选中后，`.is-on` 那两条 `color:#1a1a1a / #8E8E93` 才真正起到区分作用。
  > **两条防回归护栏**：① `.sp-card.is-on .sp-card__nm` 必须钉住 `font-weight:600`，否则被压弱后的 500 带跑；② `.sp-card.is-full .sp-card__nm / __role` 必须钉回 `#1a1a1a / #8E8E93` —— 否则 40% 透明下姓名合成值会从 `#A3A3A3`（2.52:1）掉到 `#BBBDBF`（**1.88:1**，看不清是谁）。
  > **R22 的附带收益**：R21 曾把紧凑卡描边从 `#EDEDED` 提到 `#E5E5EA`，而 **> 5 人选择页的 `.sp-pick` 一直是 `1px #EDEDED`**；R22 改回 `#EDEDED` 后，两套形态的发丝描边**颜色重新统一**（线宽仍各自为紧凑 `0.5px` / 选择页 `1px`）。
  **右上角勾选组件（R23 加回 · 三态常驻同一位置 · 0 位移 0 遮挡）**：
  - **R21 的删除理由与 R23 的两版解法**：R21 删勾标是因为「59×52 文字卡上勾标会压姓名 / 职位」。R23 先试 **方案 2「内嵌右上角 + 选中态文字整块左移让位」**（勾标独占 `.is-on` 的 `padding-right:16px`、文字缩到 40px、文字轴 29.5 → 22.5），实机观感不佳（点选瞬间文字整块跳动）已否决；最终定为 **方案 1「勾选控件挂在卡片右上角外侧」** —— **卡内内距、文字位置三态完全不动**，控件只压圆角外的空白角落。
  - **位置与尺寸**：`.sp-card__tick` = **14×14**、`border-radius:50%`、`box-sizing:border-box`、**`top:-4px / right:-4px`**。绝对定位的偏移基准是卡片的**内边距盒**，卡自身有 0.5px 描边 → 实测控件**外探出卡框 3px**（上 / 右各 3.00）。绝对定位 **不参与布局**，因此卡尺寸恒 **59×52**、卡列宽度不变。
  - **三态外观（只有外观变、坐标不变）**：默认（未选中 / 置灰）= **白底 `#fff` + 1px `#C7C7CC` 空心圈**、圈内 `<svg>` `display:none`；`.sp-card.is-on .sp-card__tick` = **`background: var(--brand)`（`#F32F41`）+ `border-color:#fff` + 勾标 `display:block`**（8px 白勾，`SP_TICK_SVG`）；切换只过渡 `background-color / border-color`（各 `.18s ease`），**不做任何位移 / 缩放**。
  - **实测三态同坐标**：未选中 `tickOffX/Y = 48.00 / -3.00`，选中同为 `48.00 / -3.00`；文字轴三态恒 **卡左 + 29.5**、`padding-right` 三态恒 `2px`；姓名与职位**始终共轴**。
  - **实测 0 遮挡**：控件圆（半径 7）与姓名 / 职位**内容盒**（`Range` 实测）**均不相交**；控件外探的 3px 落在宿主内距里（最窄宿主「添加 sheet」：卡列 322 = 5×59 + 4×6，外层 `.addsheet__body` 还有 12px 内距），**不被 `.sp-face{overflow:hidden}` 裁掉**（实测最小余量 **32px**）；控件是卡的子节点，`elementFromPoint` 命中的是 `sp-card__tick` / 其 `<svg>`，事件冒泡到卡根 → **点控件 = 点卡**（可选中、可取消），**外探部分也不吃掉热区**。
  - **不发生的地方**：**> 5 人选择页的 `.sp-pick__tick`（16×16 / `top:5 right:5`）本轮不动**。
- **点卡即选中＝加入该槽位**：卡随即变为选中态（**品牌描边**（0.5px 边 + 0.5px 外扩阴影 = 视觉 1px）+ 淡粉底 + **右上角勾选组件**，R23）+ **落位弹跳**（`spCardLand`：scale .82 → 1.06 → 1，340ms，**只给「员工 id + 工位行」都匹配的那一张**播一次；同人其它行不连带）；**再点同一张卡即移出**（同一手势，无二次确认）。
- **每行上限仍是 3 人**（`SP_ROW_MAX`）：该行已有 3 人时，**其余卡置灰**（`is-full`：`opacity:.4` + `pointer-events:none` + `aria-disabled="true"`），强行点第 4 张 → toast「{工位}最多 3 人」且不写入数据。
- **跨行可重复**：同一员工可同时是「大工」与「顾客指定」，两行的选中态各自独立（不互斥）。
- **R22 区分度**：未选中态压弱后，「选中 / 未选中」的姓名由**同色**变为 **`#1A1A1A` vs `#55595E`**、字重 **600 vs 500**，并叠加品牌描边与淡粉底；一屏 12 张选中卡时仍不糊。
- **「顾客指定」行间距（R23）**：`.sp-cards__row--extra` 加 **`margin-top:6px`** → 与「小工」行行间 **6.5 → 12.5px**（发丝线随该行下移），与 > 5 人槽位形态（§6.11.1，14.5 → 20.5px）**同步**。
- **没有的**：❌「＋」虚线空槽位、❌ 员工选择页（morph）、❌ 顶部「最多 3 人」提示（R18 起已删）、❌ 卡内头像（R21 起）。**（右上角勾选控件 R23 已加回：未选中 / 置灰 = 空心圈、选中 = 实心勾，见上方勾选组件小节）**
- **实测几何**（5 人 · R21 / R22 / R23）：卡高 52；4 行合计约 **323px**；添加 / 结算 / 直接收款三处宿主仍**不横滑、不换行、不因卡面改版而滚动**（去头像后总高更矮，余量更大）。**R22 复核**：卡列 `scrollWidth = clientWidth = 322`；添加 sheet 高 **633px**（上限 85%）、s6 sheet **395px**、s2 面板 **388px**（上限）—— 三处 body 均 `scrollHeight = clientHeight`，**不滚动**；4 个入口逐个实测，**24 项断言全通过**。**R23 复核**：卡尺寸、卡列宽度、三处宿主高度**与 R22 完全一致**（勾选控件为绝对定位、不参与布局；三态都不改 `padding-right`，卡宽恒 59，因此 5 张 = 319px 的落满结论不变）；**4 个入口 + > 5 人选择页共 211 项断言全通过**。

**C. 槽位 + 选择页形态（> 5 人）的两点修改**
- **① 选择页隐藏「价格（元）」卡**（`#addSheetMask .addsheet.is-picking #asPriceCell { display:none }`）：进入选择页时该屏只留「员工」一件事；**只是隐藏显示**，价格值仍留在 `input` 内，退出选择页（落位 / 取消）**立即原样恢复**，不影响应付与结算。仅作用于**添加 sheet**（s6 的「修改服务员工」sheet 本就没有价格卡）。
- **② 高度自适应 + sheet 上限抬到 92%**：
  - `.sp-face--pick` 原为固定 `max-height:560px`（4 行卡时会提前截断），改为渲染后实测 `.sp-pick-inner` 的 `offsetHeight` 写入 `--sp-pick-h`，**选择页高度完全由内容决定**（morph 动效照旧）；
  - 选择页态 `.addsheet` 的 `max-height` **85% → 92%**（仅抬上限，高度仍由内容决定）；
  - 实测（12 人 / 4 行卡）：选择页头 38 + 网格 404 = **442px**，sheet **696px**，body `scrollHeight = clientHeight = 546` → **不滚动**；推演 6 / 9 / 12 / 15 人 = 386 / 489 / 592 / 695px，**都在 776px 上限内**；18 人（6 行卡）798px 起才滚动。
- 槽位行、落位动效、上限 3 人、摘要文案、数据键**一律不变**。

**D. 行为对照表**

| | ≤ 5 人（紧凑形态） | > 5 人（槽位 + 选择页） |
|---|---|---|
| 工位标题位置 | 每块独占一行、左侧 | 与槽位同行的左侧列（宽 52） |
| 选人路径 | **点卡即落位**（无二次点击） | 点「＋」→ 选择页 → 点卡落位 → 自动回到槽位行 |
| 已选表达 | 卡本身的选中态（描边 + 粉底 + **右上角实心勾**，R23） | 槽位里的员工卡（头像 + 姓名 + **右上角删除 ×**，R23） |
| 右上角角标 | **勾选组件**（14×14 挂卡外 `top/right:-4px`；未选中 / 置灰 = 白底 + 1px `#C7C7CC` 空心圈，选中 = 品牌实心圆 + 白勾；三态坐标零位移） | **删除 ×**（14×14 `#C7C7CC` 圆点 + 白 ×；纯视觉，热区仍是整卡） |
| 移除路径 | **再点同一张卡** | 点槽位行里的已落位卡（**整卡即热区**） |
| 「顾客指定」行间距 | 行间 **12.5px**（R23 +6） | 行间 **20.5px**（R23 +6） |
| 满 3 人 | 该行其余卡置灰 + toast | 该行「＋」消失；选择页内其余卡置灰 |
| 价格卡 | 保持显示 | 选择页态隐藏 |
| 滚动 | 4 行卡全部可见、不滚动 | 选择页高度自适应；≤ 5 行卡（15 人）不滚动 |

**E. 演示入口**
- 左侧导航 **「16 员工选择（12 人）」** → 临时把员工池换成 `STAFFS + STAFF_EXTRA`（共 12 人）→ 打开添加 sheet 并**直接落到「选择大工」选择页**；**关闭 sheet 即退出演示态**（池还原为 5 人）。
- 演示态下选中的员工（`st5`–`st11`）在摘要 / 购物车 / 挂单里都能正常显示姓名 —— 姓名查找统一走 `staffById()`（覆盖扩编池），不会露出 id。

## 6.12 底部工具栏

- hint-bar：「具体金额以结算为准」（s1）。
- bottom-bar：购物车图标（含数量角标）/ 「应付」+ 金额 / 挂单 / 下一步（s1、s5）。
- **左侧热区**：购物车图标 + 角标 + 「应付 ¥金额」整块可点；挂单 / 下一步不触发。
- **空车点热区**：不打开 Sheet，toast「请先选择项目」。
- **已选商品 Sheet**（本期重做，对齐 Figma）：
  - 从 bottom-bar **后一层**向上弹出（Sheet `z-index` 低于 bar；bar 始终浮在最前；遮罩在 Sheet 后）。
  - 标题「已选商品」；「清空」+ 垃圾桶（**二次确认**后清空全部明细、角标、合计并关闭）；右上关闭。
  - 行：名称；**下一行员工摘要**（如「张三 · 大工 · 顾客指定」；未选为「未选员工」）；单件应付价；± 数量步进（减到 0 删行，上限 999）；合计与角标随步进重算。
  - **点行回跳编辑**：点击明细任一行的**行体**（非步进器）→ 关闭 Sheet，跳回开单页（会员笔 → s5、散客笔 → s1；Tab 切到该项目/产品、左侧分类重置为「全部」），并**打开该商品的「设置内容」sheet 回填已保存数据**（数量、应付、服务员工/工位/顾客指定）；此时点 sheet「确定」为 **更新原明细（替换，不新增）**，toast「已更新该笔」。
  - 明细行的 ± 步进器**不触发跳转**，仅在 Sheet 内改数量。
  - 与 s6 共用 `cartItems`；拆分行各自独立展示。
- 下一步：有项 → 进入 **s6 开单结算**；无项 → toast「请先选择项目」。

## 6.13 开单结算 s6【本期变更】

> **交互效果与各类弹窗（员工面板、权益选择 sheet、备注 sheet、小计编辑、toast 等）以当前在线版本为准，原型仅为评审演示**；本节约定的业务规则（权益、改价、工位/顾客指定等）为开发约束。

- **顶部**：身份卡（散客/会员，随进入时的身份），点头像 / 姓名 → s4。**R10：顾客卡片在本页吸在滚动区顶部（sticky），会被自上而下的明细白卡逐步盖住，见 6.26**。
- **标题栏**：**已删除日期入口**（R3 本轮变更）；**R14 在标题栏右侧新增「更多」菜单**（**3 圆点横排**图标、**主文字色 `#1a1a1a`**、24×24 热区 / 20×20 图标、`right:10px`，**图标中心与 18px 标题墨迹中心对齐**），点击弹出**「选日期」「手工单号」**两项下拉（左侧均有图标），见 **6.28**。s7 同步（见 6.14）。
- **手工单号卡片**：**已删除**（本轮变更；单号改在成功页「改单号」维护，或在结算页用标题栏「更多 → 手工单号」预先指定，见 6.28）。
- **明细卡**（一条已加购项目/产品一张）：
  - 首行：缩略图（无图时以「项」/「产」占位）+ 类型小标签（项目「项」/产品「产」）+ 名称 + 单价（¥；**原价**，R16 起不再展示任何权益价）+ **垃圾桶**（删除该明细，同步扣减对应分类角标与应付；**R14：图标色统一 `#929292`** —— 图标为 SVG 文件 `assets/ic_trash_figma.svg`，其 `stroke` 直接写死为 `#929292`，故 `img` 引用时也保持灰，不随按钮文字色变化）。
  - **服务员工行（R16 改为可编辑）**：展示已选员工摘要（默认自动填充首位员工，每名员工一行「姓名 · 工位 · 顾客指定」）；**整行为热区、右侧带 `›` 箭头**，点击 → 打开**「修改服务员工」sheet**（槽位式选择器，见 6.11）；**R16 起不再有「员工详情」折叠块**。
  - **权益**行：默认自动匹配权益（未改价时，见 5.5）；**仅右侧「值 + ›」为热区**，点击 → 打开**「选择权益」sheet**；改价项锁定「改价商品不可用权益」。**行左侧「权益」二字后的 `?` 为独立热区**（R8 新增）→ **「权益说明」弹窗**（见 6.13.2），点击不打开权益 sheet。
  - **开单内容**行：显示已输入内容或「未输入」；点击 → 备注 sheet。
  - **小计**：已移除（v2 结算文档流仅保留「应付」）。
  - **应付（R16 改为可编辑）**：= 权益后应付；**整行为热区、右侧内联 14×14 铅笔图标（`#929292`）**，点击 → 打开**「修改应付」数字键盘 sheet** 修改**本项**金额（见 6.13.1）。改价后本项**权益锁定**（行内显示「改价商品不可用权益」）。
  - **行间分隔**：商品信息头下沿为 **0.5px 发丝浅灰实线 `#E7E7E7`**，左右各内缩 16px；「服务员工」「权益」「应付」**行之间为虚线**（1px、4px 段 + 4px 间隔的同色虚线），同样左右各内缩 16px（s6/s7 统一）。**R11：「服务员工」行的上下留白以右侧员工文字块为准 —— 行内固定上下内距 10px（行高 = 文字块高度 + 20px）**，见 6.27。
  - **服务员工行（R11 · R16）**：**摘要形态不变**（每名员工一行「姓名 · 工位 · 顾客指定」），**R16 起不再是只读** —— 整行为热区、右侧 `›`，点击打开「修改服务员工」sheet。单行（1 位员工）时行高仍为 40px（`min-height`）→ **与「权益 / 应付」行观感完全一致**；**选中 2 位及以上时行盒随文字块长高、上下各保留 10px 留白**，文字**不再贴住上下分隔线**（改前 2 行时行高 = 文字块高度、上下留白为 0）。左标签「服务员工」随整行**垂直居中**。
- **订单级「优惠券」行 + 「备注」卡**（明细卡之下，v2 注入）：优惠券行 = 「优惠券」+ 右侧值（默认「请选择」/ 已选显示券名，**仅右侧热区**）→ 打开**「选择优惠券」sheet**；备注卡 = 标题「备注」+ 多行输入（500 字上限，右下角计数）。
  - **右侧文字色（R14 修订）**：**默认态（未选）为次要色 `--text-sec`；已选态由红色 `--brand` 改为主文字色 `#1a1a1a`** —— 选用优惠券不是错误状态，红色会与「应付 / 改价」等金额强调抢色，故已选只表达「此处有值」。
  - **右侧箭头（R14 修订）**：由**文字箭头 `›` 改为 SVG**（`viewBox 0 0 24 24`、14×14、`stroke-width:2.5`、色 `#C7C7CC`）—— 此处是原型里**最后一处文字箭头**，替换后**全站同类「进入」指示图标均为 SVG**（其余早已是 SVG：权益行 `.co-arr`、s15 操作组、s14 删除等）。
- **底部 co-bar**：应付 ¥{合计}（= Σ 明细权益后应付 − 优惠券抵扣；**R16：总价为只读，不可点击、无铅笔图标，改价入口在明细行**）+ **挂单**（有明细 → 写入挂单池、清空购物车与角标、toast「挂单成功」、跳 **s14 挂单列表**；无明细 → toast「请先选择项目」）+ **结账**（无明细 toast「暂无结算项目」；有明细 → toast「结账流程说明」并进入 **s15 开单成功**，清空购物车与角标）。

### 6.13.1 按明细改价（应付 / 服务员工）【R6 新增 · R16 改版】

> **R16 变更：整单改价（底栏「应付」可点击 → `coPayOverride`）整体删除**，改价入口下移到**每条明细**；**底栏应付总价恒为系统计算值、不可修改**。**s6 与 s7 都支持**（R16 前 s7 为只读）。

- **入口**：明细卡「应付」行**整行热区**（`data-price-edit`，右侧内联 **14×14 铅笔 `#929292`**，按下底色 `#FAFAFA`）→ 底部**数字键盘 sheet**（`#coPayMask`）。
  - 底栏「应付 ¥{合计}」`#coPayBox` 由 `button` 降为**普通容器**：**铅笔图标、「已改价」标签、底栏上方黄色说明条（`coLockNote`）全部删除**。
- **数字键盘 sheet**：标题「修改应付」+ 大号数值行 + `1-9 / 0 / . / 退格` + 左侧「清零」+ 底部「取消 / 确定」（确定 `#F32F41`）。**数值行占位提示为「原价 ¥xx」**（= 单价 × 数量的原始金额）。
- **确认规则**（按明细判定）：
  - 输入值与**原价**（`(salePrice ?? price) × qty`）相差 ≤0.01 → **视为未改价**：该明细权益**恢复可用**并重新自动匹配；
  - 相差 >0.01 → **视为改价**：该明细 `discounted = true`、**权益清空并锁定**（行内显示「改价商品不可用权益」，降透明度 + 不可点），该明细实付 = 手输值；
  - **「清零」或留空后点确定 = 恢复原价**（toast「已恢复原价 ¥xx」）；否则 toast「应付已改为 ¥xx」。
- **服务员工**：明细卡「服务员工」行右侧 `›` → **「修改服务员工」sheet**（与添加 sheet 同一个槽位式选择器，见 6.11）；**员工变化不影响金额**，只更新该明细的员工记录、摘要与挂单快照。
- **s7（直接收款）**：单条明细（`qcoItem`，`payEditIdx = -1`）走同一套入口与规则，复用同一个 sheet 与同一套改价判定。
- **联动**：明细改价**只作用于该条明细** —— 其余明细的权益、优惠券抵扣、底栏总价均照常按规则重算（底栏 = Σ 明细实付 − 券抵扣）；**明细改价后优惠券仍可更换 / 取消**，按门槛重新计算抵扣。
### 6.13.2 「权益说明」弹窗【R8 新增】

- **入口**：s6 / s7 结算页「权益」行左侧的 **`?`** 圆形图标（14×14，`#C7C7CC` 描边 / `#929292` 字）；**全站仅此一处**（「选择权益」sheet 不加）。图标可点，点击**不打开权益 sheet**（捕获阶段拦截并 `stopPropagation`）。
- **形态**：**居中弹窗**（复用 `proto-confirm` 视觉 —— 白底、圆角 12、宽 320、`0 8px 28px rgba(0,0,0,.18)` 投影）；点击遮罩空白处或「我知道了」关闭。
- **结构**：标题「权益说明」（16/600，高 52）+ 可滚动正文（内距 20，`max-height:440px`，内容超出才滚动）+ 底部通栏「我知道了」（高 48，`#F32F41` 600）。
- **正文文案（定稿，逐字实现）**：

> **权益是什么？**
> 权益是指在开单时选择的开单项目，针对持有会员卡的顾客所能享受的卡折扣（储值卡、折扣卡）或次卡（套餐卡、周期卡、计次卡）抵扣
>
> **不同类型的顾客所对应的权益？**
> 1. 有卡顾客：享受有所办会员卡的折扣权益或次卡抵扣
> 2. 无卡顾客：无可使用的权益
> 3. 散客：无权益
>
> **如何使用权益？**
> 1. 开单时选择项目，会默认优先展示特殊项目折扣，其次是次卡（计次卡、套餐卡、周期卡）的抵扣权益，最后是储值卡和折扣卡的折扣权益
> 2. 根据门店的具体场景，可针对选择的每一个项目所拥有的权益再次调整，如无需要，则按照系统默认权益，最终根据应付的金额去选择支付方式进行结账

- **文字规范**：小节标题 14/600 `#1a1a1a`，正文与列表 13/400 `#666`，行高 20，列表左缩进 18。

## 6.14 直接收款结算 s7（新功能）【本期变更】

> **交互效果与各类弹窗（员工面板、权益选择 sheet、备注 sheet、小计编辑、toast 等）以当前在线版本为准，原型仅为评审演示**；本节约定的业务规则（直接收款权益四类、改价、工位/顾客指定等）为开发约束。

- **顶部**：身份卡（散客/会员），点头像 / 姓名 → s4；在 s7 换人后金额仍按 **原价** 计算，不做身份折扣。**R10：顾客卡片的吸顶与「被明细白卡盖住」行为与 s6 一致（见 6.26）**。
- **标题栏**：**已删除日期入口**；**手工单号卡片已删除**（同 s6）。**R14：与 s6 一致新增标题栏右侧「更多」菜单（「选日期」/「手工单号」）**，两页几何完全相同（同一个单实例菜单组件），见 6.28。
- **直接收款明细卡**：
  - 首行：闪电缩略图 + 红色闪电小标签 + 名称「直接收款」+ 金额（¥）+ **垃圾桶**（删除 → 回 s2，toast「已删除」）。
  - **服务员工行（R16 改为可编辑，同 6.13）**：右侧 `›` → 「修改服务员工」sheet（`data-ctx = qco`）。
  - **权益**行：**默认自动匹配**（会员=最优折扣卡、散客=「无卡权益」，与 s6 一致）；**仅右侧「值 + ›」为热区**，点击 → 打开**「选择权益」sheet**（可选权益类型见 5.5，门槛不满足置灰）；改价后锁定；用户选「不使用权益」后不再被自动匹配覆盖。
  - **开单内容**行：显示内容或「未输入」；点击 → 备注 sheet。
  - **小计**：已移除（v2 结算文档流仅保留「应付」）。
  - **应付（R16 改为可编辑）**：右侧 14×14 铅笔（`#929292`）→ 「修改应付」数字键盘 sheet；**s7 不再只读**，改价规则同 6.13.1（清零 / 留空 = 恢复原价）。
- **订单级「优惠券」行 + 「备注」卡**：与 s6 同一套组件与状态（见 6.13）；优惠券行右侧 → 打开**「选择优惠券」sheet**。
- **底部 co-bar**：应付 ¥{合计}（= 权益后应付 − 优惠券抵扣；**总价只读不改价，改价入口在明细行**）+ **挂单**（toast「直接收款不可挂单」，不入挂单池）+ **结账**（有金额 → toast「结账流程说明」并进入 **s15 开单成功**，清空 s2/s3/s7 状态）。

## 6.15 门店流水 s8（逻辑补充）【本期变更】

- 入口：原型左侧导航直达（不承载业务链路）。
- **顶部**：返回 + 标题「门店流水」+ 「尾款单」（¥ 角标）与「筛选」（图标）按钮（点击 toast 示意）。
- **Tab**：全部(1) / 项目(1) / 产品(1) / 会员卡 / 作废；带计数角标；点击切换选中态。
- **流水卡**：顾客头像 + 姓名 + 状态（已完成）｜商品行（闪电缩略图 + 红色闪电标签 + 「直接收款」+ 金额）｜服务人 | 姓名｜支付方式（微信图标 + 微信）｜扣款（-¥金额）｜实付总额：¥金额｜开单时间。

## 6.16 选择退款项目 s9（逻辑补充）【本期变更】

- 入口：原型左侧导航直达。
- **提示条**：全选项目进行退款可以选择原路退回与指定退回，选择部分项目或已有退款项目则只可指定退回。
- **退款项目列表**：闪电缩略图 + 红色闪电标签 + 「直接收款」+ 原价 / 实付 + 勾选圆点（点击勾选/取消）。
- **底部 rf-bar**：全选（与列表勾选双向联动）+ 确认退款（toast 示意）。

## 6.17 业绩设置 s10（已删除）【R6】

> **本页已从原型与本 PRD 中删除。** 线上「业绩设置」模块已重构，原原型页面（劳动业绩 / 销售业绩 Tab、「直接收款」置顶分类、工位 × 顾客指定业绩表）已不适用，故整体移除：页面 HTML、左侧导航项、相关 CSS 与交互、PRD 章节。业绩口径以线上重构后的实现为准，本 PRD 不再约定。

## 6.18 提成方案 s11（已删除）【R6】

> **本页已从原型与本 PRD 中删除。** 线上「提成方案」模块已重构，原原型页面（方案名称卡 / 劳动提成·销售提成 Tab、「直接收款」置顶分类、工位 / 顾客指定提成表）已不适用，故整体移除：页面 HTML、左侧导航项、相关 CSS 与交互、PRD 章节。提成口径以线上重构后的实现为准，本 PRD 不再约定。

## 6.19 标题栏日期控件（本轮变更）

- **位置**：标题栏右侧「日期文本 + 日历图标」为一组**可点控件**。
- **覆盖页面（本轮收窄）**：
  - **s1 / s5 / s6 / s7 的日期入口已删除**（本轮变更）；
  - 日期浮层能力保留，**仅由 s15 开单成功页「改日期」按钮唤起**（复用同一浮层，选择后更新成功页「开单日期」展示）。
- **默认值**：以**今天**为基准，格式 `YYYY.MM.DD`。
- **展开（R6 调整：s15 场景改为底部 sheet）**：自 s15「改日期」唤起时，日期控件以 **底部 sheet 形态** 升起 —— 容器 `left:0; right:0; top:auto; bottom:0`、宽度自适应、`border-radius:16px 16px 0 0`、带上投影与上滑动效（`cubic-bezier(.32,.72,0,1)`），并带轻蒙层；**点蒙层或下滑关闭**。（标题栏下拉形态已无使用场景，样式仅作为 sheet 形态的实现来源保留。）
- **浮层内容**：月份标题（`YYYY年M月`）+ 上/下月切换 + 星期表头（日→六）+ 日期网格（固定 6 行，高度稳定）+ 快捷「今天」「昨天」。
- **可选范围**：**今天往前推 3 个月**至今天（如今天 2026-09-17 → 可选 2026-06-17 ~ 2026-09-17）。**未来日期与范围外日期置灰不可选**；已无可用日期的月份，其上/下月按钮置灰。
- **选择后**：浮层关闭；在成功页场景下更新 s15 的「开单日期」文案（不回写结算页）。

## 6.20 开单页顾客卡片折叠（取代原「滚动视差与吸顶」）【本轮变更】

- **原「滚动视差 + 价目表吸顶覆盖顾客卡」方案已删除**（连同实现与样式一起移除）。
- **新方案**：顾客卡片**默认收起**，价目表白卡直接上移贴住标题栏下沿；用户通过**标题栏右侧顾客入口**展开/收起卡片（交互与动效见 6.1、4.3）。
- 卡片展开 / 收起不改变价目表的滚动位置；页面按文档流自然排布（无固定/吸顶定位）。
- **R10 补充**：开单页**没有**重新引入滚动视差；结算页（s6/s7）新增的「明细白卡盖住顾客卡片」效果是**独立新增**并与开单页**同源**（见 6.26）。
- 已选商品 Sheet、设置内容 sheet、员工面板等弹层行为不受影响。

## 6.22 挂单列表 s14【R5 改版】

- **入口**：s0 首页「取挂单（N）」按钮；s4 选择顾客标题栏右侧「挂单（N）」（R16 改名）；s1 / s5 / s6 挂单成功后自动跳入。左侧导航含「14 挂单列表」可直达（评审用）。
- **顶部**：状态栏 + 返回 + 标题「挂单列表」。
- **顶部失效提示条（R5 新增）**：标题栏之下为**全宽浅黄提示条**（高 40，`#FFF8E5` 底 / 文字 `#B26B00` 12px + info 图标），文案固定为 **「每日24:00后 挂单自动失效并删除」**；提示条**不随列表滚动**（列表滚动区自提示条下沿开始），空态下同样显示。
- **挂单卡（R6 信息分层）**：
  - **卡头**：顾客名（**16px / 600**；散客显示「散客」或「男散客 / 女散客」）+ **红色 VIP 图标**（`assets/ic_vip.svg`，**26×10**，仅会员；**R16 由渐变胶囊换成该图标** —— 与开单页顾客卡会员姓名右侧的 VIP **完全同款**）+ 右侧挂单时间（12px `#929292`，数字字体）。**R6 已删除卡上的「开单」类型标签**（直接收款不可挂单，该标签无区分意义，见 5 章）。
  - **发丝线**：`#E8E8E8`、0.5px、左右贴卡内边距。
  - **明细摘要**：商品名 × 数量，顿号分隔，**最多两行**（超出省略）。
  - **字段组**（标签左 / 值右，与结算页同构）：**「服务员工」**——取该挂单**全单服务员工并集**（按明细顺序去重），**单行显示全部姓名**（顿号分隔，**不使用「等 N 人」省略**，姓名过长可换行）；无员工显示 `—`。
  - **操作行**：左侧**金额**（**20px / 500**，`¥` 13px，数字字体）+ 右侧**删除按钮**（32×32 圆形灰图标）+ **「去结单」按钮**（高 34 / 圆角 17 / `#F32F41` 实心；**R16 由「取挂单」改名「去开单」，R20 文案定稿为「去结单」**）。
- **空态（R5 改版）**：居中 `ic_empty.svg`（72×72）+「暂无挂单」+ 说明「挂起的单据会显示在这里」；无操作按钮。
- **动效（R5 新增）**：进入本页时卡片**错峰淡入上移**（间隔 50ms，仅首次渲染播放；删除后的重渲染不重播）。
- **返回**：回到进入前的页面（首页 / s4 / 结算页）。

### 6.22.1 挂单生命周期与删除条件【R6 收敛】

> **核心规则：挂单只在三种情况下被删除，其余一切操作都不删除。** 每条挂单带 `from`（挂起来源页）与 `day`（生成日）字段。

| 删除条件 | 触发点 | 行为 |
|----------|--------|------|
| **① 手动删除** | 挂单卡右侧删除按钮 | **点击即删（无二次确认）** + toast「已删除「{顾客}」的挂单」；卡片高度塌陷 + 淡出 200ms；`N` 同步刷新 |
| **② 超时自动删除** | 进入 s14 挂单列表时 | 过滤掉 `day < 今天` 的挂单（即超过当日 24:00 失效）；若有被清理的挂单，进入列表时 toast「有 N 张挂单已超过 24:00 自动失效并删除」；`N` 同步刷新 |
| **③ 开单成功删除** | s6 结账成功（进入 s15） | 若本次结算源自某条挂单（`activeHoldId` 有值），该挂单从挂单池移除 |

- **去结单不删除（R6 修正）**：点「去结单」**不**从挂单池移除该条，仅**还原**：顾客身份 + 购物车明细（含每行 `staffIds / staffRoles / staffExtra`，工位与顾客指定原样还原）+ 优惠券状态；同时记录 `activeHoldId` 指向该条。同一张挂单**可被反复取用**。
- **去结单后的回跳（R6 新增）**：按该挂单的 `from` 决定落点 —— 从 **s1/s5 开单页**挂起 → 回到 **s1（散客）/ s5（会员）**；从 **s6 结算页**挂起 → 回到 **s6**。原型演示数据已覆盖两种来源。
- **`activeHoldId` 的失效**：去结单后如**新增商品、清空购物车、重新挂单**，则与来源挂单解绑（`activeHoldId` 置空）；此后结账**不会**删除原挂单（原挂单仍可再取）。
- **`N` 计数口径**：`N` = **未失效**的挂单条数（已过滤超时项）。
- **`from` 字段取值**：`order`（s1/s5 开单页）｜`checkout`（s6 结算页）；会员开单页额外携带顾客身份以便回到 s5。

## 6.23 开单成功 s15【R5 改版 · R13 补打印小票 · R19 打印小票去按钮感 · R20 去金额计数】

> 设计来源：Figma `开单结账-ZM` 节点 `151-7794`；原型为全屏页 `#s15`（390×844），字体全局 Noto Sans SC。

- **入口**：s6 / s7 点「结账」成功后（**先 toast「结账流程说明」，再进入本页**）；左侧导航含「15 开单成功」可直达（评审用）。
- **顶部（R9 换色 / R11 调淡，替换 R5）**：状态栏 + **无标题栏**（原标题栏「支付」与「开单成功」徽标构成双标题，已删除）+ 顶部**纯纵向渐变背景**（**高 328**，来源 Figma `RTB老版本优化` 节点 `1174-135`）：`#FFBE84 0% → #FFD996 23.558% → #FFE59D 58.173% → #FEE9AF 62.356% → #FDEDBF 66.538% → #FDF0CD 70.721% → #FCF2D9 74.904% → #FBF5E3 79.087% → #FAF6EB 83.269% → #F9F7F1 87.452% → #F9F8F5 91.635% → #F8F8F7 95.817% → #F7F7F7 100%`；**末段精确落到页面底色 `#F7F7F7`，与页面无缝**。**R11：整层不透明度 70%**（`.suc-bg { opacity:.70 }`；R10 曾为 84%，本轮再调淡一档，叠在页面底色上，色标不变）。**R5 的黄色渐变、装饰遮罩图层（`assets/suc_bg_mask.svg`，281 高光斑）与 68px 过渡带已删除**（新稿横向无光斑，也不需要过渡带）。
- **成功标题区（R5 调整）**：居中对勾（`suc_check.svg`，**22×22**）+ 文案「开单成功」（**18px / 600**）；本页**唯一主标题**。
- **内容区操作组（R16 移到金额卡下方）**：**改日期 / 改单号 / 看流水 / 客户详情** 四项 **等分一行**，位于**金额卡下方**（与卡片间距 14px，随卡片一起纵向排布；页面高度不足时才内部滚动）。
  - **形态**：**白底圆角 14 托板**（高 62、`0 2px 10px rgba(120,90,0,.05)`），**4 等分**（各 90px）；每格**竖排「图标 17px + 文字 12px」**（间距 5px，色 `#55595E`，按下转 `#1a1a1a` + 底 `#FAFAFA`）；**格间 0.5px 发丝竖线 `#EDEDED`**（高 24、垂直居中，首格不画）。
  - **客户详情（R16 由底栏移入本组第 4 格）**：人像图标，点击 toast「查看客户详情」（本期不做详情页）。
  - **改日期** → **底部 sheet 形态的日历**（见 6.19 的 R6 调整；沿用同一日期控件、`is-sheet` 样式），选完更新本页**卡内「开单日期」**；
  - **改单号** → 底部 sheet（`suc-no-sheet`）：单号输入行（`inputmode=numeric`、最多 20 位、默认带出当前单号）+ 取消 / 确定（两列等宽），确定后 toast「单号已更新」并更新本页**卡内「单号」**。**R6 修复：该 sheet 的输入行样式补全（`sucno-field`），不再出现样式丢失导致的错位/透明异常；R7：删除 sheet 内提示文案，底部留白收紧；R9：版式对齐「修改应付」sheet —— body 16px 内缩、输入行 56 高 / 圆角 10 / 白底且与按钮同宽、上下留白 16、底部等宽双按钮并与输入行左右对齐、点遮罩关闭。**
  - **看流水** → toast「查看该笔流水」并跳 **s8 门店流水**。
  - **打印小票（R13 新增 · R14 换图标 · R17 移到卡内底部 · R19 换线描图标 + 去按钮感）** → **金额卡底部一整行入口**（`.suc-print-row`，卡片最后一个子元素）：图标 **18px `#8E8E93`** + 文案「打印小票」**13px `#55595E`** 居中，**上方 0.5px 灰色实线 `#E5E5E5`**，**整行即热区（319×44）**，**圆角 0、背景透明**，按下**只加深**（文字 `#1A1A1A` / 图标 `#55595E`，无底色），`aria-label="打印小票"`；**R17 已删除原「卡内右上角 28×28 纯图标热区」**（同一功能不留双入口）。**R19：图标改为 24 画布 stroke 1.6 线描稿**（`fill:none` / `stroke:currentColor` / `stroke-linecap,linejoin:round`；**3 条路径** = 进纸 + 机身 + 出纸，另 1 枚 `currentColor` 指示灯），**渲染线宽 ≈ 1.2px**，尺寸 **17 → 18px**。**R14 的 Figma 稿 `1189-136` 作废** —— 它是 **200 画布填充式**，最细壁厚 5.3/200，缩到 17px 只剩 **≈ 0.5px** 线宽，叠 `#8E8E93` 浅灰在真机上看不清。**R17 位置**：不再落在金额区上方，而是**卡片最底部、明细字段区之下**；**不占用**卡外 4 等分操作组与底部 bar。**与字段行的区分**：字段行之间是虚线，打印行之上是**实线**，且整行**居中 + 图标灰于文字**。点击 → toast「**已发送打印小票指令**」（打印流程本身以线上为准）。
- **内容区整体上移（R17）**：`.suc-body` 的 `top` 由 **182 → 138px**（上移 **44**），用于补掉卡内新增的 **46px** 打印行 —— 卡底实测 **440.5 → 436px**（仅差 4.5px），卡外操作组与底栏位置不变；内容区高 **582px**、本页占用约 **375px**，**不触发内部滚动**。**R19**：打印行高 **46 → 44px**，卡高 **286.3 → 283.7px**、卡底 **436 → 434px**；`.suc-body` 的 `top` **仍为 138px**（无需再动）。
- **金额卡（R6 重构）**：**卡片浮起** —— 卡底 `rgba(255,255,255,.88)` + 1px 白描边 + 柔和投影。

  **① 大号金额区**：金额（**48px** + `¥` **26px**），居中。**R14：`¥` 与数值之间加 2px 间距**（`#s15 .suc-amt .cur { margin-right:2px }`）—— 原两段紧贴，盒间为 0、仅靠字形侧边距，视觉略挤；**用 `margin-right` 而非 `gap`**，因为 `.cur` / `.num` 是行内 span，不是 flex 子项。

  **② 灰色发丝实线**：金额区与其下字段区之间，一条 **0.5px 实线 `#E5E5E5`**（`.suc-hr`），**左右边距各 16px**。

  **③ 字段行区（`.suc-rows` / `.suc-row`）**：字段行统一为 **字段名左（12px `#929292`）/ 参数右（15px / 500 `#1a1a1a`）**，与「折扣卡 / 剩余」同构；**行高 44px**，行与行之间为 **0.5px 灰色发丝虚线 `#E0E0E0`**，**每行左右边距均 16px**（虚线不贴卡边）。

  **④ 字段顺序（R7 修订）**：`单号`（**置顶**）→ `开单日期` → **发丝实线分级** → **支付明细行**（支付方式 / 会员卡 + 金额或次数 + 灰字剩余权益）。
  - `单号` ↔ `开单日期` 之间仍为**发丝虚线**；`开单日期` ↔ 支付明细之间改为**发丝实线**（0.5px `#E5E5E5`，`.suc-split`）。
  - **支付明细行（`.suc-pay`）**：主行 = 左（**14px `#1a1a1a`**，支付方式名或会员卡具体卡名）/ 右（**15px Medium**，金额或次数）；次行 = 左右均 **12px `#929292`**（`剩余` + 值；**R11：折扣卡不出行，故不再有 `折扣` 次行**；**周期卡无次行**）；**主次行之间不画线**，**不同支付方式之间为发丝虚线**；行内上下内边距 8px（首行紧接实线时为 12px）。
  - 金额卡改为**内容驱动高度**（不再是固定 232 / 206 变体，随字段数量自然伸缩）：`padding: 0 16px 6px`。

  **⑤ 打印小票行（R17 新增 · R19 去按钮感，卡内最后一行）**：`.suc-print-row` —— **0.5px 实线之上 + 44px 整行**，**圆角 0、背景透明**，图标 18px `#8E8E93` + 「打印小票」13px `#55595E` 居中；`margin-bottom:-6px` 让该行贴到卡片下内沿（卡片 `padding: 0 16px 6px` 的 6px 被回收），**行高不额外增加 6px**。**R19 按下只加深**：文字 `#55595E → #1A1A1A`、图标 `#8E8E93 → #55595E`（`transition: color .12s ease`），**不再有整行浅底**。

### 6.23.1 成功页字段规则【R6 新增 · R7 修订】

**A. 单号 / 开单日期**
- **取值来源（R14 补充）**：这两个字段在进入成功页时**定稿**，来源有二 —— ① **结算页标题栏「更多」菜单**预先指定（「手工单号」/「选日期」，见 6.28）② 本页操作组的**「改单号」/「改日期」**事后修改。**两者写的是同一字段**，不存在两份数据。
- `单号`：系统生成或用户在「改单号」sheet 中修改后的值；置顶显示。**结算页「手工单号」填了值 → 开单成功时直接采用该值，替代系统自动生成**；**留空则照常自动生成**（`coManualNo` 为空 → 自动编号）。
- `开单日期`：`YYYY.MM.DD`，随「改日期」更新。**开单日期在原型里只有一处来源**（`selDate`）：**标题栏日期控件（s1/s5）/ 成功页「改日期」/ 结算页「更多 → 选日期」写的是同一个值**，故**不新增第二个字段**；**未选即默认今天**。结算页指定的日期会随本单带入成功页。

**B. 支付相关字段（与线上现有规则完全一致；R7 修订展示结构）**

> **本节以线上当前已有规则为准**；如原型展示与线上存在差异，**一律以线上为准**（原型仅作演示）。以下为线上规则在成功页的呈现口径。

- **支付方式枚举（精确到其中一种或几种，可多选相加）**：
  `会员卡`、`微信`、`支付宝`、`美团`、`抖音`、`口碑`、`大众点评`、`现金`、`银行卡`、`信用卡`、`经理签单`、`其他平台团购`。
- **展示形式：本区域等同「支付明细排列」** ——
  - **左 = 支付方式的具体名称**（如 `支付宝` / `微信`）**或会员卡的具体卡名**（如 `美发卡3000元`），**右 = 该方式对应的金额**（会员卡则为**消耗金额或次数**）。
  - **不再有独立的「支付方式」汇总行**；一笔订单使用多种支付方式时，**每种方式各占一行**（组合支付 = 多行相加）。
  - **各支付行的金额之和 = 卡片顶部大号金额**（强制闭合，可自检；会员卡未覆盖的部分记为 `微信`，散客多明细时为 `现金` + `微信`）。
  - **整单改价（手动应付）后**：权益不再参与计算 → 明细区只显示非卡方式一行（`微信` = 手输值）。
- **会员卡支付的行**按卡种展开，规则见 C。

**C. 会员卡消费字段（R7 修订为「主行 + 灰字次行」结构）**

> 会员卡在成功页承担**两种角色**：既是「支付方式」，又需要按**卡种**展示消费明细。**不同卡种显示不同字段，禁止互相串用。**
> 每张卡占**一组**：**主行**（14px 主黑：左 = 卡的具体名称，右 = 本卡消耗金额或次数）+ **次行**（12px 三级灰：左 = `剩余`，右 = 余额 / 次数 / 金额+次数）；**同一组的主次行之间不画线**。（**R11：折扣卡不出行，`折扣` 次行取消；周期卡无次行**）

| 卡种 | 主行（左 / 右） | 次行（左 / 右） | 演示数据示例 |
|------|----------------|----------------|--------------|
| **折扣卡** | —（**不出现在支付明细**，见下条） | — | — |
| **储值卡** | 卡名 / **本单扣减金额** | `剩余` / **卡余额** | `美发卡3000元　¥58.00` / `剩余　¥2942.00` |
| **套餐卡** | 卡名 / **本单消耗金额** | `剩余` / **剩余金额 / 剩余次数** | `染烫套餐卡　¥168.00` / `剩余　¥1832.00 / 3 次` |
| **计次卡** | 卡名 / **本单扣减次数** | `剩余` / **剩余次数** | `染烫10次卡　1 次` / `剩余　3 次` |
| **周期卡** | 卡名 / **本单消耗次数** | **无次行** | `半年畅享卡　1 次` |

- **折扣卡不出现在支付明细（R11 修订）**：折扣权益**只参与「计算」整单结算价**（决定应付多少），**不参与「支付」**，因此**折扣卡不再占一行**。它的折扣额**自然并入末尾的非卡支付行**（会员 → `微信`）；若一单只用了折扣卡，明细即为**单行 `微信` = 大号金额**。原型**不再渲染 `折扣 X.X折` 这一灰字次行**。
- **套餐卡（R11 新增）**：既是「次卡」又有**卡内金额**，因此**主行给本单消耗金额**、**次行同时给剩余金额与剩余次数**（`¥{剩余金额} / {剩余次数} 次`）。R11 起该卡按**卡内金额**参与实收（与储值卡同一处理），保证明细各行之和 = 大号金额。
- **周期卡（R11 新增）**：**规定时间内的规定项目范围不限次**，**只记本单消耗次数**（主行 `N 次`）、**没有灰字次行**，也不产生金额。
- **计次卡沿用现有规则**：只核销次数、**不抵扣金额**（该明细的金额由其它支付方式承担，计入非卡方式行以保证闭合）。
- **字段口径以线上为准（R11 明确）**：**该位置展示的会员卡消费相关字段，一律以线上现有版本为准**；如原型演示与线上存在差异，**以线上为准**（原型仅作演示）。
- 同一会员可能持多张卡：**按卡逐组展示**，顺序为 **储值卡 → 套餐卡 → 计次卡 → 周期卡 → 非卡支付方式**（**折扣卡不参与排序，因为不出现在明细里**）；
- 无会员卡消费的订单：只出现非卡支付方式行。
- **卡名口径（R9 修订）**：**权益名只用于「选择权益」sheet**；s15 展示的是权益背后的**会员卡具体名称**（映射表 `CARD_META[benefit].card`）：权益 `折扣卡` → `超值折扣卡`、权益 `固定金额98元（美发卡3000元）` → `美发卡3000元`、权益 `计次卡抵扣` → `染烫10次卡`、权益 `套餐卡抵扣` → `染烫套餐卡`、权益 `周期卡抵扣` → `半年畅享卡`；**同一张卡的多条明细合并为一行**（按卡名聚合后再按 储值卡 → 套餐卡 → 计次卡 → 周期卡 排序）。

**D. 演示数据闭环（R11 重造）**
- **会员单（¥510.40，5 条明细）**：

  | 明细 | 权益 / 卡 | 应付 |
  |------|-----------|------|
  | 洗剪吹 | `超值折扣卡`（8 折，**不出行**） | ¥78.40 |
  | 护理 | `美发卡3000元`（储值卡） | ¥58.00 |
  | 烫发 | `染烫套餐卡`（套餐卡） | ¥168.00 |
  | 造型 | `染烫10次卡`（计次卡） | ¥168.00 |
  | 洗吹 | `半年畅享卡`（周期卡） | ¥38.00 |

- **s15 支付明细（5 行）**：`美发卡3000元 ¥58.00 / 剩余 ¥2942.00`、`染烫套餐卡 ¥168.00 / 剩余 ¥1832.00 / 3 次`、`染烫10次卡 1 次 / 剩余 3 次`、`半年畅享卡 1 次`（无次行）、`微信 ¥284.40`；**金额行之和 = 58.00 + 168.00 + 284.40 = 510.40**（= 顶部大号金额，闭合）。折扣卡的 78.40 **不出行**，已自然并入 `微信`。
- **散客单**：演示 `现金` + `微信` 组合（**演示数据规则**：现金 = 金额最大的单条明细，其余为微信；非线上规则）。
- 原型内置 `seedSuccessDemo`，覆盖「折扣卡不出行」「储值卡」「套餐卡」「计次卡」「周期卡」「组合支付」六类场景，保证任一入口进入 s15 都能看到逻辑自洽、金额闭合的数据。
- **入场动效（R5 新增）**：对勾 `scale .6 → 1` 回弹（400ms）→ 标题 / 操作组 / 金额卡 / 底部 bar **错峰上浮淡入**（延迟 0 / 60 / 120 / 160 / 240ms，单段 ≤ 380ms，缓动 `cubic-bezier(.32,.72,0,1)`）；**R20：金额不再计数** —— 进入本页时 `#sucAmt` **一次写入终值**（`money2(应付)` 仅去掉 `.00`），不存在「0 → 目标值」的中间态（原 `sucCountUp()` 620ms easeOutCubic 与 `sucAmtRaf` 已整体删除）。对勾回弹 + 5 段错峰上浮**全部保留**，每次进入本页重新播放。
- **底部 bar（R16 收口为 3 项）**：**返回首页图标按钮**（`suc_home.svg`，**图标描边色 `#F32F41`，与「客户签字」文字同色**；按钮底 `#FFF2F2`；点击 → s0）+ **两个**文字按钮：
  - **客户签字** → toast「已进入电子签字流程」（本期不做签字板）；
  - **继续开单** → 清空单据（明细 / 券 / 单号）后 toast「已开始新的一单」并回开单页（会员 → s5，散客 → s1）。
  - **客户详情已移出底栏**（R16 → 金额卡下方的操作组第 4 格，见上条）。
- **数据来源**：由结算页写入的 `sucInfo`（见 4.3.2）；页内修改只影响本页展示，不回写结算页。

## 6.24 首页 s0「取挂单（N）」入口【本轮新增】

- **位置**：首页两大按钮（开单 / 开卡）**下方**，描边小按钮（次要样式，非实心主色）。
- **文案**：`取挂单（N）`，`N` = **挂单池实时条数**；**纯文字，无前置图标**（R5 调整，与 s4 标题栏入口保持一致）。
- **行为**：跳 **s14 挂单列表**；从挂单列表返回首页后条数实时刷新。
- **命名（R16 口径）**：**首页入口保持「取挂单（N）」不变**（R16 只把 **s4 标题栏**改为「挂单（N）」、把 **s14 卡片按钮**改为「去结单」）。
- **同步点**：与 s4 标题栏「挂单（N）」计数同源（`data-hold-count` + `window.__v2SyncHold()`）。

### 6.25 标题栏右侧文字与标题「基线对齐」（全局）【R9 新增】

> 适用于所有标题栏（`.nav-bar`）右侧的**文字组**；左侧返回键为纯图标，不参与对齐。

- **规则**：右侧文字组的**文字基线与标题（18px）文字基线重合**（中文无降部，视觉即「底对齐」，跨字体稳健）。
- **实现（按字体度量计算，Noto Sans SC：ascent `1.16em` / descent `.288em` / 行高 `1.448em`）**：
  - 标题（18px，`top:10px`）基线 = 10 + 20.88 = **30.88**；
  - **14px 组**（在 24px 行内居中，基线 28.11）→ **整体下移 2.8px**：s1/s5 顾客入口（`.nav-cust`）、s4 挂单（`.nav-hold`）、s8 操作组（`.nav-ops`，`top:12 → 15.8`，容器为 16px 行盒、内里 14px 文字，故在 14px 组位移外**再补 1px**）；
  - **12px 组**（基线 27.23）→ **整体下移 3.7px**：s2/s3 日期控件（`.date-ctl`）；
  - 变量：`--nav-shift-14: 2.8px` / `--nav-shift-12: 3.7px`（写法 `top: calc(10px + var(--nav-shift-14))`）。
- **组内图标随文字一同下移**（保持图标与文字同轴，组内相对关系不变）。
- **实测**：各页右侧文字底缘与标题文字底缘差 **≤1.8px**（改前 4–5.5px）；基线差 **≤0.2px**。
- **不在范围内**：左侧返回键（纯图标，无文字）；s15 无标题栏。

### 6.26 结算页「明细白卡盖住顾客卡片」滚动效果（s6 / s7）【R10 新增】

> **开单页（s1/s5）的滚动视差已按 R3 口径删除**（见 6.20），本节的「视差」是**结算页新增**的、与开单页**同源**的层级效果：明细白卡 z-index 高于顾客卡 + 滚动时切换层级。

- **同源机制（与开单页一一对应）**：

| 开单页 s1 / s5 | 结算页 s6 / s7 |
|---|---|
| `.v2-price-head`（tab + 分类条）`position:sticky; top:0` 吸在滚动区顶部，内容从其下方穿过 | **顾客卡片** `position:sticky; top:12px` 吸在滚动区顶部，内容从其下方穿过 |
| `.v2-price-card { position:relative; z-index:2; border-radius:12px 12px 0 0 }` | **明细区 / 券·备注区** `position:relative; z-index:2`；**首张明细卡** `border-radius:12px 12px 0 0` |
| `.v2-bill-body.is-scrolled .v2-ccard { z-index:1 }` | `.co-scroll.is-scrolled .guest-card/.member-card { z-index:1 }` |

- **参数**：顾客卡片 `position:sticky; top:12px`（= 静止时的 `margin-top:12px` → **一起滚即吸附、零跳动**，不发生「先上跳 12px 再吸住」）。
- **不透明化（关键）**：`.co-items` 与 `.co-extras` 整块加**不透明底色 `#F7F7F7`（= 页面底色）**。作用有二：① **静止时视觉零变化**（卡片之间的 12px 空隙本来透出的就是页面底色）；② 滚动时**卡片空隙不再「漏底」** —— 不会从缝隙里透出已被盖住的顾客卡片。
- **效果**：滚动时**顾客卡片保持不动，明细白卡带着 12px 圆角上缘自下而上滑过并逐步盖住它**，直到完全覆盖；其余内容仍按文档流 1:1 滚动（**没有**给任何元素加额外的位移系数，即「视差量」来自 sticky 的停驻，而非人为降速）。
- **无内容时的行为**：结算页无明细时列表为空态卡片，效果自然退化为「顾客卡吸顶 + 空态卡滑过」，无需特殊处理。
- **边界**：顾客卡片的 `z-index` 恒为 1（明细区恒为 2），因此**详情区永远压在顾客卡之上**；底部 co-bar（`z-index:30`）与改价提示条（`z-index:29`）不受影响。滚动到最底部时顾客卡仍吸在顶部、被明细区完全遮住。

### 6.27 结算页「服务员工」行的上下留白（s6 / s7）【R11 新增 · R13 扩展】

- **问题**：明细行统一为 `min-height:40px` + `align-items:center`。单行文字（14px / 行高 20px）时上下各 10px 留白；**一旦选中 2 位及以上员工**，右侧员工文字块（每行 20px + 行距 2px）把行盒撑到**恰好等于文字块高度**，**上下留白归零**，文字**贴住**上方的发丝实线与下方的虚线。
- **规则（R11）**：**行的纵向尺寸以右侧员工文字块为准** —— 员工行（`.co-staff-row`）固定 **上下内距各 10px**，即 **行高 = 员工文字块高度 + 20px**。
- **零回归**：单选 1 位员工时 `min-height:40px` 仍生效 → **行高仍为 40px**，与「权益 / 应付」单行行**观感完全一致**（实测三行等高 40px）。
- **实测**：2 位员工 → 文字块 42px、行高 **62px**、上下留白 **各 10px**（改前行高 42px、上下留白 0）；1 位员工 → 行高 40px（与「权益 / 应付」一致）。
- **左标签对齐**：「服务员工」标签随整行**垂直居中**（多行时不顶到第一行）。
- **实现**：员工行渲染时带 `co-staff-row` 类（`#s6 .co-item .co-row.co-staff-row` / `#s7 ...` 加 `padding-top/bottom:10px`），只影响员工行，**其余明细行（权益 / 应付）不变**。
- **长摘要不再被裁切（R13 新增）**：
  - **问题**：`.co-staff-line` 带 `max-width:236px` 硬上限，而「3 工位 + 顾客指定」的完整摘要（`林屿森·大工 · 中工 · 小工 · 顾客指定`）实测 **265px** → 265 > 236，被 `text-overflow:ellipsis` 截断成省略号。
  - **可用宽度实测**：行宽 390 − 左右内距 32 − 左标签「服务员工」56 = **302px**。即 **265 < 302**，**放开上限即可完整显示**（余量 37px），并非真的放不下。
  - **规则**：`max-width:236px` → **`none`**（改用满行可用宽）；摘要先按 `max-content` 取宽、超宽才收缩，故 **右侧始终与上方「权益 / 应付」的「值」右边缘对齐**；`text-overflow:ellipsis` **仅作极端情况（如 5 字以上姓名）的兜底**。
  - **同时把工位摘要降为 13px 灰字**（姓名保持 14px/500），与姓名形成层级，并再让出约 15px。**实测：3 工位 + 顾客指定 → 文本 250px ≤ 302px、无省略号、行高仍 41px（与单行一致）、右边缘与其它「值」同偏移 374**。

### 6.28 结算页标题栏「更多」菜单（s6 / s7）【R14 新增】

- **入口**：s6 与 s7 标题栏**右侧**（`right:10px`）—— **3 圆点横排**图标，**24×24 热区 / 20×20 图标**，**主文字色 `#1a1a1a`**，按下 `opacity:.55`，`aria-label="更多"`。放置方式与「标题栏右侧文字」一致（见 6.25），**图标中心与 18px 标题墨迹中心对齐**（`top: calc(10px + 1px)`，实测差 **0.00px**）。
- **形态**：**下拉菜单**（非 sheet）—— 白底、圆角 12、宽 ≥132px、`0 10px 28px rgba(0,0,0,.14)` 投影 + 0.5px 描边（同 `proto-confirm` 观感），定位 `right:10px`、`top: 状态栏 + 标题栏 + 6px`；入场 `moreMenuIn` 160ms `cubic-bezier(.22,1,.36,1)`（轻微下移 + 淡入）；`z-index:56`。
- **两项**（**左侧均有 18×18 线性图标**）：
  - **「选日期」** —— 图标 = 日历（沿用成功页操作组的图标）→ 打开**底部 sheet 形态的日历**（与成功页「改日期」**同一套数据源与 DOM**，见 6.19）。
  - **「手工单号」** —— 图标 = 铅笔（`#1a1a1a`）→ 打开**「单号」sheet**（与成功页「改单号」**同一个 `#sucNoMask`**），**标题随入口切换为「手工单号」**。
- **命名与语义（与成功页的差异是刻意的）**：
  - 结算页用 **「选日期」「手工单号」** —— 此时**日期尚未定稿、单号尚未生成**，「选」「手工」准确表达了「本单还没这个值，现在补一个」；
  - 成功页保留 **「改日期」「改单号」** —— 此时值都已存在，「改」更准。
  - **两处指向同一个 sheet 与同一份数据**，只是入口措辞不同。
- **「手工单号」语义**：**手工输入一组号码，代替开单成功时自动产生的单号**；**留空 = 不手工指定**，开单成功时照常自动生成。确认后 toast「**手工单号：{号码}**」。
- **「选日期」语义**：**手工选择这一单的开单日期**，**开单成功后的「开单日期」按所选显示**；**不选即默认今天**。确认后 toast「**开单日期：{YYYY.MM.DD}**」。**该值写入的是全局唯一的开单日期（`selDate`），与开单页标题栏日期控件同源** —— 三处入口（标题栏控件 / 成功页改日期 / 结算页选日期）**永远一致，不会出现两份真相**。
- **关闭**：点菜单外任意处（透明遮罩 `z-index:55`）、点任一菜单项、**或切换页面**均立即收起；菜单为**单实例**，不随页面复制。
- **与底栏的关系**：菜单**不占底栏空间**、不加底部 bar 按钮（底栏横向空间已满），是「低频、次要」操作的收纳位。

### 6.29 成功页「修改单号」sheet 版式（s15）【R14 改版】

- **内容区**：**取消圆角、四边贴边、完全覆盖 sheet 的灰底** —— 标题栏分隔线到页脚之间是**整段连续白底**（`.sucno-body { padding:0 }`、`.sucno-field { margin:0; border-radius:0; padding:0 16px; height:56px }`），不再出现「灰底上的白色圆角卡片」这种两层观感。
- **单号输入框**：落在**视觉更弱的输入框**内 —— 浅灰 `#F7F7F7` 填充、8px 圆角、**无描边、无投影**（区别于「修改应付」sheet 里带边框的输入行），高 36px、**数字右对齐**（数据字体 16px / 500）；聚焦时底色转 `#F2F2F2`。弱填充让「单号」成为可改的临时值，而正文仍是页面主体。**R15：定宽 240px 并靠右**（`flex:0 1 240px; margin-left:auto`）—— 原 `flex:1` 会把它撑到 **318px（占 sheet 宽 82%）**，整行读起来是一条宽条、不像「字段」；240px 足够 **20 位数字**（182 + 左右内距 24 = 206）并留 34px 余量，**右缘与页脚「确定」按钮右缘同列**（距 sheet 右 16px，实测两者均为 16px），左侧自然留白；窄屏按 `flex-shrink:1` 自动收缩，不溢出、不出现横向滚动。
- **可点击范围**：**整行都可点**（点标签、点空白、点输入框任一位置均聚焦），不是只有输入框本身热区；`inputmode="numeric"`、`enterkeyhint="done"`。
- **聚焦与覆盖输入**：打开时自动聚焦并**默认全选**，直接输入即覆盖旧值（不必先手动删），`selectionStart=0 / selectionEnd=len`。
- **不叠层**：**输入不再叠加任何子弹层 sheet** —— 复用同一个 `#sucNoMask`，只切标题与提交目标（`sucNoTarget`: `s15` / `co`）。
- **底部**：「取消 + 确定」两列等宽（同 R9 版式，不变）。

### 6.30 移动端系统键盘避让（sheet 通用）【R14 新增】

- **问题**：手机演示时系统键盘会盖住底部 sheet 的下半部分；`「修改单号」` 的输入行正好落在被盖住的区域。
- **规则**：监听 `window.visualViewport` 的 `resize` / `scroll`，计算 **键盘遮挡高度 = `innerHeight − visualViewport.height − visualViewport.offsetTop`**，换算为逻辑像素后写入根节点 CSS 变量 **`--kb-h`**；浮层在手机模式下用 `bottom: var(--kb-h, 0px)` 抬起，**整块 sheet 始终位于键盘之上**。
- **阈值与去抖**：只有遮挡高度 **> 80px（逻辑）** 才认定为键盘（过滤移动浏览器地址栏收起 / 展开造成的 40~60px 抖动），否则 `--kb-h = 0`。
- **作用范围**：`html.is-mobile` 下的**所有底部浮层** —— `.addsheet` 与 `.date-pop.is-sheet`（含「修改单号」「修改应付」「选择权益」「选择优惠券」「选择员工」「备注」「日历」）。
- **降级**：不支持 `visualViewport` 的浏览器 `--kb-h` 恒为 `0px`，行为与改版前一致，**不报错**。
- **实测**：模拟 320px 遮挡 → `--kb-h = 298px`、sheet `bottom = 298px`（sheet 顶 408，输入行完整可见）；遮挡 60px → `--kb-h = 0`（判定为浏览器 chrome）；无遮挡 → `0px`。

---

# 模块 7　数据模型与接口约定
> 读者：前端 ✓ · 后端 **必看** · 测试 ✓

**原则：凡本文未列出的接口、字段均沿用线上现有开单结账实现。**

## 7.1 沿用线上（不新增）

| 数据 | 来源 |
|------|------|
| 顾客列表（第一行 姓名 + VIP + 会员卡数，第二行 手机号） | 线上现有顾客接口 |
| 会员权益（计次/储值/折扣卡） | 线上现有会员卡接口 |
| 价目（项目/产品/分类/价格） | 线上现有价目表接口 |
| 员工 / 工位 / 顾客指定 | 线上现有员工接口 |
| 购物车 / 支付 / 收银流水 | 线上现有开单结算流程 |

## 7.2 新增 / 调整（【待联调确认】）

| 字段 | 说明 |
|------|------|
| 散客性别 `gender` | 顶部卡片 / 直接收款提交时随单记录 |
| 直接收款金额 | 直接作为账单金额提交（原价），无需关联项目 |
| 直接收款员工 | 选填，可多选，含工位 / 顾客指定语义 |
| 直接收款开单内容 | 选填，≤50 字 |
| 直接收款结账单 | 直接收款结算 s7 生成，含明细/权益/小计/应付，结账后进入收款（线上） |
| 订单流水展示 | s8 中直接收款以「直接收款」项展示（闪电标识/原价/支付方式/实付/开单时间） |
| 退款（s9） | 全选项目可原路退回与指定退回；部分选择或已有退款项目只可指定退回 |
| 业绩 / 提成配置 | **本期不涉及**：线上模块已重构（R6 §1），原 s10/s11 原型页面已删除，配置口径以线上现有为准 |

## 7.3 能力清单（路径【待联调确认】）

| 能力 | 说明 |
|------|------|
| 直接收款开单 | 金额（原价）+ 选填员工/开单内容 + 散客性别 → 直接收款结账单 |
| 散客性别选择 | 列表选择 / 卡片头像点击均写入 |
| 会员卡权益展开 | 返回全部权益（不再折叠） |
| 权益自动匹配 / 选择 | s6 明细未改价时自动匹配（会员=最优折扣权益）或手动选择；s7 直接收款默认不选、按四类权益可选；改价项锁定 |
| 应付 / 小计编辑 | 对焦清空、失焦还原、改价标记；改价判定以单价/权益优惠价 × 数量为基准 |
| 员工多选回填 | 「姓名 · 工位 · 顾客指定」格式（`staffRoles` 为数组、`staffExtra` 表顾客指定） |
| 价目卡价格 | **R16：只显示单色原价 `#929292`**（划线价 / 权益价取消）；添加 sheet「价格（元）」默认 = 原价 × 数量 |
| 开单结算明细 | 购物车明细在 s6 完整展示、删除、重算 |
| 直接收款结账 | s7 结账后清空并回到开单页（收款以线上为准） |
| 流水展示 / 退款 | s8-s9 页面展示与交互示意，业务链路以线上为准 |

## 7.4 时序（示意）

```
收银员选择散客性别/会员
  → 顶部卡片刷新（身份 + 权益）
收银员输入直接收款金额（选填员工/开单内容）
  → 点「去收款」→ 校验金额非空（否则 toast「请先输入金额」）
  → s3 直接收款-去收款（键盘可改）
  → 点「去收款」→ s7 直接收款结算（金额按原价；改价后权益锁定）
  → 点「结账」→ 清空 → 回 s1 → toast「结账成功」（收款以线上为准）
收银员添加项目/产品
  → 添加 sheet（数量 ↔ 应付联动；内嵌选员工）
  → 确定 → 应付累加 + 分类角标 +1
  → 下一步 → s6 开单结算（明细/权益/小计编辑/挂单/结账）
  → 结账 → 清空购物车 → 回 s1 → toast「结账成功」
s8-s9（门店流水 / 退款）：原型左侧导航直达展示
```

---

# 模块 8　业务规则
> 读者：前端 ✓ · 后端 **必看** · 测试 ✓

1. **身份切换**：默认散客；选会员 → 会员；选男/女散客 → 散客。s2/s3/s6/s7 顶部卡片随身份切换，后续元素随卡片高度下移。
2. **散客性别**：默认男散客；在 s4 选择「男散客 / 女散客」确定；仅影响展示与随单记录。
3. **会员卡权益**：持卡列表默认收起、右侧 chevron 展开/收起（展开＝一行一张卡：卡类型+卡名 | 剩余权益，内容与线上一致）；移除「办卡」「更换顾客」按钮（入口为点头像/姓名进 s4）。
4. **VIP 标签**：按最新 Figma 资源（渐变 `VIP`，**R6 已删除标签内最左的黑色方块 `◆`**）适配尺寸与位置；开发保持该位置现有 VIP 标签逻辑不变。
5. **金额格式**：整数≤6 位、小数≤2 位、上限 999999.99、禁「.」开头、前导 0 剔除、不可为负。
6. **直接收款**：空金额不可「去收款」（toast「请先输入金额」）；员工/开单内容选填；**金额按原价计算，不做身份折扣**；**与小程序「自助买单」的纯金额支付不是同一种功能，互不相关**。
7. **应付/小计编辑**：带笔形编辑图标；对焦即清空、失焦为空还原；改价后 s6/s7 该明细**权益锁定**（toast「改价商品不可用权益」）；**改价判定以「单价 / 权益优惠价」× 数量为基准**。
8. **权益自动匹配（s6）**：明细未改价且未手动清除时，会员自动匹配其持有的**全场最优折扣权益**（演示为超值折扣卡 8 折）、散客自动匹配首张优惠券；手动取消后不再自动覆盖。
9. **数量**：步进 1~999；**展开态下减到 0 即收起该行**（展开区清空、恢复折叠态与价格、未提交草稿丢弃）。
10. **员工选择（R12）**：点卡片展开选项卡（大工/中工/小工 **多选** + 「顾客指定」独立可勾选），点选后收起；多选员工、**不设人数上限**；**同一项目**重进保留已选；**跨项目（另一项目/行）打开时以该项目自身数据渲染、不继承其它项目的已选与中途展开态**；回填「姓名 · 工位 · 顾客指定」。
10.1 **商品「设置内容」sheet**：点商品卡片圆形加号 → 底部 sheet（数量 / 应付 / 服务员工），点「确定」写入购物车并 toast；**原行内展开区分段（商品信息行 / 服务员工行 / 价格（元）行）已随方案删除**。
10.2 **快捷添加会员（s4 添加会员行下展）**：姓名\* + 手机号（选填，`^1[3-9]\d{9}$` 校验）+ 性别\*（男/女）；只填姓 + 选性别 → 自动「\\*先生 / \\*小姐」；取消不保存，确认后按姓氏拼音归组新建会员行并定位展开。
10.3 **员工选择跨项目隔离**：商品设置内容 sheet 的员工面板始终以该项目自身的草稿数据渲染；切换商品打开时为「未设置、可重新设置」的干净状态，且每次打开都会清空遗留的卡片翻牌编辑态（同一商品重进仍保留已选）。
11. **直接收款权益（s7）**：**默认自动匹配**（会员=其持有的全场最优折扣权益，演示为 8 折；散客=「无卡权益」，与 s6 一致）；可改选类型=会员卡储值金额、全场项目适用的计次或折扣、全部项目或全部产品可用的优惠券、满足金额门槛的代金券；**门槛不满足的代金券置灰不可选**；选权益后应付按规则折算（储值/折扣按比例、满减券减额、计次不抵扣金额），选「不使用权益」恢复原价且不再被自动覆盖；权益选择不算改价。
12. **会员权益价（划线价）**：会员持有的折扣权益使价目项目/产品产生划线价（原价灰色划线 + 红色优惠价）；添加 sheet「应付」默认 = 优惠价 × 数量（**默认优惠价不算改价**）；散客无权益不划线。
13. **散客卡性别文字**：「男散客 / 女散客」文字点击进入顾客列表（s4）；头像点击仍仅切换性别。
14. **经营分析关联**：直接收款不计入「经营分析 → 门店经营报表」的项目 TOP5 排行（经营分析属另一产品线，本侧记录该规则）。
15. **开单内容**：≤50 字；空/全空格视为未填。
16. **购物车合并 / 拆分**：同项目/产品多次加购时，仅当**单件应付一致**且**服务员工/销售人员完全一致**（未选视为同一空键）才合并数量；**应付或员工不同必须拆成多行**。加购后应付重算、分类角标按确定次数 +1；删除一行扣角标；结账/清空成功后清空。
17. **下一步**：购物车有项 → 进入 s6 开单结算；无项 → toast「请先选择项目」。
18. **已选商品 Sheet**：点底部左侧热区打开；空车 toast「请先选择项目」；Sheet 在 bar 后升起；行内展示员工摘要；± 改数量（0 删行，且不触发跳转）；清空需二次确认；**点行体 → 回开单页并打开该商品的「设置内容」sheet 回填保存数据（可再编辑），重新提交为「更新该笔（替换）」**。
19. **s6/s7 结账**：无明细 toast「暂无结算项目」；有明细 → toast「结账流程说明」并进入 **s15 开单成功**，同时清空购物车 / 直接收款明细与角标。
19.1 **权益/优惠券入口热区**：s6/s7 的「权益」行与「优惠券」行**只有右侧「值 + ›」可点**（左侧标签点击无响应）；点权益右侧 → 打开**「选择权益」sheet**，点优惠券右侧 → 打开**「选择优惠券」sheet**。
19.2 **「选择权益」sheet**：打开时回显该明细当前权益；单选；**点中即生效并收起**（无「确认」按钮）；**点蒙层关闭 = 取消**；选「不使用权益」→ 权益清空为「无卡权益」且不再被自动匹配覆盖；折扣卡按比例折算、计次卡不抵扣、固定金额卡按面额抵扣；改价明细不可进入（无热区）。
19.3 **「选择优惠券」sheet**：首行「不使用优惠券」为显式清券入口；券卡单选；**点中即生效并收起**（无「确认」按钮）；**「使用规则」默认折叠**、点击展开/收起且**不影响选中态、不关闭 sheet**；**不满门槛的券置灰不可选**（点击 toast「当前订单不满该券使用门槛」且不关闭）；**点蒙层关闭 = 取消**。
19.4 **订单级优惠券与应付**：应付 = Σ 明细权益后应付 − 券抵扣（券抵扣 = min(券面额, 应付合计)，不满门槛为 0）；散客订单自动带入首张可用券，会员不自动带券；用户选过「不使用优惠券」后不再自动带入；清空购物车时优惠券一并清空；顾客身份切换时自动匹配的权益按新身份重算、手动选择的保留。
20. **s7 删除**：删除直接收款明细 → 回 s2，toast「已删除」。
21. **退款（s9）**：全选项目可原路退回与指定退回；部分选择或已有退款项目只可指定退回。
22. **直接收款业绩/提成默认值**：若用户未单独为「直接收款」进行业绩设置或提成方案配置，则**沿用当前线上系统的默认规则**赋予默认值（本侧不另定数值）。
23. **逻辑补充页面（s8-s9）**：入口为原型左侧导航；页面交互与 toast 仅为示意，业务链路以线上为准。原 s10/s11 已删除（R6 §1）。
24. **s6/s7 交互与弹窗**：开单结算 s6 / 直接收款结算 s7 的**交互效果与各类弹窗（员工面板、权益选择 sheet、备注 sheet、小计编辑、toast 等）以当前在线版本为准**，原型仅为评审演示；本 PRD 已明确写出的业务规则仍为开发约束。
25. **搜索栏**：开单结账页（s1/s5）原有搜索栏**本期已删除**，原型不再展示；搜索能力如线上另有入口，以线上现有版本为准。
26. **范围边界**：其余功能（支付、会员卡业务等）均沿用线上现有逻辑；**会员信息卡「办卡」按钮已移除**（业务入口以线上为准）。
27. **角色权限**：店员 / 店长 / 店主，仅列角色，权限沿用线上。
28. **顾客卡片默认折叠**：s1 / s5 进入时顾客卡片**收起**，价目表白卡贴住标题栏下沿；标题栏右侧为顾客入口（会员 = icon 20×20 + 姓名；散客 = 纯文字「散客」），点击展开、再点收起，动效为 iOS 风格（高度 + 透明度 + 缩放，~320ms，锚点在标题栏右侧）。
29. **商品设置内容 sheet**：点商品卡片圆形加号 → 底部 sheet（Figma `176-329`）设置数量 / 应付 / 服务员工/销售人员，点「确定」写入购物车；**原「行内展开区 + 滚动视差吸顶」方案整体删除**。
30. **价目表为空**：项目 / 产品其中一个为空 → 隐藏对应 Tab；两个都为空 → 两个 Tab 都显示 + 空态 + 管理价目表入口（本期仅说明、不实现）。演示开关 `?noProduct=1` / `?noProject=1`（两个都加 = 都空）。
31. **挂单与去结单**：s1 / s5 / s6 挂单成功 → toast「挂单成功」、写入挂单池（记录 `from`）、清空当前购物车、跳 **s14 挂单列表**；s7（直接收款）挂单 → toast「直接收款不可挂单」；**去结单（R6；R16 由「取挂单」改名）→ 还原顾客与购物车、挂单不从池中移除，并按 `from` 回到来源页（s1/s5 或 s6）**；挂单池与各处计数 `N` 实时刷新（s0 首页「取挂单（N）」、**s4 标题栏「挂单（N）」**）。**挂单仅在三种情况下删除：列表内删除 / 24:00 超时 / 由其发起的结算成功。** **线上真实落地的挂单字段与接口以线上为准。**
32. **开单成功页（s15）**：结账成功进入；**无标题栏**（避免与「开单成功」徽标双标题）；内容区「改日期 / 改单号 / 看流水」为**无框次级操作组（`#55595E`）**，其中「改日期」「改单号」均以**底部 sheet** 呈现；金额卡为**发丝线分层 + 字段左右两栏**结构（字段顺序：单号 → 开单日期 → 支付相关 → 会员卡消费）；底部 bar「返回首页（图标）/ 客户签字 / 客户详情 / 继续开单」；进场有对勾回弹 + 错峰上浮 + 金额计数动效；页内改动只更新本页展示，**不回写结算页**；「继续开单」清空单据回开单页，「看流水」跳 s8，「客户签字 / 客户详情」toast 示意。
33. **字段名称统一**：全原型（含原型内可见文案与代码标识、PRD）统一使用 **「直接收款」** 表达，不再出现「快捷开单 / 快速消费」。
34. **图标规格**：**标题栏顾客入口两种身份均为 20×20、颜色统一 `#929292`**（散客 = 双人 `ic_guest_head.svg`，会员 = 单人 `ic_member_head.svg`）；「添加会员」新图标（`ic_add_member_new.svg`）**列表行 36×36**（外层 44×44 占位）、**顾客卡片 24×24**，保持橙色 `#FF8956`；s15「返回首页」图标 20×20、描边色 `#F32F41`；s15 操作组与 s14 删除/提示条图标为 **14~18px 线性 SVG**（随文字色）；**s0 与 s4 的「取挂单（N）」不设前置图标**；s14 空态图标 `ic_empty.svg` 72×72。

> **R14 图标色与形态补充**：**结算页底栏铅笔**与**全部垃圾桶图标**（s6/s7 明细删除、s7 直接收款删除、已选商品「清空」、s14 挂单删除）统一 **`#929292`**；`assets/ic_trash_figma.svg` 的 `stroke` **直接写死 `#929292`**（该文件以 `img` 引用，颜色不继承 CSS）；**s15「打印小票」图标（R19 改为 24 画布 stroke 1.6 线描稿；R14 的 Figma 稿 `1189-136` 作废）**（**R17：已从「卡内右上角 28×28」移到「卡片底部整行入口」，色 `#B2B2B2` → `#8E8E93`，文案 13px `#55595E`；R19：尺寸 17 → 18px、渲染线宽 ≈ 1.2px、圆角 0 无底色**）；**全站「进入」指示箭头均为 SVG**（s6/s7 优惠券行的箭头亦已由文字 `›` 改为 14×14 SVG）。

---

# 模块 9　文案清单与权限
> 读者：前端 ✓ · 后端 部分 · 测试 ✓

## 9.1 文案清单（须与原型一致）

**导航/按钮**：开单结账、项目、产品、直接收款、直接收款、选择顾客、**挂单（N）**（R16 由「取挂单（N）」改名）、取挂单（N）（s0 首页）、挂单列表、挂单、下一步、去收款、完成、确定、取消、保存、男散客、女散客、散客、添加会员、姓名、手机号、性别、男、女、选择员工、选择权益、选择优惠券、不使用权益、不使用优惠券、使用规则、确认、开单内容、服务员工、销售人员、请选择、数量、**价格（元）**（R16 由「应付（元）」改名）、小计、应付、结账、已选商品、清空、门店流水、尾款单、筛选、全部、会员卡、作废、全选、确认退款、原价、实付、服务人、微信、实付总额、开单时间、顾客指定、大工、中工、小工、开单成功、改日期、改单号、看流水、返回首页、客户签字、客户详情、继续开单、剩余、开单日期、单号、**修改应付**、**修改服务员工**、**更多**、**选日期**、**手工单号**、**手工单号：{号码}**、**去结单**（R16 由「取挂单」改名）、**原价 ¥xx**、**已恢复原价 ¥xx**、**应付已改为 ¥xx**。

> **R14 命名口径（重要，不要「统一」掉）**：**结算页**下拉用「**选日期**」「**手工单号**」（值尚未存在，是「补一个」）；**成功页**操作组仍用「**改日期**」「**改单号**」（值已存在，是「改一下」）。**两者指向同一个 sheet 与同一份数据**，标题随入口切换。

> **R6 删除文案**（原 s10/s11 页面文案，全部移除）：业绩设置、高级设置、劳动业绩、销售业绩、注意此处修改业绩，而不是提成！、方案名称、劳动提成、销售提成、更多、更改多人分配规则请前往业绩设置中的高级设置。

**「选择权益」/「选择优惠券」sheet 文案**：选择权益、选择优惠券（**R13：删除「确认并设为本单默认支付方式」提示与底部「确认」按钮文案**）；权益项=不使用权益、5.0折（5折卡）、计次卡抵扣、**套餐卡抵扣**、**周期卡抵扣**、充值金额所有项目5.0折(铂金卡)、折扣卡、充值金额所有项目8.0折(金卡)、固定金额98元（美发卡3000元）；**R9：不适用于当前明细的权益行置灰不可选；散客 / 无卡顾客只显示「不使用权益」一行（无空态文案）**。**R10：删除「不适用」「暂不可用」两个灰标签（不可用只以置灰表达），全站文案表中不再出现这两个词**。**R11：权益列表由 7 行扩为 9 行，新增 `套餐卡抵扣` / `周期卡抵扣`（次卡类，同样只对绑定项目可用）**。**R13：`#s12` / `#s13` 两个整屏页撤除，两处均改为底部 sheet（`#benPickMask` / `#cpPickMask`），点中即生效并收起**。

**占位符**：请输入金额、请输入开单内容（50字以内）、请输入单号、输入顾客姓名/手机号/编号/备注。

**Toast**：请先输入金额、请先选择项目、挂单成功、直接收款不可挂单、请选择项目、选择项目或直接收款、暂无结算项目、结账成功、**结账流程说明**、**当前订单不满该券使用门槛**、**已取回「{顾客}」的挂单**、**单号已更新**、**查看该笔流水**、**已开始新的一单**、**已进入电子签字流程**、**查看客户详情**、已删除、已清空、改价商品不可用权益、已加入 · 应付 ¥{金额}、尾款单、筛选、确认退款。

**Toast（本轮新增/调整）**：请输入姓名、手机号格式不正确、请选择性别、已添加会员 · {姓名}、已加入购物车、已更新该笔（购物车明细回跳后重新提交）、该价目已变更，无法定位、**挂单成功**、**已取回「{顾客}」的挂单**、**已删除「{顾客}」的挂单**、**有 N 张挂单已超过 24:00 自动失效并删除**、**结账流程说明**、**单号已更新**、**开单日期已更新**、**应付已改为 ¥{金额}**、**已恢复系统应付**、**查看该笔流水**、**已开始新的一单**、**已进入电子签字流程**、**查看客户详情**。

**Toast（R14 新增）**：**手工单号：{号码}**、**开单日期：{YYYY.MM.DD}**（两者为结算页「更多」菜单的确认回执；成功页改单号仍用「单号已更新」，两个入口文案不同但落点相同）。

**s6 页面文案（R6 新增 · R7 修订 · R14 补色 · R16 收敛）**：修改应付、请输入金额、**原价 ¥xx**（R16：数字键盘 sheet 的数值行占位提示，替代原「已改价」标签与黄色说明条）；**R16 已删除**：底栏「已改价」标签、底栏上方「已按手动应付结算…」说明条、底栏铅笔图标（改价入口移入明细「应付」行）。

**s6/s7 标题栏「更多」菜单文案（R14 新增）**：更多、选日期、手工单号；**sheet 标题随入口切换**（「手工单号」/「修改单号」）。

**s15 页面文案**：开单成功、改日期、改单号、看流水、开单日期、单号、返回首页、客户签字、客户详情、继续开单、剩余（**无标题栏**，故不再有「支付」文案）；**R7：删除改单号 sheet 内的提示文案**；支付明细行为「支付方式名 / 会员卡具体卡名 + 金额或次数」，会员卡下方灰字行为 `剩余` + 余额 / 次数（**R11：`折扣` 文案随折扣卡行一并移除**）。

**s0 首页文案（R7 删除）**：原提示「从「开单」进入选择顾客，再开单或直接收款。本页为 2.0 简化首页（问卷 A2）」**已删除**（含 `.v2-home-hint` 样式）。

**R16 新增/变更文案**：**挂单（N）**（s4 标题栏）、**去结单**（s14 卡片按钮）、**价格（元）**（添加 sheet）、**修改服务员工**（结算页 sheet）、**原价 ¥xx**（改价 sheet 占位）、**已恢复原价 ¥xx** / **应付已改为 ¥xx**（改价 toast）、**开单** / **收款**（s4 行右侧纯文字操作，R17）、**选择{行名}**（员工选择页；R18 起不再显示「最多 3 人」提示）、**查看客户详情**（s15 操作组第 4 格）。

**s14 页面文案**：挂单列表、**每日24:00后 挂单自动失效并删除**、**服务员工**、**去结单（R16 由「取挂单」改名）**、暂无挂单、挂起的单据会显示在这里；**R6：卡片上不再有「开单 / 直接收款」类型 chip，VIP 标签内不再有黑色方块 `◆`**。

**Dialog**：确定清空已选商品？ / 取消 / 清空。

**权益行**：计次卡 / 储值卡 / 折扣卡 + 名称 + 值（余N次 / 余额xx.xx元 / x.x折）；未匹配时「无卡权益」。

**提示条**：具体金额以结算为准。

**分类**：项目=洗吹/洗剪吹/剪发/造型/干洗/接发/烫染发/护理/其他；产品=洗护/染烫/其他。（**R6：原「业绩/提成=直接收款（置顶）/洗吹」已随 s10/s11 删除。**）

**演示数据**（开发接入真实数据）：项目=高级设计师洗吹+造型 ¥58.9、资深设计师|精剪+洗吹造型(不满意免单）¥98、首席设计师洗剪吹一口价 ¥138、资深设计师|人气洗剪造型 ¥216.09；产品=卡诗黑钻钥源洗发水 ¥268、欧莱雅臻萃染发膏 ¥198、资生堂fino发膜 ¥158、施华蔻羊绒脂护发素 ¥88。

**员工演示数据（14 人）**：顾清扬(店主)、林屿森(美容师)、何苏叶(店长)、阿Ken(美容师)、Lisa(美甲师)、张明(美发师)、李华(美发师)、王芳(美甲师)、陈强(美发师)、赵敏(前台)、周杰(美发师)、吴婷(美睫师)、小陈(学徒)、阿宁(学徒)。

**权益演示数据**：会员卡=九折储值卡（余额2049.00元）、水光三次卡（余3次）、超值折扣卡（8.0折）、**染烫套餐卡（余4次）**、**半年畅享卡（不限次）**；优惠券=洗剪吹立减10元券（无门槛减10）、全场通用满200减30元优惠券（满200减30）。

**「选择权益」sheet 演示数据（9 行，R11 扩充）**：不使用权益 / 5.0折（5折卡）〔折扣 0.5〕/ 计次卡抵扣〔计次，不抵扣金额〕/ **套餐卡抵扣〔次卡，不抵扣金额；绑定 烫发、染发〕** / **周期卡抵扣〔次卡，不抵扣金额；绑定 洗剪吹、时尚洗吹〕** / 充值金额所有项目5.0折(铂金卡)〔折扣 0.5〕/ 折扣卡〔折扣 0.8，会员自动匹配项〕/ 充值金额所有项目8.0折(金卡)〔折扣 0.8〕/ 固定金额98元（美发卡3000元）〔固定金额 98〕。

**「选择优惠券」sheet 演示数据（3 张 + 不使用行）**：洗剪吹立减10元券（满减券·项目通用·无门槛·¥10）/ 全场通用满200减30元优惠券（满减券·全场通用·满200可用·¥30）/ 烫染项目立减50元代金券（代金券·项目通用·满300可用·¥50）；「使用规则」内容示例：有效期：15天 / 适用门店：右尚造型(周浦店) / 测试用账号：18297402934。

**直接收款权益规则（s7）**：九折储值卡→按 9 折计价；水光三次卡→计次核销不抵扣金额；超值折扣卡→按 8 折计价；新人8折券→满 58 减 10（全场项目/产品可用）；满200减30券→满 200 减 30（金额不足 200 置灰不可选）。

**价目卡价格（R16）**：**任何身份都只显示单色原价**（`#929292`）—— 原「会员划线价（原价灰色划线 + 红色优惠价）」作废；添加 sheet「价格（元）」默认 = 原价 × 数量。

**新注册会员（散客卡内 / s4 快捷注册）演示数据**：无任何卡/权益 → 会员卡展开区显示空态「暂无会员卡」、价目恒为单色原价、s6 结算「权益」行 `无卡权益`；「上次到店」= 注册当天日期；持卡列表与折扣在选中既有会员（如陈女士 **5 张卡**：计次卡 水光三次卡 余3次 / 储值卡 九折储值卡 余额2049.00元 / 折扣卡 超值折扣卡 8.0折 / **套餐卡 染烫套餐卡 余4次** / **周期卡 半年畅享卡 不限次**）时恢复。

**流水演示数据（s8）**：顾客 陈女士、状态 已完成、直接收款 ¥491.00、服务人 刘敏、支付 微信、实付总额 ¥491.00、开单时间 2026.08.20 15:55。

## 9.2 权限

| 角色 | 开单结账 |
|------|----------|
| 店员 | 按线上现有权限 |
| 店长 | 按线上现有权限 |
| 店主 | 按线上现有权限 |

本期不新增权限差异；原型按默认角色路径演示。

---

# 模块 10　验收 P0 + 造数场景
> 读者：测试 **必看**

## 10.1 造数

| 场景 | 说明 |
|------|------|
| 散客 | 男散客 / 女散客 两条散客身份 |
| 会员 | 至少 1 名持多张卡的会员（计次+储值+折扣） |
| 员工 | 覆盖三种工位与顾客指定的员工 |
| 项目/产品 | 至少覆盖 2 个分类 |
| 直接收款 | 空金额 / 正常金额 两条路径 |
| 权益 | 会员卡 + 优惠券两类权益 |
| 流水 / 退款 | s8-s9 演示数据（直接收款项） |

正式环境自备等价数据；演示数据不进产品。

## 10.2 Given → When → Then（P0）

**顾客卡片 / 顾客列表**
1. **Given** 散客开单页，**When** 点击女头像，**Then** 女头像彩色、男头像灰阶、文案变「女散客」；再点男头像恢复「男散客」。
2. **Given** 散客卡默认态，**When** 首次进入，**Then** 男散客选中（男彩女灰），文案「男散客」。
3. **Given** 散客开单页，**When** 点击「选择顾客」，**Then** 进入顾客列表，顶部有「男散客」「女散客」选项。
4. **Given** 顾客列表，**When** 选「女散客」，**Then** 返回来源页，散客卡更新为女散客。
5. **Given** 散客开单页，**When** 点击顶部头像或「散客 / 男散客 / 女散客」姓名，**Then** 进入顾客列表（s4）。
6. **Given** 顾客列表，**When** 选会员（如陈女士），**Then** 顶部切换为会员卡；从 s1 进入时跳 s5。
7. **Given** 会员身份，**When** 点顶部卡头像/姓名进顾客列表并选男散客，**Then** 切回散客卡（从 s5 进入则回 s1）。
8. **Given** 会员列表，**When** 查看 VIP 标签，**Then** 按最新 Figma 资源的渐变 `VIP` 样式与尺寸位置正确；文案/逻辑以线上现有 VIP 标签为准。
8.1 **Given** 散客开单页（s1）或会员开单页（s5）标题栏，**When** 查看右侧顾客入口，**Then** 散客显示双人图标 + 「散客 / 男散客 / 女散客」、会员显示单人图标 + 姓名，两态图标颜色均为 `#929292`。**（R6 验收）**
9. **Given** 会员卡，**When** 查看顶部操作区，**Then** 无「更换顾客」也无「办卡」按钮（入口为点击头像/姓名）。

**会员卡权益展开**
10. **Given** 会员卡，**When** 查看权益区，**Then** 持卡列表 **默认收起**，右侧 chevron 可见。
11. **Given** 会员卡，**When** 点 chevron，**Then** 展开显示**持卡列表**（一行一张卡：左＝卡类型 pill + 卡名，右＝卡剩余权益，内容与线上现有版本一致）；再次点击收起。
12. **Given** 会员卡，**When** 查看字段，**Then** 显示规则维持现有（余N次 / 余额xx元 / x.x折）。

**添加项目/产品 sheet**
13. **Given** 项目卡片，**When** 点圆形加号，**Then** 打开添加 sheet，标题为项目名 + 红色价格，默认数量 1、应付 = 单价。
14. **Given** 添加 sheet，**When** 步进数量，**Then** 应付 = 单价 × 数量。
15. **Given** 添加 sheet，**When** 对焦应付输入框，**Then** 原值清空；输入即当前金额；失焦为空时还原原值。
16. **Given** 添加 sheet，**When** 手动改应付（≠ 单价/优惠价×数量），**Then** 判定改价；确定后 s6 该明细权益锁定。
17. **Given** 添加 sheet（项目），**When** 查看员工行，**Then** 标签「服务员工」，未选显示「请选择」。
18. **Given** 添加 sheet（产品），**When** 查看员工行，**Then** 标签「销售人员」。
19. **Given** 会员身份（陈女士持折扣卡），**When** 查看 s5 价目卡片，**Then** **R16：价格区只有单色原价（`#929292`）**，不出现划线价与红色优惠价（散客 s1 同口径）。
20. **Given** 会员身份，**When** 打开添加 sheet，**Then** 「应付」默认 = 优惠价 × 数量（如 ¥58.9 → ¥47.12），标题价格同步显示优惠价。
21. **Given** 会员身份添加 sheet，**When** 以默认优惠价点「确定」，**Then** 不算改价，s6 该明细权益仍可用。
22. **Given** 添加 sheet，**When** 点确定，**Then** 应付总额累加应付金额、分类角标 +1、toast「已加入 · 应付 ¥x.xx」、弹层关闭。
23. **Given** 添加 sheet，**When** 点遮罩关闭，**Then** 不写入购物车。

**选择员工**
24. **Given** 员工面板，**When** 点员工卡片，**Then** 该卡**横向撑满整行**，平铺「大工 / 中工 / 小工 / 顾客指定」4 张选项卡；同行其余卡片淡出。
25. **Given** 展开态，**When** 点「大工」，**Then** 该工位勾选；**再点一次**则取消该工位；点选后**等勾选动效播完再收起**。
26. **Given** 展开态，**When** 只点「顾客指定」而工位全不选，**Then** **该员工仍计入已选**（摘要只显示「顾客指定」，不出现「无工位」）。
27. **Given** 员工完成选择，**When** 查看，**Then** 收缩态卡片为**粉底 + 粉描边 + 红色摘要**（**无勾选控件**）；添加 sheet / 直接收款 / s6 / s7 摘要回填「姓名 · 工位 · 顾客指定」。
28. **Given** 员工面板，**When** 多选多位员工后完成，**Then** 全部回填、顿号连接，**不设人数上限**。
29. **Given** 员工面板关闭后再次打开，**When** 查看，**Then** 已选员工保留已选状态不重置。
30. **Given** s6/s7「服务员工」行，**When** 查看，**Then** 为摘要（每名员工一行「姓名 · 工位 · 顾客指定」）；**R16：该行为热区、右侧 `›`，点击打开「修改服务员工」sheet**（不再是只读）。

**直接收款**
31. **Given** 直接收款页，**When** 不输入金额点「去收款」，**Then** toast「请先输入金额」，不跳转。
32. **Given** 直接收款页，**When** 输入金额后点「去收款」，**Then** 进入去收款页 s3 并显示该金额。
33. **Given** 直接收款页，**When** 点「员工」行，**Then** 内嵌展开选择员工面板，选填。
34. **Given** 直接收款页，**When** 输入开单内容，**Then** 可输入 ≤50 字，超长被截断。
35. **Given** 去收款页，**When** 修改金额，**Then** 键盘可改，员工/开单内容与直接收款页共享同步。
36. **Given** 去收款页 s3，**When** 点「去收款」，**Then** 进入 s7 直接收款结算，显示直接收款明细。
37. **Given** s7，**When** 会员身份下查看金额，**Then** 金额本身按输入原价（不做 9 折/身份折扣）；权益行默认自动匹配最优折扣卡（演示 8 折）并按权益重算应付。
38. **Given** 直接收款，**When** 从 s4 选会员后进入，**Then** 顶部显示会员卡；选散客后显示散客卡。
39. **Given** s7 直接收款结算，**When** 散客身份下查看权益行，**Then** 默认「无卡权益」，应付 = 输入金额；**Given** 会员身份，**Then** 默认自动匹配最优折扣卡（演示 8 折，¥299 → ¥239.20）。
40. **Given** s7 会员默认已匹配折扣卡，**When** 点权益行右侧打开「选择权益」sheet 并点「不使用权益」（即生效并收起），**Then** 应付恢复原价，且后续重渲/切身份**不再被自动匹配覆盖**。
41. **Given** s7 输入金额 150，**When** 打开「选择权益」sheet，**Then** 九折储值卡/超值折扣卡/新人8折券可用，「满200减30券」置灰不可选（门槛未满足）。
42. **Given** s7 金额 150，**When** 选择「超值折扣卡」，**Then** 应付 = 120（8 折）；再选「新人8折券」→ 应付 = 140（减 10）；选「不使用权益」→ 恢复 150。**Given** 已选权益且改小计 ≠ 权益价，**Then** 判定改价、权益清除并锁定；改回权益价不改价。

**开单结算 s6 / 直接收款结算 s7**
> 本区块仅校验本期已明确的**业务规则**；交互效果与各类弹窗样式以当前在线版本为准（原型仅为评审演示）。
43. **Given** s1/s5 购物车有项，**When** 点「下一步」，**Then** 进入 s6，明细卡展示（类型标签/名称/单价/垃圾桶）；页面**无单号卡片、无日期入口**。
44. **Given** 会员身份加购后进入 s6，**When** 查看明细，**Then** **R16：单价 = 原价**、应付 = 权益后应付、权益自动匹配「超值折扣卡」。
45. **Given** s6 明细未改价，**When** 查看权益行，**Then** 自动匹配权益（会员=其持有的全场最优折扣权益，散客=首张优惠券）。
46. **Given** s6/s7 明细，**When** 编辑小计使金额 ≠ 单价/权益优惠价 × 数量，**Then** 判定改价，权益行变为「改价商品不可用权益」，点击 toast 同文案。
47. **Given** s6/s7 明细未改价，**When** 点权益行**右侧「值 + ›」**，**Then** 打开**「选择权益」sheet**，单选；**点中即生效并收起**，点蒙层关闭不生效（详见下方「选择权益 / 选择优惠券 sheet」）。
48. **Given** s6，**When** 点垃圾桶删除明细，**Then** 该明细移除，应付重算，对应分类角标 -1。
49. **Given** s7，**When** 点垃圾桶，**Then** 回 s2，toast「已删除」。
50. **Given** s6 有明细，**When** 点「挂单」，**Then** toast「挂单成功」、购物车与角标清空、跳 **s14 挂单列表**；**Given** s7，**Then** toast「直接收款不可挂单」，不写入挂单池。
51. **Given** s6/s7 有明细，**When** 点「结账」，**Then** toast「结账流程说明」、进入 **s15 开单成功**，购物车明细与角标清空。
52. **Given** s6/s7 无明细，**When** 点「结账」，**Then** toast「暂无结算项目」。
53. **Given** 直接收款与小程序「自助买单」，**When** 对照功能定义，**Then** 二者为不同功能：直接收款可关联员工/开单内容/权益并计入门店流水，自助买单为纯金额支付。

**门店流水 s8**
54. **Given** s8，**When** 查看 Tab，**Then** 全部/项目/产品/会员卡/作废 横向排列，带计数角标，点击切换选中态。
55. **Given** s8，**When** 查看流水卡，**Then** 直接收款以闪电缩略图 + 红色闪电标签 +「直接收款」展示，含金额/服务人/微信/实付总额/开单时间。

**选择退款项目 s9**
56. **Given** s9，**When** 点项目勾选，**Then** 勾选态切换；全部勾选时「全选」点亮。
57. **Given** s9，**When** 点「全选」，**Then** 全部项目勾选；再次点击取消全部。
58. **Given** s9，**When** 取消任一勾选，**Then** 「全选」熄灭。
59. **Given** s9，**When** 全选项目，**Then** 退款可选原路退回与指定退回；部分选择或已有退款项目只可指定退回（提示条文案一致）。

**删除 s10/s11 与命名统一（R6）**
60. **Given** 左侧导航，**When** 逐项检查，**Then** **不再出现**「业绩设置」「提成方案」入口（页面、导航项、CSS 与交互已整体移除，R6 §1）。
61. **Given** 全原型所有页面，**When** 检索文案与代码标识，**Then** **不再出现**「快捷开单」「快速消费」，一律为「**直接收款**」。
62. **Given** 线上业绩 / 提成模块（已重构），**When** 需要配置直接收款的业绩与提成，**Then** **以线上现有模块为准**，本 PRD 不含该部分设计。
63. **Given** 原 s10/s11 已删除，**When** 代码侧检查，**Then** 相关 HTML / CSS / 脚本引用与演示数据均无残留。
64. **Given** 用户未单独为「直接收款」配置业绩或提成，**When** 系统结算该笔直接收款，**Then** 业绩/提成按当前线上系统默认规则赋予默认值（本侧不另定数值）。
65. **Given** 经营分析 → 门店经营报表，**When** 查看项目 TOP5 排行，**Then** 排行不包含「直接收款」（经营分析属另一产品线，本侧记录该规则）。

**金额格式边界**
66. **Given** 金额键盘，**When** 输入 `.5`，**Then** 以「.」开头被拒绝，不进入。
67. **Given** 金额键盘，**When** 输入 `0123`，**Then** 前导 0 剔除，结果为 `123`。
68. **Given** 金额键盘，**When** 输入 `999999.999`，**Then** 小数第 3 位被拒；总值不超过 999999.99。
69. **Given** 金额键盘，**When** 输入 `9999999`，**Then** 超上限被拒。
70. **Given** 金额键盘，**When** 输入 `58.95`，**Then** 合法显示；再输数字被拒（小数已 2 位）。
71. **Given** 添加 sheet 应付/s6 小计，**When** 输入超过 999999.99 或 2 位以上小数，**Then** 自动截断/封顶。

**下一步 / 底部 / 已选商品**
72. **Given** 购物车为空，**When** 点「下一步」，**Then** toast「请先选择项目」。
73. **Given** 购物车有项，**When** 点「下一步」，**Then** 直接进入 s6。
74. **Given** 购物车为空，**When** 点底部左侧热区（图标+应付），**Then** toast「请先选择项目」，不打开 Sheet。
75. **Given** 购物车有项，**When** 点底部左侧热区，**Then** 已选商品 Sheet 从 bar 后升起，bar 仍可见可点；行内有名称、员工摘要、单价、±数量。
76. **Given** 同一项目两次加购、员工不同，**When** 查看购物车 / Sheet / s6，**Then** 拆成两行，不得合并数量。
77. **Given** 同一项目两次加购、单件应付不同，**When** 查看购物车，**Then** 拆成两行。
78. **Given** 同一项目两次加购、单件应付与员工均相同（含均未选员工），**When** 确定，**Then** 合并为一行，数量相加。
79. **Given** Sheet 内某行数量，**When** 点 − 减到 0，**Then** 该行删除并重算合计；点 + 上限 999。
80. **Given** Sheet，**When** 点「清空」并确认，**Then** 全部清空、角标归零、Sheet 关闭；点取消则不变。
81. **Given** s1/s5/s6 有明细，**When** 点「挂单」，**Then** toast「挂单成功」、购物车与角标清空、跳 **s14 挂单列表**；**Given** s7，**Then** 按钮置灰 + toast「直接收款不可挂单」。

**搜索栏 / 范围**
82. **Given** 开单结账页 s1/s5，**When** 查看顶部区域，**Then** 不包含搜索栏（本期已删除）。
83. **Given** s6/s7 各类弹窗（员工面板/权益选择/备注/小计编辑等），**When** 对照线上验收，**Then** 交互效果与弹窗样式以当前在线版本为准，原型细节不作为验收约束。

**直接收款页布局**
84. **Given** s2/s3，**When** 查看字段卡片，**Then** 自上而下顺序为「开单内容 → 收款金额 → 员工」。

**添加会员（s4 行下展）**
85. **Given** s4，**When** 点「添加会员」行，**Then** 该行向下展开表单（姓名\* / 手机号 / 性别\*），且顾客行展开区同时收起（互斥）。
86. **Given** 添加会员表单，**When** 不填姓名点「确认添加」，**Then** toast「请输入姓名」，不新建。
87. **Given** 添加会员表单，**When** 手机号填 5 位数字点「确认添加」，**Then** 输入框红框 + 「手机号格式不正确」+ toast，不新建。
88. **Given** 添加会员表单，**When** 姓名「王」+ 选「女」+ 确认，**Then** 列表中 W 分组新建「王小姐」行（**第一行 姓名，第二行 手机号；R16 已删除「最近消费」**）、滚动定位到该行；点其右侧「开单」进入 s5 且顶部卡显示「王小姐」+ 女性头像。
89. **Given** 添加会员表单已填数据，**When** 点「取消」，**Then** 表单收起、输入清空、不产生新会员。
90. **Given** 已展开添加会员表单，**When** 点任一顾客行，**Then** 添加会员表单收起、该顾客行展开。

**商品「设置内容」sheet 与归零规则**
91. **Given** s1/s5 商品列表，**When** 点某商品的圆形加号，**Then** 打开底部「设置内容」sheet（Figma 176-329：商品名 + **价格（元）** + 服务员工；**添加产品另有数量步进，添加项目没有**），页面**不再有行内展开区**。
92. **Given** 商品设置 sheet 已打开，**When** 点「取消」或点遮罩空白处，**Then** sheet 关闭且**不写入购物车**（草稿丢弃，角标不变）。

**购物车明细回跳编辑**
93. **Given** 已选商品 Sheet 内有会员笔与散客笔，**When** 点某一笔的行体，**Then** 关闭 Sheet 并跳到对应页（会员笔 → s5 / 散客笔 → s1）、切到对应 Tab、分类重置「全部」，并**打开该商品的设置 sheet 回填数量/应付/服务员工**。
94. **Given** 从明细回跳后打开的商品设置 sheet，**When** 修改数量并点「确定」，**Then** **更新原明细（行数不变）**，toast「已更新该笔」。
95. **Given** 已选商品 Sheet，**When** 点行内 ± 步进器，**Then** 仅改数量，不关闭 Sheet、不跳转。

**员工选择跨项目隔离**
96. **Given** 在商品 A 的设置 sheet 已为 A 选好服务员工，**When** 打开商品 B 的设置 sheet 并按「添加」打开员工面板，**Then** B 的面板全部为未选状态（不出现 A 的已选员工）。
97. **Given** 在商品 A 的面板里点了某张员工卡进入展开态后点遮罩收起，**When** 打开商品 B 的员工面板，**Then** 无任何卡片处于展开编辑态。
98. **Given** 同一商品 A 已选 2 名员工，**When** 关闭并重新打开 sheet 的员工面板，**Then** 已选 2 人仍保留（同一商品不重置）；点「×」可移除；**R12：不设人数上限**（原「满 3 人隐藏添加 / 已达上限」规则作废）。

**散客卡内快捷注册会员**
99. **Given** 顾客为「男散客」，**When** 查看 s1 顶部卡，**Then** 卡片右侧显示「添加会员」图标（**24×24**，与 s4 同款、等比缩小）；顾客为默认「散客」时**不显示**。
100. **Given** 顾客为「男散客」（或「女散客」），**When** 查看 s1/s2/s3/s6/s7 顶部卡姓名，**Then** 字色为 `#751616`（与会员卡一致）、正常流排布并与头像中轴对齐，无白色字/错位。
101. **Given** 「男散客」卡，**When** 点「添加会员」图标，**Then** 表单展开且性别已默认选中「男」（「女散客」则默认「女」）；重复点击已选中的性别不会取消选中；输入框为白底（含失焦态）。
102. **Given** 已选「男散客」，**When** 点卡片右侧「添加会员」图标，**Then** 卡片向下展开快捷注册表单（姓名\* / 手机号 / 性别\*），再次点图标或点「取消」则收起、输入清空、不保存。
103. **Given** 卡内注册表单，**When** 姓名「李」+ 手机号 5 位 + 选「男」+ 确认，**Then** 红框 + 「手机号格式不正确」+ toast，卡片不切换身份。
104. **Given** 卡内注册表单，**When** 姓名「赵」+ 手机号 13900002222 + 选「女」+「确认添加」，**Then** 表单收起；顶部卡就地变为会员卡（「赵小姐」+ 女性头像 + VIP 标签 + 「上次到店」= 当天日期），散客卡隐藏。
105. **Given** 刚注册的新会员（无卡无权益），**When** 展开卡片权益区，**Then** 显示空态「暂无会员卡」；价目为单色原价；进入 s6 时「权益」行显示「无卡权益」。
106. **Given** 在散客页注册会员且购物车已有明细，**When** 点「下一步」进入 s6，**Then** 顶部卡为会员卡（会员姓名 + 当天日期），已加购明细内容与应付不变。
107. **Given** 在 s2「直接收款」页点了散客卡的「添加会员」图标并成功注册，**When** 查看 s2 顶部卡，**Then** 就地切换为会员卡（会员姓名 + 当天日期），面板收起。
108. **Given** 已注册新会员，**When** 回 s4 重新选「陈女士」开单，**Then** 顶部卡恢复「陈女士」档案（**5 张会员卡** / 2049 储值 / 8.0 折扣 / 套餐卡余4次 / 周期卡不限次），折扣权益恢复生效。

**s4 标题栏与搜索**
109. **Given** 在 s4，**When** 查看页面顶部，**Then** 状态栏下方为标题栏（返回键 + 「选择顾客」），搜索栏位于标题栏之下、列表之上，无日期控件。
110. **Given** 在 s4，**When** 点标题栏左侧返回键，**Then** 返回首页 s0。
111. **Given** 在 s4 搜索框输入「陈」，**When** 查看列表，**Then** 仅平铺显示「陈女士」「陈昕然」，字母分组头、右侧 A-Z 索引条、「添加会员」行与散客组全部隐藏。
112. **Given** 在 s4 搜索框输入手机号「13800001122」，**When** 查看列表，**Then** 仅显示「高海泉」。
113. **Given** 在 s4 搜索框输入「zzzz」，**When** 查看列表，**Then** 显示空状态（图标 +「未找到相关顾客」+「换个姓名或手机号试试」）。
114. **Given** 已输入搜索词，**When** 点「取消」，**Then** 输入清空并退出输入态、列表恢复分组态、**不跳转首页**。
115. **Given** 搜索态下，**When** 展开「添加会员」并成功确认添加「郑小姐」，**Then** 自动退出搜索态，新行可见、定位并保持展开；再次搜索「郑」可命中该新会员。

**标题栏日期控件（本轮收窄）**
116. **Given** 进入 s1 / s5 / s6 / s7，**When** 查看标题栏右侧，**Then** **没有日期入口**（已删除）；**s4 标题栏右侧为「挂单（N）」（R16 改名）**。
117. **Given** 在 s15 开单成功页，**When** 点内容区「改日期」，**Then** 从标题栏下方下拉日期浮层（含蒙层、月份标题、星期表头、日期网格、快捷「今天 / 昨天」）。
118. **Given** 日期浮层已展开，**When** 查看日期网格，**Then** **今天之后的日期、以及今天往前 3 个月之前的日期**均为置灰不可选；当月为可选范围的最后一个月时「下一月」按钮置灰。
119. **Given** 日期浮层已展开，**When** 点选范围内某一天，**Then** **s15 的「开单日期」更新为该日期**、浮层关闭（不回写结算页）。
120. **Given** 在 s15 已改日期，**When** 依次点快捷「昨天」「今天」，**Then** 日期分别为「今天 − 1 天」与「今天」。
121. **Given** 日期浮层已展开，**When** 点蒙层空白处或再次点触发区域，**Then** 浮层关闭且日期不变。

**开单页顾客卡片折叠（s1/s5）**
122. **Given** 首次进入 s1，**When** 查看页面顶部，**Then** **顾客卡片为收起状态**，价目表白卡上移贴住标题栏下沿；标题栏右侧显示「散客」文字（无 icon）。
123. **Given** 顾客为会员（如陈女士），**When** 查看标题栏右侧，**Then** 显示**会员 icon（20×20，`#929292`）+ 会员姓名**。
124. **Given** 在 s1/s5，**When** 点标题栏右侧顾客区域，**Then** 顾客卡片以 iOS 风格动效展开（高度 + 透明度 + 缩放，锚点在标题栏右侧，约 320ms），价目表随文档流下移。
125. **Given** 卡片已展开，**When** 再次点标题栏右侧区域，**Then** 卡片收起，价目表回到贴住标题栏的位置。
126. **Given** 顾客为散客（默认「散客」），**When** 点标题栏右侧「散客」文字，**Then** 同样展开顾客卡片（展开后可点头像/姓名进 s4）。
127. **Given** 展开过卡片，**When** 离开并重新进入 s1/s5，**Then** 卡片回到**收起**态（状态不跨页记忆）。
128. **Given** s1/s5，**When** 上下滚动价目表，**Then** **没有任何视差/吸顶位移**（原方案已删除），页面按文档流滚动。
129. **Given** 在 s1 卡片展开态，**When** 点散客卡的「添加会员」图标，**Then** 快捷注册面板正常展开（浮于价目表白卡之上）。

**结算页标题栏与单号卡片（s6/s7）**
130. **Given** 进入 s6 或 s7，**When** 查看标题栏右侧与明细卡上方，**Then** **无日期入口、无单号卡片**（均已删除）。
131. **Given** s6/s7 明细卡，**When** 查看「服务员工→权益」「权益→应付」两根分隔线，**Then** 均为**虚线**、左右各内缩 16px；商品信息头下沿那根为 0.5px 实线。
132. **Given** s6/s7 有明细，**When** 点「结账」，**Then** toast「结账流程说明」并进入 **s15 开单成功**，购物车明细与角标清空。

**「选择权益」sheet（R13 改回底部 sheet） / 「选择优惠券」sheet（R13 新增 sheet 形态）**
133. **Given** s6 或 s7 明细卡，**When** 分别点「权益」行与「优惠券」行的**左侧标签**，**Then** 无任何响应（不打开 sheet、不弹层）。
134. **Given** s6 或 s7，**When** 点「权益」行右侧「值 + ›」，**Then** **从底部升起「选择权益」sheet**（标题「选择权益」）；**页内不再有「确认」按钮与「确认并设为本单默认支付方式」提示行**（DOM 中 `.pick-foot` / `.pick-tip` 均不存在）。
134.1 **Given** 打开「选择权益」sheet，**When** 量测 sheet 高度，**Then** **高度随内容自适应**（会员 9 行 ≈ 635px ≈ 视口 75%），内容区 `scrollHeight === clientHeight`（**不出现内部滚动条**）；仅当内容超过 85% 上限时才滚动。
135. **Given** 打开「选择权益」sheet，**When** 查看列表，**Then** 显示 **9 行**（不使用权益 / 5.0折（5折卡）/ 计次卡抵扣 / **套餐卡抵扣** / **周期卡抵扣** / 充值金额所有项目5.0折(铂金卡) / 折扣卡 / 充值金额所有项目8.0折(金卡) / 固定金额98元（美发卡3000元）），且**回显该明细当前权益**（会员自动匹配为「折扣卡」，散客为「不使用权益」），单选。
136. **Given** 「选择权益」sheet，**When** 点蒙层空白处，**Then** sheet 关闭且**权益与应付均不变**（取消语义）。
137. **Given** 「选择权益」sheet，**When** 点「5.0折（5折卡）」行，**Then** **立即生效并自动收起**：该明细权益显示「5.0折（5折卡）」、应付按 5 折重算（示例：¥58.00 → ¥29.00），**无需任何二次确认**。
138. **Given** 「选择权益」sheet，**When** 点「不使用权益」行，**Then** sheet 收起，该明细权益显示「无卡权益」、应付恢复原价，且后续不再被自动匹配覆盖。
139. **Given** s7（直接收款）点权益行右侧打开 sheet，**When** 点某权益行，**Then** sheet 收起后**仍停留在 s7**（sheet 不改变当前页），直接收款明细应付按权益重算。
140. **Given** 改价明细，**When** 查看权益行，**Then** 显示「改价商品不可用权益」，右侧无热区、无法打开权益 sheet。
141. **Given** s6 或 s7，**When** 点「优惠券」行右侧「值 + ›」，**Then** **从底部升起「选择优惠券」sheet**（标题「选择优惠券」，**无底部「确认」按钮**）。
142. **Given** 打开「选择优惠券」sheet，**When** 查看列表，**Then** 首行为「不使用优惠券」+ 单选圈；券卡为 3 张（¥10 无门槛 / ¥30 满200可用 / ¥50 满300可用），角标为「满减券 / 满减券 / 代金券」。
143. **Given** 「选择优惠券」sheet，**When** 查看券卡的「使用规则」，**Then** **默认为折叠态**（不展开）；点该行展开显示规则条目，再点收起；**再次打开仍为折叠态**；**展开/收起不改变选中态、也不关闭 sheet**。
144. **Given** 「选择优惠券」sheet，**When** 点不满门槛的券（如应付 ¥60.40 时的 ¥30/¥50 券），**Then** toast「当前订单不满该券使用门槛」、选中态不变、**sheet 保持打开**。
145. **Given** 「选择优惠券」sheet，**When** 点 ¥10 券，**Then** **立即生效并自动收起**：券行显示该券名 + 「›」，底部应付 = 原应付 − 10（示例：¥58.00 → ¥48.00）。
146. **Given** 已选券，**When** 再次打开「选择优惠券」sheet，**Then** 该券为选中态、「不使用优惠券」为未选中态（单选回显）。
147. **Given** 已选券，**When** 在该 sheet 点「不使用优惠券」，**Then** sheet 收起，券行恢复「请选择 ›」，应付回到未抵扣金额。
148. **Given** 散客订单进入 s6/s7，**When** 查看优惠券行，**Then** 自动带入首张可用券（¥10 无门槛券）并已抵扣；**会员订单不自动带券**（显示「请选择 ›」）。
149. **Given** 用户曾选「不使用优惠券」（点中即生效收起），**When** 结算页重新渲染，**Then** 不再自动带入券（保持「请选择」）。
150. **Given** 已选券的 s6 订单，**When** 清空购物车，**Then** 券状态一并清空，应付归 0。
151. **Given** s7 直接收款已选券，**When** 从 s7 打开「选择权益」sheet 并点中某权益（期间会连带渲染 s6），**Then** s7 的券**不被清掉**，s7 应付仍含券抵扣。
152. **Given** s6/s7 有明细，**When** 点「结账」，**Then** toast「结账流程说明」并进入 **s15 开单成功**（明细与角标清空）。
153. **Given** s6/s7 无明细，**When** 点「结账」，**Then** toast「暂无结算项目」，停留当前页。
154. **Given** 左侧导航，**When** 查看，**Then** 含「12 选择权益」「13 选择优惠券」「14 挂单列表」「15 开单成功」「16 员工选择（12 人）」**五项**，可直达对应页面或演示态（评审用）。

**挂单 / 去结单 / 挂单列表（s14）**
155. **Given** s1 购物车有明细，**When** 点底部「挂单」，**Then** toast「挂单成功」、购物车与角标清空、跳转到 **s14 挂单列表**，列表出现该笔挂单（顾客名 + VIP + 时间 + 明细摘要 + **服务员工** + 金额；**R6：卡片上不再有「开单」类型 chip，VIP 标签内不再有黑色方块 `◆`**）。
155a. **Given** s1 某行已选 3 名服务员工，**When** 挂单成功进入 s14，**Then** 该挂单卡「服务员工」**单行显示全部 3 个姓名**（顿号分隔），**不得**使用「等 N 人」省略。
155b. **Given** 进入 s14，**When** 查看标题栏下方，**Then** 有全宽浅黄提示条「**每日24:00后 挂单自动失效并删除**」，且**滚动列表时提示条保持不动**；空态下同样显示。
155c. **Given** s14 有挂单，**When** 点某张卡的**删除（垃圾桶）图标**，**Then** **直接删除、无二次确认弹窗**，toast「已删除「{顾客}」的挂单」，卡片塌陷淡出、列表与 s0/s4 的 `N` 同步 −1，且其余卡片**不重播**入场动效。
155d. **Given** 进入 s14，**When** 观察列表，**Then** 卡片自上而下**错峰淡入上移**（间隔 50ms）。
156. **Given** 首页 s0，**When** 查看两大按钮下方，**Then** 有描边小按钮「取挂单（N）」（**纯文字、无前置图标**），`N` 为挂单池实时条数。
157. **Given** s4 选择顾客页，**When** 查看标题栏右侧，**Then** 有 **「挂单（N）」（R16 改名）**入口，文字左侧有 20×20 图标；列表顶部**不再有**旧的挂单条。
158. **Given** 在 s14 有挂单，**When** 点某条挂单的「**去结单**」（R16 改名），**Then** 卡片淡出缩小后还原该挂单的顾客身份与购物车明细（**含每行员工的工位与顾客指定**，结算页「服务员工」展示与挂单前一致）。**R6：该条挂单不从列表移除、`N` 不变，可再次取用**。
159. **Given** s7（直接收款），**When** 点「挂单」，**Then** toast「直接收款不可挂单」，不写入挂单池、不跳转。
160. **Given** 挂单池无挂单，**When** 进入 s14，**Then** 显示空态（`ic_empty.svg` 72×72 +「暂无挂单」+「挂起的单据会显示在这里」），顶部提示条仍在。

**开单成功页（s15）**
161. **Given** s6 结账成功，**When** 进入 s15，**Then** 依次看到：状态栏（**无标题栏**）+ 黄色渐变背景（高 300）+ 渐变过渡带（高 68）+ 遮罩图层 / 对勾 + 「开单成功」（18/600，**本页唯一主标题**）/ 无框操作组（改日期、改单号、看流水）/ 金额卡（金额 48 → **灰色发丝实线** → 字段行：`单号`（置顶）/`开单日期`/ 支付相关字段 / 卡类字段，行间为**灰色发丝虚线**、**每行左右边距 16**）/ 底部 bar。
162. **Given** 在 s15，**When** 查看底部 bar，**Then** 左侧为**返回首页图标按钮**，右侧依次为「客户签字」「客户详情」「继续开单」三个文字按钮。
163. **Given** 在 s15，**When** 点「返回首页」图标，**Then** 回到 s0 首页。
164. **Given** 在 s15，**When** 点「改单号」，**Then** 弹出底部 sheet（数字单号输入 + 提示 + 取消/确定）；输入新单号并确定 → toast「单号已更新」、本页「单号」更新；留空确定 → 恢复系统自动生成单号。
165. **Given** 在 s15，**When** 点「看流水」，**Then** toast「查看该笔流水」并进入 **s8 门店流水**。
166. **Given** 在 s15，**When** 点「继续开单」，**Then** toast「已开始新的一单」、明细/券/单号被清空，并回到开单页（会员 → s5，散客 → s1）。
167. **Given** 在 s15，**When** 点「客户签字」「客户详情」，**Then** 分别 toast「已进入电子签字流程」「查看客户详情」（本期不做后续页面）。
168. **Given** s7（直接收款）结账成功，**When** 进入 s15，**Then** 页面同样展示（金额按直接收款原价 − 券抵扣计算），底部 bar 一致。
169. **Given** 在 s15 改过单号/日期，**When** 返回结算页查看，**Then** 结算页**不受影响**（成功页改动不回写）。
169a. **Given** 页面无「支付」标题栏，**When** 进入 s15，**Then** 页面内**只有一处**「开单成功」标题文案，不存在与标题栏重复的双标题。
169b. **Given** s15 已进入，**When** 观察金额，**Then** 金额**进入即为应付终值**（`≠ 0`，连续采样无变化；**R20 已删除 0 → 目标值 620ms 计数**）；对勾回弹、标题 / 操作组 / 金额卡 / 底部 bar **错峰上浮**；每次进入本页重新播放。
169c. **Given** 在 s15 查看金额卡，**When** 对比页面底色，**Then** 卡片底 `rgba(255,255,255,.88)` + 1px 白描边 + 柔和投影，卡片**明显浮于渐变之上**；渐变下沿有 68px 过渡带，**无灰白硬切断层**。
169d. **Given** 在 s15 查看内容区操作组，**When** 观察「改日期 / 改单号 / 看流水」，**Then** 均为**无边框**「图标（14）+ 文字（13px `#55595E`）」样式（R6 加深一级），**不存在**白底描边胶囊。
169e. **Given** 在 s15，**When** 点「改日期」，**Then** 日期控件以**底部 sheet 形态**升起（全宽、上圆角 16、带上滑动效与蒙层），选完日期后本页「开单日期」更新；**不是**标题栏下拉浮层。
169f. **Given** 在 s15，**When** 点「改单号」，**Then** 底部 sheet 正常显示 —— 单号输入行（白底圆角、高 54）+ 12px 灰字提示 +「取消/确定」双按钮，**无样式丢失、无错位或透明异常**。

**价目表为空（Tab 隐藏）**
170. **Given** 以 `index.html?noProduct=1` 打开，**When** 进入 s1/s5，**Then** 只显示「项目」Tab（产品 Tab 隐藏），默认可选项目；点商品加号、加购、进 s6 均正常。
171. **Given** 以 `index.html?noProject=1` 打开，**When** 进入 s1/s5，**Then** 只显示「产品」Tab。
172. **Given** 以 `index.html?noProduct=1&noProject=1` 打开，**When** 进入 s1/s5，**Then** 两个 Tab 都显示，并给出空态与「管理价目表」入口说明（本期仅说明）。

**名称与图标**
173. **Given** 全原型任意页面，**When** 检索文案，**Then** **不出现**「快捷开单 / 快速消费」，统一为「直接收款」。
174. **Given** s4「添加会员」行与顶部顾客卡片的「添加会员」图标，**When** 查看，**Then** 为最新 Figma 图标（`ic_add_member_new.svg`）、颜色 `#FF8956`，行内 **36×36**、卡片内 **24×24**。
175. **Given** s1/s5 标题栏右侧（会员身份），**When** 查看会员 icon，**Then** 尺寸 **20×20**，颜色 **`#929292`**（R6 已由 `#1a1a1a` 改为 `#929292`，与散客双人图标同色）。

**R4 补充用例**

176. **Given** s4「添加会员」行，**When** 查看行左图标，**Then** 图标渲染尺寸 **36×36**（`ic_add_member_new.svg`，`#FF8956`），且该行行高与其它顾客行一致（外层 44×44 占位、同中轴）。
177. **Given** s1/s5 顶部散客卡（男/女散客），**When** 查看右侧「添加会员」图标，**Then** 尺寸 **24×24**（等比缩小，与 s4 同款、同色）；点图标仍能正常展开卡内快捷注册面板。
178. **Given** 会员身份（陈女士）进入 s1/s5，**When** 点卡片右侧 chevron 展开，**Then** 展开区为**持卡列表**：**一行一张卡**，左＝卡类型浅色 pill + 卡名，右＝卡剩余权益（`储值卡 美发卡2000元 / 余额1900.00元`、`计次卡 染烫10次卡 / 余4次`、`折扣卡 超值折扣卡 / 8.0折`），权益值右缘对齐卡内右缘（内距 16）；**不再出现原「会员卡 / 储值余额 / 折扣」三宫格指标**；s6/s7 淡色会员卡同款生效。
179. **Given** 新注册会员（无卡无权益），**When** 展开卡片 chevron，**Then** 展开区显示空态「暂无会员卡」，不显示任何卡行。
180. **Given** 点开 s1/s5 商品圆形加号，**When** 查看「设置内容」sheet 标题区，**Then** **只显示商品名，其下方不再有价格行**；数量 / 应付 / 服务员工等其它行与「确定」行为不变。
181. **Given** 在 s15 开单成功页，**When** 查看金额卡，**Then** 卡内底部有发丝线分隔 + 居中次要字段 `开单日期 YYYY.MM.DD · 单号 xxxxx`（12px `#929292`，数字字体），**卡外不再有元信息行**；点「改日期」「改单号」后卡内该字段即时更新；非会员扣卡（无「剩余」行）时卡高收窄、次要字段上移，排版无空洞。
182. **Given** 在 s15，**When** 查看「返回首页」图标按钮，**Then** 图标为 `suc_home.svg`、描边色 **`#F32F41`**（与「客户签字」文字同色），按钮底色 `#FFF2F2`、尺寸 48×48（图标 20×20）。

**R6 补充用例**

*标题栏顾客入口（s1 / s5）*
183. **Given** 散客身份进入 s1，**When** 查看标题栏右侧，**Then** 显示**双人图标**（`ic_guest_head.svg`，20×20，`#929292`）+ 「散客 / 男散客 / 女散客」。
184. **Given** 会员身份进入 s5，**When** 查看标题栏右侧，**Then** 显示**单人图标**（`ic_member_head.svg`，20×20，`#929292`）+ 会员姓名。
185. **Given** 在 s4 将身份由散客改为会员（或反向），**When** 返回来源页，**Then** 标题栏图标**随身份切换**为对应的单人 / 双人图标，**两态均有图标**。

*直接收款金额对齐（s2 / s3）*
186. **Given** 进入 s2（或 s3），**When** 查看金额输入区，**Then** 「¥」与「请输入金额 / 金额数字」**中轴对齐**（同一 40px 行高中垂直居中），无高低错位。

*结算页手动改价（s6）*
187. **Given** s6 有明细，**When** 点底栏「应付 ¥{金额}」，**Then** 升起**底部数字键盘 sheet**（标题「修改应付」+ 大号金额 + 1-9/0/./退格 + 清零/取消/确定）。
188. ~~**Given** 数字键盘 sheet，**When** 输入 `288` 后点「确定」，**Then** 底栏应付被手动锁定、出现「已改价」标签与黄色说明条~~ —— **R16 作废（整单改价已删除）：改价入口下移到明细「应付」行，见 10.3 第 325–328 条。**
189. **Given** 某明细已改价（R16），**When** 查看明细卡权益行，**Then** **仅该明细**的权益行为只读态（文案 `改价商品不可用权益`、降透明度、点击无响应），其余明细权益不受影响。
189.1. **R16 作废（整单改价已删除）**：明细改价后优惠券行仍可点击更换 / 取消，券**照常参与抵扣**（不再有「券不参与结算价」的整单改价口径），见 10.3 第 327 条。
189.2. **Given** 已手动改价且已选用一张券，**When** 再次更换另一张券或取消券，**Then** **结算价始终等于手输值**，且手输值**不失效**（失效基准为「券前合计」，仅明细 / 权益变化才失效）。
190. **Given** 已手动改价，**When** 点「结账」，**Then** toast「结账流程说明」并进入 s15，**s15 大号金额 = 手动应付**；s15 支付明细只显示非卡方式一行（`微信` = 手输值）。
191. **R16 作废（整单改价已删除：「自动失效 / 恢复系统应付」这套联动不再存在）**；明细改价只作用于该条明细，清零即恢复原价，见 10.3 第 328 条。
192. **Given** 已手动改价，**When** 再次点底栏「应付」，**Then** sheet 预填当前生效金额，可再次修改；点「取消」不改动。
193. **Given** 空值或 `0`，**When** 查看键盘 sheet 的「确定」，**Then** 置灰不可点，提示「请输入金额」。
194. **Given** s7 直接收款结算，**When** 查看底栏应付，**Then** **不可点击、不可改价**（与 s6 区分）。

*挂单生命周期（s14）*
195. **Given** 从 s1 挂起一单（`from=order`），**When** 在 s14 点该单「**去结单**」，**Then** 回到 **s1**（散客）并还原明细；从 s5 挂起 → 回到 **s5**；从 s6 挂起（`from=checkout`）→ 回到 **s6**。
196. **Given** 取回某张挂单后未做任何改动，**When** 再次回到 s14，**Then** 该挂单**仍在列表中**（`N` 不变），可再次取用。
197. **Given** 取回挂单后**新增了商品**（或清空购物车 / 重新挂单），**When** 完成结账，**Then** 原挂单**不被删除**（已解绑 `activeHoldId`）。
198. **Given** 取回挂单后**未增删任何内容**，**When** 结账成功进入 s15，**Then** 该挂单**从列表移除**、各处 `N` 同步 −1。
199. **Given** 挂单池中存在 `day < 今天` 的挂单，**When** 进入 s14，**Then** 这些挂单**被自动过滤**，并 toast「有 N 张挂单已超过 24:00 自动失效并删除」。
200. **Given** s14 挂单卡（会员），**When** 查看 VIP 标签，**Then** 为渐变 `VIP`，**不含最左侧黑色方块 `◆`**；卡片上**不存在**「开单」标签。

*成功页字段规则（s15）*
201. **Given** 会员使用**折扣卡**结算，**When** 查看 s15 支付明细，**Then** **不出现任何折扣卡行**（折扣只参与结算价的计算、不参与支付）；该折扣额**并入末尾的非卡支付行**（会员 → `微信`）。若一单**只**用了折扣卡，明细即为**单行 `微信` = 大号金额**。**R11 修订（原 R7 口径「主行 `超值折扣卡 ¥金额` + 次行 `折扣 8.0折`」已废弃）**。
201a. **Given** 会员使用**套餐卡**结算，**When** 查看 s15 支付明细，**Then** 主行 = **`染烫套餐卡　¥{本单消耗金额}`**，次行灰字 = **`剩余　¥{剩余金额} / {剩余次数} 次`**（次行同时给出金额与次数）。
201b. **Given** 会员使用**周期卡**结算，**When** 查看 s15 支付明细，**Then** 主行 = **`半年畅享卡　{N} 次`**，**没有灰字次行**（周期卡不限次、不产生金额）。
201c. **Given** 该位置展示的会员卡消费字段，**Then** **一律以线上现有版本为准**；如原型演示与线上存在差异，**以线上为准**。
202. **Given** 会员使用**储值卡**结算，**When** 查看 s15 支付明细，**Then** 主行 = **`美发卡3000元　¥{本卡实收金额}`**，次行灰字 = **`剩余　¥{卡余额}`**。
203. **Given** 会员使用**计次卡**结算，**When** 查看 s15 支付明细，**Then** 主行 = **`染烫10次卡　{N} 次`**，次行灰字 = **`剩余　{M} 次`**（计次卡只核销次数，不产生金额）。
204. **Given** 一笔订单使用多种支付方式（如会员卡 + 微信 / 微信 + 现金），**When** 查看 s15 支付明细，**Then** **每种方式各占一行**（左 = 支付方式名或会员卡具体卡名，右 = 该方式金额），**各金额行之和 = 顶部大号金额**（不漏行、不合并同名方式）。
205. **Given** 支付方式为线上枚举（会员卡 / 微信 / 支付宝 / 美团 / 抖音 / 口碑 / 大众点评 / 现金 / 银行卡 / 信用卡 / 经理签单 / 其他平台团购），**When** 展示在 s15，**Then** 名称与线上**完全一致**；若原型与线上有差异，**以线上为准**。
206. **Given** 查看 s15 金额卡的分隔线，**Then** `单号` ↔ `开单日期` 为**发丝虚线**，`开单日期` ↔ 支付明细为**发丝实线**，**支付明细行之间为发丝虚线**，**同一张卡的主行 / 灰色次行之间无线**；所有划线左右边距 16。
207. **Given** 会员单演示数据进入 s15，**Then** 支付明细为 `美发卡3000元 ¥58.00 / 剩余 ¥2942.00`、`染烫套餐卡 ¥168.00 / 剩余 ¥1832.00 / 3 次`、`染烫10次卡 1 次 / 剩余 3 次`、`半年畅享卡 1 次`（无次行）、`微信 ¥284.40`；**金额行之和 = ¥510.40** = 顶部大号金额；**折扣卡不出行**（其 ¥78.40 已并入 `微信`）。
208. **Given** 查看 s15 金额卡，**Then** **不出现优惠券字段 / 行**（成功页只列支付明细）。
209. **Given** 进入 s0 首页，**Then** **不存在**「从「开单」进入选择顾客…（问卷 A2）」提示文案。
210. **Given** 打开 s15「改单号」sheet，**Then** 只有单号输入行 + 取消 / 确定双按钮，**不存在**「单号仅支持数字，长度 1-20 位…」提示文案。

*演示数据*
211. **Given** 全原型演示数据，**When** 检查，**Then** 券名为 **`洗剪吹立减10元券`**（长度正常，无超长券名）；顾客列表 **≥15 人**，覆盖 **≥8** 个字母分组。
212. **Given** 任意入口进入 s15、s14、s6，**When** 检查演示数据，**Then** 数据逻辑自洽（金额、支付明细闭合、卡种字段、挂单来源、`N` 计数、券门槛等）**无相互矛盾**，可完整走通闭环。
213. **Given** 会员单从开单页/结算页真实结账（非演示种子），**Then** s15 支付明细按「卡行（储值 / 套餐 / 计次 / 周期）+ 非卡行」渲染（**折扣卡不出行**），**各金额行之和 = 大号金额**；**散客单含 ≥2 条明细时**演示 `现金 + 微信` 组合（现金 = 金额最大的单条明细，**演示规则，非线上规则**）。

**R9 补充用例**

*结算页可编辑提示（s6）*
214. **Given** s6 有明细，**When** 查看底栏，**Then** 应付金额右侧有 **14×14 铅笔图标**（**R14：色为 `#929292`**，距金额 6px），「应付 ¥{金额} + 铅笔」整组可点（按下底色 `#FFF2F2`）。**R10 修订：铅笔由「与金额垂直居中」改为「与金额数字底对齐」，验收口径见用例 236**。
215. **Given** s6 底栏，**When** 量测，**Then** 「应付」文字左缘落在 **16px 栏距**（底栏 `padding-left:8px` + 按钮内距 8），热区左沿 8px、高 37px；点击仍按 R6/R7 规则弹出数字键盘 sheet。
216. **Given** 进入 s7 直接收款结算，**When** 查看底栏应付，**Then** **不出现铅笔图标**、不可点。**R14：s7 标题栏同样有「更多」菜单（见用例 294）**。

*「修改单号」sheet（s15）*
217. **Given** 打开 s15「修改单号」，**When** 量测，**Then** body 左右内缩 **16px**、输入行 **高 56 / 圆角 10 / 白底**、右侧数字 16px；输入行距标题 **16px**、距按钮区 **16px**。
218. **Given** 「修改单号」sheet，**When** 查看底部按钮，**Then** 为**等宽双按钮（取消 : 确定 = 1:1，各 174px + 10px 间距）**，左右边与输入行对齐；**点遮罩空白处关闭**（等同取消）。
219. **Given** 「修改单号」sheet 已打开，**When** 输入新单号并点确定，**Then** toast「单号已更新」并更新卡内「单号」；输入为空时按系统单号规则生成。

*「选择权益」sheet 权益适用性*
220. **Given** 明细为**洗剪吹**，**When** 打开「选择权益」sheet，**Then** `计次卡抵扣` 与 `固定金额98元（美发卡3000元）` **均可选**（两项均绑定「洗剪吹」）。
221. **Given** 明细为**时尚造型**，**When** 打开「选择权益」sheet，**Then** `计次卡抵扣` 可选，`固定金额98元（美发卡3000元）` **置灰**（名称 `#B2B2B2` + 单选圈 45% 透明；**R10 修订：不再有「不适用」标签**），**点击无反应、选中态不变**。
222. **Given** 明细为**时尚洗吹**（未绑定），**When** 打开「选择权益」sheet，**Then** `计次卡抵扣` 与 `固定金额98元（美发卡3000元）` **均置灰**；`5.0折（5折卡）` / `铂金卡` / `折扣卡` / `金卡`（折扣类）保持可选。
223. **Given** 明细为**任意产品**（如剑琅系列），**When** 打开「选择权益」sheet，**Then** 次卡类与固定金额类**均置灰**（演示口径：仅折扣 / 储值类为全场通用）。
224. **Given** **散客**或**无卡会员**，**When** 打开「选择权益」sheet，**Then** 列表**只有「不使用权益」一行**，且**不显示空态说明文案**。
225. **Given** 用户此前为明细选过某权益，**When** 该权益对当前明细不适用（或身份变为散客）后再次打开权益 sheet，**Then** 草稿**自动回落为「不使用权益」**（不出现置灰行被选中的矛盾态）。

*「挂单」图标（s4）*
226. **Given** s4 标题栏，**When** 查看「挂单（N）」（R16 改名），**Then** 文字左侧有 **20×20 图标**（`assets/ic_hold_take_nav.svg`，`#929292`，1.6px 描边，图形 = 单据 + 时钟，与文字间距 6px）。
227. **Given** 进入 s0 首页，**When** 查看「取挂单（N）」，**Then** 仍为**纯文字**（s4 以外不加图标）。

*标题栏基线对齐（全局）*
228. **Given** 分别进入 s1 / s2 / s4 / s5 / s8，**When** 对比标题文字与右侧文字组的底缘，**Then** 两者基线重合（实测底缘差 ≤1.8px）；14px 组下移 2.8px、12px 组下移 3.7px（`--nav-shift-14` / `--nav-shift-12`）。
229. **Given** 上述任一页面，**When** 查看右侧图标与文字，**Then** **图标与文字整体同轴下移**（组内相对位置不变），无「图标在上、文字在下」的错位。

*s15 顶部渐变（R9 换色）*
230. **Given** 进入 s15，**When** 查看顶部背景，**Then** 为**纯纵向渐变**（`#FFBE84` → 杏黄 → `#F7F7F7`，高 328），逐点取样与 Figma `1174-135` 差 **≤1/255**，且**末段与页面底色 `#F7F7F7` 无缝**（无硬切断层）。
231. **Given** 进入 s15，**Then** **不存在**装饰遮罩图层（`assets/suc_bg_mask.svg`）与 68px 过渡带元素，**横向无光斑**。
232. **Given** 会员从 s6 **真实结账**（自动匹配权益 `折扣卡`），**When** 查看 s15 支付明细，**Then** 左列为**卡的具体名称** `超值折扣卡`（**不是权益名 `折扣卡`**）；权益 `固定金额98元（美发卡3000元）` → 左列 `美发卡3000元`；权益 `计次卡抵扣` → 左列 `染烫10次卡`；同一张卡的多条明细**合并为一行**。

**R10 补充用例**

*修改应付 sheet（s6）*
233. **Given** s6 有明细，**When** 点底栏「应付」打开数字键盘 sheet，**Then** sheet 内**只有「标题 + 金额数值 + 键盘」三段**，**不再有任何说明/提示文案**（DOM 中 `.paysheet__tip` 不存在）；数值行到键盘的留白约 18px，无「贴在一起」的空洞感。
234. **R16 作废**：底栏上方的黄色状态说明与「已改价」标签**已随整单改价一并删除**；改价规则改见 10.3 第 325–328 条。

*s15 顶部渐变*
235. **Given** 进入 s15，**When** 取 `.suc-bg` 的计算样式，**Then** `opacity` = **0.84**；渐变列表（色标与顺序）与 R9 一致，末端仍为 `#F7F7F7`、与页面无缝。

*s6 底栏铅笔（底对齐）*
236. **Given** s6 有明细，**When** 量测底栏「应付 ¥{金额}」与右侧铅笔，**Then** **铅笔底缘与金额数字的字形底缘齐平**（实测差 0；改前铅笔比数字底高 1.7px）；铅笔仍为 14×14、距金额 6px，热区与点击行为不变（**R14：颜色改为 `#929292`**）。
237. **Given** s7 直接收款结算，**Then** 仍**无铅笔图标**。

*「选择权益」/「选择优惠券」sheet 不可用标签*
238. **Given** 明细为**时尚造型**，**When** 打开「选择权益」sheet，**Then** `固定金额98元（美发卡3000元）` 行**仍然置灰**（名称 `#B2B2B2`、圈 45%、行底 `#FAFAFA`、点击无反应），但**行尾不再出现「不适用」标签**（DOM 中 `.off-tag` 不存在）。
239. **Given** 订单金额不满券门槛（如 ¥60.40 时的满 200 / 满 300 券），**When** 打开「选择优惠券」sheet，**Then** 该券卡**仍然浅灰降饱和且不可选**（点击仅 toast），但**不再显示「暂不可用」标签**（DOM 中 `.cp-off` 不存在）。

*「选择优惠券」sheet 券卡右侧布局*
240. ~~**Given** 打开「选择优惠券」sheet，**When** 量测任一券卡，**Then** 单选圈 `right:16px / top:47px`……~~ **【本条已被 R14 作废】** —— R14 已**删除券卡上的单选圈**，故「圈体位置」不再有断言对象；改为断言 **券额与门槛文案均右对齐到卡片内边距 16px**（`right:16px`），且**券卡内不存在任何单选圈 DOM**（见用例 291）。
241. ~~**Given** 展开某券卡的「使用规则」（卡高 125 → 181px），**When** 对比展开前后的圈体位置……~~ **【本条已被 R14 作废】** —— 控件已删；**保留其有效结论**：券卡展开「使用规则」**不影响右侧金额 / 门槛的位置**（两者为 `top` 固定值 18 / 53，与卡高无关）。
242. **Given** 不满门槛的券卡，**When** 查看其降权表现，**Then** 降权只作用于角标 / 券名 / 适用范围 / 券额，**门槛文案保持可读**（R14：圈体已删，原「圈体保持 100% 不透明」一条随之失效）。
243. **Given** 任一券卡，**When** 点击券卡（含圈体所在区域），**Then** **可选券 → 立即生效并收起 sheet**（R13）；**不可选券 → toast「当前订单不满该券使用门槛」且 sheet 保持打开**；选中态仍由 `ic_radio_on/off` 图标切换表达。

*结算页顾客卡片被明细白卡盖住（s6 / s7）*
244. **Given** s6 有明细且滚动区可滚，**When** 滚动时量测顾客卡片位置，**Then** 顾客卡片**恒定停在 `top:12px`（滚动区顶部下 12px）不动**（改前随内容 1:1 上移）；**启动滚动瞬间不发生跳动**（吸附位置 = 静止位置）。
245. **Given** 上一条状态，**When** 继续滚动，**Then** 明细白卡（首张明细卡带 **12px 上缘圆角**）**自下而上滑过并盖住顾客卡片**：量测被重叠区域内的最上层元素为明细卡内容（而非顾客卡片）。
246. **Given** 结算页**静止**（未滚动），**When** 与改前对比截图，**Then** **视觉零变化** —— 顾客卡与明细卡的间距仍为 12px、卡片之间 12px 空隙仍透出页面底色（`.co-items`/`.co-extras` 的 `#F7F7F7` 与页面底色同色）。
247. **Given** 滚动中，**When** 检查卡片之间的 12px 空隙，**Then** **不会透出已被盖住的顾客卡片**（`.co-items` / `.co-extras` 为不透明底）。
248. **Given** s6 滚动区已加 `is-scrolled`，**Then** 顾客卡片 `z-index` 为 **1**、明细区为 **2**（与开单页 `.v2-bill-body.is-scrolled .v2-ccard { z-index:1 }` 同源）；底部 co-bar（30）与改价提示条（29）层级不受影响。
249. **Given** s7 直接收款结算，**When** 滚动，**Then** 行为与 s6 完全一致；**Given** 进入 s14 / s15 等页面，**Then** 顾客卡片**不吸顶**（本效果仅 s6 / s7）。

**R11 补充用例**

*s15 支付明细：折扣卡不出行（s15）*
250. **Given** 会员单的某条明细走**折扣卡**（如自动匹配的 `折扣卡` = 8.0 折），**When** 进入 s15，**Then** **支付明细中不存在该折扣卡行**（DOM 中无该卡名、也无 `折扣` 灰字次行）；该明细的应付额**并入末尾的非卡支付行**（会员 → `微信`）。
251. **Given** 会员单**所有**明细都走同一张折扣卡，**When** 进入 s15，**Then** 支付明细为**单行 `微信` = 顶部大号金额**（金额闭合、无空行、无 `¥0.00` 行）。
252. **Given** 会员从 s6 用折扣卡真实结账，**When** 核对 s15，**Then** `Σ 金额行 = 顶部大号金额`（折扣额不漏记、不重复计）。

*套餐卡 / 周期卡行（s15）*
253. **Given** 会员使用**套餐卡**，**When** 查看 s15，**Then** 主行 `染烫套餐卡　¥{本单消耗金额}`，次行灰字 `剩余　¥{剩余金额} / {剩余次数} 次`（**同时**给出金额与次数）；该行金额计入实收。
254. **Given** 会员使用**周期卡**，**When** 查看 s15，**Then** 主行 `半年畅享卡　{N} 次`，**无灰字次行**；该卡**不产生金额**。
255. **Given** 一张单同时用到储值卡 / 套餐卡 / 计次卡 / 周期卡，**When** 查看 s15，**Then** 顺序为 **储值卡 → 套餐卡 → 计次卡 → 周期卡 → 非卡方式**；同一张卡的多条明细**合并为一行**。
256. **Given** 会员单演示数据（¥510.40），**Then** 5 行支付明细为 `美发卡3000元 ¥58.00 / 剩余 ¥2942.00`、`染烫套餐卡 ¥168.00 / 剩余 ¥1832.00 / 3 次`、`染烫10次卡 1 次 / 剩余 3 次`、`半年畅享卡 1 次`、`微信 ¥284.40`，**金额行之和 = 510.40 = 大号金额**。
257. **Given** 演示数据一致性，**Then** s5 会员卡列表（`5 张`：计次卡 / 储值卡 / 折扣卡 / 套餐卡 / 周期卡）与「选择权益」sheet 列表（9 行，含 `套餐卡抵扣` / `周期卡抵扣`）与 s15 出现过的卡**一一对应，无孤立卡种**。

*结算页「服务员工」行留白（s6 / s7）*
258. **Given** s6 某明细**只选 1 位**服务员工，**When** 量测员工行，**Then** 行高 = **40px**（=「权益 / 应付」行高），上下留白各 10px（**与改前一致，零回归**）。
259. **Given** s6 某明细**选 2 位及以上**服务员工，**When** 量测员工行，**Then** 行高 = **员工文字块高度 + 20px**（2 位 → 文字块 42px、行高 **62px**），**文字块与上方实线 / 下方虚线的留白各 10px**（改前为 0，文字贴线）。
260. **Given** 上述多员工状态，**When** 查看左标签，**Then** 「服务员工」**随整行垂直居中**；「权益 / 应付」两行行高**仍为 40px**（不受影响）。

*s15 顶部渐变层不透明度*
261. **Given** 进入 s15，**When** 取 `.suc-bg` 的计算样式，**Then** `opacity` = **0.7**；渐变色标（13 个停靠点）与高度 328 与 R9 一致，末端仍为 `#F7F7F7`、与页面无缝。

**R12 补充用例**

*员工卡展开与多选*
262. **Given** 员工面板，**When** 点某员工卡，**Then** 该卡横向撑满整行、平铺 4 张选项卡，点选后收起；**再点已勾选工位 = 取消该工位**（无抖动动效）。
263. **Given** 展开态，**When** 先后点「大工」与「中工」，**Then** 两工位同时勾选，摘要为「大工 · 中工」。
264. **Given** 展开态，**When** 只勾「顾客指定」，**Then** 该员工**计入已选**，摘要与卡片面只显示「顾客指定」，**不出现「无工位」**。
265. **Given** 展开态，**When** 点掉全部选项（工位与顾客指定皆空），**Then** 撤销该员工（回到未选态）。

*摘要与换行*
266. **Given** 某员工选「大工 + 顾客指定」，**When** 查看卡片面，**Then** 单行显示「大工 · 顾客指定」。
267. **Given** 某员工选「大工 + 中工 + 小工」，**When** 查看卡片面，**Then** 拆为「大工 · 中工」+「小工」两行，**无省略号**。
268. **Given** s6 某明细选 2 名员工，**When** 查看「服务员工」行，**Then** 每名员工一行「姓名 · 工位 · 顾客指定」；**R16：该行为热区，右侧 `›` 可进入「修改服务员工」sheet**。
269. **Given** s14 有挂单且全单员工较多，**When** 查看挂单卡「服务员工」字段，**Then** 全部姓名顿号连接、**可换行**显示（不省略、不出现「等 N 人」）。

*人数与数据*
270. **Given** 同一商品连选 4 名员工，**When** 查看，**Then** 全部计入选（**不设上限**，原「满 3 人 toast」作废）。
271. **Given** 一条旧结构挂单（含 `staffDesignated` 与单值 `staffRoles`），**When** 去结单，**Then** 自动迁移为新结构（`staffExtra` + 工位数组），摘要展示与挂单前一致。
272. **Given** 添加 sheet 已为商品 A 选好员工后收起，**When** 打开商品 B 的员工面板，**Then** 无任何卡片处于展开编辑态、B 全部未选。

*动效与门控*
273. **Given** 点选某工位，**When** 在勾选 / 收起动画期间连点，**Then** 门控忽略重复点击，最终状态正确、动画不重叠。
274. **Given** 开启「减少动态效果」，**When** 选人，**Then** 勾选 / 收起动效关闭、直接落定，状态与常规一致。

**R13 补充用例**

*sheet 化与点中即生效*
275. **Given** 原型页面清单，**When** 检查 `.screen` 节点，**Then** **不存在 `#s12` / `#s13`**（两页已撤除），其功能分别由 `#benPickMask` / `#cpPickMask` 两个 `.addsheet-mask` 承载。
276. **Given** s6 明细卡，**When** 点「权益」行右侧「值 + ›」，**Then** sheet 从底部升起，**页面不发生任何跳转**（`navStack` 栈顶仍为 `s6`），关闭后仍在原位。
277. **Given** 「选择权益」sheet 已打开且当前为「折扣卡」，**When** 点「5.0折（5折卡）」行，**Then** 单选立即生效、sheet 收起、该明细应付按 5 折重算；**再打开时选中态回显为「5.0折（5折卡）」**。
278. **Given** 「选择权益」sheet，**When** 点不可用（置灰）的权益行，**Then** 无任何反应：选中态不变、**sheet 不关闭**。
279. **Given** 「选择优惠券」sheet，**When** 点「使用规则」，**Then** 仅该券卡展开 / 收起，**不选中、不关闭 sheet**；**When** 点券卡主体，**Then** 立即生效并收起。
280. **Given** 「选择优惠券」sheet，**When** 点蒙层空白处，**Then** sheet 关闭且 `__v2OrderCoupon` **不变**（取消语义）。

*打印小票*
281. **Given** s15，**When** 检查金额卡，**Then** **卡片右上角不存在任何图标按钮**；**卡片最后一行**为「打印小票」整行入口（`aria-label="打印小票"`、图标 18px `#8E8E93` + 文案 13px `#55595E`、上方 0.5px 实线 `#E5E5E5`、整行可点）；**底栏按钮数量不变**（返回首页 / 客户签字 / 继续开单）。
282. **Given** s15，**When** 点卡片底部「打印小票」行，**Then** toast「已发送打印小票指令」，**停留在 s15**（不跳转）；**Given** 该行，**Then** 热区 **319×44**（整行可点）、与卡内字段行**左右对齐**、在明细字段区**下方**。

*服务员工长摘要*
283. **Given** s6 某明细服务员工为「3 工位 + 顾客指定」，**When** 量测「服务员工」行摘要行，**Then** `max-width` 为 `none`、文本完整、**无省略号**（`scrollWidth === clientWidth`），文本宽 250px ≤ 可用 302px。
284. **Given** 上述状态，**When** 对比该行与同卡其它「值」的右边缘，**Then** 偏移一致（均为 374），**不换行**、行高与单行员工一致（41px）。
285. **Given** 摘要为「姓名（14px/500）+ 工位（13px/次要色）」，**When** 查看字号，**Then** 姓名为 `14px`、工位摘要为 `13px`，层级可辨。

*导航演示入口*
286. **Given** 左侧导航，**When** 点「12 选择权益」，**Then** 以**演示态**打开权益 sheet（**无目标明细**）：显示全部 **9 行**、**无置灰行**；点中任一行**仅更新回显、不关闭 sheet**（便于评审逐项查看）。
287. **Given** 左侧导航，**When** 点「13 选择优惠券」，**Then** 打开优惠券 sheet（含「不使用优惠券」+ 3 张券卡）；**Given** 两个导航项，**Then** 均不再带 `data-target`（不对应任何 `.screen`，不会点亮导航高亮）。

**R14 补充用例**

*sheet 留白与安全区*
288. **Given** 打开「选择优惠券」sheet，**When** 量测内容区计算样式，**Then** `padding` 为 `12px 12px 46px`（= 上 12 · 左右 12 · 下 12 + `var(--safe-bottom)` 34）；**Given** 标题栏下沿到首项、卡片彼此之间，**Then** **均为 12px**；**Given** 末张券卡下沿到 sheet 底，**Then** **恰为 46px**（末卡不计底部行距）。
289. **Given** 打开「选择权益」sheet，**When** 量测 9 行权益行，**Then** **行距 12px**（改前 8px）、首行与标题栏间距 **12px**、末行下沿到 sheet 底 **46px**。

*选中态与勾选控件*
290. **Given** 「选择权益」sheet，**When** 选中某行，**Then** 该行同时具备 **1px 红色内描边 + `#FFF8F8` 底** 与 **右侧「选中」勾选圈**（两者并存，不互相替代）。
291. **Given** 「选择优惠券」sheet，**When** 检查券卡，**Then** **券卡内不存在任何单选圈 DOM**（`.cp-rd` / `.rd` 计数为 0）；**Given** 「不使用优惠券」行，**Then** **仍有 1 个勾选圈**（全 sheet 唯一保留的控件）。
292. **Given** 已选某张券，**When** 打开 sheet，**Then** 该券卡为 1px 红色内描边 + `#FFF8F8`，且「不使用优惠券」行**无描边、圈为未选态**（互斥）。
293. **Given** 券卡右侧两列已撤除，**When** 量测券额与门槛文案右边缘，**Then** **均距卡片右内边 16px**（改前 52px）；**Given** 券名，**Then** 可用宽度 **260px**，演示数据中最长券名不截断。

*结算页「更多」菜单*
294. **Given** s6 标题栏，**When** 检查右侧，**Then** 存在 24×24「更多」按钮（3 圆点 SVG、`color:#1a1a1a`）；**Given** 图标中心与 18px 标题墨迹中心，**Then** 纵向差 **0.00px**；**Given** s7，**Then** 同款按钮存在且几何一致。
295. **Given** s6，**When** 点「更多」，**Then** 弹出下拉菜单，含「选日期」「手工单号」两项、**每项左侧均有图标**；**When** 点菜单外任意处 / 切换页面，**Then** 菜单收起。
296. **Given** 菜单已打开，**When** 点「选日期」并选「昨天」，**Then** 日历以**底部 sheet** 打开、确认后 toast「开单日期：{YYYY.MM.DD}」、**该日期成为本单开单日期**；**When** 随后开单成功，**Then** s15 卡内「开单日期」为该日（**不选时默认今天**）。
297. **Given** 菜单已打开，**When** 点「手工单号」，**Then** 打开单号 sheet 且**标题为「手工单号」**；**When** 输入 `20260919007` 并确定，**Then** toast「手工单号：20260919007」；**When** 随后开单成功，**Then** s15 卡内「单号」=`20260919007`（**代替自动编号**）。
298. **Given** 未使用「手工单号」，**When** 开单成功，**Then** 单号照常**自动生成**（11 位）；**Given** 进入成功页后再点「改单号」，**Then** sheet 标题为「**修改单号**」（入口措辞随页面切换）。

*图标与金额间距*
299. **Given** s6 底栏「应付」，「When」检查铅笔图标，**Then** `color` 为 `#929292`（改前随金额为 `#F32F41`）；**Given** 全部垃圾桶图标（s6/s7 明细删除、s7 直接收款删除、已选商品「清空」、s14 挂单删除），**Then** **均为 `#929292`**。
300. **Given** s6 已选优惠券，**When** 检查券行右侧文字，**Then** `color` 为主文字色 `#1a1a1a`（改前 `#F32F41`）；**Given** 该行右侧箭头，**Then** **是 SVG 元素而非文字 `›`**；**Given** 全原型渲染后的 DOM，**When** 扫描文本节点中的 `›` / `＞`，**Then** **命中数为 0**。
301. **Given** s15，**When** 检查打印小票图标，**Then** `viewBox="0 0 24 24"`、`fill="none"` / `stroke="currentColor"` / `stroke-width="1.6"`、3 条路径 + 1 枚 `currentColor` 指示灯（**R19 改写**：原 200 画布填充稿作废）、计算色 **`#8E8E93`**、渲染 **18px**（渲染线宽 **≈ 1.2px**），位于**金额卡底部整行入口**内（`.suc-print-row`，**不再是右上角 28×28 热区**）。
302. **Given** s15 大号金额，**When** 量测 `¥` 盒右缘与数字盒左缘，**Then** 间距为 **2px**（`margin-right:2px`）；**Given** s15 对勾图标，**Then** `assets/suc_check.svg` 的圆底 `fill` 为 **`#FF571A`**。

*「修改单号」sheet 版式与键盘*
303. **Given** s15 点「改单号」，**When** 量测内容区，**Then** `.sucno-body` 内距 `0`、`.sucno-field` 为 **390 宽（满宽）· 圆角 0 · 左右贴边**，白色块**完全覆盖 sheet 灰底**（无任何窄于 sheet 的子元素）；**Then** 标题栏下沿到输入行、输入行到页脚**均为 0**（连续白底）。
304. **Given** 该 sheet 的输入框，**When** 检查样式，**Then** 背景 `#F7F7F7`、圆角 8、**无描边、无投影**、高 36、数字右对齐；**When** 聚焦，**Then** 背景转 `#F2F2F2`。
305. **Given** 打开该 sheet，**When** 检查 `document.activeElement`，**Then** **是单号输入框**且 **默认全选**（`selectionStart=0`、`selectionEnd=value.length`）；**Given** 点该行任意位置（标签 / 空白），**Then** 输入框获得焦点。
306. **Given** 该 sheet 已打开，**When** 统计 `.addsheet-mask.show` 的数量，**Then** **为 1**（不存在叠加的第二层 sheet）；**Given** 成功页与结算页两个入口，**Then** 复用**同一个** `#sucNoMask`。
307. **Given** 手机模式并模拟 320px 键盘遮挡，**When** 打开任意底部浮层，**Then** `--kb-h` = 298px、浮层 `bottom` = 298px、**输入行完整可见不被键盘遮挡**；**Given** 遮挡 60px，**Then** `--kb-h` = 0（判定为浏览器 chrome 抖动）；**Given** 无 `visualViewport` 的环境，**Then** `--kb-h` 恒为 0 且不报错。
308. **Given** 打开「修改单号 / 手工单号」sheet，**When** 量测单号输入框，**Then** 宽 **240px**（`flex-basis:240px`）、**右缘距 sheet 右 16px**，与页脚「确定」按钮右缘**同列**（实测两者均为 16px，差 0）；**Given** 填入 20 位数字，**Then** 文字不溢出（可用宽 216px ≥ 20 位数字 182px）；**Given** sheet 收窄，**Then** 输入框按 `flex-shrink:1` 收缩、不溢出。

> **R16 说明**：本节为历史累积用例，其中下列条目已被 R16 改写或作废，**以改写后的表述与 10.3 为准** ——
> **改写**：#19（价目卡价格）、#44（明细单价）、#88（新会员行内容）、#105（无卡会员）、#116 / #157（s4 入口改名）、#158 / #195 / #226 / #271（去结单）、#189 / #268（员工行可编辑）；
> **作废**：#188 / #189.1 / #191（整单改价与自动失效）、#234（底栏黄色说明与「已改价」标签）。
> **R16 新增用例见 10.3。**
>
> **R18 说明**：本节 **#315**（删「最多 3 人」）、**#335**（顾客指定回主文字色）两条已按 R18 改写，另见 10.5。
>
> **R17 说明**：本节第 **#281 / #282 / #301 / #322** 四条已按 R17 改写（打印小票入口由「卡内右上角图标」改为「卡片底部整行」）；s4 相关条目里「分段胶囊 / 左段实心红」的表述以 **§6.8** 与 **10.4（331–334）** 为准。
>
> **R19 说明**：本节第 **#281 / #282 / #301 / #337 / #338 / #339** 六条已按 R19 改写（图标换 24 画布线描稿、17 → 18px；该行去圆角去底色、46 → 44px、按下仅加深），另见 **10.6（353–364）**。
>
> **R22 / R23 说明**：本节 **#370 / #372 / #383** 三条中关于紧凑卡「右上角勾选控件」的表述已按 **R23 改写** —— R21 删勾标（会压字）→ R23 以「控件挂在卡右上角外侧、三态常驻同一坐标」**加回**，判定口径由「**没有** `.sp-card__tick`」反转为「**有**、三态都常驻、且与姓名 / 职位不相交」；R22 的三态色值以 **385–390** 为准，R23 的勾选控件与行间距以 **391–400** 为准。

### 10.7 R20–R23 验收用例（365–400）

**s14 按钮文案**

365. **Given** s14 挂单列表，**When** 取挂单卡操作行，**Then** 主按钮文案为 **「去结单」**（不是「去开单」/「取挂单」），样式仍为高 34 / 圆角 17 / `#F32F41` 实心；**按钮仍是每卡一个**，与左侧删除按钮成对。
366. **Given** 全原型渲染后的 DOM，**When** 检索「去开单」字样，**Then** **命中 0**（文案、注释、PRD 一并收敛）。
367. **Given** 点某卡「去结单」，**Then** 还原顾客 + 明细 + 每行 `staffIds / staffRoles / staffExtra` 并按 `from` 回跳 s1/s5/s6；**挂单池条数不变**（同一挂单可反复取用）。

**≤ 5 人：紧凑形态**

368. **Given** 门店 5 人，**When** 打开添加「设置内容」sheet，**Then** 员工区内是 `.sp-cards`（**不是** `.sp-box` / `.sp-face--pick`），**没有** `.sp-slot--add`「＋」槽位，也**没有** `.sp-slot`。
369. **Given** 该紧凑区，**Then** 有 **4 个 `.sp-cards__row`**，标题依次为 **大工 / 中工 / 小工 / 顾客指定**；**每行 5 张卡**（横列全部员工）。
370. **Given** 任一 `.sp-card`，**Then** 尺寸 **59×52**、**无头像**、姓名 **13px**（取自 `st.name`）、职位 **11px `st.role`**、卡圆角 **12**；行标题仍 **13px `#1a1a1a`**。**三态色值见 385–387**（R22 起未选中描边为 `#EDEDED`，不再是 `#E5E5EA`）。
371. **Given** 某行标题与卡列，**Then** **第一张卡的左边缘 = 标题的左边缘**（误差 < 1px）；卡列 `flex-wrap:nowrap` 且 `scrollWidth ≤ clientWidth`（**5 张恰好落满、不横滑不换行**）；卡间距 6px。
372. **Given** 点某行第 1 张卡，**Then** 该卡变选中态（`is-on`：品牌描边 + 淡粉底 + **右上角控件由空心圈切成实心勾**（R23，见 392）+ `aria-pressed="true"`）、播一次落位弹跳，**数据层** `staffRoles[sid]` 加入该工位；**再点一次** → 取消选中、数据层移出该工位（若该员工不在其它工位，则从本单移除）。
373. **Given** 某行已选 3 人，**Then** 该行**其余卡置灰**（`opacity .4` + `pointer-events:none`）、带 `aria-disabled="true"`；点第 4 张 → toast「{工位}最多 3 人」且**人数仍为 3**。
374. **Given** 同一员工分别点「大工」与「顾客指定」两行的卡，**Then** **两张卡同时选中**，摘要文案 = `大工 · 顾客指定`（跨行可重复，互不排斥）。
375. **Given** 5 人门店的添加 sheet，**Then** **「价格（元）」卡仍显示**（`#asPriceCell` 不是 `display:none`）、body `scrollHeight = clientHeight`（不滚动）、4 行卡都在 sheet 可视区内。
376. **Given** s6「修改服务员工」sheet 与 s2/s3 直接收款面板，**Then** 同样渲染紧凑形态（4 行 × 5 卡）；**s6 sheet 不滚动**；**s2 面板 `max-height:388px`、`scrollHeight = clientHeight`、面板底不越过手机框底且 4 行末行在面板内**。
377. **Given** 关闭添加 sheet 后重新打开，**Then** 草稿是干净的（`.sp-card.is-on` 命中 **0**），且仍是紧凑形态。
383. **Given** 紧凑态任一张卡，**When** 检查 DOM，**Then** **没有** `.sp-av` / `img` 头像节点；**有** `.sp-card__nm` 与 `.sp-card__role`；**每张卡都有 `.sp-card__tick` 节点且三态常驻**（**R23 两次反转**：R21–R22 曾断言「没有勾标」，R23 先改成「只有 `.is-on` 才 `display:flex`」，现为 **`display:flex` 三态恒常驻**，只有外观随状态切换）；`aria-label` 含「姓名 · 职位」；**Given** 左侧导航「16」进入 >5 人选择页，**Then** 选择页卡仍**保留头像与勾标**（`.sp-pick__tick` 未被 R23 改动，见 394）。
384. **Given** 同一员工已在「大工」行选中，**When** 再点「顾客指定」行的同人卡，**Then** **只有刚点的那一张**带 `is-land`（播落位弹跳），「大工」行同人卡**仅保持 `is-on`、不带 `is-land`**。

**> 5 人：槽位 + 选择页**

378. **Given** 左侧导航「16 员工选择（12 人）」，**When** 点击，**Then** 员工池扩到 **12 人**、打开添加 sheet 并**停在「选择大工」选择页**（`data-picking="1"`）；员工区**没有** `.sp-cards`。
379. **Given** 该选择页，**Then** **12 张 `.sp-pick`（4 行 × 3 列）**、每张 **102×95**；`#asPriceCell` 的 `display` = **none**；`.addsheet` 的 `max-height` = **92%**。
380. **Given** 该选择页，**Then** `.sp-box` 的 `--sp-pick-h` 已被写入且**等于 `.sp-pick-inner` 的高度**（实测 442px）、`.sp-face--pick` 高度与之相等；body `scrollHeight = clientHeight`（**12 张卡全部可见、sheet 不滚动**），最后一张卡的底边在 sheet 可视区内。
381. **Given** 点选择页任一张卡，**Then** 自动回到槽位行（`data-picking="0"`）、该员工落入对应工位槽位、**价格卡随即恢复显示**、`.addsheet` 上限恢复 **85%**；**关闭 sheet 后员工池还原为 5 人**，再开添加 sheet 回到紧凑形态。
382. **Given** s15，**When** 进入本页，**Then** 金额**立即**等于应付终值（≠ 0、无 0 → 目标值中间态；连续两次采样值不变），源码中 `sucCountUp` / `sucAmtRaf` / 620ms 计数常量**均为 0 命中**；**同时**对勾 `sucPop` 与标题 / 金额卡 / 操作组 / 底栏的 `sucRise` **5 条入场动画仍在**（进入本页仍播放）。

385. **Given** 紧凑态任一**未选中**卡，**Then** 描边 **0.5px `#EDEDED`**、姓名 **`#55595E` / font-weight 500**、职位 **`#A6A6AB` / 400**（R22 前为 `#E5E5EA` / `#1A1A1A`·600 / `#8E8E93`）。
386. **Given** 紧凑态任一**选中**卡（`is-on`），**Then** 姓名 **`#1A1A1A` / font-weight 600**、职位 **`#8E8E93`**、描边 `#F32F41`（0.5px 边 + 0.5px 外扩阴影）、淡粉底 —— **与 R22 之前完全一致**（选中态本轮不动）；**Given** 同一屏上的未选中卡，**Then** 两态**姓名颜色不同**（`#1A1A1A` ≠ `#55595E`）、**字重不同**（600 ≠ 500）。
387. **Given** 该行已选 3 人，**Then** 置灰卡（`is-full`）仍然 `opacity:.4` + `pointer-events:none`，但其姓名/职位**被钉回 `#1a1a1a` / `#8E8E93`**，40% 透明合成后姓名实际着色 **≈ `#A3A3A3`、对比度 ≈ 2.52:1**（与 R22 之前同值；**不得**降到 `#BBBDBF` / 1.88:1）。
388. **Given** R22 后的紧凑态，**Then** 三态姓名对比度依次为 **未选中 7.05:1 / 选中 ≈15.8:1 / 置灰 ≈2.52:1**（无头浏览器计算样式 + WCAG 公式实测），三档均 ≥ 2.5:1 且层次可辨。
389. **Given** 卡尺寸与排布，**Then** 仍为 **59×52**、圆角 12、卡间距 6，卡列 `scrollWidth = clientWidth`（**5 张恰好落满、不横滑不换行**），宿主（添加 sheet / s6 sheet / s2 面板）**不产生滚动**。
390. **Given** 4 个入口（添加「设置内容」sheet、s6 · s7「修改服务员工」sheet、s2 · s3 直接收款面板），**When** 逐个渲染紧凑态，**Then** 均使用同一套三态色值（未选中 `#55595E/500`、选中 `#1A1A1A/600`）；**Given** 左侧导航「16」进入 >5 人选择页（`.sp-pick`），**Then** 卡面色值**不受本轮影响**。

**R23 · ≤ 5 人紧凑卡的勾选控件（三态常驻卡外右上角 · 0 位移 0 遮挡）**

391. **Given** 紧凑态任一**未选中**卡，**Then** 卡内 `.sp-card__tick` **`display = flex`（三态常驻，该位置不再留空）**、**14×14**、`border-radius:50%`、`box-sizing:border-box`、**`background-color = rgb(255,255,255)`（白底）**、**`border = 1px solid rgb(199,199,204)`（`#C7C7CC` 空心圈）**，圈内 1 个 `<svg>` 但 **`display = none`（无勾）**；**Given** CSS 定位，**Then** **`top = right = -4px`**；**Given** 该卡，**Then** `.sp-card` 的 `padding-right = 2px`、文字轴 = **卡左 + 29.5**（内容宽 54 居中）。
392. **Given** 点某卡使其选中，**Then** 该卡 `.sp-card__tick` **仍 14×14 且坐标不变**，外观切为 **`background-color = rgb(243,47,65)`（`#F32F41`）+ `border = 1px solid rgb(255,255,255)`**、圈内 **8px 白勾 svg 变为 `display = block`**（`stroke-width:3.4`）；**Given** 该属性切换，**Then** `background-color / border-color` 各带 **`.18s ease`** 过渡且**不含任何位移 / 缩放**；**Given** `.sp-card` 内距，**Then** `padding-right` 仍为 **2px**（**不再有 16px 让位**）。
393. **Given** 同一张卡在「未选中 → 选中」的两次量测，**Then** 控件 `tickOffX / tickOffY` **完全一致**（实测均 **48.00 / -3.00**）、文字轴**完全一致**（实测均 **卡左 + 29.5**）、`padding-right` **完全一致**（均 2px）；**Given** 选中卡，**When** 取姓名 / 职位 / 控件三个元素的矩形，**Then** **控件圆（半径 7）与姓名 / 职位的内容盒（`Range` 实测）均不相交**；**Given** 同一张卡的姓名与职位，**Then** 两者**共轴**（中心 X 之差 ≤ 1px）；**Given** 卡宽，**Then** 仍为 **59**（5 张 = 319px 的落满结论不变）。
394. **Given** 左侧导航「16」进入 > 5 人选择页，**Then** `.sp-pick__tick` 仍为 **16×16 / `top:5` / `right:5`**（**未被 R23 改动**）；**Given** 选择页内任意卡，**Then** **不出现** `.sp-slot__x`。

**R23 · > 5 人槽位卡的删除 ×（热区仍是整卡）**

395. **Given** > 5 人槽位行任一已落位卡，**Then** 卡内**有** `.sp-slot__x`：**14×14**、`border-radius:50%`、`background-color = rgb(199,199,204)`（`#C7C7CC`）、`top/right = 1px`、**`pointer-events: none`**；`.sp-slot` 为 `position:relative`；卡根仍带 **`data-sr-rm`** 与 `aria-label="移除 {姓名}"`。
396. **Given** 该角标，**When** 取角标 / 头像 / 姓名的矩形，**Then** **均不相交**（角标 x **46.5–60.5**、y **1.5–15.5**；头像右缘 **41.25**、姓名 y **34–47**）；**Given** `document.elementFromPoint(角标中心)`，**Then** 命中 **`BUTTON.sp-slot` 本体**；**When** 点该点位，**Then** 该员工被移除、该行**补回「＋」** —— 证明**删除热区仍是整张卡、未新增独立热区**。
397. **Given** 紧凑形态任一入口，**When** 量测「小工」行与「顾客指定」行的**内容到内容**间距，**Then** 为 **12.5px**（`.sp-cards__row--extra` 的 `margin-top = 6px`，改前 6.5px）；**Given** > 5 人槽位形态，**Then** 为 **20.5px**（`.sp-row--extra` 的 `margin-top = 6px`，改前 14.5px）；**Given** 两种形态，**Then** 发丝线仍贴在「顾客指定」行顶部（随该行一起下移）。

**R23 二次修订 · 未选中态也常驻勾选控件**

398. **Given** 紧凑态任一**未选中**卡（4 个入口逐个验证），**Then** `.sp-card__tick` **`display = flex`**（该位置不再是空的）、14×14、白底 + 1px `#C7C7CC` 环、无勾（`svg display = none`）；**Given** 同一屏上的选中卡，**Then** 两者控件**同尺寸同坐标**（`tickOffX / tickOffY` 完全一致）—— 即「空心圈 ⇄ 实心勾」只切外观、**不产生任何位移**。
399. **Given** 该行已选满 3 人，**Then** 置灰卡（`is-full`）**同样保留空心圈**（`display = flex` + 1px `#C7C7CC` 环 + 白底 + 无勾），随整卡 `opacity:.4` 一起淡出，`padding-right` 仍 **2px**、文字轴仍 **卡左 + 29.5**；**Given** 三态口径，**Then** **只有选中态是实心勾**，未选中与置灰都是空心圈。
400. **Given** 勾选控件现在**三态常驻**，**When** 在未选中态量测它相对**所有 `overflow != visible` 祖先**（`.addsheet__body` / `.addsheet` / `#frame`）的余量，**Then** **全部 ≥ 0**（最窄宿主「添加 sheet」实测最小余量 **32px**）—— 常驻的圈**不会被 `.sp-face{overflow:hidden}` 裁掉**；**Given** 点在圈上（含卡外 3px 的外探部分），**Then** 命中的仍是该卡并正常切换选中态（**外探部分不吃掉热区**）。

### 10.6 R19 验收用例（353–364）

**图标：200 填充式 → 24 画布线描**

353. **Given** s15，**When** 取 `.suc-print-row` 内的 `svg`，**Then** 全行只有 1 个 svg、`viewBox="0 0 24 24"`（**不再是 `0 0 200 200`**）、`fill="none"`、`stroke="currentColor"`、`stroke-width="1.6"`、`stroke-linecap="round"`、`stroke-linejoin="round"`。
354. **Given** 该图标，**When** 数结构，**Then** **3 条 `path`**（进纸 / 机身 / 出纸）+ **1 枚 `circle`**（指示灯，`fill="currentColor"`、`stroke="none"`）；`d` 属性全部落在 0–24 坐标区间，**无 `163.158` / `182.105` 等 200 画布残留**。
355. **Given** 该图标，**When** 量测渲染尺寸，**Then** 为 **18px**（R19 前 17px）；**渲染线宽 = `stroke-width ÷ 24 × 18` ≈ 1.2px**（R19 前 ≈ 0.5px），**≥ 1.1px**。
356. **Given** 该图标，**When** 检查颜色，**Then** 常态计算色 **`rgb(142,142,147)`（`#8E8E93`）**、`flex-grow` 与 `flex-shrink` 均为 **0**（不被挤压变形）。

**行形态：去按钮感**

357. **Given** s15，**When** 量测 `.suc-print-row` 计算样式，**Then** `border-radius` = **`0px`**（R19 前 12px）、`background-color` = **`rgba(0,0,0,0)`**、`border-top` = **0.5px 实线 `#E5E5E5`**。
358. **Given** 该行，**When** 量测，**Then** 高 **44px**（R19 前 46px，与字段行 `min-height:44` 同节奏）、宽 **= 卡内字段行宽（319px）**、**是金额卡的最后一个子元素**、位于明细字段区**下方**；文案仍为「打印小票」13px `#55595E`。
359. **Given** `#s15 .suc-print-row` 与 `#s15 .suc-print-row:active` 两条规则的 CSSOM，**Then** **不再有任何 `background` 声明**（`#F7F7F8` 全局检索命中 0）；`:active` 的 `color` = **`rgb(26,26,26)`**，`:active svg` 的 `color` = **`rgb(85,89,94)`**。
360. **Given** 该行，**When** 检查标签与热区，**Then** 仍是 **`button` 元素**、带 `data-suc-print` 与 `aria-label="打印小票"`，**点击 → toast「已发送打印小票指令」并停留在 s15**。

**几何与波及面**

361. **Given** s15（1 条明细），**When** 量测，**Then** 卡高 **≈ 283.7px**（R17 的 46px 行时为 286.3px）、卡底 **≈ 434px**（R17 为 436px）、`.suc-body` 的 `top` **仍 138px**、`scrollHeight ≤ clientHeight`（**无内部滚动**）。
362. **Given** s15，**When** 检查卡外，**Then** 操作组仍 **4 等分 / 高 62px / 距卡底 14px**，底栏仍 **3 个按钮**（返回首页图标 + 客户签字 + 继续开单）；**各格图标仍 17px**（本次只改打印小票这一个图标）。
363. **Given** 全原型渲染后的 DOM，**When** 检索 `#s15 .suc-print`（不含 `-row`）、`viewBox="0 0 200 200"` 与旧类名 `.suc-print-row` 命中数，**Then** 依次为 **0 / 0 / 1**（无残留死代码、无重复入口）。
364. **Given** 本轮改动，**When** 逐条跑本轮的校验清单，**Then** **34 条全部通过**；R16 / R17 / R18 三轮的自校验清单（28 / 25 条）**均无失败**。
    > **注（R20 后清理）**：各轮的校验页（`_verify_*.html`）与探针页（`_probe_*.html`）是**开发期临时工具，不属于交付物**，已随脚手架一并清理出仓库（`_*` 已加入 `.gitignore`）。**后续验收以本 PRD 的用例为准**，不再依赖这些临时页面。
### 10.5 R18 验收用例（343–352）

**s2 直接收款返回目标**

343. **Given** s4 选择顾客页，**When** 点某行右侧「收款」，**Then** 进入 s2 直接收款（标题「直接收款」不变）。
344. **Given** 在 s2，**When** 点标题栏左侧返回键，**Then** **回到 s4 选择顾客页**（R18 前错回 s1 散客开单页），且 s4 列表 17 行操作区完整渲染。
345. **Given** s2 返回键实现，**When** 读取 `onclick`，**Then** 为 **`go('s4')`**（**不再出现 `go('s1')`**）；**Given** 从左侧导航直达 s2，**Then** 返回同样落在 s4。
346. **Given** s3 去收款页，**When** 点返回键，**Then** 仍回 s2（不受本次改动影响）。

**员工选择页：删提示、留规则**

347. **Given** 点任一行「＋」进入选择页，**When** 检查标题行，**Then** 文案 = **「选择{行名}」+「取消」**，**无「最多 3 人」**（样式类 `.sp-picker__hd .s` 元素数 = **0**，`index.html` 内该规则亦已删除）。
348. **Given** 员工选择器，**When** 同一行（如「大工」）连续落位 3 人，**Then** 该行**已落 3 人**且**「＋」槽位消失**，选择页自动回到槽位行视图（`data-picking="0"`）。
349. **Given** 某行已满 3 人，**When** 调用 `spAddToRow(第4人, 该行)`，**Then** 返回 **`false`**、行内人数仍为 3（**规则保留**）；`SP_ROW_MAX` = **3**。

**「顾客指定」行标签色**

350. **Given** 打开员工选择器（sheet 内嵌 / s2 内嵌 / s6「修改服务员工」任一处），**When** 量测 4 行行名，**Then** **四行颜色全部为 `rgb(26,26,26)`**（主文字色）。
351. **Given** 「顾客指定」行标签与槽位卡，**When** 扫描计算色，**Then** **无 `rgb(249,115,22)`（橙）与 `rgb(243,47,65)`（红）命中**（颜色标记全部清除）。
352. **Given** R18 改动后，**When** 走一遍「选择大工 / 顾客指定」落位与移除，**Then** **4 行工位齐全、顺序不变（大工 / 中工 / 小工 / 顾客指定）**，落位后右侧**仍补出新「＋」**，摘要文案仍按「工位 · 顾客指定」拼装。

### 10.4 R17 验收用例（331–342）

**s4 行内「开单｜收款」（纯文字 + 发丝竖线）**

331. **Given** s4 任一行（含散客行），**When** 检查右侧操作区 `#s4 .pick-row-actions`，**Then** **无描边、无底色、无圆角容器**（`border-width` = `0px`、`background-color` = `rgba(0,0,0,0)`）；**「开单」= `#F32F41` 13/600（透明底）**、**「收款」= `#8E8E93` 13/500**，两段之间为 **1×14px 的 `#E5E5E5` 竖线**。
332. **Given** 该控件，**When** 量测，**Then** 两段热区**高均 ≥ 44px**，控件总宽 **≤ 110px**（实测 **101px**，R16 为 103px）；**17 行全部常驻**（`.pick-row-actions` 数 = 17）。
333. **Given** 该控件与右侧 A–Z 字母索引条，**When** 比位，**Then** **胶囊右缘 360px / 索引条 370–388px → 净空 10px**（R17 前为**重叠 8px**，首字母 x 375.8–382.2 压在红块上）。
334. **Given** s4 行 `.pick-row-wrap`，**When** 检查行内距右，**Then** 为 **30px**（R17 前 12px）、行高仍 **74px**；**When** 点「开单」→ `s1/s5`（会员行进 s5），**When** 点「收款」→ `s2`，各 **1 次点击直达**；**When** 点行本身 → **无响应**。

**sheet 内「顾客指定」橙色**

335. **Given** 打开员工选择器（含 sheet 内嵌），**When** 量测 4 行工位行名，**Then** **四行全部 = `rgb(26,26,26)`（主文字色）**（**R18：R17 的橙 `#F97316` 作废**）；`.sp-row--extra` 仅 **1** 个，槽位卡与摘要文案**不变色**。

**s15「打印小票」入口与金额卡上移**

336. **Given** s15，**When** 检查金额卡，**Then** **不存在** `.suc-print`（右上角 28×28 图标已删除），**存在** `.suc-print-row` 且**是卡片的最后一个子元素**、位于明细字段区**下方**。
337. **Given** 该行，**When** 量测，**Then** **宽 = 卡内字段行宽（319px）× 高 44px**（误差 ≤ 1.2px），上方 `border-top` 为 **0.5px `#E5E5E5` 实线**（字段行之间仍为 `0.5px #E0E0E0` 虚线）。
338. **Given** 该行内容，**When** 检查，**Then** 图标 **18px / `#8E8E93`**、文案「打印小票」**13px / `#55595E`**、水平居中、`aria-label="打印小票"`、**圆角 0 / 背景透明**；按下**只加深（文字 `#1A1A1A`、图标 `#55595E`），无底色**。
339. **Given** s15，**When** 量测 `.suc-body`，**Then** 计算 `top` = **138px**（R17 前 182px），卡底实测 **≈ 434px**（R17 前 440.5px；**R19** 行高 46 → 44 后再上移 2px）。
340. **Given** s15（1 条明细），**When** 检查内容区，**Then** `scrollHeight ≤ clientHeight`（**无内部滚动**）；卡外操作组仍为 **4 等分 / 高 62px / 与卡片间距 14px**，底栏仍为 **3 个按钮**（返回首页图标 + 客户签字 + 继续开单）。
341. **Given** s15，**When** 点卡片底部「打印小票」行，**Then** toast「**已发送打印小票指令**」且**停留在 s15**（不跳转、不清空单据）。
342. **Given** 全原型渲染后，**When** 全局检索旧类名 `.suc-print`（不含 `-row`）与旧样式 `#s15 .suc-print {`，**Then** **命中 0**（无残留死代码）；**Given** `sucPrint()`，**Then** 仍保留且仅由 `.suc-print-row` 调用。

### 10.3 R16 验收用例（309–330）

**价目表 / 添加 sheet**

309. **Given** s1 散客开单页，**When** 查看价目卡价格区，**Then** **只有一处价格**（**无划线原价**、无红色权益价），`color` = `rgb(146,146,146)`。
310. **Given** s5 会员开单页（陈女士持折扣卡），**When** 查看价目卡，**Then** **同样只有单色原价**（划线价元素数 = 0，字色 `#929292`），**不出现 8 折权益价**。
311. **Given** 点项目卡片圆形加号，**When** 打开添加 sheet，**Then** 价格字段标签为 **「价格（元）」**，默认值 = **原价 × 数量**（≠ 权益价）。
312. **Given** 添加项目 sheet，**When** 查看布局，**Then** **不存在数量卡片**（`.addsheet-cell--qty` 的 `display` 为 `none`），价格格**通栏**（宽 ≈ 366px），`#asGrid` 带 `is-single`。
313. **Given** 添加产品 sheet，**When** 查看布局，**Then** **数量卡片存在且为两列**（数量 | 价格），步进数量后价格 = 单价 × 数量。

**选择服务员工（槽位式）**

314. **Given** 任意员工选择入口，**When** 打开，**Then** 显示 **4 行工位**（大工 / 中工 / 小工 / 顾客指定），**每行恰好 1 个「＋」空槽位**（共 4 个）。
315. **Given** 槽位行，**When** 点任一「＋」，**Then** 容器切到选择页（`data-picking="1"`），标题为 **「选择{行名}」**、**有「取消」**；**取消** → 回到槽位行且数据不变。**R18**：标题行**不含「最多 3 人」提示**（`.sp-picker__hd .s` 命中 0）。
316. **Given** 选择页，**When** 点任一员工卡，**Then** **该员工立即落到对应槽位行**并**播放落位动效**，且**该行右侧出现新的「＋」空槽位**。
317. **Given** 同一行已落 1 人，**When** 再次进入选择页，**Then** 已在该行的卡片带**红色描边 + 对勾**，再点**不产生变化**。
318. **Given** 某行已有 3 人，**When** 查看该行，**Then** **「＋」槽位消失**；**When** 进入选择页，**Then** 其余卡片**置灰且不可点**（`is-full` / `aria-disabled="true"`）。
319. **Given** 某行已落 1 人，**When** **点槽位行里的该员工卡**，**Then** 该员工被移除、「＋」槽位补回。
320. **Given** 员工 甲 已落「大工」行，**When** 在「顾客指定」行再次选择甲，**Then** **允许**（跨行可重复），两行各持一张卡。

**选择顾客 s4**

321. **Given** s4 顾客列表，**When** 检查任一行，**Then** **不存在「最近消费」文案**（`.sub` 元素数 = 0），**手机号在第二行**（`.line2 .phone`），第一行只有 姓名（+ VIP + 卡数 `(N)`）。
322. **Given** s4 任一行（含散客行），**When** 查看右侧，**Then** 常驻**一组纯文字操作**（`.pick-seg--main` + `.pick-seg__div` + `.pick-seg--sec`）：**「开单」红字 `#F32F41` 13/600（主）**、**「收款」灰字 `#8E8E93` 13/500（次）**、中间 **1px × 14px `#E5E5E5` 竖线**（**R17 改版：R16 的「实心红描边胶囊」已作废**），**两段各自一次点击直达**；行内距右 **30px**，与右侧字母索引条净空 10px。
323. **Given** s4 顾客行，**When** **点行本身（非胶囊）**，**Then** **无任何响应**（不展开、不跳转）；**不存在** `is-open` 折叠态与 `#EFF0FF` 选中底。
324. **Given** s4 某行，**When** 点「收款」→ `s2`；**When** 点「开单」→ `s1/s5`（会员行进 s5）。

**结算页 s6 / s7（按明细改价）**

325. **Given** s6 有 3 条明细，**When** 查看底栏，**Then** 应付为 **`div`（非 button）**、**无铅笔图标**、**无「已改价」标签**、**无底栏上方说明条**，且 `window.coPayOverride` 为 `undefined`。
326. **Given** s6 某明细「应付」行，**When** 查看，**Then** 右侧有 **14×14 铅笔（`#929292`）**且整行为热区；**When** 点击，**Then** 打开「修改应付」数字键盘 sheet，占位文案为 **「原价 ¥xx」**。
327. **Given** 该 sheet，**When** 改价并确定，**Then** 该明细实付 = 手输值、**该行权益锁定**（「改价商品不可用权益」）、底栏 = 新合计，toast「应付已改为 ¥xx」；其余明细**不受影响**。
328. **Given** 某明细已改价，**When** 点「清零」后确定，**Then** 恢复原价、权益解除锁定并重新自动匹配，toast「已恢复原价 ¥xx」。
329. **Given** s6/s7 明细「服务员工」行，**When** 点击，**Then** 打开「修改服务员工」sheet（4 行槽位、`data-ctx` 为 `co` / `qco`）；落位员工后**摘要即时更新且金额不变**。
330. **Given** s7 明细，**When** 查看「应付」行，**Then** **同样可编辑**（R16 前为只读），改价规则与 s6 一致。

---

# 模块 11　非功能（极简）
> 读者：前端 ✓ · 后端 ✓ · 测试 ✓

- 画布与动效对齐原型 390 × 844。
- 原型为静态单页，所有状态为内存模拟；刷新重置。
- 埋点：无专表；若现网统一规范另案。
- 员工/价目数据为演示资源；正式接入真实数据源。
- s8-s9 为逻辑补充页面，业务链路（流水详情 / 退款）以线上为准；原 s10/s11 已删除。
- 本期仅单店（与线上一致）。

---

# 模块 12　交付物

| 交付 | 说明 |
|------|------|
| 本文 `PRD-开单结账.md` | 开发与测试准稿 |
| 交互原型 `index.html` | 唯一可视依据 |
| `PRD-开单结账.html` | PRD 自包含 HTML 预览版 |

开发与测试只需 **PRD + 原型**。冲突时以原型界面为准修正 PRD。

> **仓库卫生（R20 后清理）**：开发期的脚手架文件 —— 截图（`_r*_*.png`）、探针页（`_probe_*.html`）、校验页（`_verify_*.html`）、一次性脚本（`_run_*.js` / `_shot*.js`）—— **不属于交付物**，已统一从仓库移除，并在 `.gitignore` 中加了 **`_*`** 规则，避免再次进仓。清理前共 **124 个文件 / 5.6 MB**（60 PNG + 53 HTML + 6 JS + 5 OUT）。
