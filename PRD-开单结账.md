# PRD：开单结账2.0（散客开单 · 会员开单 · 快捷开单 · 商户端）

| 字段 | 内容 |
|------|------|
| 文档版本 | v2.0-draft |
| 日期 | 2026-09-15 |
| 交互原型（唯一可视依据） | 同目录 `index.html` |
| 画布 | 390 × 844（演示区默认 1.25×） |
| 目的 | 供前端 / 后端 / 测试及 AI 工具落地「开单结账2.0」；**只需本文 + 交互原型** |

> **v2.0 主链路（问卷锁定 + 纠偏；本轮确认 C1/D1/E1/F1）**：首页（入口下沿贴中线）→ 选择顾客（取挂单右对齐；选中 `#EFF0FF`；行展「开单 / 快捷开单」）→ 开单页对齐 demo（无会员卡/快速消费；价目取 demo **美发子集**；分类芯片最左「全部」且进页/切 Tab **默认全部平铺、无分组标题**；点「+」**行下展**：红圆步进器（加号 SVG 居中）；价格移入展开卡底部「价格（元）」行、铅笔图标可改（数字键盘 sheet）；服务员工为**槽位**；点槽位弹出**普通底部员工 sheet**（高度随内容、最高约 55% 视口，**可盖住展开区**，展开区不位移）；点外丢弃；加购橙→红飞入）→ 结算文档流（明细下：**优惠券行** + Figma 备注卡，再到底栏应付）。**全页顾客卡**统一粉白淡色卡；会员权益指标 **默认收起**，右侧 chevron 展开；**去掉「更换顾客」**；点头像/姓名进 s4。快捷开单仍走 s2→s3→s7。

---

## 怎么读这份 PRD

正文按模块编排。**不用通读全文**，按角色跳章即可。

| # | 模块 | 前端 | 后端 | 测试 | 说明 |
|---|------|:----:|:----:|:----:|------|
| 1 | 背景 / 目标 / 范围 / 能力映射 | ✓ | ✓ | ✓ | 本期变更叙事、防范围蔓延 |
| 2 | 用户场景与成功标准 | ✓ | 了解 | ✓ | 测「业务对不对」 |
| 3 | 信息架构与页面清单 | ✓ | 了解 | ✓ | 做哪些页、哪些层 |
| 4 | 状态机 | ✓ | **必看** | **必看** | 顾客身份、性别、会员卡展开、购物车、快捷开单明细 |
| 5 | 交互细则 / 异常流 | ✓ | 了解 | ✓ | 输入规则、校验、toast |
| 6 | 功能规格 | ✓ | 了解 | ✓ | 逐屏字段与跳转 |
| 7 | 数据模型与接口约定 | ✓ | **必看** | ✓ | 沿用线上接口；新增字段【待联调确认】 |
| 8 | 业务规则 | ✓ | **必看** | ✓ | 金额口径、改价、范围边界 |
| 9 | 文案清单与权限 | ✓ | 部分 | ✓ | 文案；店员/店长/店主 |
| 10 | 验收 P0 + 造数场景 | 了解 | 了解 | **必看** | Given→When→Then，含输入边界 |
| 11 | 非功能（极简） | ✓ | ✓ | ✓ | |
| 12 | 交付物 | ✓ | — | ✓ | 仅 PRD + 原型 |

**先读这几条（贯穿全文）**

1. 本期是基于 **RTB 线上开单结账** 的 **优化改版**，**不改变其他未涉及的功能逻辑**。凡本文未单独描述的交互、文案、接口，均沿用线上现有实现。
2. 页面顶部顾客卡片变化：深色卡统一改为 **粉白淡色卡**（圆角 12）；**点头像或姓名进入顾客列表（s4）**；「更换顾客」按钮已移除。顾客列表新增「男散客 / 女散客」选项与 **「添加会员」行（行下展快捷添加会员表单）**，列表内 **VIP 标签按最新 Figma 资源适配**。
3. 从顾客列表选择 **会员** 后，开单结账页顶部卡片切换为 **会员样式**（粉白淡色卡）：头像 / 姓名 / VIP 标签 / 上次到店；**会员权益指标默认收起，右侧 chevron 可展开/收起**；**「更换顾客」「办卡」按钮均已移除，点头像或姓名进入顾客列表（s4）**。字段显示规则维持现有规则。**VIP 标签按最新 Figma 资源（渐变 ◆VIP）适配尺寸与位置**；开发时保持该位置现有 VIP 标签逻辑不变。
4. 添加项目 / 产品时的 **UI UX 是本期最大的修改**：参照博卡小店通的 sheet 弹窗样式，按现有逻辑做了适配；弹窗内展示 **数量 + 应付（元）**（应付带笔形编辑图标），并在弹窗内**内嵌「选择员工」翻转选择 UI**（点击卡片翻转成按钮进行点客/散客 → 小工/中工/大工选择）。**同项多次加购时，若应付或服务员工不同必须拆成多行**；购物车有项后点底部左侧热区打开 **已选商品 Sheet**，点「下一步」进入 **开单结算页（s6）**。**当会员持有的权益使项目/产品产生划线价（原价灰色划线 + 红色优惠价）时，弹窗「应付」默认即为该优惠价。**
5. **新增「快捷开单」功能**：无需常规流程的繁琐操作，**只需输入金额即可完成开单结算**，并 **可选填** 服务员工信息和开单内容。为线下调研后决定加入的重要功能；其结算第二步为 **快捷开单结算页（s7）**，**金额按原价计算，不做身份折扣**。**快捷开单与小程序「自助买单」的纯金额支付不是同一种功能、互不相关**。
6. **补充展示页面**：门店流水（s8，快捷开单在流水中的展示）、选择退款项目（s9，快捷开单退款）、业绩设置（s10，劳动业绩含「快捷开单」分类）、提成方案（s11，劳动提成含「快捷开单」分类）。**入口为原型左侧导航直达，不承载业务链路**；对应业务逻辑以线上为准。**其中「经营分析 → 门店经营报表」的项目 TOP5 排行不包含「快捷开单」**（经营分析属另一产品线，此处仅记录本侧规则）。
7. 该模块的 **其他功能保持现有状态**（挂单、支付、会员卡业务等均以线上为准）。**本期已移除会员信息卡上的「办卡」与「更换顾客」按钮**（入口改为点击头像/姓名进 s4）。**开单结账页（s1/s5）原有的搜索栏本期已删除**，原型与文案均不再包含；搜索能力如线上另有入口，以线上现有版本为准。
8. **开单结算 s6 / 快捷开单结算 s7 的交互效果与各类弹窗（员工面板、权益选择 sheet、备注 sheet、小计编辑、toast 等）以当前在线版本为准**，原型中的仅为评审演示，原型细节不作为开发验收约束；本 PRD 已明确写出的本期业务规则（会员划线价、快捷开单权益、点客/散客、应付默认优惠价等）仍为开发约束。
9. 输入与联动口径（原型已同步）：金额格式 **整数≤6位、小数≤2位、上限 999999.99**；快捷开单 **空金额不可「去收款」**；开单内容 **≤50 字**；应付/小计 **可手动编辑**（对焦即清空，失焦为空时还原），**改价后该明细不可使用权益**（改价判定以「应付/小计 ≠ 单价/权益优惠价 × 数量」为准）。

交互行为与文案以 **`index.html` 当前表现** 为准。

---

# 模块 1　背景 / 目标 / 范围 / 能力映射
> 读者：前端 ✓ · 后端 ✓ · 测试 ✓

## 1.1 背景

RTB 线上开单结账经过一段时间运营，线下调研反馈：

- 常规开单需先选顾客、选项目、配员工、填备注，流程较长，存在「只想快速收一笔钱」的场景；
- 顾客卡片与顾客列表在性别表达、头像素材上体验欠佳；
- 会员所持权益信息默认折叠，收银员核对权益时需要额外操作；
- 添加项目/产品时的信息组织与操作路径不符合当前门店操作习惯；
- 快捷开单缺少结算第二步、流水展示、退款与业绩/提成配置等闭环展示。

本期在不改变其他功能逻辑的前提下，对开单结账做定点优化，并为快捷开单补齐逻辑补充页面。

## 1.2 目标

| 目标 | 说明 |
|------|------|
| 快捷开单 | 仅输入金额即可开单结算，选填员工与开单内容 |
| 顾客卡片优化 | 全页统一粉白淡色卡；点头像 / 姓名进入顾客列表（s4）；移除「更换顾客」按钮 |
| 顾客列表优化 | 新增「男散客 / 女散客」散客选项与「添加会员」行（行下展快捷添加会员表单）；VIP 标签按 Figma 适配 |
| 会员权益可见 | 会员权益指标默认收起，右侧 chevron 展开/收起；移除「办卡」「更换顾客」按钮 |
| 会员权益价 | 会员持有的折扣权益在价目卡片上产生划线价（原价划线 + 红色优惠价），添加 sheet 应付默认=优惠价 |
| 添加体验升级 | 参照博卡小店通 sheet 弹窗样式，数量 + 应付（可编辑）+ 内嵌翻转选员工 |
| 开单结算页 | 购物车明细在结算页 s6 完整展示（明细卡/权益/小计/应付/挂单/结账） |
| 快捷开单结算 | 快捷开单结算第二步 s7（明细/权益/小计/应付/挂单/结账），金额按原价 |
| 快捷开单权益 | s7 默认自动匹配（会员=最优折扣卡、散客=无卡权益）；可改选会员卡储值金额、全场项目适用的计次/折扣、全场项目或产品可用的优惠券、满足金额门槛的代金券 |
| 逻辑补充页面 | 门店流水 s8、选择退款项目 s9、业绩设置 s10、提成方案 s11 |
| 快捷开单边界 | 明确快捷开单与小程序「自助买单」的纯金额支付不是同一种功能（见 8） |
| 经营分析关联 | 快捷开单不计入「经营分析 → 门店经营报表」项目 TOP5 排行（见 8） |
| 回归可控 | 其余功能沿用线上，避免范围蔓延 |

## 1.3 本期做（In）

- 顶部顾客卡片：男女头像更换；点击头像切换散客性别（男散客 / 女散客）；卡片展示性别文案；**点击「男散客 / 女散客」文字进入顾客列表（s4）**
- 顾客列表：新增「男散客」「女散客」选项与「添加会员」行（行下展快捷添加会员）；会员 VIP 标签按最新 Figma 资源适配
- 会员卡：改为 **粉白淡色卡 + 权益指标默认收起（右侧 chevron 展开/收起）**；**移除「办卡」「更换顾客」按钮**，点头像/姓名进 s4；VIP 标签按最新 Figma 资源适配
- 添加项目/产品 sheet 弹窗：标题（项目名+价格）、数量（步进 1~999）、应付（元）（带笔形编辑图标，对焦清空、失焦还原）、服务员工（产品为销售人员）、确定；**会员持有折扣权益时应付默认=优惠价**
- 选择员工：卡片翻转两步选择（点客/散客 → 小工/中工/大工），多选，保留已选
- 快捷开单：金额键盘直接开单 + 选填员工/开单内容（≤50 字）+ 去收款
- 会员权益价：价目卡片展示划线原价 + 红色优惠价（散客无权益不划线）
- 开单结算 s6：购物车明细结算页（手工单号、明细卡、权益自动匹配/选择、小计编辑、应付、挂单、结账）
- 快捷开单结算 s7：快捷开单结算第二步（手工单号、红色闪电快捷开单明细、权益、小计编辑、应付、挂单、结账；金额按原价）
- 逻辑补充页面（原型左侧导航直达）：s8 门店流水（快捷开单展示）、s9 选择退款项目、s10 业绩设置、s11 提成方案
- 下一步：购物车有项 → 进入 **s6 开单结算页**

## 1.4 本期不做（Out）

- 支付 / 收款 / 收银流水（沿用线上；原型仅以「结账」toast 示意，支付流程不实现）
- 挂单、储值/计次卡核销、会员卡业务（办卡/开卡入口不在本期范围，原型已移除会员卡「办卡」按钮，业务逻辑沿用线上）（沿用线上，原型 toast 示意）
- ~~线上自有购物车明细 sheet~~（本期已按 Figma 重做「已选商品」Sheet，见 §6.12；s6 内明细展示与删除仍保留）
- 顾客列表搜索 / 新增顾客 / 编辑顾客（沿用线上）
- s8-s11 的业务链路（流水详情、退款审批、业绩/提成保存与生效）以线上为准，原型仅展示页面与交互示意
- 多门店、权限体系改造（沿用线上现有实现，本期不改造）

## 1.5 旧 → 新能力映射

| 旧体系能力（语义） | 新体系如何表现 |
|--------------------|----------------|
| 散客卡仅展示固定头像 | 全页统一粉白淡色卡；点头像/姓名进 s4；无「更换顾客」按钮 |
| 选择顾客仅会员列表 | 列表顶部新增「男散客」「女散客」散客选项 + 「添加会员」行下展快捷添加会员 |
| 会员卡默认折叠权益 | 权益指标默认收起，右侧 chevron 展开/收起 |
| 添加项目/产品信息组织分散 | sheet 弹窗：标题 + 数量/应付 + 内嵌选员工 + 确定 |
| 无快速路径 | 快捷开单：输入金额即可开单，选填员工/开单内容 |
| 常规开单无结算明细页 | 购物车有项 → 下一步 → s6 开单结算页（明细/权益/小计/应付/挂单/结账） |
| 快捷开单直接收款 | 金额 → 去收款（s3）→ 快捷开单结算第二步（s7）→ 结账 |
| 快捷开单无闭环展示 | 门店流水 s8（流水展示）、选择退款项目 s9（退款）、业绩设置 s10 / 提成方案 s11（业绩提成配置） |
| 应付金额静态数字 | 应付/小计可编辑（笔形图标、对焦清空、失焦还原），改价后权益锁定 |
| 价目仅展示原价 | 会员持有的折扣权益 → 卡片原价灰色划线 + 红色优惠价；添加 sheet 应付默认=优惠价（默认优惠价不算改价） |
| 快捷开单 9 折（会员） | 快捷开单默认按原价，不做身份折扣；权益默认不选，可手动选择（四类权益，见 8） |
| 快捷开单与自助买单同源 | 明确区分：快捷开单是门店收银侧开单能力，与小程序「自助买单」纯金额支付无关（见 8） |

映射不足时以原型字段与行为为准。

---

# 模块 2　用户场景与成功标准
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

## 2.1 角色

| 角色 | 说明 |
|------|------|
| 店员 | 日常收银开单 |
| 店长 | 日常收银开单、门店管理 |
| 店主 | 门店最高权限 |

本期 **仅列角色**，**不定义角色间权限差异**；操作权限沿用线上现有规则。

## 2.2 核心场景

| 场景 | 用户期望 | 成功标准 |
|------|----------|----------|
| 散客开单 | 新客不建档案直接开单 | 顶部显示散客卡；可切换性别；加购项目/产品后下一步进入 s6 结算 |
| 会员开单 | 老客识别身份、查看权益 | 选择会员后卡片切换为会员样式；权益全部展开可见 |
| 快捷开单 | 只需输入金额即可收银 | 输入金额 + 选填员工/开单内容 → 去收款 → 快捷开单结算 s7 |
| 更换顾客 | 开单中换人 | 顶部卡片、身份状态同步切换 |
| 添加项目 | 加购服务并指定员工、调整应付 | sheet 弹窗数量/应付联动正确；员工可多选；应付可编辑 |
| 结算明细 | 结账前核对明细、权益 | s6/s7 明细完整展示；改价项权益锁定；小计可编辑 |
| 流水查看 | 查看快捷开单在流水中的展示 | s8 中快捷开单以「⚡快捷开单」项展示，含金额/服务人/支付方式 |
| 退款 | 对快捷开单发起退款 | s9 全选/部分选择联动；退回方式按选择范围约束 |
| 业绩/提成配置 | 配置快捷开单的业绩与提成比例 | s10/s11 中「快捷开单」分类固定置顶，比例可配置 |

---

# 模块 3　信息架构与页面清单
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

```
开单结账
 ├─ s1 散客开单
 │    ├─ 顶部散客卡（双头像·性别文案·选择顾客）
 │    ├─ 项目/产品 Tab + 快捷开单胶囊（Tab 上方）
 │    ├─ 左侧分类（选中白底）+ 项目/产品卡片（圆形加号）
 │    └─ 底部 hint-bar + bottom-bar（左侧热区打开已选商品 Sheet / 应付 / 挂单 / 下一步）
 ├─ s2 快捷开单
 │    ├─ 顶部身份卡（散客/会员 按身份切换）
 │    ├─ 开单内容 / 收款金额 / 员工
 │    └─ 数字键盘 + 去收款
 ├─ s3 快捷开单去收款
 │    ├─ 顶部身份卡（同上）
 │    ├─ 开单内容 / 收款金额（只读显示，键盘可改）/ 员工
 │    └─ 数字键盘 + 去收款（→ s7）
 ├─ s4 选择顾客
 │    ├─ 搜索栏 + 取消
 │    ├─ 散客（男散客 / 女散客）
 │    ├─ 会员列表（姓名 / VIP 标签 / 编号 / 最近消费）
 │    └─ 字母索引 A-Z#
 ├─ s5 会员开单
 │    ├─ 顶部会员卡（权益全部展开）
 │    ├─ 项目/产品 Tab + 快捷开单胶囊（Tab 上方）
 │    ├─ 左侧分类 + 项目/产品卡片（圆形加号）
 │    └─ 底部 bottom-bar（左侧热区打开已选商品 Sheet / 应付 / 挂单 / 下一步）
 ├─ 已选商品 Sheet（bottom-bar 后升起）
 │    ├─ 已选商品 · 清空（二次确认）· 关闭
 │    └─ 行：名称 / 员工摘要 / 单件价 / ±步进
 ├─ s6 开单结算（购物车明细结算）
 │    ├─ 顶部身份卡（散客/会员）
 │    ├─ 手工单号 + 明细卡列表（员工/权益/小计/应付）
 │    └─ 底部 co-bar（应付 + 挂单/结账）
 ├─ s7 快捷开单结算（快捷开单第二步）
 │    ├─ 顶部身份卡（散客/会员）
 │    ├─ 手工单号 + 快捷开单明细卡（员工/权益/小计/应付）
 │    └─ 底部 co-bar（应付 + 挂单/结账）
 ├─ s8 门店流水（逻辑补充）
 │    ├─ 尾款单 / 筛选
 │    ├─ Tab：全部/项目/产品/会员卡/作废
 │    └─ 流水卡（顾客/商品/服务人/支付方式/实付/开单时间）
 ├─ s9 选择退款项目（逻辑补充）
 │    ├─ 提示条（退回方式约束）
 │    ├─ 退款项目列表（勾选）
 │    └─ 底部 rf-bar（全选 + 确认退款）
 ├─ s10 业绩设置（逻辑补充）
 │    ├─ 高级设置 / Tab（劳动业绩/销售业绩）
 │    ├─ 左侧分类（快捷开单置顶 + 洗吹）
 │    └─ 业绩表（点客/散客 × 大工/中工/小工）
 ├─ s11 提成方案（逻辑补充）
 │    ├─ 方案名称卡 + 更多
 │    ├─ Tab（劳动提成/销售提成）
 │    ├─ 左侧分类（快捷开单置顶 + 洗吹）
 │    └─ 提成表（点客/散客）
 └─ 弹层
      ├─ addsheet 添加项目/产品（数量 + 应付 + 员工 + 确定）
      ├─ staff-sheet 选择员工（网格 + 两步翻转 + 完成）
      └─ remark-sheet 备注（开单内容 50 字 + 完成）
```

> 原「选择权益」底部 sheet 已被 **s12 选择权益页**取代（见 6.10）；「选择优惠券」为 **s13 选择优惠券页**（见 6.21）。

### 3.0 页面流（示意）

```mermaid
flowchart LR
  S1[散客开单] -->|快捷开单| S2[快捷开单]
  S1 -->|选择顾客| S4[选择顾客]
  S2 -->|去收款| S3[快捷开单去收款]
  S3 -->|去收款| S7[快捷开单结算]
  S4 -->|选男/女散客| S1
  S4 -->|选会员| S5[会员开单]
  S5 -->|选择顾客/更换顾客| S4
  S1 -->|加购| AS[[添加项目/产品]]
  AS -->|内嵌| SS[[选择员工]]
  S2 -->|内嵌| SS
  S1 -->|下一步(有项)| S6[开单结算]
  S5 -->|下一步(有项)| S6
  S6 -->|点权益行右侧| S12[选择权益]
  S7 -->|点权益行右侧| S12
  S6 -->|点优惠券行右侧| S13[选择优惠券]
  S7 -->|点优惠券行右侧| S13
  S12 -->|确认| S6
  S13 -->|确认| S6
  S12 -->|确认| S7
  S13 -->|确认| S7
  S6 -->|结账| TST[toast 已进入线上结账流程]
  S7 -->|结账| TST
  S8[[门店流水]] -.逻辑补充.-> S1
  S9[[选择退款项目]] -.逻辑补充.-> S1
  S10[[业绩设置]] -.逻辑补充.-> S1
  S11[[提成方案]] -.逻辑补充.-> S1
```

| 页面/层 | 导航标题（原型） | 说明 |
|---------|------------------|------|
| 散客开单 s1 | 开单结账 | 散客卡 + 项目/产品选品 + 底部工具栏 |
| 快捷开单 s2 | 开单结账 | 金额键盘 + 选填员工/开单内容 + 去收款 |
| 快捷开单去收款 s3 | 开单结账 | 金额显示（键盘可改）+ 员工/开单内容 + 去收款 → s7 |
| 选择顾客 s4 | — | 搜索栏 + 散客/会员列表 + 字母索引 |
| 会员开单 s5 | 开单结账 | 会员卡（权益展开）+ 项目/产品选品 + 底部工具栏 |
| 开单结算 s6 | 开单结账 | 购物车明细结算（手工单号/明细卡/权益/应付/优惠券/备注/挂单/结账） |
| 快捷开单结算 s7 | 开单结账 | 快捷开单第二步（手工单号/快捷开单明细/权益/应付/优惠券/备注/挂单/结账） |
| 门店流水 s8 | 门店流水 | 快捷开单流水展示（全部/项目/产品/会员卡/作废 Tab） |
| 选择退款项目 s9 | 选择退款项目 | 退款项目勾选 + 全选联动 + 确认退款 |
| 业绩设置 s10 | 业绩设置 | 劳动业绩含置顶「快捷开单」分类；销售业绩暂无设计 |
| 提成方案 s11 | 提成方案 | 劳动提成含置顶「快捷开单」分类；销售提成暂无设计 |
| 添加项目/产品 | — | sheet 弹窗（标题/数量/应付/员工/确定） |
| 选择权益 s12 | 选择权益 | 会员卡权益列表（7 项演示数据），单选 + 底部「确认」后生效 |
| 选择优惠券 s13 | 选择优惠券 | 「不使用优惠券」+ 券卡列表（使用规则默认折叠），单选 + 底部「确认」后生效 |
| 选择员工 | 选择员工 | 员工网格 + 两步翻转选择 + 完成 |
| 备注 | 备注 | 文本框（50 字）+ 完成 |

---

# 模块 4　状态机
> 读者：前端 ✓ · 后端 **必看** · 测试 **必看**

## 4.1 顾客身份 `currentCust`

| 状态 | 含义 | 顶部卡片 |
|------|------|----------|
| `guest` | 散客 | 深色散客卡（双头像 · 性别文案 · 选择顾客） |
| `member` | 会员 | 会员卡（头像/姓名/VIP/上次到店/更换顾客 + 权益展开） |

```
guest ──选会员──► member
member ──选男/女散客──► guest
```

- 默认 `guest`。
- s2 / s3 / s6 / s7 顶部卡片按该状态切换；后续元素随卡片高度下移。
- 从 s4 选会员：回到来源页；s2/s3 返回原页，s1 进入则跳 s5，s6/s7 进入则更新身份并返回原页。
- 从 s4 选男/女散客：回到来源页（从 s5 进入则回 s1，s6/s7 进入则更新身份并返回原页）。
- **散客卡「男散客 / 女散客」文字**：点击进入顾客列表（s4）。（头像点击仍仅切换性别）

## 4.2 散客性别 `data-g`

| 状态 | 卡片文案 | 头像表现 |
|------|----------|----------|
| `male`（默认） | 男散客 | 男头像彩色，女头像灰阶 |
| `female` | 女散客 | 女头像彩色，男头像灰阶 |

- 点击顶部卡片头像可切换性别；切换仅影响展示，不改变身份（仍为散客）。
- **点击「男散客 / 女散客」文字** → 进入顾客列表（s4），可与选择顾客按钮等效；头像点击不受影响。
- s4 散客列表选择「男散客 / 女散客」同样更新。

## 4.3 会员卡权益展开

- **默认收起**：会员权益指标（会员卡 / 储值余额 / 折扣）默认不展示，卡片右侧 chevron 点击展开，再次点击收起；状态不跨页面记忆（重进为收起）。
- 字段显示规则维持现有规则不变（如：计次卡「余N次」、储值卡「余额xx元」、折扣卡「x.x折」）。

## 4.4 购物车明细 `cartItems`（s6 / 已选商品 Sheet 共用数据源）

- 添加 sheet 点「确定」写入明细（散客页面加购计入 s1，会员页面加购计入 s5）。
- 明细字段：名称 / 单价 / 数量 / 应付 / 类型（项目/产品）/ 分类 / 缩略图 / 员工 / 权益 / 改价标记。
- **合并 / 拆分（同项多次加购）**：
  - 比对键 = **同一名称（同项目/产品）** + **单件应付一致**（行应付 ÷ 数量）+ **服务员工/销售人员完全一致**（含点客/散客与工位；员工集合排序后比对）。
  - **应付或员工任一不同 → 必须拆成多行**，不得合并数量。
  - 两者皆相同 → 合并：数量相加，行应付 = 单件应付 × 新数量。
  - **未选员工**视为同一空键，可与另一次未选合并。
- 加购后 s1/s5 应付金额重算、对应左侧分类角标按「确定」次数 +1；删除一行明细时角标 -1。
- 底部 bar 角标 = 各行数量合计。
- s6 结账成功 / Sheet「清空」确认后 → 清空明细与角标，回到列表态。
- **已选商品 Sheet**：点底部 bar 左侧热区打开（见 §6.11），与 s6 共用 `cartItems`。

## 4.5 快捷开单明细 `qcoItem`（s7 数据源）

- s3 点「去收款」时按当前金额生成快捷开单明细（**金额 = 原价，不做身份折扣**），并进入 s7。
- 明细字段：名称「快捷开单」/ 金额 / 应付 / 员工 / 开单内容 / 权益 / 改价标记。
- 员工 / 开单内容与 s2/s3 共享同一状态；在 s7 修改后同步回 s2/s3。
- **权益：默认不选（「无卡权益」）**；点击权益行进入选择 sheet，可选用权益类型见 5.5；选择权益后应付按权益规则重算（见 5.5），取消权益恢复原价。
- s7 删除明细 → 返回 s2 并清空；s7 结账成功 → 清空全部并回到 s1。

## 4.6 添加弹窗字段联动

| 字段 | 输入 | 联动 |
|------|------|------|
| 数量 | 步进 1~999 | 应付 = 单价 × 数量 |
| 应付（元） | 手动可改（带笔形图标） | 判定改价（与单价/权益优惠价 × 数量不等）；改价明细在 s6 不可用权益 |
| 权益优惠价 | 会员持有折扣权益时生效 | 卡片原价划线 + 红色优惠价；弹窗「应付」默认 = 优惠价 × 数量（默认优惠价**不算改价**，权益仍可用） |

## 4.7 退款勾选（s9）

- 单项勾选 / 取消；全选按钮与列表勾选状态 **双向联动**（全部勾选时「全选」点亮，取消任一项则熄灭）。

---

# 模块 5　交互细则 / 异常流
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

## 5.1 金额输入规则（快捷开单金额 + 应付/小计统一）

| 规则 | 说明 |
|------|------|
| 整数位 | ≤ 6 位 |
| 小数位 | ≤ 2 位 |
| 上限 | 999999.99 |
| 下限 | 0（不可为负） |
| 小数点 | 最多 1 个；**禁止以「.」开头** |
| 前导 0 | 自动剔除（输入 `0` 后再输数字视为替换，如 `05` → `5`） |
| 空值 | 不可提交；快捷开单空金额点「去收款」toast「请先输入金额」 |

## 5.2 应付 / 小计编辑

- 金额右侧有 **笔形图标**，表示该金额可编辑。
- **对焦即清空原值**，随后输入的内容即为当前金额；**失焦时若为空则还原原值**。
- 金额输入过程规范化：超上限封顶 999999.99，小数第 3 位起截断，负数归零。
- 修改后的应付/小计 **与「单价 / 权益优惠价」× 数量不等** 即视为 **改价**；**改价明细在 s6/s7 中权益锁定**（行文案「改价商品不可用权益」，点击 toast 同文案）。会员权益优惠价作为默认值不判定为改价。

## 5.3 员工 / 开单内容（选填）

- 员工：非必填；快捷开单与添加弹窗各自维护选择状态，互不影响；s2/s3/s7 共享同一状态。
- 开单内容：非必填；**≤ 50 字**，实时计数 `n/50`；空 / 全空格视为未填，未填时 s6/s7 明细显示「未输入」。

## 5.4 校验失败 / 交互反馈（toast）

| 条件 | 提示 |
|------|------|
| 快捷开单空金额点「去收款」 | 请先输入金额 |
| 下一步（购物车为空） | 请先选择项目 |
| 下一步（购物车有项） | 直接进入 s6（无 toast） |
| s3 去收款 | 直接进入 s7（无 toast） |
| 挂单 | 已挂单 |
| 底部「+」按钮 | 请选择项目 |
| 底部「+」按钮（s5 触发） | 选择项目或快捷开单 |
| s6/s7 结账（无明细） | 暂无结算项目 |
| s6/s7 结账成功 | 结账成功 |
| s7 删除快捷开单明细 | 已删除 |
| 改价明细点权益 | 改价商品不可用权益 |
| s8「尾款单」 | 尾款单 |
| s8「筛选」 | 筛选 |
| s9「确认退款」 | 确认退款 |
| s10「高级设置」 | 高级设置 |
| s10「销售业绩」Tab | 销售业绩暂无设计 |
| s11「更多」 | 更多 |
| s11「销售提成」Tab | 销售提成暂无设计 |

## 5.5 弹层交互

- 遮罩点击关闭；员工选择关闭（点遮罩/完成）**保留已选状态**，同一项目重进不重置；**跨项目（另一个项目/行）打开时改为该项目自己的数据行渲染，不继承其它项目的已选与中途翻牌态**。
- 添加弹窗遮罩关闭不写入购物车；「确定」后写入并 toast「已加入 · 应付 ¥x.xx」。
- 权益选择：单选；**在 s12 页内选中后必须点底部「确认」才生效**（点返回或遮罩不生效）；选「不使用权益」则恢复「无卡权益」。
- **权益/优惠券入口（s6 / s7）**：只有「权益」行与「优惠券」行的**右侧「值 + ›」区域**是热区（左侧标签不可点）；点「权益」行右侧 → **s12 选择权益页**；点「优惠券」行右侧 → **s13 选择优惠券页**。
- **权益自动匹配（s6）**：明细在 **未改价且未手动清除权益** 时，进入 s6 自动匹配——会员匹配其持有的**全场最优折扣权益**（演示数据为超值折扣卡 8.0 折，与价目划线价同源）、散客为「**无卡权益**」；用户手动取消后不再覆盖。**顾客身份切换时，自动匹配的权益按新身份重算；手动选择的权益保留。**
- **订单级优惠券（s6 / s7）**：整单一张券，影响**底部应付**（应付 = 各项权益后应付之和 − 券抵扣；不满门槛则不抵扣）。
  - 散客订单**自动带入首张可用券**（演示为「店长洗剪吹新人优惠券」满减 10 元、无门槛）；会员**不自动带券**，需手动选择。
  - 用户手动选过「不使用优惠券」后，不再自动带入；清空购物车时券一并清空。
- **快捷开单权益（s7）**：**默认自动匹配**——会员自动匹配其持有的**全场最优折扣权益**（演示为超值折扣卡 8.0 折，与 s6 规则一致）、散客为「**无卡权益**」；点权益行**右侧「值 + ›」**可改选以下四类权益：
  1. **会员卡内的储值金额**（如九折储值卡：按卡折扣 9 折计价）；
  2. **全场项目可适用的计次或折扣**（如水光三次卡计次核销不抵扣金额；超值折扣卡按 8 折计价）；
  3. **全部项目或全部产品可用的优惠券**（如新人8折券：满 58 减 10）；
  4. **快捷开单金额符合使用门槛的代金券**（如满200减30券：金额 ≥200 才可选，减 30）。
  - 金额不满足门槛的代金券在列表中**置灰不可选**；选择权益后应付 = 原金额按规则折算，取消权益恢复原价；权益选择不算改价（可继续改小计）。

---

# 模块 6　功能规格
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

下列字段、按钮、跳转均须与原型一致；视觉以原型为准（品牌色 `#F32F41` 等）。

## 6.1 顶部顾客卡片

**散客卡（粉白淡色卡）【本期变更】**

- 头像 + 姓名（「男散客 / 女散客 / 散客」）；**深色卡已全部换成粉白淡色卡**（圆角 12、极浅投影）。
- **点头像或点姓名均进入顾客列表（s4）**；性别由 s4 的「男散客 / 女散客」选项决定。
- **「更换顾客」按钮已移除**（入口改为点卡片）。
- s1 / s2 / s3 / s6 / s7 共用同一散客卡（默认男散客）。

**会员卡（会员身份）【本期变更】**

- 头像 / 姓名 / VIP 标签 / 上次到店（粉白淡色卡，样式同上）。
- **点头像或点姓名进入 s4**；**「更换顾客」「办卡」按钮均已移除**。
- **会员权益指标默认收起**，右侧 chevron 点击展开 / 收起（展开显示 会员卡 / 储值余额 / 折扣 三项）。
- 权益行：标签（计次卡/储值卡/折扣卡）+ 名称 + 值（余N次 / 余额xx元 / x.x折）。
- **VIP 标签按 Figma 资源（渐变 ◆VIP）实现，尺寸与位置对齐设计稿**；开发保持现有 VIP 标签逻辑不变。

**散客卡「添加会员」入口（卡内快捷注册）【本期新增】**

- 仅当散客性别为 **男散客 / 女散客** 时，卡片右侧区域出现「添加会员」图标（**与 s4 顾客列表同款**：`#FF8956` 细描边圆环 + 圆角＋，32×32，右内距 16px）；默认「散客」不显示该图标。
- 入口在 **s1 / s2 / s3 / s6 / s7 所有共用散客卡的页面** 一致出现。
- 点图标 → **卡片就地向下展开快捷注册表单**（复用 s4 字段样式）：姓名\* / 手机号（选填）/ 性别\*（男 / 女）+ 右下「取消」「确认添加」；再次点图标或点「取消」→ 收起并清空，不保存。
- **性别默认跟随散客性别**：卡片为「男散客」展开时已预选「男」，「女散客」预选「女」；性别为必填单选，点击即选中（提供默认值后不再支持点空，避免误点导致校验失败）。输入框（含未聚焦态）为**白色填充**。
- 校验同 s4：姓名为空 → toast「请输入姓名」；手机号非 11 位合规号码 → 红框 + 「手机号格式不正确」+ toast；未选性别 → toast「请选择性别」；只填姓（1 个字）+ 已选性别 → 自动识别为「\*先生 / \*小姐」。
- 确认添加成功 → **表单收起，顶部卡片就地变为会员卡**：头像按性别、姓名 = 刚注册的会员、显示 VIP 标签、**「上次到店」= 当天日期**；同时顾客身份切为会员（散客卡隐藏、会员卡显示，s2/s3/s6/s7 同步）。
- 散客卡姓名样式与会员卡统一：字色 `#751616`、正常流排布并与头像中轴对齐（s1 / s2 / s3 / s6 / s7 一致）。
- 新注册会员**无任何会员卡 / 权益**：展开权益指标显示 `0 会员卡 / 0 储值余额 / — 折扣`；**不参与会员折扣**（价目不产生划线价），结算页「权益」行显示「无卡权益」（不自动套用折扣权益）。
- 注册会员**不改变当前页面**（停留原页继续开单）；**已加入购物车的明细保持原样**，不追溯改写。
- 从 s4 重新选择其他顾客后，顶部卡按所选身份与顾客档案重新渲染（选会员恢复其卡项指标与折扣权益）。

## 6.2 项目 / 产品 Tab 与左侧分类

- Tab：「项目 / 产品」；**Tab 卡片上方两角圆角 12**，**下沿无描边**；选中项红色文字 + 红色指示条。
- 分类芯片（横向滚动）：**最左固定「全部」，且进入页面 / 切换 Tab 时默认选中「全部」**。
- 选中「全部」时右侧为**平铺列表、不显示分组标题**；选中具体分类时仅展示该分类项目。
- 分类右侧红色数字角标：显示该分类已加购数量（初始隐藏，加购后累加，上限显示 99+）。

## 6.3 项目 / 产品卡片

- 卡片：缩略图 + 名称（15/500 `#1a1a1a`）+ 价格（`¥` 12/500 + 数值 15/700，均为 `#929292`，间距 2px）+ 圆形加号（28×28 红底白＋）。
- **展开态**：行内只保留 名称 + 红圆步进器（减/加 24px 圆形 + 数量 16/500）；**价格移入展开卡底部「价格（元）」行**，行标签 **「价格（元）」14/500**`#1a1a1a`（行高 22，与右侧价格药丸同高、行盒高度不变）；价格本身为可点药丸（`¥` + 数值 + 橙色铅笔，底 `rgba(224,224,224,.07)`、圆角 6px、内距 2/8），点击唤出数字键盘 sheet；右侧「加入购物车」84×37 胶囊（渐变 `#F32F41 → #FF6C3B`，白色实心购物车图标）。
- 展开卡底色 `#F2F3FF`；员工 Chip 92×40（**无头像**：姓名 12/700 + 角色 10/`#929292`，× 16×16 `#F2F2F2`）；未满 3 人时追加「＋添加」Chip 66×36（`#FAFAFA` 底、1px `#D0D0D0` **实线**框）。
- **展开卡分段**：展开后三段内容 —— **① 商品信息行（名称 + 步进器）② 服务员工行 ③ 价格（元）行**；**在「服务员工行」与「价格（元）行」之间有一条 0.5px 发丝线 `#E7E7E7`，左右各内缩 16px，且位于员工行下沿*再下移 2px*处**（商品信息行与下方之间不画线）。
- **步进器归零即收起**：展开态下减号可减到 0，数量变为 0 时该行自动收起（展开区清空、右侧回到「＋」、折叠态价格恢复显示），未提交的价格/员工草稿一并丢弃。
- **会员身份**（且持有全场适用的折扣权益，如超值折扣卡 8.0 折）时：价格区显示 **原价（灰色划线）+ 红色优惠价**；散客无权益不划线。
- 点加号 → 打开添加 sheet（`addCart` / `addCartM`）。
- 数据为演示数据；开发接入线上价目表实时数据。

## 6.4 搜索栏（本期已删除）

- 开单结账页（s1/s5）原有的搜索栏 **本期已删除**，原型不再展示；**s4 选择顾客页新增的真实搜索见 6.8**。

## 6.5 快捷开单胶囊

- s1 / s5 中「⚡ 快捷开单」胶囊，位于项目/产品 **Tab 上方**；点击 → 设置身份为散客并进入 s2。

## 6.6 快捷开单 s2（新功能）【本期变更】

- 顶部身份卡：散客（默认）或会员（从 s4 选会员进入时）。
- 字段顺序（自上而下）：**开单内容 → 收款金额 → 员工**。
- **开单内容**：选填；≤50 字；placeholder「请输入开单内容（50字以内）」。
- **收款金额**：数字键盘输入；placeholder「请输入金额」；格式见 5.1；空金额「去收款」拦截。
- **员工**：选填；点击 → 内嵌展开选择员工面板；回填格式「姓名·点客·大工」（多人顿号连接）；未选显示空（不显示「选填」）。
- 数字键盘：1-9 / 0 / . / ⌫；「去收款」→ s3。

## 6.7 快捷开单去收款 s3【本期变更】

- 结构同 s2（字段顺序同为 **开单内容 → 收款金额 → 员工**）；金额初始取 s2 输入值，键盘可改；员工/开单内容与 s2 共享同一状态。
- 「去收款」→ 进入 **s7 快捷开单结算**。

## 6.8 选择顾客 s4【本期变更】

- **标题栏（新增）**：s4 顶部新增标题栏（高 44，位于状态栏之下），左侧返回键 → 返回首页 s0，标题「选择顾客」；**该页不含日期控件**。因搜索栏「取消」不再返回首页（见下），返回首页统一走标题栏左侧返回键。
- **搜索栏（新增真实搜索）**：胶囊输入框（placeholder「输入顾客姓名/手机号/编号/备注」）+ 右侧「取消」。**输入即实时筛选**，匹配字段为 **姓名 + 手机号**（模糊匹配，忽略大小写与空格）。
  - **搜索态列表**：结果**平铺展示**，隐藏字母分组头、右侧 A-Z 索引条、「添加会员」行与散客组；新增会员（列表内注册）同样可被搜到。
  - **无结果**：显示空状态（圆形搜索图标 +「未找到相关顾客」+「换个姓名或手机号试试」）。
  - **「取消」**：清空输入并退出输入态（**不返回首页**，返回首页见上方标题栏）；无输入时列表恢复分组态。
  - 在搜索态下确认注册新会员时，先自动退出搜索态，以保证新行可见并定位展开。
- **散客组**：新增「男散客」「女散客」两个选项；选择后返回来源页（从 s5 进入则回 s1；s6/s7 进入则更新身份并返回），并更新散客卡性别与身份。
- **添加会员入口（行下展快捷添加）**：散客组之后、「C」分组之前新增「添加会员」行（行高 74、内距 14/28/14/12、间距 10；44×44 细描边圆环 + 圆角＋图标，两处 `#FF8956`；「添加会员」16/700 `#929292`）。
  - 点击该行 → **该行向下展开快捷添加会员表单**（与顾客行展开互斥，二者只开一个），字段：**姓名\* / 手机号（选填）/ 性别\***（男、女两个 chip 单选）。
  - **校验**：姓名空 → toast「请输入姓名」；手机号填写时须符合 `^1[3-9]\d{9}$`，不合规 → 输入框红框 + 下方红字「手机号格式不正确」+ toast；性别未选 → toast「请选择性别」。
  - **自动识别命名**：姓名只填 1 个字（只填了姓）且已选性别时，自动生成「**\\*先生 / \\*小姐**」（如「王」+ 女 → 王小姐）；姓名 ≥2 字则用姓名原文。
  - **底部按钮**：右下方「取消」（浅灰次要）+「确认添加」（主色 `#F32F41`）。取消 → 收起且**清空输入、不保存**；确认添加 → 生成新会员并**在列表中定位到该行且保持展开**（露出「开单 / 直接收款」）。
  - **新会员落位**：按**姓氏拼音首字母**归入对应字母分组（组头不存在则在字母序正确位置新建组头，如「王」→ W 组）；行内容为 姓名 / 手机号（填了才显示）/「最近消费：未消费」；头像按性别取男性或女性默认图；点其「开单」进入 s5（顶部顾客卡显示该会员名与对应性别头像）。
  - 顾客列表可纵向滚动，保证展开表单与新增行始终可滚到可视区。
- **行展开操作区**：选中行整行（行 + 操作区）底色 `#EFF0FF`；操作区内距 12/16/12/70、右对齐、间距 10，「开单」84×34 圆角 8 `#F32F41` +「直接收款」84×34 圆角 8 `#FF8956`，文字 14/500 白。
- **会员组**：按姓氏分组（A-Z# 索引）；行含姓名 / **VIP 标签（微调为渐变 ◆VIP）** / 手机号 / 最近消费；点选会员 → 会员身份 + 返回来源页（s2/s3 返回原页，s1 进入则跳 s5，s6/s7 进入则更新身份并返回）。

## 6.9 添加项目 / 产品 sheet【本期变更】

- **标题区**：项目名 + 价格（红色，两行左对齐）；会员持折扣权益时价格显示 **划线原价 + 红色优惠价**。
- **数量**：步进器（默认 1，1~999）；步进后应付 = 单价 × 数量。
- **应付（元）**：数字输入（**会员持折扣权益时默认 = 优惠价 × 数量，否则 = 单价 × 数量**）；右侧 **笔形编辑图标**；对焦清空、失焦为空还原（见 5.2）；金额格式见 5.1。
- **员工行**：标签为「服务员工」（项目）/「销售人员」（产品）；点击 → 内嵌展开选择员工面板；未选显示「请选择」。
- **确定**：写入购物车（应付总额累加、分类角标 +1），toast「已加入 · 应付 ¥x.xx」；关闭弹层。

## 6.10 选择权益页 s12（新页面，取代原「选择权益」底部 sheet）【本期变更】

> 设计来源：Figma `开单结账-ZM` 节点 `151-7268`；原型为全屏页 `#s12`（390×844），字体全局 Noto Sans SC。

- **入口**：s6 / s7 明细卡「权益」行的**右侧「值 + ›」热区**（左侧标签不可点）。
- **顶部**：状态栏 + 返回 + 标题「选择权益」。返回**不生效**（放弃本次改动，保留原权益）。
- **列表**：**7 行单选**，每行 = 名称 + 右侧单选圈（未选 `ic_radio_off` / 选中 `ic_radio_on`）。
  - 演示数据（与 Figma 一致）：`不使用权益` / `5.0折（5折卡）` / `计次卡抵扣` / `充值金额所有项目5.0折(铂金卡)` / `折扣卡` / `充值金额所有项目8.0折(金卡)` / `固定金额98元（美发卡3000元）`。
  - 进入时**回显该明细当前权益**（自动匹配的为「折扣卡」，散客为「不使用权益」）。
- **底部**：提示文案「确认并设为本单默认支付方式」+ 通栏「确认」按钮；**点确认才生效并返回来源页**。
- **规则**：
  - 折扣类按比例折算应付（如 5 折卡 → 应付 × 0.5；8 折卡 → 应付 × 0.8）；
  - 计次卡仅核销次数、**不抵扣金额**；
  - 固定金额卡按卡面金额抵扣（不超过应付）；
  - 选「不使用权益」→ 该明细权益清空，显示「无卡权益」并**不再被自动匹配覆盖**。
- **锁定**：改价明细不可进入本页（行内显示「改价商品不可用权益」）。
- **入口补充**：左侧导航含「12 选择权益」可直达（评审用）。

## 6.21 选择优惠券页 s13（新页面）【本期变更】

> 设计来源：Figma `开单结账-ZM` 节点 `151-7329`（订单级「优惠券」行）；原型为全屏页 `#s13`（390×844），字体全局 Noto Sans SC。

- **入口**：s6 / s7 订单级「优惠券」行的**右侧「值 + ›」热区**（该行位于明细之下、备注卡之上）。
- **顶部**：状态栏 + 返回 + 标题「选择优惠券」。返回**不生效**。
- **首行固定项**「不使用优惠券」+ 单选圈：**显式清券入口**（选它并确认 → 券行回到「请选择」）。
- **券卡列表**（单选，右侧单选圈；未选/选中同 s12）：
  - 角标（如「满减券」）+ 券名 + 适用范围 + 分隔线 + 「使用规则」折叠行 + 券额（¥xx）+ 门槛文案。
  - **「使用规则」默认折叠**，点击该行展开/收起（展开显示有效期、适用门店、不与其他优惠同享等条目）；展开态不改变选中态。
  - 演示数据：`店长洗剪吹新人优惠券，洗剪吹套餐立减10元`（满减券 · 项目通用 · 无门槛 · ¥10）/ `全场通用满200减30元优惠券`（满减券 · 全场通用 · 满200可用 · ¥30）/ `烫染项目立减50元代金券`（代金券 · 项目通用 · 满300可用 · ¥50）。
  - **不满门槛的券置灰不可选**（演示订单 ¥60.40 时后两张置灰）；点击置灰卡 toast「当前订单不满该券使用门槛」，不改变选中态。
- **底部**：通栏「确认」按钮；点确认 → 写入订单级券 → 返回来源页并重算应付。
- **金额影响**：应付 = Σ 明细权益后应付 − 券抵扣；券抵扣 = min(券面额, 应付合计)，不满门槛为 0。
- **入口补充**：左侧导航含「13 选择优惠券」可直达（评审用）。

## 6.11 选择员工【本期变更】

- 员工网格（演示 14 人，含头像/姓名/职位），可内嵌于添加 sheet / 快捷开单页 / s6 / s7，也可独立面板。
- **两步翻转选择**：点卡片 → 卡片翻转显示「点客 / 散客」按钮 → 选择后翻转显示「小工 / 中工 / 大工」按钮 → 完成该员工选择。
- **完成后收起**：在开单页（s1/s5）展开区的员工 sheet 中，**成功新增一名员工后 sheet 自动收起**（展开卡员工 Chip 同步更新），需用户再次点「添加」才重新弹出；点「×」移除员工、或修改已选员工配置时不收起。达到 3 人上限仅 toast。
- **多选**；已选员工卡片带勾选角标；**保留已选状态**（同一项目重进不重置）。
- **每个项目（每行）相互独立**：开单页展开区的员工 sheet 始终以**该项目自己的草稿数据**渲染 —— 在某项目已选员工后，打开另一个项目的员工 sheet 应显示「未设置、可重新设置」的干净状态；且每次打开 sheet 都会清掉上一次残留的**卡片翻牌编辑态**（不会出现某张卡停在中途状态）。
- 已选员工在添加 sheet / 快捷开单页 / s6 / s7 回填。
- 员工详情行：角色 · 姓名 + 「点客 / 散客」切换按钮。

## 6.12 底部工具栏

- hint-bar：「具体金额以结算为准」（s1）。
- bottom-bar：购物车图标（含数量角标）/ 「应付」+ 金额 / 挂单 / 下一步（s1、s5）。
- **左侧热区**：购物车图标 + 角标 + 「应付 ¥金额」整块可点；挂单 / 下一步不触发。
- **空车点热区**：不打开 Sheet，toast「请先选择项目」。
- **已选商品 Sheet**（本期重做，对齐 Figma）：
  - 从 bottom-bar **后一层**向上弹出（Sheet `z-index` 低于 bar；bar 始终浮在最前；遮罩在 Sheet 后）。
  - 标题「已选商品」；「清空」+ 垃圾桶（**二次确认**后清空全部明细、角标、合计并关闭）；右上关闭。
  - 行：名称；**下一行员工摘要**（如「张三·点客·大工」；未选为「未选员工」）；单件应付价；± 数量步进（减到 0 删行，上限 999）；合计与角标随步进重算。
  - **点行回跳编辑**：点击明细任一行的**行体**（非步进器）→ 关闭 Sheet，跳转到开单页（会员笔 → s5、散客笔 → s1）对应 Tab（项目/产品）并把左侧分类重置为「全部」，**自动展开该行并回填已保存的数据**（单价、数量、服务员工/点客散客/工位），可继续编辑；此时再点「加入购物车」为 **更新原明细（替换，不新增）**，toast「已更新该笔」。
  - 明细行的 ± 步进器**不触发跳转**，仅在 Sheet 内改数量。
  - 与 s6 共用 `cartItems`；拆分行各自独立展示。
- 下一步：有项 → 进入 **s6 开单结算**；无项 → toast「请先选择项目」。

## 6.13 开单结算 s6【本期变更】

> **交互效果与各类弹窗（员工面板、权益选择 sheet、备注 sheet、小计编辑、toast 等）以当前在线版本为准，原型仅为评审演示**；本节约定的业务规则（权益、改价、点客/散客等）为开发约束。

- **顶部**：身份卡（散客/会员，随进入时的身份），含「选择顾客 / 更换顾客」→ s4。
- **手工单号**：右侧默认展示灰色文案「**自动生成·点击可修改**」；点击右侧进入手动输入自定义单号（同行变为输入框，placeholder「请输入手工单号」）；未填写则结账时由系统自动生成（日期+序号，如 20260821-001）。
- **明细卡**（一条已加购项目/产品一张）：
  - 首行：缩略图（无图时以「项」/「产」占位）+ 类型小标签（项目「项」/产品「产」）+ 名称 + 单价（¥；**会员持折扣权益的明细单价为划线原价，应付为优惠价**）+ **垃圾桶**（删除该明细，同步扣减对应分类角标与应付）。
  - **选择员工**行：展示已选员工（默认自动填充首位员工）；点击内嵌展开选择员工面板。
  - **员工详情**：角色 · 姓名 + 「点客 / 散客」切换。
  - **权益**行：默认自动匹配权益（未改价时，见 5.5）；**仅右侧「值 + ›」为热区**，点击 → **s12 选择权益页**；改价项锁定「改价商品不可用权益」。
  - **开单内容**行：显示已输入内容或「未输入」；点击 → 备注 sheet。
  - **小计**：已移除（v2 结算文档流仅保留「应付」）。
  - **应付**：只读，= 权益后应付。
  - **行间分隔**：商品信息头下沿为 **0.5px 发丝浅灰实线 `#E7E7E7`**，左右各内缩 16px；「服务员工」「权益」「应付」**行之间为虚线**（1px、4px 段 + 4px 间隔的同色虚线），同样左右各内缩 16px（s6/s7 统一）。
- **订单级「优惠券」行 + 「备注」卡**（明细卡之下，v2 注入）：优惠券行 = 「优惠券」+ 右侧值（默认「请选择 ›」/ 已选显示券名 + ›，**仅右侧热区**）→ **s13 选择优惠券页**；备注卡 = 标题「备注」+ 多行输入（500 字上限，右下角计数）。
- **底部 co-bar**：应付 ¥{合计}（= Σ 明细权益后应付 − 优惠券抵扣）+ 挂单（toast「已挂单」）+ **结账**（无明细 toast「暂无结算项目」；有明细 → **原地 toast「已进入线上结账流程」，不清空购物车、不跳转**）。

## 6.14 快捷开单结算 s7（新功能）【本期变更】

> **交互效果与各类弹窗（员工面板、权益选择 sheet、备注 sheet、小计编辑、toast 等）以当前在线版本为准，原型仅为评审演示**；本节约定的业务规则（快捷开单权益四类、改价、点客/散客等）为开发约束。

- **顶部**：身份卡（散客/会员），含「选择顾客 / 更换顾客」→ s4；在 s7 换人后金额仍按 **原价** 计算，不做身份折扣。
- **手工单号**：右侧默认展示灰色文案「**自动生成·点击可修改**」；点击可手动输入；未填写则结账时系统生成；placeholder「请输入手工单号」。
- **快捷开单明细卡**：
  - 首行：闪电缩略图 + 红色闪电小标签 + 名称「快捷开单」+ 金额（¥）+ **垃圾桶**（删除 → 回 s2，toast「已删除」）。
  - **选择员工**行 / 员工详情（同 6.13）。
  - **权益**行：**默认自动匹配**（会员=最优折扣卡、散客=「无卡权益」，与 s6 一致）；**仅右侧「值 + ›」为热区**，点击 → **s12 选择权益页**（可选权益类型见 5.5，门槛不满足置灰）；改价后锁定；用户选「不使用权益」后不再被自动匹配覆盖。
  - **开单内容**行：显示内容或「未输入」；点击 → 备注 sheet。
  - **小计**：已移除（v2 结算文档流仅保留「应付」）。
  - **应付**：只读，= 权益后应付。
- **订单级「优惠券」行 + 「备注」卡**：与 s6 同一套组件与状态（见 6.13）；优惠券行右侧 → **s13 选择优惠券页**。
- **底部 co-bar**：应付 ¥{合计}（= 权益后应付 − 优惠券抵扣）+ 挂单（toast「快捷开单不可挂单」）+ **结账**（**原地 toast「已进入线上结账流程」，不清空 s2/s3/s7 状态、不跳转**）。

## 6.15 门店流水 s8（逻辑补充）【本期变更】

- 入口：原型左侧导航直达（不承载业务链路）。
- **顶部**：返回 + 标题「门店流水」+ 「尾款单」（¥ 角标）与「筛选」（图标）按钮（点击 toast 示意）。
- **Tab**：全部(1) / 项目(1) / 产品(1) / 会员卡 / 作废；带计数角标；点击切换选中态。
- **流水卡**：顾客头像 + 姓名 + 状态（已完成）｜商品行（闪电缩略图 + 红色闪电标签 + 「快捷开单」+ 金额）｜服务人 | 姓名｜支付方式（微信图标 + 微信）｜扣款（-¥金额）｜实付总额：¥金额｜开单时间。

## 6.16 选择退款项目 s9（逻辑补充）【本期变更】

- 入口：原型左侧导航直达。
- **提示条**：全选项目进行退款可以选择原路退回与指定退回，选择部分项目或已有退款项目则只可指定退回。
- **退款项目列表**：闪电缩略图 + 红色闪电标签 + 「快捷开单」+ 原价 / 实付 + 勾选圆点（点击勾选/取消）。
- **底部 rf-bar**：全选（与列表勾选双向联动）+ 确认退款（toast 示意）。

## 6.17 业绩设置 s10（逻辑补充）【本期变更】

- 入口：原型左侧导航直达。
- **顶部**：返回 + 标题「业绩设置」+「高级设置」按钮（toast 示意）。
- **Tab**：劳动业绩（默认）/ 销售业绩（toast「销售业绩暂无设计」）。
- **提示条**：注意此处修改业绩，而不是提成！
- **左侧分类**：「快捷开单」（置顶选中）+「洗吹」（toast 示意）；**「快捷开单」为固定分类**，对应快捷开单项目的业绩配置。
- **业绩表**：点客 / 散客 × 大工 / 中工 / 小工，默认 100.00%。
- **未配置时的默认值**：若用户未单独为「快捷开单」配置业绩设置，则**沿用当前线上系统的默认业绩规则**（本侧不另定数值；原型以 100.00% 示意）。

## 6.18 提成方案 s11（逻辑补充）【本期变更】

- 入口：原型左侧导航直达。
- **顶部**：返回 + 标题「提成方案」。
- **方案名称卡**：方案名称 + 说明「更改多人分配规则请前往业绩设置中的高级设置」+「更多」按钮（toast 示意）。
- **Tab**：劳动提成（默认）/ 销售提成（toast「销售提成暂无设计」）。
- **左侧分类**：「快捷开单」（置顶选中）+「洗吹」（toast 示意）；**「快捷开单」为固定分类**。
- **提成表**：点客 / 散客，默认 100.00%。
- **未配置时的默认值**：若用户未单独为「快捷开单」配置提成方案，则**沿用当前线上系统的默认提成规则**（本侧不另定数值；原型以 100.00% 示意）。

## 6.19 标题栏日期控件（新增）

- **位置**：标题栏右侧「日期文本 + 日历图标」为一组**可点控件**；覆盖页面：**s1 / s2 / s3 / s5 / s6 / s7**。
- **默认值**：以**今天**为基准，格式 `YYYY.MM.DD`（6 个页面初始一致）。
- **展开**：点击该控件 → 从标题栏正下方（靠右对齐控件）下拉日期浮层，并带轻蒙层；**点蒙层或再次点控件关闭**。
- **浮层内容**：月份标题（`YYYY年M月`）+ 上/下月切换 + 星期表头（日→六）+ 日期网格（固定 6 行，高度稳定）+ 快捷「今天」「昨天」。
- **可选范围**：**今天往前推 3 个月**至今天（如今天 2026-09-17 → 可选 2026-06-17 ~ 2026-09-17）。**未来日期与范围外日期置灰不可选**；已无可用日期的月份，其上/下月按钮置灰。
- **选择后**：该日期写为「开单日期」，**6 个页面日期全局同步更新**，浮层关闭。
- **快捷**：「今天」→ 回到今天；「昨天」→ 今天减 1 天（两者均在可选范围内）。

## 6.20 开单页滚动视差与吸顶（新增）

- **适用范围**：**s1 / s5**（开单页）。
- **结构**：价目区整体为一张**价目表白卡**（项目/产品 Tab + 分类 chips + 商品列表），层级高于顶部顾客卡片。
- **视差交互**：向下滚动价目表时，**顾客卡片以约 0.5× 速度上移**（视差），价目表白卡以 1× 速度上移并压在其上，逐渐覆盖顾客卡片。
- **终点**：滚动到价目卡偏移量（顾客卡上边距 12 + 卡高 84 + 卡与价目卡间距 12 = **108**）时，价目表**上移贴住标题栏**且顾客卡片被**完全遮挡**；继续滚动位移不再增加。
- **反向滚动**：位移线性回退，回到顶部时顾客卡片**完全恢复原位置**（无残留位移）。
- **吸顶**：贴住标题栏后，**项目/产品 Tab + 分类 chips 一起吸顶固定**，之后仅商品列表滚动。
- **层级规则**：滚动中顾客卡片降到价目白卡之下（保证「盖住」）；未滚动时恢复到价目白卡之上，保证卡片展开的快捷注册面板仍浮于价目表之上。
- **状态保持**：切换项目/产品 Tab、切换分类、列表内注册会员等导致价目区重绘后，视差位移与吸顶状态按当前滚动位置重新应用。

---

# 模块 7　数据模型与接口约定
> 读者：前端 ✓ · 后端 **必看** · 测试 ✓

**原则：凡本文未列出的接口、字段均沿用线上现有开单结账实现。**

## 7.1 沿用线上（不新增）

| 数据 | 来源 |
|------|------|
| 顾客列表（会员 + 手机号 + 最近消费） | 线上现有顾客接口 |
| 会员权益（计次/储值/折扣卡） | 线上现有会员卡接口 |
| 价目（项目/产品/分类/价格） | 线上现有价目表接口 |
| 员工 / 工位 / 点客·散客 | 线上现有员工接口 |
| 购物车 / 支付 / 收银流水 | 线上现有开单结算流程 |

## 7.2 新增 / 调整（【待联调确认】）

| 字段 | 说明 |
|------|------|
| 散客性别 `gender` | 顶部卡片 / 快捷开单提交时随单记录 |
| 快捷开单金额 | 直接作为账单金额提交（原价），无需关联项目 |
| 快捷开单员工 | 选填，可多选，含点客/散客/工位语义 |
| 快捷开单开单内容 | 选填，≤50 字 |
| 快捷开单结账单 | 快捷开单结算 s7 生成，含明细/权益/小计/应付，结账后进入收款（线上） |
| 订单流水展示 | s8 中快捷开单以「快捷开单」项展示（闪电标识/原价/支付方式/实付/开单时间） |
| 退款（s9） | 全选项目可原路退回与指定退回；部分选择或已有退款项目只可指定退回 |
| 业绩 / 提成配置（s10/s11） | 「快捷开单」作为固定分类，配置点客/散客 × 工位的业绩比例与提成比例；**未单独配置时沿用线上系统默认规则** |

## 7.3 能力清单（路径【待联调确认】）

| 能力 | 说明 |
|------|------|
| 快捷开单开单 | 金额（原价）+ 选填员工/开单内容 + 散客性别 → 快捷开单结账单 |
| 散客性别选择 | 列表选择 / 卡片头像点击均写入 |
| 会员卡权益展开 | 返回全部权益（不再折叠） |
| 权益自动匹配 / 选择 | s6 明细未改价时自动匹配（会员=最优折扣权益）或手动选择；s7 快捷开单默认不选、按四类权益可选；改价项锁定 |
| 应付 / 小计编辑 | 对焦清空、失焦还原、改价标记；改价判定以单价/权益优惠价 × 数量为基准 |
| 员工多选回填 | 「姓名·点客·大工」格式 |
| 会员权益价 | 会员持折扣权益 → 价目卡片划线原价+优惠价，添加 sheet 应付默认=优惠价 |
| 开单结算明细 | 购物车明细在 s6 完整展示、删除、重算 |
| 快捷开单结账 | s7 结账后清空并回到开单页（收款以线上为准） |
| 流水展示 / 退款 / 业绩提成 | s8-s11 页面展示与交互示意，业务链路以线上为准 |

## 7.4 时序（示意）

```
收银员选择散客性别/会员
  → 顶部卡片刷新（身份 + 权益）
收银员输入快捷开单金额（选填员工/开单内容）
  → 点「去收款」→ 校验金额非空（否则 toast「请先输入金额」）
  → s3 快捷开单去收款（键盘可改）
  → 点「去收款」→ s7 快捷开单结算（金额按原价；改价后权益锁定）
  → 点「结账」→ 清空 → 回 s1 → toast「结账成功」（收款以线上为准）
收银员添加项目/产品
  → 添加 sheet（数量 ↔ 应付联动；内嵌选员工）
  → 确定 → 应付累加 + 分类角标 +1
  → 下一步 → s6 开单结算（明细/权益/小计编辑/挂单/结账）
  → 结账 → 清空购物车 → 回 s1 → toast「结账成功」
s8-s11（门店流水 / 退款 / 业绩 / 提成）：原型左侧导航直达展示
```

---

# 模块 8　业务规则
> 读者：前端 ✓ · 后端 **必看** · 测试 ✓

1. **身份切换**：默认散客；选会员 → 会员；选男/女散客 → 散客。s2/s3/s6/s7 顶部卡片随身份切换，后续元素随卡片高度下移。
2. **散客性别**：默认男散客；在 s4 选择「男散客 / 女散客」确定；仅影响展示与随单记录。
3. **会员卡权益**：权益指标默认收起、右侧 chevron 展开/收起；移除「办卡」「更换顾客」按钮（入口为点头像/姓名进 s4）。
4. **VIP 标签**：按最新 Figma 资源（渐变 ◆VIP）适配尺寸与位置；开发保持该位置现有 VIP 标签逻辑不变。
5. **金额格式**：整数≤6 位、小数≤2 位、上限 999999.99、禁「.」开头、前导 0 剔除、不可为负。
6. **快捷开单**：空金额不可「去收款」（toast「请先输入金额」）；员工/开单内容选填；**金额按原价计算，不做身份折扣**；**与小程序「自助买单」的纯金额支付不是同一种功能，互不相关**。
7. **应付/小计编辑**：带笔形编辑图标；对焦即清空、失焦为空还原；改价后 s6/s7 该明细**权益锁定**（toast「改价商品不可用权益」）；**改价判定以「单价 / 权益优惠价」× 数量为基准**。
8. **权益自动匹配（s6）**：明细未改价且未手动清除时，会员自动匹配其持有的**全场最优折扣权益**（演示为超值折扣卡 8 折，与价目划线价同源）、散客自动匹配首张优惠券；手动取消后不再自动覆盖。
9. **数量**：步进 1~999；**展开态下减到 0 即收起该行**（展开区清空、恢复折叠态与价格、未提交草稿丢弃）。
10. **员工选择**：两步翻转（点客/散客 → 小工/中工/大工）；多选；**同一项目**重进保留已选；**跨项目（另一项目/行）打开时以该项目自身数据渲染、不继承其它项目的已选与中途翻牌态**；回填「姓名·点客·大工」。
10.1 **展开卡分段**：商品信息行 / 服务员工行 / 价格（元）行三段，仅「服务员工行 → 价格（元）行」之间一条 0.5px 发丝线 `#E7E7E7`、左右内缩 16px、位于员工行下沿**下移 2px** 处（商品信息行下方无线）；「价格（元）」行标签 14/500（行高 22，与右侧价格药丸同高）。
10.2 **快捷添加会员（s4 添加会员行下展）**：姓名\* + 手机号（选填，`^1[3-9]\d{9}$` 校验）+ 性别\*（男/女）；只填姓 + 选性别 → 自动「\\*先生 / \\*小姐」；取消不保存，确认后按姓氏拼音归组新建会员行并定位展开。
10.3 **员工选择跨项目隔离**：开单页展开区的员工 sheet 始终以该项目自身的草稿数据渲染；切换项目打开时为「未设置、可重新设置」的干净状态，且每次打开都会清空遗留的卡片翻牌编辑态（同一项目重进仍保留已选）。
11. **快捷开单权益（s7）**：**默认自动匹配**（会员=其持有的全场最优折扣权益，演示为 8 折；散客=「无卡权益」，与 s6 一致）；可改选类型=会员卡储值金额、全场项目适用的计次或折扣、全部项目或全部产品可用的优惠券、满足金额门槛的代金券；**门槛不满足的代金券置灰不可选**；选权益后应付按规则折算（储值/折扣按比例、满减券减额、计次不抵扣金额），选「不使用权益」恢复原价且不再被自动覆盖；权益选择不算改价。
12. **会员权益价（划线价）**：会员持有的折扣权益使价目项目/产品产生划线价（原价灰色划线 + 红色优惠价）；添加 sheet「应付」默认 = 优惠价 × 数量（**默认优惠价不算改价**）；散客无权益不划线。
13. **散客卡性别文字**：「男散客 / 女散客」文字点击进入顾客列表（s4）；头像点击仍仅切换性别。
14. **经营分析关联**：快捷开单不计入「经营分析 → 门店经营报表」的项目 TOP5 排行（经营分析属另一产品线，本侧记录该规则）。
15. **开单内容**：≤50 字；空/全空格视为未填。
16. **购物车合并 / 拆分**：同项目/产品多次加购时，仅当**单件应付一致**且**服务员工/销售人员完全一致**（未选视为同一空键）才合并数量；**应付或员工不同必须拆成多行**。加购后应付重算、分类角标按确定次数 +1；删除一行扣角标；结账/清空成功后清空。
17. **下一步**：购物车有项 → 进入 s6 开单结算；无项 → toast「请先选择项目」。
18. **已选商品 Sheet**：点底部左侧热区打开；空车 toast「请先选择项目」；Sheet 在 bar 后升起；行内展示员工摘要；± 改数量（0 删行，且不触发跳转）；清空需二次确认；**点行体 → 回到开单页对应行自动展开并回填保存的数据（可再编辑），重新提交为「更新该笔（替换）」**。
19. **s6/s7 结账**：无明细 toast「暂无结算项目」；有明细 → **原地 toast「已进入线上结账流程」，不清空购物车、不清角标、不跳转**（模拟进入线上结账流程）。
19.1 **权益/优惠券入口热区**：s6/s7 的「权益」行与「优惠券」行**只有右侧「值 + ›」可点**（左侧标签点击无响应）；点权益右侧 → s12，点优惠券右侧 → s13。
19.2 **s12 选择权益**：进入时回显该明细当前权益；单选；**点底部「确认」才生效**（返回不生效）；选「不使用权益」→ 权益清空为「无卡权益」且不再被自动匹配覆盖；折扣卡按比例折算、计次卡不抵扣、固定金额卡按面额抵扣；改价明细不可进入。
19.3 **s13 选择优惠券**：首行「不使用优惠券」为显式清券入口；券卡单选；**「使用规则」默认折叠**、点击展开/收起且不影响选中态；**不满门槛的券置灰不可选**（点击 toast「当前订单不满该券使用门槛」）；点底部「确认」生效并返回。
19.4 **订单级优惠券与应付**：应付 = Σ 明细权益后应付 − 券抵扣（券抵扣 = min(券面额, 应付合计)，不满门槛为 0）；散客订单自动带入首张可用券，会员不自动带券；用户选过「不使用优惠券」后不再自动带入；清空购物车时优惠券一并清空；顾客身份切换时自动匹配的权益按新身份重算、手动选择的保留。
20. **s7 删除**：删除快捷开单明细 → 回 s2，toast「已删除」。
21. **退款（s9）**：全选项目可原路退回与指定退回；部分选择或已有退款项目只可指定退回。
22. **快捷开单业绩/提成默认值**：若用户未单独为「快捷开单」进行业绩设置或提成方案配置，则**沿用当前线上系统的默认规则**赋予默认值（本侧不另定数值）。
23. **逻辑补充页面（s8-s11）**：入口为原型左侧导航；页面交互与 toast 仅为示意，业务链路以线上为准。
24. **s6/s7 交互与弹窗**：开单结算 s6 / 快捷开单结算 s7 的**交互效果与各类弹窗（员工面板、权益选择 sheet、备注 sheet、小计编辑、toast 等）以当前在线版本为准**，原型仅为评审演示；本 PRD 已明确写出的业务规则仍为开发约束。
25. **搜索栏**：开单结账页（s1/s5）原有搜索栏**本期已删除**，原型不再展示；搜索能力如线上另有入口，以线上现有版本为准。
26. **范围边界**：其余功能（挂单、支付、会员卡业务等）均沿用线上现有逻辑；**会员信息卡「办卡」按钮已移除**（业务入口以线上为准）。
27. **角色权限**：店员 / 店长 / 店主，仅列角色，权限沿用线上。

---

# 模块 9　文案清单与权限
> 读者：前端 ✓ · 后端 部分 · 测试 ✓

## 9.1 文案清单（须与原型一致）

**导航/按钮**：开单结账、项目、产品、快捷开单、直接收款、选择顾客、挂单、下一步、去收款、完成、确定、取消、男散客、女散客、散客、添加会员、姓名、手机号、性别、男、女、选择员工、选择权益、选择优惠券、不使用权益、不使用优惠券、使用规则、确认、开单内容、服务员工、销售人员、请选择、数量、应付（元）、小计、应付、结账、手工单号、已选商品、清空、门店流水、尾款单、筛选、全部、会员卡、作废、全选、确认退款、原价、实付、业绩设置、高级设置、劳动业绩、销售业绩、注意此处修改业绩，而不是提成！、方案名称、劳动提成、销售提成、更多、更改多人分配规则请前往业绩设置中的高级设置、服务人、微信、实付总额、开单时间、点客、散客、大工、中工、小工。

**s12 / s13 页面文案**：选择权益、确认并设为本单默认支付方式、选择优惠券；权益项=不使用权益、5.0折（5折卡）、计次卡抵扣、充值金额所有项目5.0折(铂金卡)、折扣卡、充值金额所有项目8.0折(金卡)、固定金额98元（美发卡3000元）。

**占位符**：请输入金额、请输入开单内容（50字以内）、请输入手工单号、输入顾客姓名/手机号/编号/备注。

**Toast**：请先输入金额、请先选择项目、已挂单、快捷开单不可挂单、请选择项目、选择项目或快捷开单、暂无结算项目、结账成功、**已进入线上结账流程**、**当前订单不满该券使用门槛**、已删除、已清空、改价商品不可用权益、已加入 · 应付 ¥{金额}、尾款单、筛选、确认退款、高级设置、销售业绩暂无设计、销售提成暂无设计、更多。

**Toast（本轮新增/调整）**：请输入姓名、手机号格式不正确、请选择性别、已添加会员 · {姓名}、已加入购物车、已更新该笔（购物车明细回跳后重新提交）、该价目已变更，无法定位。

**Dialog**：确定清空已选商品？ / 取消 / 清空。

**权益行**：计次卡 / 储值卡 / 折扣卡 + 名称 + 值（余N次 / 余额xx.xx元 / x.x折）；未匹配时「无卡权益」。

**提示条**：具体金额以结算为准。

**分类**：项目=洗吹/洗剪吹/剪发/造型/干洗/接发/烫染发/护理/其他；产品=洗护/染烫/其他；业绩/提成=快捷开单（置顶）/洗吹。

**演示数据**（开发接入真实数据）：项目=高级设计师洗吹+造型 ¥58.9、资深设计师|精剪+洗吹造型(不满意免单）¥98、首席设计师洗剪吹一口价 ¥138、资深设计师|人气洗剪造型 ¥216.09；产品=卡诗黑钻钥源洗发水 ¥268、欧莱雅臻萃染发膏 ¥198、资生堂fino发膜 ¥158、施华蔻羊绒脂护发素 ¥88。

**员工演示数据（14 人）**：顾清扬(店主)、林屿森(美容师)、何苏叶(店长)、阿Ken(美容师)、Lisa(美甲师)、张明(美发师)、李华(美发师)、王芳(美甲师)、陈强(美发师)、赵敏(前台)、周杰(美发师)、吴婷(美睫师)、小陈(学徒)、阿宁(学徒)。

**权益演示数据**：会员卡=九折储值卡（余额2049.00元）、水光三次卡（余3次）、超值折扣卡（8.0折）；优惠券=新人8折券（满58减10）、满200减30券（满200减30）。

**s12 选择权益 演示数据（7 行，与 Figma 一致）**：不使用权益 / 5.0折（5折卡）〔折扣 0.5〕/ 计次卡抵扣〔计次，不抵扣金额〕/ 充值金额所有项目5.0折(铂金卡)〔折扣 0.5〕/ 折扣卡〔折扣 0.8，会员自动匹配项〕/ 充值金额所有项目8.0折(金卡)〔折扣 0.8〕/ 固定金额98元（美发卡3000元）〔固定金额 98〕。

**s13 选择优惠券 演示数据（3 张 + 不使用行）**：店长洗剪吹新人优惠券，洗剪吹套餐立减10元（满减券·项目通用·无门槛·¥10）/ 全场通用满200减30元优惠券（满减券·全场通用·满200可用·¥30）/ 烫染项目立减50元代金券（代金券·项目通用·满300可用·¥50）；「使用规则」内容示例：有效期：15天 / 适用门店：右尚造型(周浦店) / 测试用账号：18297402934。

**快捷开单权益规则（s7）**：九折储值卡→按 9 折计价；水光三次卡→计次核销不抵扣金额；超值折扣卡→按 8 折计价；新人8折券→满 58 减 10（全场项目/产品可用）；满200减30券→满 200 减 30（金额不足 200 置灰不可选）。

**会员划线价（s1/s5）**：会员持折扣权益时价目卡片显示原价（灰色划线）+ 红色优惠价（演示为超值折扣卡 8 折）；添加 sheet 应付默认=优惠价；散客不划线。

**新注册会员（散客卡内 / s4 快捷注册）演示数据**：无任何卡/权益 → 会员卡权益指标 `0 会员卡 / 0 储值余额 / — 折扣`、价目**不划线**、s6 结算「权益」行 `无卡权益`；「上次到店」= 注册当天日期；会员卡项与折扣在选中既有会员（如陈女士 3 张卡 / 2049 / 8.0 折）时恢复。

**流水演示数据（s8）**：顾客 陈女士、状态 已完成、快捷开单 ¥491.00、服务人 刘敏、支付 微信、实付总额 ¥491.00、开单时间 2026.08.20 15:55。

## 9.2 权限

| 角色 | 开单结账 |
|------|----------|
| 店员 | 按线上现有权限 |
| 店长 | 按线上现有权限 |
| 店主 | 按线上现有权限 |

本期不新增权限差异；原型按默认角色路径演示。

---

# 模块 10　验收 P0 + 造数场景
> 读者：测试 **必看**

## 10.1 造数

| 场景 | 说明 |
|------|------|
| 散客 | 男散客 / 女散客 两条散客身份 |
| 会员 | 至少 1 名持多张卡的会员（计次+储值+折扣） |
| 员工 | 覆盖点客/散客与三种工位的员工 |
| 项目/产品 | 至少覆盖 2 个分类 |
| 快捷开单 | 空金额 / 正常金额 两条路径 |
| 权益 | 会员卡 + 优惠券两类权益 |
| 流水 / 退款 / 业绩提成 | s8-s11 演示数据（快捷开单项） |

正式环境自备等价数据；演示数据不进产品。

## 10.2 Given → When → Then（P0）

**顾客卡片 / 顾客列表**
1. **Given** 散客开单页，**When** 点击女头像，**Then** 女头像彩色、男头像灰阶、文案变「女散客」；再点男头像恢复「男散客」。
2. **Given** 散客卡默认态，**When** 首次进入，**Then** 男散客选中（男彩女灰），文案「男散客」。
3. **Given** 散客开单页，**When** 点击「选择顾客」，**Then** 进入顾客列表，顶部有「男散客」「女散客」选项。
4. **Given** 顾客列表，**When** 选「女散客」，**Then** 返回来源页，散客卡更新为女散客。
5. **Given** 散客开单页，**When** 点击顶部头像或「散客 / 男散客 / 女散客」姓名，**Then** 进入顾客列表（s4）。
6. **Given** 顾客列表，**When** 选会员（如陈女士），**Then** 顶部切换为会员卡；从 s1 进入时跳 s5。
7. **Given** 会员身份，**When** 点顶部卡头像/姓名进顾客列表并选男散客，**Then** 切回散客卡（从 s5 进入则回 s1）。
8. **Given** 会员列表，**When** 查看 VIP 标签，**Then** 按最新 Figma 资源的渐变 ◆VIP 样式与尺寸位置正确；文案/逻辑以线上现有 VIP 标签为准。
9. **Given** 会员卡，**When** 查看顶部操作区，**Then** 无「更换顾客」也无「办卡」按钮（入口为点击头像/姓名）。

**会员卡权益展开**
10. **Given** 会员卡，**When** 查看权益区，**Then** 权益指标 **默认收起**，右侧 chevron 可见。
11. **Given** 会员卡，**When** 点 chevron，**Then** 展开显示全部持有权益（会员卡 / 储值余额 / 折扣）；再次点击收起。
12. **Given** 会员卡，**When** 查看字段，**Then** 显示规则维持现有（余N次 / 余额xx元 / x.x折）。

**添加项目/产品 sheet**
13. **Given** 项目卡片，**When** 点圆形加号，**Then** 打开添加 sheet，标题为项目名 + 红色价格，默认数量 1、应付 = 单价。
14. **Given** 添加 sheet，**When** 步进数量，**Then** 应付 = 单价 × 数量。
15. **Given** 添加 sheet，**When** 对焦应付输入框，**Then** 原值清空；输入即当前金额；失焦为空时还原原值。
16. **Given** 添加 sheet，**When** 手动改应付（≠ 单价/优惠价×数量），**Then** 判定改价；确定后 s6 该明细权益锁定。
17. **Given** 添加 sheet（项目），**When** 查看员工行，**Then** 标签「服务员工」，未选显示「请选择」。
18. **Given** 添加 sheet（产品），**When** 查看员工行，**Then** 标签「销售人员」。
19. **Given** 会员身份（陈女士持折扣卡），**When** 查看 s5 价目卡片，**Then** 价格区显示原价（灰色划线）+ 红色优惠价（8 折）；散客身份 s1 不划线。
20. **Given** 会员身份，**When** 打开添加 sheet，**Then** 「应付」默认 = 优惠价 × 数量（如 ¥58.9 → ¥47.12），标题价格同步显示优惠价。
21. **Given** 会员身份添加 sheet，**When** 以默认优惠价点「确定」，**Then** 不算改价，s6 该明细权益仍可用。
22. **Given** 添加 sheet，**When** 点确定，**Then** 应付总额累加应付金额、分类角标 +1、toast「已加入 · 应付 ¥x.xx」、弹层关闭。
23. **Given** 添加 sheet，**When** 点遮罩关闭，**Then** 不写入购物车。

**选择员工**
24. **Given** 选择员工，**When** 点员工卡片，**Then** 卡片翻转显示「点客 / 散客」。
25. **Given** 已选「点客」，**When** 进入下一步，**Then** 翻转显示「小工 / 中工 / 大工」。
26. **Given** 员工完成选择，**When** 查看，**Then** 卡片带勾选角标；添加 sheet / 快捷开单 / s6 / s7 回填「姓名·点客·大工」。
27. **Given** 员工面板，**When** 多选多位员工后完成，**Then** 全部回填，顿号连接。
28. **Given** 员工面板关闭后再次打开，**When** 查看，**Then** 已选员工保留勾选状态不重置。
29. **Given** 员工面板，**When** 未选员工直接完成，**Then** 允许关闭，回填区显示「请选择」（快捷开单页为空）。
30. **Given** s6/s7 员工详情行，**When** 查看「散客」按钮，**Then** 点击可切换为「点客」，再点恢复「散客」。

**快捷开单**
31. **Given** 快捷开单页，**When** 不输入金额点「去收款」，**Then** toast「请先输入金额」，不跳转。
32. **Given** 快捷开单页，**When** 输入金额后点「去收款」，**Then** 进入去收款页 s3 并显示该金额。
33. **Given** 快捷开单页，**When** 点「员工」行，**Then** 内嵌展开选择员工面板，选填。
34. **Given** 快捷开单页，**When** 输入开单内容，**Then** 可输入 ≤50 字，超长被截断。
35. **Given** 去收款页，**When** 修改金额，**Then** 键盘可改，员工/开单内容与快捷开单页共享同步。
36. **Given** 去收款页 s3，**When** 点「去收款」，**Then** 进入 s7 快捷开单结算，显示快捷开单明细。
37. **Given** s7，**When** 会员身份下查看金额，**Then** 金额本身按输入原价（不做 9 折/身份折扣）；权益行默认自动匹配最优折扣卡（演示 8 折）并按权益重算应付。
38. **Given** 快捷开单，**When** 从 s4 选会员后进入，**Then** 顶部显示会员卡；选散客后显示散客卡。
39. **Given** s7 快捷开单结算，**When** 散客身份下查看权益行，**Then** 默认「无卡权益」，应付 = 输入金额；**Given** 会员身份，**Then** 默认自动匹配最优折扣卡（演示 8 折，¥299 → ¥239.20）。
40. **Given** s7 会员默认已匹配折扣卡，**When** 点权益行右侧进 s12 改选「不使用权益」并确认，**Then** 应付恢复原价，且后续重渲/切身份**不再被自动匹配覆盖**。
41. **Given** s7 输入金额 150，**When** 打开 s12，**Then** 九折储值卡/超值折扣卡/新人8折券可用，「满200减30券」置灰不可选（门槛未满足）。
42. **Given** s7 金额 150，**When** 选择「超值折扣卡」，**Then** 应付 = 120（8 折）；再选「新人8折券」→ 应付 = 140（减 10）；选「不使用权益」→ 恢复 150。**Given** 已选权益且改小计 ≠ 权益价，**Then** 判定改价、权益清除并锁定；改回权益价不改价。

**开单结算 s6 / 快捷开单结算 s7**
> 本区块仅校验本期已明确的**业务规则**；交互效果与各类弹窗样式以当前在线版本为准（原型仅为评审演示）。
43. **Given** s1/s5 购物车有项，**When** 点「下一步」，**Then** 进入 s6，单号行右侧显示灰色「自动生成·点击可修改」；点击可手动输入，未填写则结账时系统生成；明细卡展示（类型标签/名称/单价/垃圾桶）。
44. **Given** 会员身份加购后进入 s6，**When** 查看明细，**Then** 单价 = 划线原价、应付 = 优惠价、权益自动匹配「超值折扣卡」。
45. **Given** s6 明细未改价，**When** 查看权益行，**Then** 自动匹配权益（会员=其持有的全场最优折扣权益，散客=首张优惠券）。
46. **Given** s6/s7 明细，**When** 编辑小计使金额 ≠ 单价/权益优惠价 × 数量，**Then** 判定改价，权益行变为「改价商品不可用权益」，点击 toast 同文案。
47. **Given** s6/s7 明细未改价，**When** 点权益行**右侧「值 + ›」**，**Then** 进入 **s12 选择权益页**，单选；**点「确认」才生效**，点返回不生效（详见下方「选择权益页 s12 / 选择优惠券页 s13」）。
48. **Given** s6，**When** 点垃圾桶删除明细，**Then** 该明细移除，应付重算，对应分类角标 -1。
49. **Given** s7，**When** 点垃圾桶，**Then** 回 s2，toast「已删除」。
50. **Given** s6/s7，**When** 点「挂单」，**Then** toast「已挂单」。
51. **Given** s6/s7 有明细，**When** 点「结账」，**Then** 清空购物车/角标、回 s1、toast「结账成功」。
52. **Given** s6/s7 无明细，**When** 点「结账」，**Then** toast「暂无结算项目」。
53. **Given** 快捷开单与小程序「自助买单」，**When** 对照功能定义，**Then** 二者为不同功能：快捷开单可关联员工/开单内容/权益并计入门店流水，自助买单为纯金额支付。

**门店流水 s8**
54. **Given** s8，**When** 查看 Tab，**Then** 全部/项目/产品/会员卡/作废 横向排列，带计数角标，点击切换选中态。
55. **Given** s8，**When** 查看流水卡，**Then** 快捷开单以闪电缩略图 + 红色闪电标签 +「快捷开单」展示，含金额/服务人/微信/实付总额/开单时间。

**选择退款项目 s9**
56. **Given** s9，**When** 点项目勾选，**Then** 勾选态切换；全部勾选时「全选」点亮。
57. **Given** s9，**When** 点「全选」，**Then** 全部项目勾选；再次点击取消全部。
58. **Given** s9，**When** 取消任一勾选，**Then** 「全选」熄灭。
59. **Given** s9，**When** 全选项目，**Then** 退款可选原路退回与指定退回；部分选择或已有退款项目只可指定退回（提示条文案一致）。

**业绩设置 s10 / 提成方案 s11**
60. **Given** s10，**When** 查看，**Then** 劳动业绩 Tab 默认选中，「快捷开单」分类置顶选中，业绩表 点客/散客 × 大工/中工/小工 = 100.00%。
61. **Given** s10，**When** 点「销售业绩」，**Then** toast「销售业绩暂无设计」。
62. **Given** s11，**When** 查看，**Then** 劳动提成 Tab 默认选中，「快捷开单」分类置顶选中，提成表 点客/散客 = 100.00%。
63. **Given** s11，**When** 点「销售提成」，**Then** toast「销售提成暂无设计」。
64. **Given** 用户未单独为「快捷开单」配置业绩或提成，**When** 系统结算该笔快捷开单，**Then** 业绩/提成按当前线上系统默认规则赋予默认值（本侧不另定数值）。
65. **Given** 经营分析 → 门店经营报表，**When** 查看项目 TOP5 排行，**Then** 排行不包含「快捷开单」（经营分析属另一产品线，本侧记录该规则）。

**金额格式边界**
66. **Given** 金额键盘，**When** 输入 `.5`，**Then** 以「.」开头被拒绝，不进入。
67. **Given** 金额键盘，**When** 输入 `0123`，**Then** 前导 0 剔除，结果为 `123`。
68. **Given** 金额键盘，**When** 输入 `999999.999`，**Then** 小数第 3 位被拒；总值不超过 999999.99。
69. **Given** 金额键盘，**When** 输入 `9999999`，**Then** 超上限被拒。
70. **Given** 金额键盘，**When** 输入 `58.95`，**Then** 合法显示；再输数字被拒（小数已 2 位）。
71. **Given** 添加 sheet 应付/s6 小计，**When** 输入超过 999999.99 或 2 位以上小数，**Then** 自动截断/封顶。

**下一步 / 底部 / 已选商品**
72. **Given** 购物车为空，**When** 点「下一步」，**Then** toast「请先选择项目」。
73. **Given** 购物车有项，**When** 点「下一步」，**Then** 直接进入 s6。
74. **Given** 购物车为空，**When** 点底部左侧热区（图标+应付），**Then** toast「请先选择项目」，不打开 Sheet。
75. **Given** 购物车有项，**When** 点底部左侧热区，**Then** 已选商品 Sheet 从 bar 后升起，bar 仍可见可点；行内有名称、员工摘要、单价、±数量。
76. **Given** 同一项目两次加购、员工不同，**When** 查看购物车 / Sheet / s6，**Then** 拆成两行，不得合并数量。
77. **Given** 同一项目两次加购、单件应付不同，**When** 查看购物车，**Then** 拆成两行。
78. **Given** 同一项目两次加购、单件应付与员工均相同（含均未选员工），**When** 确定，**Then** 合并为一行，数量相加。
79. **Given** Sheet 内某行数量，**When** 点 − 减到 0，**Then** 该行删除并重算合计；点 + 上限 999。
80. **Given** Sheet，**When** 点「清空」并确认，**Then** 全部清空、角标归零、Sheet 关闭；点取消则不变。
81. **Given** 任意页，**When** 点「挂单」，**Then** toast「已挂单」（线上按现有逻辑）。

**搜索栏 / 范围**
82. **Given** 开单结账页 s1/s5，**When** 查看顶部区域，**Then** 不包含搜索栏（本期已删除）。
83. **Given** s6/s7 各类弹窗（员工面板/权益选择/备注/小计编辑等），**When** 对照线上验收，**Then** 交互效果与弹窗样式以当前在线版本为准，原型细节不作为验收约束。

**快捷开单页布局**
84. **Given** s2/s3，**When** 查看字段卡片，**Then** 自上而下顺序为「开单内容 → 收款金额 → 员工」。

**添加会员（s4 行下展）**
85. **Given** s4，**When** 点「添加会员」行，**Then** 该行向下展开表单（姓名\* / 手机号 / 性别\*），且顾客行展开区同时收起（互斥）。
86. **Given** 添加会员表单，**When** 不填姓名点「确认添加」，**Then** toast「请输入姓名」，不新建。
87. **Given** 添加会员表单，**When** 手机号填 5 位数字点「确认添加」，**Then** 输入框红框 + 「手机号格式不正确」+ toast，不新建。
88. **Given** 添加会员表单，**When** 姓名「王」+ 选「女」+ 确认，**Then** 列表中 W 分组新建「王小姐」行（含手机号/最近消费：未消费）、滚动定位到该行并保持展开；点其「开单」进入 s5 且顶部卡显示「王小姐」+ 女性头像。
89. **Given** 添加会员表单已填数据，**When** 点「取消」，**Then** 表单收起、输入清空、不产生新会员。
90. **Given** 已展开添加会员表单，**When** 点任一顾客行，**Then** 添加会员表单收起、该顾客行展开。

**展开卡分段与归零收起**
91. **Given** s1/s5 展开某项目，**When** 查看展开卡，**Then** 仅「服务员工行 → 价格（元）行」之间有 0.5px 发丝线 `#E7E7E7`（左右内缩 16px、位于员工行下沿下移 2px 处），商品信息行与下方之间**无**分隔线；「价格（元）」行标签为 14px/500。
92. **Given** s1/s5 某行展开且数量为 1，**When** 点减号，**Then** 数量归 0、该行收起（展开区清空、恢复折叠态与价格、右侧回到「＋」）。

**购物车明细回跳编辑**
93. **Given** 已选商品 Sheet 内有会员笔与散客笔，**When** 点某一笔的行体，**Then** 关闭 Sheet 并跳到对应页（会员笔 → s5 / 散客笔 → s1）、切到对应 Tab、分类重置「全部」、该行自动展开并回填单价/数量/服务员工。
94. **Given** 从明细回跳后已展开的行，**When** 修改数量并点「加入购物车」，**Then** **更新原明细（行数不变）**，toast「已更新该笔」。
95. **Given** 已选商品 Sheet，**When** 点行内 ± 步进器，**Then** 仅改数量，不关闭 Sheet、不跳转。

**员工选择跨项目隔离**
96. **Given** 在项目 A 展开区已为 A 选好服务员工，**When** 展开项目 B 并点「添加」打开员工 sheet，**Then** B 的 sheet 全部为未选状态（不出现 A 的已选员工）。
97. **Given** 在项目 A 的 sheet 里点了某张员工卡进入「点客/散客」翻牌态后点遮罩收起，**When** 打开项目 B 的员工 sheet，**Then** 无任何卡片处于翻牌编辑态。
98. **Given** 同一项目 A 已选 2 名员工，**When** 关闭并重新点「添加」打开 sheet，**Then** 已选 2 人仍保留（同一项目不重置）；点「×」可移除；满 3 人时「添加」chip 隐藏、其余卡片显示「已达上限」。

**散客卡内快捷注册会员**
99. **Given** 顾客为「男散客」，**When** 查看 s1 顶部卡，**Then** 卡片右侧显示「添加会员」图标（32×32，与 s4 同款）；顾客为默认「散客」时**不显示**。
100. **Given** 顾客为「男散客」（或「女散客」），**When** 查看 s1/s2/s3/s6/s7 顶部卡姓名，**Then** 字色为 `#751616`（与会员卡一致）、正常流排布并与头像中轴对齐，无白色字/错位。
101. **Given** 「男散客」卡，**When** 点「添加会员」图标，**Then** 表单展开且性别已默认选中「男」（「女散客」则默认「女」）；重复点击已选中的性别不会取消选中；输入框为白底（含失焦态）。
102. **Given** 已选「男散客」，**When** 点卡片右侧「添加会员」图标，**Then** 卡片向下展开快捷注册表单（姓名\* / 手机号 / 性别\*），再次点图标或点「取消」则收起、输入清空、不保存。
103. **Given** 卡内注册表单，**When** 姓名「李」+ 手机号 5 位 + 选「男」+ 确认，**Then** 红框 + 「手机号格式不正确」+ toast，卡片不切换身份。
104. **Given** 卡内注册表单，**When** 姓名「赵」+ 手机号 13900002222 + 选「女」+「确认添加」，**Then** 表单收起；顶部卡就地变为会员卡（「赵小姐」+ 女性头像 + VIP 标签 + 「上次到店」= 当天日期），散客卡隐藏。
105. **Given** 刚注册的新会员（无卡无权益），**When** 展开卡片权益，**Then** 显示 `0 会员卡 / 0 储值余额 / — 折扣`；价目**不出现**划线优惠价；进入 s6 时「权益」行显示「无卡权益」。
106. **Given** 在散客页注册会员且购物车已有明细，**When** 点「下一步」进入 s6，**Then** 顶部卡为会员卡（会员姓名 + 当天日期），已加购明细内容与应付不变。
107. **Given** 在 s2「直接收款」页点了散客卡的「添加会员」图标并成功注册，**When** 查看 s2 顶部卡，**Then** 就地切换为会员卡（会员姓名 + 当天日期），面板收起。
108. **Given** 已注册新会员，**When** 回 s4 重新选「陈女士」开单，**Then** 顶部卡恢复「陈女士」档案（3 张会员卡 / 2049 储值 / 8.0 折扣），折扣权益恢复生效。

**s4 标题栏与搜索**
109. **Given** 在 s4，**When** 查看页面顶部，**Then** 状态栏下方为标题栏（返回键 + 「选择顾客」），搜索栏位于标题栏之下、列表之上，无日期控件。
110. **Given** 在 s4，**When** 点标题栏左侧返回键，**Then** 返回首页 s0。
111. **Given** 在 s4 搜索框输入「陈」，**When** 查看列表，**Then** 仅平铺显示「陈女士」「陈昕然」，字母分组头、右侧 A-Z 索引条、「添加会员」行与散客组全部隐藏。
112. **Given** 在 s4 搜索框输入手机号「13800001122」，**When** 查看列表，**Then** 仅显示「高海泉」。
113. **Given** 在 s4 搜索框输入「zzzz」，**When** 查看列表，**Then** 显示空状态（图标 +「未找到相关顾客」+「换个姓名或手机号试试」）。
114. **Given** 已输入搜索词，**When** 点「取消」，**Then** 输入清空并退出输入态、列表恢复分组态、**不跳转首页**。
115. **Given** 搜索态下，**When** 展开「添加会员」并成功确认添加「郑小姐」，**Then** 自动退出搜索态，新行可见、定位并保持展开；再次搜索「郑」可命中该新会员。

**标题栏日期控件**
116. **Given** 首次进入，**When** 查看 s1/s2/s3/s5/s6/s7 标题栏，**Then** 6 个页面的日期均显示为**今天**（`YYYY.MM.DD`）。
117. **Given** 在 s1，**When** 点标题栏日期控件，**Then** 从标题栏正下方下拉日期浮层（含蒙层、月份标题、星期表头、日期网格、快捷「今天 / 昨天」）。
118. **Given** 日期浮层已展开，**When** 查看日期网格，**Then** **今天之后的日期、以及今天往前 3 个月之前的日期**均为置灰不可选；当月为可选范围的最后一个月时「下一月」按钮置灰。
119. **Given** 日期浮层已展开，**When** 点选 3 个月范围内某一天（如 2026-06-17），**Then** s1/s2/s3/s5/s6/s7 **全部**标题栏日期同步更新为该日期，浮层关闭。
120. **Given** 已把日期改为过去某天，**When** 依次点快捷「昨天」「今天」，**Then** 日期分别为「今天 − 1 天」与「今天」。
121. **Given** 日期浮层已展开，**When** 点蒙层空白处或再次点日期控件，**Then** 浮层关闭且日期不变。

**开单页滚动视差与吸顶（s1/s5）**
122. **Given** 在 s1 顶部（滚动 0），**When** 查看顾客卡片与价目表，**Then** 卡片在原位（无位移）、价目表白卡紧贴卡片下方，与改动前一致。
123. **Given** 在 s1，**When** 向下滚动价目表，**Then** 顾客卡片以约 0.5× 速度上移、价目表白卡以 1× 上移并压在其上，卡片露出的高度随滚动递减（视差）。
124. **Given** 在 s1，**When** 滚动到 108px，**Then** 价目表白卡上移**贴住标题栏**，顾客卡片被**完全遮挡**；继续滚动位移不再增加。
125. **Given** 已滚动到价目表贴住标题栏，**When** 继续向下滚动，**Then** 项目/产品 Tab 与分类 chips **吸顶固定**，仅商品列表滚动。
126. **Given** 已滚动一段距离，**When** 反向滚回顶部，**Then** 顾客卡片**完全恢复**原位置与层级（无残留位移、无 `is-scrolled` 态）。
127. **Given** 已滚动一段距离，**When** 切换项目/产品 Tab 或切换分类，**Then** 价目区重绘后视差位移与吸顶状态按当前滚动位置重新应用，效果不丢失。
128. **Given** 顾客为散客且价目表已滚动，**When** 展开某个项目，**Then** 展开区（服务员工 / 价格行 / 加入购物车）正常可用，价格药丸可唤出数值 sheet，加入购物车正常。
129. **Given** 在 s1 顶部未滚动，**When** 点散客卡的「添加会员」图标，**Then** 展开的快捷注册面板仍浮于价目表白卡之上（未被价目表压住）。

**结算页单号与明细卡分隔线（s6/s7）**
130. **Given** 进入 s6 或 s7，**When** 查看单号行，**Then** 左侧「单号」，右侧为灰色文案「**自动生成·点击可修改**」（不再显示「结账后自动生成」）。
131. **Given** s6/s7 明细卡，**When** 查看商品信息头下沿的分隔线，**Then** 为 **0.5px 实线**、左右各内缩 16px（与改动前一致）。
132. **Given** s6/s7 明细卡，**When** 查看「服务员工→权益」「权益→应付」两根分隔线，**Then** 均为**虚线**、左右各内缩 16px；商品信息头下沿那根仍为实线。

**选择权益页 s12 / 选择优惠券页 s13（本期新增）**
133. **Given** s6 或 s7 明细卡，**When** 分别点「权益」行与「优惠券」行的**左侧标签**，**Then** 无任何响应（不跳转、不弹层）。
134. **Given** s6 或 s7，**When** 点「权益」行右侧「值 + ›」，**Then** 进入 s12（标题「选择权益」，底部提示「确认并设为本单默认支付方式」+「确认」）。
135. **Given** 进入 s12，**When** 查看列表，**Then** 显示 7 行（不使用权益 / 5.0折（5折卡）/ 计次卡抵扣 / 充值金额所有项目5.0折(铂金卡) / 折扣卡 / 充值金额所有项目8.0折(金卡) / 固定金额98元（美发卡3000元）），且**回显该明细当前权益**（会员自动匹配为「折扣卡」，散客为「不使用权益」），单选。
136. **Given** s12，**When** 点另一行进中（单选态切换），**Then** 只有被点击行选中；**点返回**则放弃改动，s6/s7 权益与应付不变。
137. **Given** s12 选中「5.0折（5折卡）」，**When** 点底部「确认」，**Then** 返回来源页（s6/s7），该明细权益显示「5.0折（5折卡）」、应付按 5 折重算（示例：¥28.00 → ¥14.00）。
138. **Given** s12，**When** 选「不使用权益」并确认，**Then** 该明细权益显示「无卡权益」、应付恢复原价，且后续不再被自动匹配覆盖。
139. **Given** s7 从 s12 选权益并确认，**When** 返回 s7，**Then** 停留页为 **s7**（不是 s6/s0），快捷开单明细应付按权益重算。
140. **Given** 改价明细，**When** 查看权益行，**Then** 显示「改价商品不可用权益」，右侧无热区、无法进入 s12。
141. **Given** s6 或 s7，**When** 点「优惠券」行右侧「值 + ›」，**Then** 进入 s13（标题「选择优惠券」）。
142. **Given** 进入 s13，**When** 查看列表，**Then** 首行为「不使用优惠券」+ 单选圈；券卡为 3 张（¥10 无门槛 / ¥30 满200可用 / ¥50 满300可用），角标为「满减券 / 满减券 / 代金券」。
143. **Given** 进入 s13，**When** 查看券卡的「使用规则」，**Then** **默认为折叠态**（不展开）；点该行展开显示规则条目，再点收起；**再次进入 s13 仍为折叠态**；展开/收起不改变选中态。
144. **Given** 进入 s13，**When** 查看不满门槛的券（如应付 ¥60.40 时的 ¥30/¥50 券），**Then** 该卡置灰不可选；点击 toast「当前订单不满该券使用门槛」，选中态不变。
145. **Given** s13 选中 ¥10 券，**When** 点底部「确认」，**Then** 返回来源页，券行显示该券名 + 「›」，底部应付 = 原应付 − 10（示例：¥60.40 → ¥50.40）。
146. **Given** 已选券，**When** 再次进入 s13，**Then** 该券为选中态（单选回显）。
147. **Given** 已选券，**When** 在 s13 选「不使用优惠券」并确认，**Then** 券行恢复「请选择 ›」，应付回到未抵扣金额。
148. **Given** 散客订单进入 s6/s7，**When** 查看优惠券行，**Then** 自动带入首张可用券（¥10 无门槛券）并已抵扣；**会员订单不自动带券**（显示「请选择 ›」）。
149. **Given** 用户曾选「不使用优惠券」并确认，**When** 返回结算页重新渲染，**Then** 不再自动带入券（保持「请选择」）。
150. **Given** 已选券的 s6 订单，**When** 清空购物车，**Then** 券状态一并清空，应付归 0。
151. **Given** s7 快捷开单已选券，**When** 从 s7 进 s12 选权益并确认（期间会连带渲染 s6），**Then** s7 的券**不被清掉**，s7 应付仍含券抵扣。
152. **Given** s6/s7 有明细，**When** 点「结账」，**Then** **原地** toast「已进入线上结账流程」，**停留当前页**、购物车明细与角标不清空、不跳转。
153. **Given** s6/s7 无明细，**When** 点「结账」，**Then** toast「暂无结算项目」。
154. **Given** 左侧导航，**When** 查看，**Then** 含「12 选择权益」「13 选择优惠券」两项，可直达对应页面（评审用）。

---

# 模块 11　非功能（极简）
> 读者：前端 ✓ · 后端 ✓ · 测试 ✓

- 画布与动效对齐原型 390 × 844。
- 原型为静态单页，所有状态为内存模拟；刷新重置。
- 埋点：无专表；若现网统一规范另案。
- 员工/价目数据为演示资源；正式接入真实数据源。
- s8-s11 为逻辑补充页面，业务链路（流水详情/退款/业绩提成保存）以线上为准。
- 本期仅单店（与线上一致）。

---

# 模块 12　交付物

| 交付 | 说明 |
|------|------|
| 本文 `PRD-开单结账.md` | 开发与测试准稿 |
| 交互原型 `index.html` | 唯一可视依据 |
| `PRD-开单结账.html` | PRD 自包含 HTML 预览版 |

开发与测试只需 **PRD + 原型**。冲突时以原型界面为准修正 PRD。
